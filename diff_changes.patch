diff --git a/.env.example b/.env.example
index 16cb66f..3ba4885 100644
--- a/.env.example
+++ b/.env.example
@@ -2,10 +2,10 @@
 # Local development typically uses the URL created by `npx convex dev`.
 NEXT_PUBLIC_CONVEX_URL=http://127.0.0.1:3210
 
 # Required for server-side internal Convex calls in cloud/production environments.
 # Keep this private (never expose via NEXT_PUBLIC_*).
-# Local development can fall back to `.convex/local/*/config.json`.
+# Local development can fall back to Convex local state under `~/.convex/`.
 CONVEX_DEPLOY_KEY=
 
 # JWT signing secret for session cookies.
 AUTH_JWT_SECRET=replace-with-a-long-random-secret
diff --git a/README.md b/README.md
index 22af126..f8ded20 100644
--- a/README.md
+++ b/README.md
@@ -58,24 +58,25 @@ Both accounts are intended for local development and test environments. Preview
    npm install
    ```
 
 3. Start Convex locally:
    ```bash
-   npx convex dev --once
+   npx convex dev --local --once
    ```
 
 4. Copy the environment template:
    ```bash
    cp .env.example .env.local
    ```
 
 5. Configure `.env.local` with at least:
    ```env
-   NEXT_PUBLIC_CONVEX_URL=http://127.0.0.1:3210
    AUTH_JWT_SECRET=<long-random-secret>
    ```
 
+   `NEXT_PUBLIC_CONVEX_URL` is typically written into `.env.local` automatically by the Convex CLI during local setup. If needed, the local default remains `http://127.0.0.1:3210`.
+
 6. Seed demo accounts and lesson data as needed:
    ```bash
    npx convex run seed:seedDemoAccounts
    npx convex run seed:seedPublishedCurriculum
    ```
@@ -87,11 +88,11 @@ Both accounts are intended for local development and test environments. Preview
    For cloud-hosted server-side internal Convex calls, also set:
    ```env
    CONVEX_DEPLOY_KEY=<server-only deploy key>
    ```
 
-   Local development does not require `CONVEX_DEPLOY_KEY` when `.convex/local/*/config.json` exists from `npx convex dev`.
+   Local development does not require `CONVEX_DEPLOY_KEY` when Convex local state exists under `~/.convex/`.
 
 7. Start the local stack:
    ```bash
    npm run dev:stack
    ```
@@ -109,11 +110,11 @@ NEXT_PUBLIC_CONVEX_URL=<your-convex-url>
 CONVEX_DEPLOY_KEY=<server-only deploy key for cloud internal Convex calls>
 AUTH_JWT_SECRET=<server-only JWT secret for session cookies>
 ```
 
 Keep `CONVEX_DEPLOY_KEY` and `AUTH_JWT_SECRET` server-only and never expose them via `NEXT_PUBLIC_*`.
-When `CONVEX_DEPLOY_KEY` is unset in local development, the app falls back to the local Convex CLI admin key stored in `.convex/local/*/config.json`.
+When `CONVEX_DEPLOY_KEY` is unset in local development, the app falls back to the local Convex CLI admin key stored in Convex local state under `~/.convex/` when available.
 
 ## Project Structure
 
 ```
 bus-math-v2/
@@ -148,20 +149,20 @@ bus-math-v2/
 - **TypeScript**: Strict mode enabled
 
 ## Scripts
 
 - `npm run dev` - Start the Vinext development server
-- `npm run dev:stack` - Start both `npx convex dev` and `vinext dev` together for local work
+- `npm run dev:stack` - Start the local Convex deployment and run `vinext dev` through the Convex parent process
 - `npm run build` - Build for production
 - `npm run start` - Start production server
 - `npm run lint` - Run ESLint through Vinext
 - `npm test` - Run the non-watch Vitest suite
 - `npm run test:watch` - Run tests in watch mode
 
 ## Convex Notes
 
-- `npx convex dev` creates local runtime state under `.convex/local/`.
+- `npx convex dev --local` creates local runtime state under `~/.convex/`.
 - Server-side internal Convex calls use `CONVEX_DEPLOY_KEY` in cloud environments and the local Convex CLI `adminKey` in development.
 - Identity-sensitive dashboard, progress, profile, and submission flows now run through server-only internal Convex helpers. Public page data can remain queryable, but authenticated server routes/pages should not call those sensitive functions through the public API surface.
 - The activity API now reads sensitive activity records through internal Convex queries and only returns redacted payloads to student callers.
 - Student-owned write endpoints now use a shared request guard in `lib/auth/server.ts` so non-student preview sessions cannot create learner progress, draft, submission, or assessment rows.
 - Demo credentials can be reprovisioned through `POST /api/users/ensure-demo` only in local development and automated test environments.
diff --git a/__tests__/components/exercises/JournalEntryBuilding.test.tsx b/__tests__/components/exercises/JournalEntryBuilding.test.tsx
index a85f3f5..34e5e30 100644
--- a/__tests__/components/exercises/JournalEntryBuilding.test.tsx
+++ b/__tests__/components/exercises/JournalEntryBuilding.test.tsx
@@ -67,7 +67,7 @@ describe('JournalEntryBuilding', () => {
           activityId: 'activity-journal',
           scenarioId: 'scenario-1'
         })
       )
     })
-  })
+  }, 15_000)
 })
diff --git a/__tests__/components/financial-reports/CashFlowStatementDetailed.test.tsx b/__tests__/components/financial-reports/CashFlowStatementDetailed.test.tsx
index c98ef4a..7cb6a16 100644
--- a/__tests__/components/financial-reports/CashFlowStatementDetailed.test.tsx
+++ b/__tests__/components/financial-reports/CashFlowStatementDetailed.test.tsx
@@ -97,7 +97,7 @@ describe('CashFlowStatementDetailed', () => {
     expect(screen.queryByText(/Total Adjustments/i)).not.toBeInTheDocument();
 
     await user.click(screen.getByRole('button', { name: /show details/i }));
 
     expect(screen.getByText(/Total Adjustments/i)).toBeInTheDocument();
-  });
+  }, 15_000);
 });
diff --git a/__tests__/lib/convex/config.test.ts b/__tests__/lib/convex/config.test.ts
index 43da9f3..2b9990d 100644
--- a/__tests__/lib/convex/config.test.ts
+++ b/__tests__/lib/convex/config.test.ts
@@ -14,11 +14,11 @@ async function loadModule() {
 async function loadAdminModule() {
   vi.resetModules();
   return import('@/lib/convex/admin');
 }
 
-function writeLocalConvexConfig(rootDir: string, adminKey: string) {
+function writeProjectLocalConvexConfig(rootDir: string, adminKey: string) {
   const configDir = path.join(rootDir, '.convex', 'local', 'default');
   fs.mkdirSync(configDir, { recursive: true });
   fs.writeFileSync(
     path.join(configDir, 'config.json'),
     JSON.stringify({
@@ -28,10 +28,25 @@ function writeLocalConvexConfig(rootDir: string, adminKey: string) {
     }),
     'utf8',
   );
 }
 
+function writeHomeLocalConvexConfig(homeDir: string, adminKey: string) {
+  const configDir = path.join(homeDir, '.convex', 'anonymous-convex-backend-state');
+  fs.mkdirSync(configDir, { recursive: true });
+  fs.writeFileSync(
+    path.join(configDir, 'config.json'),
+    JSON.stringify({
+      adminKey,
+      deploymentName: 'anonymous-local-deployment',
+      ports: { cloud: 3210, site: 3211 },
+      uuid: 'local-anonymous-state',
+    }),
+    'utf8',
+  );
+}
+
 describe('lib/convex/config', () => {
   afterEach(() => {
     process.env = { ...originalEnv };
     vi.unstubAllEnvs();
   });
@@ -54,11 +69,11 @@ describe('lib/convex/config', () => {
     expect(DEFAULT_LOCAL_CONVEX_URL).toBe('http://127.0.0.1:3210');
   });
 
   it('prefers CONVEX_DEPLOY_KEY over local admin-key fallback', async () => {
     const tempDir = fs.mkdtempSync(path.join(os.tmpdir(), 'convex-config-test-'));
-    writeLocalConvexConfig(tempDir, 'local-admin-key');
+    writeProjectLocalConvexConfig(tempDir, 'local-admin-key');
     vi.stubEnv('CONVEX_DEPLOY_KEY', 'deploy-key');
     vi.stubEnv('NODE_ENV', 'development');
 
     const { resolveConvexAdminAuth } = await loadAdminModule();
 
@@ -66,13 +81,13 @@ describe('lib/convex/config', () => {
       source: 'deploy-key',
       token: 'deploy-key',
     });
   });
 
-  it('uses the local .convex admin key when deploy key is absent in development', async () => {
+  it('uses the project-local .convex admin key when deploy key is absent in development', async () => {
     const tempDir = fs.mkdtempSync(path.join(os.tmpdir(), 'convex-config-test-'));
-    writeLocalConvexConfig(tempDir, 'local-admin-key');
+    writeProjectLocalConvexConfig(tempDir, 'local-admin-key');
     delete process.env.CONVEX_DEPLOY_KEY;
     vi.stubEnv('NODE_ENV', 'development');
 
     const { resolveConvexAdminAuth } = await loadAdminModule();
 
@@ -80,10 +95,27 @@ describe('lib/convex/config', () => {
       source: 'local-admin-key',
       token: 'local-admin-key',
     });
   });
 
+  it('uses the home-directory Convex state when the project-local path is absent', async () => {
+    const tempDir = fs.mkdtempSync(path.join(os.tmpdir(), 'convex-config-test-'));
+    const tempHomeDir = fs.mkdtempSync(path.join(os.tmpdir(), 'convex-home-test-'));
+    writeHomeLocalConvexConfig(tempHomeDir, 'home-local-admin-key');
+    delete process.env.CONVEX_DEPLOY_KEY;
+    vi.stubEnv('NODE_ENV', 'development');
+
+    const { resolveConvexAdminAuth } = await loadAdminModule();
+
+    await expect(
+      resolveConvexAdminAuth({ cwd: tempDir, homeDir: tempHomeDir }),
+    ).resolves.toEqual({
+      source: 'local-admin-key',
+      token: 'home-local-admin-key',
+    });
+  });
+
   it('fails explicitly in production when no admin auth is configured', async () => {
     const tempDir = fs.mkdtempSync(path.join(os.tmpdir(), 'convex-config-test-'));
     delete process.env.CONVEX_DEPLOY_KEY;
     vi.stubEnv('NODE_ENV', 'production');
 
diff --git a/__tests__/proxy.test.ts b/__tests__/proxy.test.ts
index 739aa29..a890d81 100644
--- a/__tests__/proxy.test.ts
+++ b/__tests__/proxy.test.ts
@@ -80,10 +80,20 @@ describe('proxy', () => {
 
     const response = await proxy(createRequest('/api/users/ensure-demo'));
     expect(response.status).toBe(200);
   });
 
+  it('allows unauthenticated login API requests', async () => {
+    const response = await proxy(createRequest('/api/auth/login'));
+    expect(response.status).toBe(200);
+  });
+
+  it('allows unauthenticated auth session API requests', async () => {
+    const response = await proxy(createRequest('/api/auth/session'));
+    expect(response.status).toBe(200);
+  });
+
   it('redirects unauthenticated preview demo provisioning requests to login', async () => {
     vi.stubEnv('NODE_ENV', 'production');
     vi.stubEnv('VERCEL_ENV', 'preview');
 
     const response = await proxy(createRequest('/api/users/ensure-demo'));
diff --git a/__tests__/setup/dev-stack-script.test.ts b/__tests__/setup/dev-stack-script.test.ts
index 80ee468..a991535 100644
--- a/__tests__/setup/dev-stack-script.test.ts
+++ b/__tests__/setup/dev-stack-script.test.ts
@@ -7,6 +7,38 @@ describe('Dev Stack Script', () => {
     const pkgPath = path.resolve(process.cwd(), 'package.json');
     const pkg = JSON.parse(fs.readFileSync(pkgPath, 'utf8'));
 
     expect(pkg.scripts['dev:stack']).toBe('node scripts/dev-stack.mjs');
   });
+
+  it('pins the Convex CLI package and runs Vinext through a local Convex parent process', async () => {
+    const packageLockPath = path.resolve(process.cwd(), 'package-lock.json');
+    const packageLock = JSON.parse(fs.readFileSync(packageLockPath, 'utf8'));
+    const lockedConvexVersion = packageLock.packages['node_modules/convex']?.version;
+
+    expect(lockedConvexVersion).toBeTruthy();
+
+    const scriptModulePath = path.resolve(process.cwd(), 'scripts/dev-stack.mjs');
+    const { createProcesses, resolveConvexCliPackageSpec } = await import(scriptModulePath);
+    const processes = createProcesses(process.cwd());
+    const convexProcess = processes.find(
+      (processConfig: { name: string }) => processConfig.name === 'convex',
+    );
+
+    expect(resolveConvexCliPackageSpec(process.cwd())).toBe(`convex@${lockedConvexVersion}`);
+    expect(convexProcess).toEqual({
+      name: 'convex',
+      command: 'npx',
+      args: [
+        '--yes',
+        '--package',
+        `convex@${lockedConvexVersion}`,
+        'convex',
+        'dev',
+        '--local',
+        '--run-sh',
+        'npx vinext dev',
+      ],
+    });
+    expect(processes).toHaveLength(1);
+  });
 });
diff --git a/conductor/codex-cron.log b/conductor/codex-cron.log
index 4e2a031..9c688b8 100644
--- a/conductor/codex-cron.log
+++ b/conductor/codex-cron.log
@@ -1,225 +1,451 @@
-2026-03-11T08:30:06.923949Z ERROR codex_core::models_manager::manager: failed to refresh available models: timeout waiting for child process to exit
-OpenAI Codex v0.112.0 (research preview)
+OpenAI Codex v0.114.0 (research preview)
 --------
-workdir: /home/daniel-bo/Desktop/bus-math-v2-experimental
+workdir: /home/daniel-bo/Desktop/bus-math-v2
 model: gpt-5.4
 provider: openai
 approval: never
 sandbox: danger-full-access
-reasoning effort: medium
+reasoning effort: high
 reasoning summaries: none
-session id: 019cdc04-7a4c-7ff2-8c59-9decf88f09d8
+session id: 019ce9c0-09a3-7461-80e0-fc83bf835720
 --------
 user
-$conductor
-
-Operate in a strict Just-In-Time execution model with one centralized directive.
-
-Step 1: Read `conductor/current_directive.md`, `conductor/tech-debt.md`, and `conductor/lessons-learned.md`.
-
-Step 2: Check `conductor/tracks.md` and `conductor/tracks/` for active work. If there is an `[~] In Progress` track, finish it before doing anything else. If there is no `[~] In Progress` track, plan exactly ONE new track that aligns with `conductor/current_directive.md`. Do not plan multiple tracks, do not create a backlog burst, and do not open unrelated cleanup tracks.
-
-Step 3: Implement the selected track completely, following `conductor/workflow.md`, the project tech stack, and the applicable product documentation.
-
-Step 4: Verify the track with automated tests. Use the relevant project test commands and keep execution non-interactive.
-
-Step 5: Finalize the completed track end-to-end:
-- commit the implementation work with non-interactive git commands
-- update memory files as needed, keeping `conductor/tech-debt.md` and `conductor/lessons-learned.md` under 50 lines each
-- archive the completed track by updating its `metadata.json`, moving the track directory into `conductor/archive/`, removing its active entry from `conductor/tracks.md`, and committing the cleanup
-
-CRITICAL:
-- All shell commands MUST use non-interactive flags where applicable. This workflow is unattended.
-- The autonomous agent MUST NOT drift from `conductor/current_directive.md`.
-- Do not plan or implement more than one new track per run unless you are finishing a pre-existing `[~] In Progress` track.
-
+/conductor
+Step 1: Check State. Read `conductor/current_directive.md`, `conductor/tech-debt.md`, and `conductor/lessons-learned.md`. 
+Step 2: Resume or Plan. 
+  - If there is an `[~] In Progress` track in `conductor/tracks.md`, DO NOT create a new track. Your sole job is to finish the existing track.
+  - If there are no incomplete tracks, define exactly ONE new track that strictly adheres to the goals in `conductor/current_directive.md`. Create the track artifacts.
+Step 3: Implement the track autonomously with high fidelity. For visual layout or print layout (Paged.js) tasks, prioritize adding automated layout tests or snapshot checks if the framework supports it.
+Step 4: Verify the implementation. You MUST run the full automated test suite and ensure a successful production build. Do not commit if tests fail.
+Step 5: Finalize. 
+  - Commit changes and push to remote (include model/version in commit message). 
+  - Update `tech-debt.md` and `lessons-learned.md` (keep under 50 lines).
+  - Move the completed track folder to `conductor/archive/` and remove it from `conductor/tracks.md`.
+  - Commit this cleanup with the message `chore(conductor): Archive completed track`.
+CRITICAL: All shell commands MUST use non-interactive flags (e.g., --yes, --no-interactive). This is an unattended run.
 deprecated: `collab` is deprecated. Use `[features].multi_agent` instead.
   Enable it with `--enable multi_agent` or `[features].multi_agent` in config.toml. See https://developers.openai.com/codex/config-basic#feature-flags for details.
 mcp: codex_apps starting
+2026-03-14T00:30:07.762901Z ERROR codex_api::endpoint::responses_websocket: failed to connect to websocket: IO error: failed to lookup address information: Try again, url: wss://chatgpt.com/backend-api/codex/responses
 mcp: codex_apps ready
 mcp startup: ready: codex_apps
-2026-03-11T08:30:17.459141Z ERROR codex_core::models_manager::manager: failed to refresh available models: timeout waiting for child process to exit
 codex
-Using the `conductor` skill because this is a track-driven implementation run. I’m loading the directive, memory files, workflow, and current track state first so I can either finish the active track or create exactly one new one within scope.
+Using the `conductor` skill first because you explicitly requested a Conductor-driven resume/plan, then I’ll inspect the active track state and project directives before making any changes.
 exec
-/bin/bash -lc "sed -n '1,220p' conductor/current_directive.md" in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc "sed -n '1,220p' conductor/tech-debt.md" in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 50ms:
-# Current Strategic Directive
-
-> CRITICAL FOR THE AUTONOMOUS AGENT: YOU MUST NOT DRIFT FROM THIS DIRECTIVE. Every planned track, implementation choice, and verification step must directly support this focus. Do not start side quests, umbrella cleanup work, or unrelated enhancements.
-
-## Strategic Focus
-
-Coherent curriculum implementation with fidelity from the curriculum documentation.
-
-## Near-Term Execution Rule
-
-Start with components for each lesson phase that match the unit, lesson, and phase objectives.
- succeeded in 50ms:
+/bin/bash -lc "sed -n '1,220p' conductor/tech-debt.md" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
 ## Tech Debt Registry
 
-> This file is curated working memory, not an append-only log. Keep it at or below **50 lines**.
-> Remove or summarize resolved items when they no longer need to influence near-term planning.
->
-> **Severity:** `Critical` | `High` | `Medium` | `Low`
-> **Status:** `Open` | `Resolved`
+> Keep this file at or below **50 lines**. Track only debt that should influence near-term planning.
 
 | Date | Track | Item | Severity | Status | Notes |
 |------|-------|------|----------|--------|-------|
-| 2026-03-10 | react_test_signal_cleanup_20260310 | `InventoryManager` tests emitted duplicate React key warnings during full Vitest runs, masking real rendering regressions in CI logs | Medium | Resolved | Simulation runtime ids are now deterministic and unique for notifications and market events, even during same-tick updates. |
-| 2026-03-10 | react_test_signal_cleanup_20260310 | Several React tests emitted `act(...)` warnings, reducing signal quality in unattended verification logs | Low | Resolved | `SubmissionDetailModal` tests now await fetch-driven renders and `usePhaseCompletion` tests wrap async mutations in `act(...)`. |
-| 2026-03-10 | published_progress_consistency_20260310 | Student and teacher progress views could read draft or historical lesson versions, leaking unreleased metadata and miscounting completion totals | High | Resolved | Shared published-curriculum helpers now drive dashboard snapshots, lesson delivery, and phase-completion resolution from the latest published lesson version only. |
-| 2026-03-10 | server_role_guard_cleanup_20260310 | Admin dashboard shell allowed any authenticated session because it checked auth presence without enforcing the `admin` role | High | Resolved | Shared server role guards now protect privileged App Router pages and admin coverage pins the fix in tests. |
-| 2026-03-10 | security_surface_hardening_20260310 | Preview demo-account reprovisioning exposed fixed teacher/admin credentials via a public route | Critical | Resolved | Route and proxy now restrict runtime reprovisioning to local development and test only. |
-| 2026-03-10 | security_surface_hardening_20260310 | Public Convex activity/profile helpers could bypass server-side activity redaction | High | Resolved | Sensitive activity lookup was moved to internal-only access and unused public profile exposure was removed. |
-| 2026-03-10 | teacher_student_detail_analytics_20260310 | Teacher student detail page only exposed an aggregate progress card, leaving intervention follow-up without unit-level context or a clear next lesson | Medium | Resolved | Internal teacher detail data now includes published unit progress and a shared analytics view-model that drives status, guidance, and next-lesson recommendations. |
-| 2026-03-11 | student_dashboard_boundary_refactor_20260311 | Student dashboard accepted any authenticated role, creating redirect drift versus teacher/admin dashboard boundaries and duplicating next-lesson presentation across student and teacher surfaces | High | Resolved | `requireStudentSessionClaims` now redirects teacher/admin sessions to role-appropriate dashboards, and student/teacher detail views share one next-lesson card plus shared unit-status presentation helpers. |
-| 2026-03-11 | activity_component_contract_refactor_20260311 | Activity component keys drifted across runtime registry, docs, and Zod validation, so documented aliases failed to render and schemas could accept props for the wrong component key | High | Resolved | Alias resolution is now centralized, documented aliases map to canonical runtime keys, and activity row validation checks props against the declared component key instead of a stale free-floating union. |
-| 2026-03-11 | student_lesson_access_boundary_cleanup_20260311 | Student-write APIs accepted any authenticated role, allowing teacher/admin preview sessions to mutate learner progress, draft, submission, and assessment records | High | Resolved | Shared `requireStudentRequestClaims(...)` now gates the affected routes and negative tests pin the `403` boundary. |
+| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+| 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+| 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+| 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
 exec
-/bin/bash -lc "sed -n '1,220p' conductor/lessons-learned.md" in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 50ms:
+/bin/bash -lc "sed -n '1,220p' conductor/lessons-learned.md" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
 ## Lessons Learned
 
-> This file is curated working memory, not an append-only log. Keep it at or below **50 lines**.
-> Remove or condense entries that are no longer relevant to near-term planning.
+> Keep this file at or below **50 lines**. It is curated working memory, not a log.
 
-### Architecture & Design
-<!-- Decisions made that future tracks should be aware of -->
+### Architecture and Planning
 
-- (2026-03-10, published_progress_consistency_20260310) Progress-sensitive student and teacher flows must resolve the latest published `lesson_versions` row, not the numerically newest version, before deriving phases or titles.
-- (2026-03-10, server_role_guard_cleanup_20260310) Privileged App Router pages should enter through shared `lib/auth/server` role guards so authentication, role checks, and redirect targets stay centralized and testable.
-- (2026-03-10, security_surface_hardening_20260310) Sensitive curriculum/activity lookups must stay behind internal Convex queries even when a server route performs extra redaction logic.
-- (2026-03-10, teacher_student_detail_analytics_20260310) Teacher-facing intervention pages can safely reuse student dashboard progression math when the Convex query returns published lesson progress as a unit/lesson tree.
-- (2026-03-11, student_dashboard_boundary_refactor_20260311) Student-facing dashboard routes need an explicit student-role server guard even when lesson pages intentionally allow teacher/admin preview bypasses.
-- (2026-03-11, activity_component_contract_refactor_20260311) Activity contracts drift quickly when alias handling lives separately from the runtime registry and schema validators; documented curriculum keys need one canonical resolver.
-- (2026-03-11, published_progress_viewmodel_consolidation_20260311) Student dashboard rows, teacher student-detail rows, and lesson phase-status payloads should derive from the same published-progress helpers or published-version/locking fixes will diverge again.
-- (2026-03-11, student_lesson_access_boundary_cleanup_20260311) Student-owned API writes need an explicit request-level student guard even when teacher/admin lesson preview is intentionally allowed elsewhere; auth presence alone is not a safe mutation boundary.
+- (2026-03-11, replan) Convex must remain the only runtime source of truth or product, monitoring, and curriculum delivery drift immediately.
+- (2026-03-11, replan) Published lesson version helpers are foundational; student delivery and teacher monitoring must share them rather than recomputing progress independently.
+- (2026-03-11, replan) Student write routes need explicit student-role request guards even when teacher preview paths exist elsewhere.
+- (2026-03-11, replan) Curriculum planning and runtime implementation drift unless lesson archetypes are declared explicitly and reused across docs, seeds, and UI shells.
+- (2026-03-11, replan) Cloudflare-hosted Vinext should be treated as the production target early so Vercel-only assumptions do not keep leaking back into the repo.
 
 ### Recurring Gotchas
-<!-- Problems encountered repeatedly; save future tracks from the same pain -->
 
+- (2026-03-11, replan) Old PRDs and migration-era docs look authoritative unless they are moved entirely out of the active Conductor surface.
+- (2026-03-11, replan) “Full curriculum” claims are misleading unless the seeded and published runtime content actually matches the planned curriculum count.
+- (2026-03-11, replan) Activity and standards contracts drift quickly when validators and Convex lookups use different identifiers.
+- (2026-03-12, curriculum_runtime_foundation) Public prerendered pages must tolerate empty Convex results so a clean production build does not depend on seeded local data.
+- (2026-03-12, curriculum_runtime_foundation) Migration-era Supabase and seed utilities can still break the production TypeScript graph even after the active runtime has moved to Convex.
+- (2026-03-13, curriculum_authoring_publish_pipeline) Legacy authored lesson modules can block Convex/server bundling unless they are converted into a pure generated source module first.
+exec
+/bin/bash -lc "sed -n '1,220p' /home/daniel-bo/.codex/skills/conductor/SKILL.md" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+---
+name: conductor
+description: |
+  Spec-driven development framework for AI-assisted software projects. Conductor organizes work into "tracks" (features, bugs, or chores) with specifications and phased implementation plans, following Test-Driven Development principles.
+
+  Use this skill when:
+  - User wants to set up a new project with Conductor ("conductor setup", "set up conductor", "initialize conductor")
+  - User wants to create a new track/feature ("new track", "create track", "add feature", "plan feature")
+  - User wants to implement tasks from a track ("implement", "start implementing", "work on track", "execute plan")
+  - User wants to review completed work ("review", "code review", "verify track")
+  - User wants to check project status ("conductor status", "show progress", "track status")
+  - User wants to revert previous work ("revert", "undo track", "rollback task")
+  - User mentions conductor commands or spec-driven development
+---
+
+# Conductor
+
+Spec-driven development framework that organizes AI-assisted software development into structured, trackable units of work.
+
+## Core Concepts
 exec
-/bin/bash -lc "sed -n '1,260p' conductor/tracks.md" in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc 'rg --files conductor/tracks' in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc "sed -n '1,260p' conductor/workflow.md" in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
+/bin/bash -lc "sed -n '1,260p' conductor/tracks.md" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
 # Project Tracks
 
-This file is the source of truth for active execution order and archived track history.
+This file is the source of truth for active execution order. Archived tracks live in `conductor/archive/`. The milestone-level delivery sequence lives in [./roadmap.md](./roadmap.md).
 
 ---
 
-## Active Execution Queue (progressive order)
-
-## Upcoming / Unplanned
+## Active Track
 
-1. Convex Auth Runtime Verification & Closeout
-     Goal: prove username/password auth works locally and in the intended deployment model, including internal Convex calls, seeded demo users, session persistence,
-     and failure handling.
-     Deliverables: smoke-tested login/logout/session flow, seeded local workflow, cleaned-up stale migration notes, archive/supersede old migration track.
-  2. Instructional Component Framework
-     Goal: define the canonical component contract for pedagogically meaningful lesson blocks.
-     Deliverables: phase-2 demonstration components, phase-3 guided-practice components, shared event/attempt/progress schema, and Convex persistence that records
-     useful learning data rather than just completion flags.
-  3. Spreadsheet Component System
-     Goal: ship both noninteractive spreadsheet display and interactive spreadsheet practice components.
- exited 1 in 50ms:
+- [~] **Track: Capstone and Textbook Completion**
+  *Link: [./tracks/capstone_textbook_completion_20260314/](./tracks/capstone_textbook_completion_20260314/)*
 
- succeeded in 51ms:
-# Project Workflow
+## Planned Queue
 
-## Guiding Principles
+1. **Cloudflare Production Hardening and Launch**
+  Finalize Worker deployment, runtime verification, auth hardening, and launch quality gates.
 
-1. **The Plan is the Source of Truth:** All work must be tracked in `plan.md`
-2. **The Tech Stack is Deliberate:** Changes to the tech stack must be documented in `tech-stack.md` *before* implementation
-3. **Test-Driven Development:** Write unit tests before implementing functionality
-4. **High Code Coverage:** Aim for >80% code coverage for all modules
-5. **User Experience First:** Every decision should prioritize user experience
-6. **Non-Interactive & CI-Aware:** Prefer non-interactive commands. Use `CI=true` for watch-mode tools (tests, linters) to ensure single execution.
+## Archive Ledger
 
-## Task Workflow
+- [x] **Track: Curriculum Rollout Wave 2 (Units 5-8)**
+  *Link: [./archive/curriculum_rollout_wave2_20260314/](./archive/curriculum_rollout_wave2_20260314/)*
+exec
+/bin/bash -lc "sed -n '1,220p' conductor/current_directive.md" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+# Current Strategic Directive
 
-All tasks follow a strict lifecycle:
+Deliver the full 8-unit, 11-lesson-per-unit curriculum plus capstone on a Convex-backed Vinext app hosted on Cloudflare, with only two phase-1 product roles: `student` and `teacher`.
 
-### Standard Task Workflow
+Every active track must directly support one of these outcomes:
 
-1. **Select Task:** Choose the next available task from `plan.md` in sequential order
+- complete published curriculum delivery
+- accurate student study progress
+- useful teacher monitoring
+- removal of stale platform assumptions that block the above
 
-2. **Mark In Progress:** Before beginning work, edit `plan.md` and change the task from `[ ]` to `[~]`
-2026-03-11T08:30:37.352151Z ERROR codex_core::models_manager::manager: failed to refresh available models: timeout waiting for child process to exit
+Admin tooling and in-app curriculum authoring are deferred until later tracks.
+exec
+/bin/bash -lc 'git status --short --branch' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+## main...origin/main
+ M .env.example
+ M README.md
+ M __tests__/app/curriculum/page.test.tsx
+ M __tests__/app/student/dashboard/page.test.tsx
+ M __tests__/app/teacher/students/[studentId]/page.test.tsx
+ M __tests__/components/dashboard/NextLessonCard.test.tsx
+ M __tests__/components/teacher/CourseOverviewGrid.test.tsx
+ M __tests__/curriculum/published-manifest.test.ts
+ M __tests__/lib/convex/config.test.ts
+ M __tests__/proxy.test.ts
+ M __tests__/setup/dev-stack-script.test.ts
+ M app/curriculum/CurriculumUnitCard.tsx
+ M app/student/dashboard/page.tsx
+ M app/teacher/students/[studentId]/page.tsx
+ M app/teacher/units/[unitNumber]/page.tsx
+ M components/dashboard/NextLessonCard.tsx
+ M components/student/LessonRenderer.tsx
+ M components/student/PhaseHeader.tsx
+ M components/teacher/CourseOverviewGrid.tsx
 codex
-There’s no active entry in `tracks.md`, so I’m checking the track folders themselves before I create anything. If there’s a lingering in-progress track on disk, that takes priority over planning a new one.
+There’s already one active Conductor track, so I’m staying on that track only. The worktree is dirty across the curriculum, dashboard, teacher, and Conductor files, so I’m reading the capstone track spec/plan next and then I’ll assess which parts are already implemented versus still missing.
 exec
-/bin/bash -lc "rg -n \"\\[~\\]|in_progress|In Progress|\\\"status\\\"\\s*:\\s*\\\"in_progress\\\"\" conductor/tracks conductor/archive -g 'plan.md' -g 'metadata.json'" in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 50ms:
-conductor/archive/unit1_content_20260218/plan.md:11:- [~] Task: Audit `complete_activity_atomic` RPC and `student_progress` for standard-code recording
-conductor/archive/sprint3_core_platform_20251111/metadata.json:4:  "status": "in_progress",
-conductor/archive/vinext_convex_migration_20260225/plan.md:28:- [~] Task 1: Implement Convex-backed username/password auth (custom, no self-registration/email flows) to replace Supabase auth.
-conductor/archive/vinext_convex_migration_20260225/plan.md:36:- [~] Task 2: Update login/auth UX to reflect admin-managed username/password model.
-conductor/archive/security_surface_hardening_20260310/plan.md:21:- [~] Task: Update security-facing documentation and closeout artifacts
+/bin/bash -lc "sed -n '1,320p' conductor/tracks/capstone_textbook_completion_20260314/plan.md" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+# Implementation Plan
+
+## Phase 1: Capstone Contract Guards
+
+- [ ] Task: Add regression tests for authored capstone curriculum coverage
+  - [ ] Write tests that fail if the published capstone lesson is still generated
+  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+- [ ] Task: Add regression tests for textbook capstone labeling
+  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+- [ ] Task: Mark the new track active in Conductor and capture implementation status
+  - [~] Update track/task status markers as execution progresses
+
+## Phase 2: Capstone Runtime Completion
+
+- [ ] Task: Create canonical authored capstone runtime content
+  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+- [ ] Task: Normalize capstone labeling across key textbook surfaces
+  - [ ] Add a shared capstone label helper for published curriculum surfaces
 exec
-/bin/bash -lc "find conductor/tracks -maxdepth 2 -type f \\( -name 'metadata.json' -o -name 'plan.md' -o -name 'spec.md' \\) | sort" in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
+/bin/bash -lc "sed -n '1,260p' conductor/tracks/capstone_textbook_completion_20260314/spec.md" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+# Specification
+
+## Overview
+
+Replace the generated capstone publish scaffold with canonical repository-authored runtime content and close the remaining textbook-completion gaps so the published course reads coherently from Unit 1 through the capstone for both students and teachers.
+
+## Functional Requirements
+
+1. The capstone must publish from canonical repository-authored lesson content rather than the generic generated capstone scaffold.
+2. The authored capstone runtime content must define explicit milestone checkpoints, final presentation expectations, and evidence requirements aligned to the active curriculum narrative and roadmap deliverables.
+3. The published curriculum manifest must continue to cover 8 instructional units plus 1 capstone lesson, but the capstone lesson must now publish as `source: 'authored'`.
+4. Student, teacher, and public curriculum surfaces must present the capstone as a distinct culminating experience instead of leaking a generic `Unit 9` label in the main textbook flow.
+5. Teacher monitoring and student progress surfaces must continue to read the same published curriculum/progress model without introducing a second source of truth or capstone-only special-case storage.
+
+## Non-Functional Requirements
 
+- Convex remains the only runtime source of truth.
+- No dependency changes.
+- Tests must fail if the capstone regresses to generated placeholder content or if key textbook surfaces regress to generic `Unit 9` labeling.
+- Active Conductor docs must reflect that the full phase-1 textbook now includes authored capstone runtime content.
 exec
-/bin/bash -lc 'git status --short' in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
- M conductor/autonomous_prompt.md
- M conductor/codex-cron.log
- M conductor/codex-last.txt
- M conductor/workflow.md
-2026-03-11T08:31:03.065904Z ERROR codex_core::models_manager::manager: failed to refresh available models: timeout waiting for child process to exit
+/bin/bash -lc "sed -n '1,220p' conductor/tracks/capstone_textbook_completion_20260314/metadata.json" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+{
+  "track_id": "capstone_textbook_completion_20260314",
+  "type": "feature",
+  "status": "in_progress",
+  "created_at": "2026-03-14T00:00:00Z",
+  "updated_at": "2026-03-14T00:00:00Z",
+  "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+  "estimated_tasks": 6,
+  "actual_tasks": null,
+  "deviation_notes": ""
+}
+exec
+/bin/bash -lc 'git diff --stat -- conductor/tracks/capstone_textbook_completion_20260314 app components lib convex __tests__ docs conductor/tech-debt.md conductor/lessons-learned.md conductor/tracks.md README.md .env.example proxy.ts scripts/dev-stack.mjs' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+ .env.example                                       |   2 +-
+ README.md                                          |  13 +-
+ __tests__/app/curriculum/page.test.tsx             |  27 ++++
+ __tests__/app/student/dashboard/page.test.tsx      |  31 +++++
+ .../app/teacher/students/[studentId]/page.test.tsx |  46 ++++++
+ .../components/dashboard/NextLessonCard.test.tsx   |  19 +++
+ .../components/teacher/CourseOverviewGrid.test.tsx |  22 +++
+ __tests__/curriculum/published-manifest.test.ts    |  29 +++-
+ __tests__/lib/convex/config.test.ts                |  40 +++++-
+ __tests__/proxy.test.ts                            |  10 ++
+ __tests__/setup/dev-stack-script.test.ts           |  32 +++++
+ app/curriculum/CurriculumUnitCard.tsx              |   3 +-
+ app/student/dashboard/page.tsx                     |   5 +-
+ app/teacher/students/[studentId]/page.tsx          |   3 +-
+ app/teacher/units/[unitNumber]/page.tsx            |   5 +-
+ components/dashboard/NextLessonCard.tsx            |   5 +-
+ components/student/LessonRenderer.tsx              |   3 +-
+ components/student/PhaseHeader.tsx                 |  11 +-
+ components/teacher/CourseOverviewGrid.tsx          |   5 +-
+ components/teacher/TeacherLessonPlan.tsx           |   3 +-
 codex
-The active track folder list came back empty, and the only `[~]` markers are inside archived history. I’m loading the Conductor track-creation/implementation references and the project docs needed to choose one track that matches the current directive.
+I’ve got the active track scope and can see a partial implementation already in flight. Next I’m reviewing the changed curriculum/runtime/test files, then I’ll close whatever gaps remain, update the track status markers, and run the full verification gates before finalizing.
+Plan update
+Resuming the existing Conductor track and verifying the partially implemented capstone work before making targeted fixes.
+  → Inspect active track diffs and determine remaining implementation gaps against spec and plan
+  • Complete any missing code/tests/docs for capstone authored content and labeling
+  • Run lint, full test suite, and production build; fix regressions until green
+  • Finalize Conductor artifacts, archive the completed track, and commit/push the changes
 exec
-/bin/bash -lc 'ls -la conductor' in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
-total 132
-drwxrwxr-x  6 daniel-bo daniel-bo  4096 Mar 11 12:59 .
-drwxrwxr-x 24 daniel-bo daniel-bo  4096 Mar  9 12:58 ..
--rw-rw-r--  1 daniel-bo daniel-bo 11519 Feb 25 20:02 REFACTOR.md
--rw-rw-r--  1 daniel-bo daniel-bo  5555 Feb 25 20:02 architecture.md
-drwxrwxr-x 35 daniel-bo daniel-bo  4096 Mar 11 12:47 archive
--rw-rw-r--  1 daniel-bo daniel-bo  1600 Mar 11 12:59 autonomous_prompt.md
-drwxrwxr-x  2 daniel-bo daniel-bo  4096 Feb 25 20:02 code_styleguides
--rw-rw-r--  1 daniel-bo daniel-bo 14425 Mar 11 16:31 codex-cron.log
--rw-rw-r--  1 daniel-bo daniel-bo  2151 Mar 11 12:50 codex-last.txt
--rw-rw-r--  1 daniel-bo daniel-bo   514 Mar 11 12:59 current_directive.md
-drwxrwxr-x  6 daniel-bo daniel-bo  4096 Feb 25 20:02 docs
--rw-rw-r--  1 daniel-bo daniel-bo  6102 Mar 11 12:46 lessons-learned.md
--rw-rw-r--  1 daniel-bo daniel-bo  1889 Feb 25 20:02 product-guidelines.md
--rw-rw-r--  1 daniel-bo daniel-bo  3669 Mar  8 15:08 product.md
--rw-rw-r--  1 daniel-bo daniel-bo    55 Feb 25 20:02 setup_state.json
--rw-rw-r--  1 daniel-bo daniel-bo  4357 Mar 11 12:46 tech-debt.md
--rw-rw-r--  1 daniel-bo daniel-bo  1592 Mar  8 15:08 tech-stack.md
-drwxrwxr-x  2 daniel-bo daniel-bo  4096 Mar 11 12:47 tracks
--rw-rw-r--  1 daniel-bo daniel-bo  9783 Mar 11 12:47 tracks.md
+/bin/bash -lc 'git diff -- lib/curriculum/published-manifest.ts' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+diff --git a/lib/curriculum/published-manifest.ts b/lib/curriculum/published-manifest.ts
+index 4a16720..e2deb6e 100644
+--- a/lib/curriculum/published-manifest.ts
++++ b/lib/curriculum/published-manifest.ts
+@@ -11,6 +11,7 @@ import {
+   type Wave2AuthoredLessonBlueprint,
+   type Wave2AuthoredUnitBlueprint,
+ } from './generated/wave2-authored';
++import { AUTHORED_CAPSTONE_BLUEPRINT } from './generated/capstone-authored';
+ 
+ export type PublishedLessonType =
+   | 'core_instruction'
+@@ -1391,6 +1392,57 @@ function buildWave1AuthoredLessons(plan: UnitPlan): PublishedCurriculumLesson[]
+   return blueprints.map((blueprint) => buildWave1AuthoredLesson(plan, blueprint));
+ }
+ 
++function buildCapstonePhaseSections(phaseKey: PublishedPhaseKey): PublishedSection[] {
++  const [milestone1, milestone2] = AUTHORED_CAPSTONE_BLUEPRINT.milestones;
++
++  switch (phaseKey) {
 exec
-/bin/bash -lc 'ls -la conductor/tracks' in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc "sed -n '1,260p' /home/daniel-bo/.codex/skills/conductor/references/new-track.md" in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc "sed -n '1,320p' /home/daniel-bo/.codex/skills/conductor/references/implement.md" in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
-total 8
-drwxrwxr-x 2 daniel-bo daniel-bo 4096 Mar 11 12:47 .
-drwxrwxr-x 6 daniel-bo daniel-bo 4096 Mar 11 12:59 ..
- succeeded in 51ms:
-# New Track Workflow
-
-Create a new track with spec and plan through interactive specification gathering.
-
-## Prerequisites
+/bin/bash -lc 'git diff -- __tests__/curriculum/published-manifest.test.ts' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+diff --git a/__tests__/curriculum/published-manifest.test.ts b/__tests__/curriculum/published-manifest.test.ts
+index 0bb1a8b..9a2015a 100644
+--- a/__tests__/curriculum/published-manifest.test.ts
++++ b/__tests__/curriculum/published-manifest.test.ts
+@@ -32,7 +32,7 @@ describe('published curriculum manifest', () => {
+ 
+     expect(manifest.lessons.at(-1)).toMatchObject({
+       slug: 'capstone-investor-ready-plan',
+-      source: 'generated',
++      source: 'authored',
+     });
+   });
+ 
+@@ -96,6 +96,33 @@ describe('published curriculum manifest', () => {
+     expect(seedPlan.lessons.filter((lesson) => lesson.metadata?.tags?.includes('capstone'))).toHaveLength(1);
+   });
+ 
++  it('publishes the capstone as authored runtime content with milestone and final-presentation guidance', () => {
++    const manifest = buildPublishedCurriculumManifest();
++    const capstone = manifest.lessons.find(
+exec
+/bin/bash -lc 'git diff -- app/curriculum/CurriculumUnitCard.tsx app/student/dashboard/page.tsx app/teacher/students/[studentId]/page.tsx app/teacher/units/[unitNumber]/page.tsx components/dashboard/NextLessonCard.tsx components/student/LessonRenderer.tsx components/student/PhaseHeader.tsx components/teacher/CourseOverviewGrid.tsx components/teacher/TeacherLessonPlan.tsx' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+diff --git a/app/curriculum/CurriculumUnitCard.tsx b/app/curriculum/CurriculumUnitCard.tsx
+index a8baf2b..f52861e 100644
+--- a/app/curriculum/CurriculumUnitCard.tsx
++++ b/app/curriculum/CurriculumUnitCard.tsx
+@@ -14,6 +14,7 @@ import {
+ } from "@/components/ui/card";
+ 
+ import type { UnitCurriculum } from "./types";
++import { formatCurriculumSegmentLabel } from "@/lib/curriculum/segment-labels";
+ 
+ interface CurriculumUnitCardProps {
+   unit: UnitCurriculum;
+@@ -48,7 +49,7 @@ export function CurriculumUnitCard({ unit }: CurriculumUnitCardProps) {
+     >
+       <CardHeader>
+         <div className="flex items-center justify-between text-sm text-muted-foreground">
+-          <Badge variant="outline">Unit {unit.unitNumber}</Badge>
++          <Badge variant="outline">{formatCurriculumSegmentLabel(unit.unitNumber)}</Badge>
+           <span>
+             {unit.lessons.length} lesson{unit.lessons.length === 1 ? "" : "s"}
+exec
+/bin/bash -lc 'git diff -- __tests__/app/curriculum/page.test.tsx __tests__/app/student/dashboard/page.test.tsx __tests__/app/teacher/students/[studentId]/page.test.tsx __tests__/components/dashboard/NextLessonCard.test.tsx __tests__/components/teacher/CourseOverviewGrid.test.tsx' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+diff --git a/__tests__/app/curriculum/page.test.tsx b/__tests__/app/curriculum/page.test.tsx
+index 049d39c..32bc547 100644
+--- a/__tests__/app/curriculum/page.test.tsx
++++ b/__tests__/app/curriculum/page.test.tsx
+@@ -64,6 +64,33 @@ describe("CurriculumPage", () => {
+     expect(screen.getByText("2 lessons")).toBeInTheDocument();
+   });
+ 
++  it("labels the capstone as a distinct culminating experience instead of Unit 9", async () => {
++    mockQuery.mockResolvedValueOnce([
++      {
++        unitNumber: 9,
++        title: "Capstone: Investor-Ready Plan",
++        description: "Build the final investor-ready workbook and pitch.",
++        objectives: ["Defend one integrated business plan."],
++        lessons: [
++          {
++            id: "capstone-lesson",
++            title: "Capstone: Investor-Ready Plan",
++            slug: "capstone-investor-ready-plan",
+exec
+/bin/bash -lc "rg -n \"capstone|Unit 9|culminating|authored\" lib app components __tests__ conductor docs | sed -n '1,260p'" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+lib/test-utils/mock-factories.ts:227:        description: 'Overview of the capstone challenge',
+components/header-simple.tsx:53:              href="/capstone"
+components/teacher/UnitOverview.tsx:18:  capstoneConnection?: string;
+components/teacher/UnitOverview.tsx:21:export function UnitOverview({ lesson, gradeLevel = "9-12", course = "Business Operations", capstoneConnection }: UnitOverviewProps) {
+components/teacher/UnitOverview.tsx:50:  // Use provided capstone connection or build from unit content
+components/teacher/UnitOverview.tsx:51:  const capstoneText = capstoneConnection ||
+components/teacher/UnitOverview.tsx:53:    "This unit builds essential skills for the semester capstone project.";
+components/teacher/UnitOverview.tsx:215:              {capstoneText}
+__tests__/components/dashboard/NextLessonCard.test.tsx:47:  it('renders the capstone label instead of Unit 9 for the culminating lesson', () => {
+__tests__/components/dashboard/NextLessonCard.test.tsx:56:          slug: 'capstone-investor-ready-plan',
+__tests__/components/dashboard/NextLessonCard.test.tsx:63:    expect(screen.queryByText(/^Unit 9$/)).not.toBeInTheDocument();
+components/student/StudentUnitOverview.tsx:42:    tip: 'Complete the capstone lesson before the drill so you can narrate the end-to-end close process during review.'
+components/navigation-sidebar.tsx:42:  { title: 'Capstone', url: '/capstone', icon: GraduationCap },
+components/activities/quiz/FeedbackCollector.tsx:98: * - Supporting capstone project validation with authentic audiences
+__tests__/components/teacher/UnitOverview.test.tsx:149:  it('renders capstone connection', () => {
+__tests__/components/teacher/UnitOverview.test.tsx:150:    const customCapstone = 'This unit provides the foundation for creating complete financial models in the capstone project.';
+__tests__/components/teacher/UnitOverview.test.tsx:151:    render(<UnitOverview lesson={mockLesson} capstoneConnection={customCapstone} />);
+app/preface/page.tsx:53:  description: 'Check your understanding of the course structure and capstone.',
+app/preface/page.tsx:57:    description: 'Check your understanding of the course structure and capstone.',
+app/preface/page.tsx:80:        explanation: 'Formative checkpoints are 60%; summative capstone artifacts are 40%.',
+exec
+/bin/bash -lc "sed -n '1,220p' lib/curriculum/segment-labels.ts" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+export const CAPSTONE_UNIT_NUMBER = 9;
 
-Validate every tool call. If any fails, halt immediately and inform the user.
+export function isCapstoneUnitNumber(unitNumber: number) {
+  return unitNumber === CAPSTONE_UNIT_NUMBER;
+}
 
-## 1.0 Setup Check
+export function formatCurriculumSegmentLabel(unitNumber: number) {
+  return isCapstoneUnitNumber(unitNumber) ? 'Capstone' : `Unit ${unitNumber}`;
+}
 
-**PROTOCOL: Verify that the Conductor environment is properly set up.**
+export function formatCurriculumSegmentTitle(unitNumber: number, title: string) {
+  return isCapstoneUnitNumber(unitNumber) ? title : `${formatCurriculumSegmentLabel(unitNumber)}: ${title}`;
+}
 
-1.  **Verify Core Context:** Using the **Universal File Resolution Protocol**, resolve and verify the existence of:
-    -   **Product Definition**
-    -   **Tech Stack**
-    -   **Workflow**
+export function formatCurriculumSegmentLessonLabel(unitNumber: number, lessonNumber: number) {
+  const formattedLessonNumber = lessonNumber.toString().padStart(2, '0');
+  return `${formatCurriculumSegmentLabel(unitNumber)} • Lesson ${formattedLessonNumber}`;
+}
+exec
+/bin/bash -lc "sed -n '1,220p' lib/curriculum/generated/capstone-authored.ts" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+export interface AuthoredCapstoneMilestone {
+  title: string;
+  focus: string;
+  evidence: readonly string[];
+}
 
-2.  **Handle Failure:** If ANY of these are missing (or their resolved paths do not exist), Announce: "Conductor is not set up. Please run setup first." and HALT.
+export interface AuthoredCapstoneBlueprint {
+  workbookTemplate: string;
+  planningGuide: string;
+  pitchRubric: string;
+  modelTourChecklist: string;
+  audience: string;
+  finalPresentationExpectation: string;
+  reflectionPrompt: string;
+  milestones: readonly [AuthoredCapstoneMilestone, AuthoredCapstoneMilestone];
+}
 
-## 2.0 Track Initialization
- succeeded in 51ms:
+export const AUTHORED_CAPSTONE_BLUEPRINT: AuthoredCapstoneBlueprint = {
+  workbookTemplate: 'capstone_investor_ready_workbook.xlsx',
+  planningGuide: 'capstone_business_plan_guide.pdf',
+exec
+/bin/bash -lc 'git diff -- README.md .env.example conductor/tracks.md conductor/docs/architecture/runtime.md docs/security-api-route-matrix.md' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+diff --git a/.env.example b/.env.example
+index 16cb66f..3ba4885 100644
+--- a/.env.example
++++ b/.env.example
+@@ -4,7 +4,7 @@ NEXT_PUBLIC_CONVEX_URL=http://127.0.0.1:3210
+ 
+ # Required for server-side internal Convex calls in cloud/production environments.
+ # Keep this private (never expose via NEXT_PUBLIC_*).
+-# Local development can fall back to `.convex/local/*/config.json`.
++# Local development can fall back to Convex local state under `~/.convex/`.
+ CONVEX_DEPLOY_KEY=
+ 
+ # JWT signing secret for session cookies.
+diff --git a/README.md b/README.md
+index 22af126..f8ded20 100644
+--- a/README.md
++++ b/README.md
+@@ -60,7 +60,7 @@ Both accounts are intended for local development and test environments. Preview
+ 
+ 3. Start Convex locally:
+exec
+/bin/bash -lc "rg -n \"Unit 9\" app components __tests__ lib --glob '"'!conductor/**'"'" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+__tests__/components/dashboard/NextLessonCard.test.tsx:47:  it('renders the capstone label instead of Unit 9 for the culminating lesson', () => {
+__tests__/components/dashboard/NextLessonCard.test.tsx:63:    expect(screen.queryByText(/^Unit 9$/)).not.toBeInTheDocument();
+__tests__/components/teacher/CourseOverviewGrid.test.tsx:77:  it('labels the capstone column distinctly instead of Unit 9', () => {
+__tests__/components/teacher/CourseOverviewGrid.test.tsx:96:    expect(screen.queryByRole('link', { name: 'Unit 9' })).not.toBeInTheDocument();
+__tests__/app/teacher/students/[studentId]/page.test.tsx:288:    expect(screen.queryByText(/^Unit 9$/)).not.toBeInTheDocument();
+__tests__/app/curriculum/page.test.tsx:67:  it("labels the capstone as a distinct culminating experience instead of Unit 9", async () => {
+__tests__/app/curriculum/page.test.tsx:90:    expect(screen.queryByText(/^Unit 9$/)).not.toBeInTheDocument();
+__tests__/app/student/dashboard/page.test.tsx:178:    expect(screen.queryByText(/^Unit 9$/)).not.toBeInTheDocument();
+exec
+/bin/bash -lc 'rg -n "formatCurriculumSegmentLabel|formatCurriculumSegmentLessonLabel" lib app components __tests__' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 26ms:
+components/dashboard/NextLessonCard.tsx:12:import { formatCurriculumSegmentLabel } from '@/lib/curriculum/segment-labels';
+components/dashboard/NextLessonCard.tsx:39:                {formatCurriculumSegmentLabel(lesson.unitNumber)}
+components/teacher/CourseOverviewGrid.tsx:12:import { formatCurriculumSegmentLabel } from '@/lib/curriculum/segment-labels';
+components/teacher/CourseOverviewGrid.tsx:91:                  {formatCurriculumSegmentLabel(unit.unitNumber)}
+components/teacher/CourseOverviewGrid.tsx:138:                        aria-label={`${row.displayName} ${formatCurriculumSegmentLabel(unit.unitNumber)} — ${cellColorLabel(color)}`}
+components/teacher/TeacherLessonPlan.tsx:28:import { formatCurriculumSegmentLabel } from '@/lib/curriculum/segment-labels';
+components/teacher/TeacherLessonPlan.tsx:115:              {formatCurriculumSegmentLabel(lesson.unitNumber)} - Lesson {lessonNumber}
+lib/curriculum/segment-labels.ts:7:export function formatCurriculumSegmentLabel(unitNumber: number) {
+lib/curriculum/segment-labels.ts:12:  return isCapstoneUnitNumber(unitNumber) ? title : `${formatCurriculumSegmentLabel(unitNumber)}: ${title}`;
+lib/curriculum/segment-labels.ts:15:export function formatCurriculumSegmentLessonLabel(unitNumber: number, lessonNumber: number) {
+lib/curriculum/segment-labels.ts:17:  return `${formatCurriculumSegmentLabel(unitNumber)} • Lesson ${formattedLessonNumber}`;
+components/student/LessonRenderer.tsx:14:import { formatCurriculumSegmentLabel } from '@/lib/curriculum/segment-labels';
+components/student/LessonRenderer.tsx:174:            {formatCurriculumSegmentLabel(lesson.unitNumber)}
+components/student/PhaseHeader.tsx:18:  formatCurriculumSegmentLabel,
+components/student/PhaseHeader.tsx:19:  formatCurriculumSegmentLessonLabel,
+components/student/PhaseHeader.tsx:130:          {unit?.title ?? formatCurriculumSegmentLabel(lesson.unitNumber)}
+components/student/PhaseHeader.tsx:163:                    {formatCurriculumSegmentLessonLabel(
+app/teacher/students/[studentId]/page.tsx:17:import { formatCurriculumSegmentLabel } from '@/lib/curriculum/segment-labels';
+app/teacher/students/[studentId]/page.tsx:183:                          {formatCurriculumSegmentLabel(unit.unitNumber)}
+app/teacher/units/[unitNumber]/page.tsx:8:import { formatCurriculumSegmentLabel } from '@/lib/curriculum/segment-labels';
+codex
+The capstone-specific implementation is partly there: authored blueprint, manifest wiring, and several labeling regressions. I’m checking whether the supporting docs/plan markers and any remaining capstone surfaces are still incomplete before I edit files and run the gates.
+exec
+/bin/bash -lc "sed -n '1,260p' /home/daniel-bo/.codex/skills/conductor/references/implement.md" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
 # Implement Workflow
 
 Execute tasks from a track's plan following the TDD workflow.
 
 ## Prerequisites
@@ -237,649 +463,316 @@ Validate every tool call. If any fails, halt immediately and inform the user.
 
 2.  **Handle Failure:** If ANY of these are missing (or their resolved paths do not exist), Announce: "Conductor is not set up. Please run setup first." and HALT.
 
 ## 2.0 Track Selection
 exec
-/bin/bash -lc "sed -n '1,260p' conductor/tech-stack.md" in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc "sed -n '1,260p' conductor/product.md" in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 50ms:
-# Tech Stack: Math for Business Operations v2
-
-## Core Framework & Language
-- **Vinext (React 19 App Router runtime)**: Next-compatible routing and production builds powered by Vite/Vinext.
-- **TypeScript**: Ensuring type safety and robust developer experience across the codebase.
-
-## Frontend & Styling
-- **Tailwind CSS**: Utility-first CSS framework for rapid UI development.
-- **Radix UI / Shadcn UI**: Accessible, unstyled primitives for building high-quality design systems and components.
-- **Lucide React**: Consistent and clean iconography.
-- **Recharts**: Responsive charting library for student progress and financial data visualization.
-
-## Backend & Database
-- **Convex**: Source of truth for curriculum, progress, teacher/student data, realtime queries, and internal functions.
-- **Custom JWT Session Auth**: Username/password credentials stored in Convex with middleware-enforced role claims.
-- **Legacy Supabase/Drizzle Assets**: Historical migration/reference material only, not the active production data path.
-
-## Testing & Quality Assurance
-- **Vitest**: Fast, modern unit and component testing framework.
-- **Playwright**: Comprehensive end-to-end testing for critical user flows and accessibility.
- succeeded in 51ms:
-# Product Guide: Math for Business Operations v2
-
-## Initial Concept
-An interactive, Convex-backed digital textbook for teaching business mathematics to high school students. This App Router application provides a comprehensive curriculum covering accounting fundamentals, financial analysis, and practical business calculations.
-
-## Target Audience
-- **High School Students**: The primary learners engaging with the curriculum, activities, and simulations.
-- **Teachers**: Educators who manage the curriculum, monitor student progress, and utilize analytics to guide instruction.
-- **Administrators**: School or district staff managing organization-level access and data.
-
-## Core Value Proposition
-- **Interactive Learning**: Moves beyond static text with hands-on activities, drag-and-drop exercises, and simulations.
-- **Real-World Application**: Integrated Excel-like spreadsheet components and business simulations (e.g., cash flow, inventory) provide practical skills.
-- **Comprehensive Tracking**: Detailed analytics allow teachers to monitor student performance across six distinct learning phases.
-- **Role-Based Experience**: Tailored interfaces for students and teachers to maximize usability for each group.
-
-## Key Features
-- **Curriculum Delivery**: Structured "8 Units + Capstone" curriculum, utilizing granular, file-per-lesson infrastructure with three core instructional formats:
-    - **Six-Phase Learning Model**: Foundational and application lessons (Entry to Reflection).
-    - **Project Sprint Format**: Three-day immersive project build phases.
-2026-03-11T08:31:24.441086Z ERROR codex_core::models_manager::manager: failed to refresh available models: timeout waiting for child process to exit
-codex
-I’ve confirmed there’s no live track, so I need to create one. The directive is narrower than the backlog themes: start with lesson-phase components that match curriculum objectives, which points to a small instructional-component track rather than auth or assessment work.
-exec
-/bin/bash -lc 'ls -la bus-math-v2' in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc 'ls -la' in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc "rg -n \"phase 2|phase 3|guided practice|demonstration|componentKey|activity components|lesson phase\" bus-math-v2 -g '"'!**/node_modules/**'"'" in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc 'rg --files bus-math-v2 | rg "(lesson|curriculum|activity|component|phase|seed|author)"' in /home/daniel-bo/Desktop/bus-math-v2-experimental exited 2 in 51ms:
-ls: cannot access 'bus-math-v2': No such file or directory
- succeeded in 51ms:
-total 43728
-drwxrwxr-x  24 daniel-bo daniel-bo     4096 Mar  9 12:58 .
-drwxr-xr-x  26 daniel-bo daniel-bo     4096 Mar  8 06:36 ..
-drwxrwxr-x   3 daniel-bo daniel-bo     4096 Feb 25 20:25 .agents
-drwxrwxr-x   4 daniel-bo daniel-bo     4096 Mar  8 19:39 .claude
-drwxrwxr-x   3 daniel-bo daniel-bo     4096 Feb 25 21:01 .convex
--rw-rw-r--   1 daniel-bo daniel-bo     1958 Feb 25 22:32 .env
--rw-rw-r--   1 daniel-bo daniel-bo      509 Mar  8 15:08 .env.example
--rw-rw-r--   1 daniel-bo daniel-bo     2013 Feb 27 15:51 .env.local
--rw-rw-r--   1 daniel-bo daniel-bo       84 Feb 25 20:02 .git
-drwxrwxr-x   3 daniel-bo daniel-bo     4096 Feb 25 20:02 .github
--rw-rw-r--   1 daniel-bo daniel-bo      590 Feb 25 20:02 .gitignore
--rw-rw-r--   1 daniel-bo daniel-bo     2983 Feb 25 20:02 .project-migrate-manifest-20251105_211544.json
-drwxrwxr-x   3 daniel-bo daniel-bo     4096 Feb 25 20:02 .synthesisflow-backup-20251105-211545
--rw-rw-r--   1 daniel-bo daniel-bo      209 Feb 25 20:02 .vercelignore
--rw-rw-r--   1 daniel-bo daniel-bo     1104 Feb 25 22:16 AGENTS.md
--rw-rw-r--   1 daniel-bo daniel-bo       11 Feb 25 20:02 CLAUDE.md
--rw-rw-r--   1 daniel-bo daniel-bo     1088 Feb 25 20:02 LICENSE
--rw-rw-r--   1 daniel-bo daniel-bo    11774 Mar 11 12:39 README.md
-drwxrwxr-x  16 daniel-bo daniel-bo     4096 Mar 11 04:33 __tests__
- exited 2 in 51ms:
-rg: bus-math-v2: IO error for operation on bus-math-v2: No such file or directory (os error 2)
- exited 1 in 50ms:
-rg: bus-math-v2: IO error for operation on bus-math-v2: No such file or directory (os error 2)
-2026-03-11T08:31:45.570959Z ERROR codex_core::models_manager::manager: failed to refresh available models: timeout waiting for child process to exit
-codex
-The workspace root is the app itself; there isn’t a nested `bus-math-v2/` directory here. I’m treating the current repo as the allowed app scope and inspecting the existing lesson/activity system so the new track stays tightly bounded.
+/bin/bash -lc "rg -n \"Unit \\{.*unitNumber|Unit \\"'$'"\\{unit|Unit 9|formatCurriculumSegmentTitle|Capstone\" app components lib/__tests__ __tests__ lib | sed -n '1,260p'" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+rg: lib/__tests__: No such file or directory (os error 2)
+components/header-simple.tsx:56:              Capstone
+components/teacher/UnitLessonPlan.tsx:128:          Unit {lesson.unitNumber}: {lesson.title}
+__tests__/components/dashboard/NextLessonCard.test.tsx:47:  it('renders the capstone label instead of Unit 9 for the culminating lesson', () => {
+__tests__/components/dashboard/NextLessonCard.test.tsx:54:          title: 'Capstone: Investor-Ready Plan',
+__tests__/components/dashboard/NextLessonCard.test.tsx:62:    expect(screen.getByText(/^Capstone$/)).toBeInTheDocument();
+__tests__/components/dashboard/NextLessonCard.test.tsx:63:    expect(screen.queryByText(/^Unit 9$/)).not.toBeInTheDocument();
+__tests__/components/teacher/UnitOverview.test.tsx:150:    const customCapstone = 'This unit provides the foundation for creating complete financial models in the capstone project.';
+__tests__/components/teacher/UnitOverview.test.tsx:151:    render(<UnitOverview lesson={mockLesson} capstoneConnection={customCapstone} />);
+__tests__/components/teacher/UnitOverview.test.tsx:153:    expect(screen.getByText('Connection to Capstone Project')).toBeInTheDocument();
+__tests__/components/teacher/UnitOverview.test.tsx:154:    expect(screen.getByText(customCapstone)).toBeInTheDocument();
+__tests__/components/teacher/CourseOverviewGrid.test.tsx:77:  it('labels the capstone column distinctly instead of Unit 9', () => {
+__tests__/components/teacher/CourseOverviewGrid.test.tsx:92:    expect(screen.getByRole('link', { name: 'Capstone' })).toHaveAttribute(
+__tests__/components/teacher/CourseOverviewGrid.test.tsx:96:    expect(screen.queryByRole('link', { name: 'Unit 9' })).not.toBeInTheDocument();
+app/preface/page.tsx:338:            <h2 className="text-2xl font-semibold">8 Units + Capstone</h2>
+app/preface/page.tsx:357:                            Unit {unit.unitNumber}
+app/preface/page.tsx:374:            <Badge className="bg-blue-100 text-blue-800">Capstone</Badge>
+app/preface/page.tsx:375:            <h2 className="text-2xl font-semibold">Second-Semester Capstone: Investor-Ready</h2>
+app/preface/page.tsx:419:                <p className="text-xs text-muted-foreground mt-2">See Capstone guidelines and rubrics for full details.</p>
+app/preface/page.tsx:421:                  <Link href="/capstone" className="underline">Capstone Overview</Link>
 exec
-/bin/bash -lc "rg -n \"phase 2|phase 3|guided practice|demonstration|componentKey|activity component|lesson phase|Interactive\" . -g '"'!node_modules/**'"' -g '"'!dist/**'"'" in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 50ms:
-Total output lines: 293
+/bin/bash -lc 'git diff -- conductor/tracks/capstone_textbook_completion_20260314/plan.md conductor/tracks/capstone_textbook_completion_20260314/metadata.json' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
 
-./components/activities/quiz/FillInTheBlank.tsx:13:export type FillInTheBlankActivity = Omit<Activity, 'componentKey' | 'props'> & {
-./components/activities/quiz/FillInTheBlank.tsx:14:  componentKey: 'fill-in-the-blank';
-./components/activities/quiz/TieredAssessment.tsx:14:export type TieredAssessmentActivity = Omit<Activity, 'componentKey' | 'props'> & {
-./components/activities/quiz/TieredAssessment.tsx:15:  componentKey: 'tiered-assessment';
-./components/activities/quiz/PeerCritiqueForm.tsx:63:export type PeerCritiqueActivity = Omit<Activity, 'componentKey' | 'props'> & {
-./components/activities/quiz/PeerCritiqueForm.tsx:64:  componentKey: 'peer-critique-form'
-./components/teacher/UnitLessonPlan.tsx:668:                <h5 className="font-medium text-primary">Interactive Activities:</h5>
-./components/activities/quiz/ComprehensionCheck.tsx:13:export type ComprehensionCheckActivity = Omit<Activity, 'componentKey' | 'props'> & {
-./components/activities/quiz/ComprehensionCheck.tsx:14:  componentKey: 'comprehension-quiz';
-./components/activities/quiz/ReflectionJournal.tsx:19:type ReflectionJournalActivity = Omit<Activity, 'componentKey' | 'props'> & {
-./components/activities/quiz/ReflectionJournal.tsx:20:  componentKey: 'reflection-journal'
-./components/teacher/TeacherLessonPlan.tsx:50:    'Interactive instruction',
-./components/activities/simulations/LemonadeStand.tsx:116:export type LemonadeStandActivity = Omit<Activity, 'componentKey' | 'props'> & {
-./components/activities/simulations/LemonadeStand.tsx:117:  componentKey: 'lemonade-stand'
-./components/lesson/ActivityRenderer.tsx:268:  const ActivityComponent = getActivityComponent(activity.componentKey);
-./components/lesson/ActivityRenderer.tsx:275:            Activity component not found: {activity.componentKey}
-./components/activities/simulations/PitchPresentationBuilder.tsx:26: * 3. **Interactive Content Builder**:
-./fix_tests.log:189:     [33m[2m✓[22m[39m renders lesson phases when provided [33m 507[2mms[22m[39m
 exec
-/bin/bash -lc 'cat package.json' in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc "rg --files . -g '"'!node_modules/**'"' -g '"'!dist/**'"' | rg \"(lesson|curriculum|activity|component|phase|seed|author|registry)\"" in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 50ms:
-{
-  "private": true,
-  "scripts": {
-    "dev": "vinext dev",
-    "dev:stack": "node scripts/dev-stack.mjs",
-    "build": "vinext build",
-    "start": "vinext start",
-    "lint": "vinext lint",
-    "test": "vitest run",
-    "test:watch": "vitest watch"
-  },
-  "dependencies": {
-    "@hello-pangea/dnd": "^18.0.1",
-    "@radix-ui/react-avatar": "^1.1.11",
-    "@radix-ui/react-checkbox": "^1.3.1",
-    "@radix-ui/react-dropdown-menu": "^2.1.14",
-    "@radix-ui/react-label": "^2.1.6",
-    "@radix-ui/react-slot": "^1.2.2",
-    "@radix-ui/react-tabs": "^1.1.13",
-    "@supabase/ssr": "latest",
- succeeded in 51ms:
-./components/tutorial/fetch-data-steps.tsx
-./components/tutorial/sign-up-user-steps.tsx
-./components/tutorial/connect-supabase-steps.tsx
-./components/tutorial/code-block.tsx
-./components/tutorial/tutorial-step.tsx
-./components.json
-./components/dashboard/NextLessonCard.tsx
-./components/hero.tsx
-./components/auth/AuthProvider.tsx
-./components/header-simple.tsx
-./components/teacher/CourseOverviewGrid.tsx
-./components/teacher/TeacherCredentialsSheet.tsx
-./components/teacher/TeacherDashboardContent.tsx
-./components/teacher/GradebookGrid.tsx
-./components/teacher/TeacherCsvExportButton.tsx
-./components/teacher/TeacherCreateStudentDialog.tsx
-./components/teacher/UnitLessonPlan.tsx
-./components/teacher/TeacherLessonPlan.tsx
-./components/teacher/TeacherStudentActions.tsx
-./components/teacher/csvUtils.ts
-2026-03-11T08:32:04.680661Z ERROR codex_core::models_manager::manager: failed to refresh available models: timeout waiting for child process to exit
+/bin/bash -lc "rg -n \"formatCurriculumSegmentTitle\\(|formatCurriculumSegmentLessonLabel\\(\" app components lib __tests__" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 19ms:
+lib/curriculum/segment-labels.ts:11:export function formatCurriculumSegmentTitle(unitNumber: number, title: string) {
+lib/curriculum/segment-labels.ts:15:export function formatCurriculumSegmentLessonLabel(unitNumber: number, lessonNumber: number) {
+components/student/PhaseHeader.tsx:163:                    {formatCurriculumSegmentLessonLabel(
 codex
-I’ve narrowed this to the lesson-content layer rather than auth/runtime. Next I’m checking the curriculum templates and the current phase renderer so I can define one track that delivers an actual phase component improvement, not another infrastructure-only refactor.
+I don’t see the track plan/status updated yet, and there are still a few likely capstone-facing surfaces to sanity-check beyond the tests already added. I’m scanning those now so I can make one focused edit pass instead of chasing failures after the fact.
 exec
-/bin/bash -lc "sed -n '1,260p' docs/curriculum/templates/accounting.md" in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc "sed -n '1,260p' docs/curriculum/templates/excel.md" in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc "sed -n '1,320p' components/lesson/PhaseRenderer.tsx" in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc "sed -n '1,320p' app/student/lesson/[lessonSlug]/page.tsx" in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc "sed -n '1,260p' types/curriculum.ts" in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc "sed -n '1,260p' lib/curriculum/types.ts" in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
----
-lesson_id: "UXXLXX"
-type: "accounting"
-objectives:
-  - "Objective 1"
-  - "Objective 2"
-narrative_hook: "Sarah Chen's context for this accounting concept"
-standards:
-  primary: "ACC-X.X"
-assets:
-  starter_sheet: "starter_sheet_url"
-quality_rules:
-  guided_phase_requires_activity: true
-  independent_phase_requires_auto_grade: true
-  independent_min_passing_score: 60
-  auto_graded_requires_problem_template: true
----
-
-# Lesson: {{lesson_title}}
-
- succeeded in 51ms:
----
-lesson_id: "UXXLXX"
-type: "excel"
-objectives:
-  - "Objective 1"
-  - "Objective 2"
-narrative_hook: "Sarah Chen's context for this Excel workflow"
-standards:
-  primary: "ACC-X.X"
-assets:
-  starter_sheet: "starter_sheet_url"
-quality_rules:
-  guided_phase_requires_activity: true
-  independent_phase_requires_teacher_submission: true
-  checkpoint_min_passing_score: 80
-  checkpoint_requires_problem_template: true
----
-
-# Lesson: {{lesson_title}}
-
- succeeded in 50ms:
-'use client';
-
-import Image from 'next/image';
-import { contentBlockSchema, type ContentBlock } from '@/types/curriculum';
-import { VideoPlayer } from './VideoPlayer';
-import { Callout } from './Callout';
-import { MarkdownRenderer } from './MarkdownRenderer';
-import { ActivityRenderer } from './ActivityRenderer';
-import { ContentBlockErrorBoundary } from './ContentBlockErrorBoundary';
-
-interface PhaseRendererProps {
-  contentBlocks: ContentBlock[];
-  lessonId: string;
-  phaseNumber: number;
-}
-
-/**
- * PhaseRenderer iterates over phase.contentBlocks and renders each block
- * according to its type using discriminated union pattern.
- */
- succeeded in 51ms:
-import { notFound, redirect } from 'next/navigation';
-import { LessonRenderer } from '@/components/student/LessonRenderer';
-import { getServerSessionClaims } from '@/lib/auth/server';
-import { fetchInternalQuery, fetchQuery, api, internal } from '@/lib/convex/server';
-import type { ContentBlock } from '@/types/curriculum';
-
-// eslint-disable-next-line @typescript-eslint/no-explicit-any
-const apiAny = api as any;
+/bin/bash -lc "sed -n '1,260p' app/teacher/units/[unitNumber]/page.tsx" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+import { redirect, notFound } from 'next/navigation';
+import Link from 'next/link';
+import { ChevronLeft } from 'lucide-react';
+import { requireTeacherSessionClaims } from '@/lib/auth/server';
+import { fetchInternalQuery, internal } from '@/lib/convex/server';
+import { GradebookGrid } from '@/components/teacher/GradebookGrid';
+import type { GradebookLesson, GradebookRow } from '@/lib/teacher/gradebook';
+import { formatCurriculumSegmentLabel } from '@/lib/curriculum/segment-labels';
 
-interface LessonPageProps {
-  params: Promise<{
-    lessonSlug: string;
-  }>;
-  searchParams: Promise<{
-    phase?: string;
-  }>;
+interface PageProps {
+  params: Promise<{ unitNumber: string }>;
 }
 
-const PHASE_TYPE_BY_NUMBER = {
-  1: 'intro',
- succeeded in 51ms:
-import type { InferInsertModel, InferSelectModel } from 'drizzle-orm';
-import { lessons } from '@/lib/db/schema';
-import type { Phase as SchemaPhase, NewPhase as SchemaNewPhase } from '@/lib/db/schema/validators';
-import type {
-  LessonMetadata as SchemaLessonMetadata,
-  UnitAssessment as SchemaUnitAssessment,
-  UnitContent as SchemaUnitContent,
-  UnitDifferentiation as SchemaUnitDifferentiation,
-  UnitDrivingQuestion as SchemaUnitDrivingQuestion,
-  UnitResource as SchemaUnitResource,
-  UnitLearningSequence as SchemaUnitLearningSequence,
-  UnitObjectives as SchemaUnitObjectives,
-  UnitPrerequisites as SchemaUnitPrerequisites,
-  UnitStudentChoices as SchemaUnitStudentChoices,
-  UnitIntroduction as SchemaUnitIntroduction,
-} from '@/lib/db/schema/lessons';
-import type {
-  ContentBlock as SchemaContentBlock,
-  PhaseMetadata as SchemaPhaseMetadata,
-} from '@/lib/db/schema/phase-content';
- succeeded in 51ms:
-export type LessonType = 'accounting' | 'excel' | 'project' | 'assessment';
+export default async function UnitGradebookPage({ params }: PageProps) {
+  const { unitNumber: unitNumberParam } = await params;
+  const unitNumber = parseInt(unitNumberParam, 10);
 
-export type PhaseRequirement = {
-  phaseNumber: number;
-  titlePattern: RegExp;
-  requiresInteractiveActivity?: boolean;
-  requiresTeacherSubmission?: boolean;
-  requiresAutoGradedActivity?: {
-    minPassingScore: number;
-  };
-  requiresDeliverables?: boolean;
-};
-
-export type LessonTypeSpec = {
-  lessonType: LessonType;
-  phaseRequirements: PhaseRequirement[];
-  summary: string;
-};
-
-export const CURRICULUM_LESSON_TYPE_SPECS: Record<LessonType, LessonTypeSpec> = {
-2026-03-11T08:32:32.542702Z ERROR codex_core::models_manager::manager: failed to refresh available models: timeout waiting for child process to exit
+  if (Number.isNaN(unitNumber) || unitNumber < 1) {
+    notFound();
+  }
 exec
-/bin/bash -lc "sed -n '1,320p' components/student/LessonRenderer.tsx" in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc "sed -n '1,260p' components/student/PhaseHeader.tsx" in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc "sed -n '1,260p' components/student/PhaseFooter.tsx" in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc "sed -n '1,260p' __tests__/components/lesson/PhaseRenderer.test.tsx" in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc "sed -n '1,220p' __tests__/app/student/lesson/[lessonSlug]/page.test.tsx" in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 50ms:
-'use client';
-
-import { useEffect } from 'react';
-import { useRouter } from 'next/navigation';
-import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
-import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-import { usePhaseProgress } from '@/hooks/usePhaseProgress';
-import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
-import { Button } from '@/components/ui/button';
-import { cn } from '@/lib/utils';
-import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
-import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
-
-interface Phase {
-  id: string;
-  phaseNumber: number;
-  title: string;
-  contentBlocks?: ContentBlock[];
-  estimatedMinutes: number | null;
-  metadata?: PhaseMetadata;
- succeeded in 50ms:
-import Link from 'next/link';
+/bin/bash -lc "sed -n '1,260p' components/teacher/UnitLessonPlan.tsx" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"
+import { Badge } from "@/components/ui/badge"
 import {
-  ArrowRight,
+  Target,
+  CheckCircle,
   BookOpen,
-  CheckCircle2,
-  Home,
+  Clock,
+  Users,
+  AlertTriangle,
   Lightbulb,
-  PlayCircle,
-  Target,
-  Users
-} from 'lucide-react';
-
-import { Badge } from '@/components/ui/badge';
-import { Card, CardHeader, CardTitle } from '@/components/ui/card';
-import { Progress } from '@/components/ui/progress';
-import type { Lesson } from '@/lib/db/schema/validators';
-import type { ContentBlock, PhaseMetadata } from '@/types/curriculum';
-import { cn } from '@/lib/utils';
-import { ResourceBasePathFixer } from '@/components/ResourceBasePathFixer';
+  Info,
+  FileText,
+  Settings,
+  MessageSquare,
+  Code,
+  Flag,
+  TrendingUp,
+  Briefcase,
+  GraduationCap
+} from "lucide-react"
+exec
+/bin/bash -lc "sed -n '1,260p' components/teacher/TeacherLessonPlan.tsx" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+'use client'
 
- succeeded in 51ms:
-import Link from 'next/link';
+import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
+import { Badge } from "@/components/ui/badge"
+import { Button } from "@/components/ui/button"
 import {
-  ArrowLeft,
-  ArrowRight,
-  CheckCircle2,
+  Target,
+  CheckCircle,
+  BookOpen,
+  Clock,
+  Users,
+  Settings,
   Lightbulb,
+  FileText,
+  ChevronLeft,
+  ChevronRight,
   PlayCircle,
+  PenTool,
+  UserCheck,
+  Flag,
+exec
+/bin/bash -lc "sed -n '1,260p' components/teacher/UnitOverview.tsx" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
+import { Badge } from "@/components/ui/badge"
+import {
   Target,
+  CheckCircle,
+  BookOpen,
+  Clock,
   Users,
-  BookOpen
-} from 'lucide-react';
+  FileText,
+  TrendingUp
+} from "lucide-react"
+import { type Lesson } from '@/lib/db/schema/validators';
 
-import { Button } from '@/components/ui/button';
-import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-import type { Lesson } from '@/lib/db/schema/validators';
-import type { PhaseMetadata } from '@/types/curriculum';
-import { ResourceBasePathFixer } from '@/components/ResourceBasePathFixer';
+interface UnitOverviewProps {
+  lesson: Lesson;
+  gradeLevel?: string;
+  course?: string;
+  capstoneConnection?: string;
+}
 
-const phaseIcons: Record<string, typeof PlayCircle> = {
-  intro: PlayCircle,
- succeeded in 51ms:
-import { describe, it, expect, vi, beforeEach } from 'vitest';
-import { render, screen } from '@testing-library/react';
-import { PhaseRenderer } from '../../../components/lesson/PhaseRenderer';
-import type { ContentBlock } from '@/types/curriculum';
+exec
+/bin/bash -lc "sed -n '1,260p' conductor/docs/curriculum/status.md" in /home/daniel-bo/Desktop/bus-math-v2 exited 2 in 0ms:
+sed: can't read conductor/docs/curriculum/status.md: No such file or directory
+exec
+/bin/bash -lc "sed -n '1,260p' conductor/roadmap.md" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+# Product Roadmap: Complete Online Textbook
 
-// Mock the child components
-vi.mock('../../../components/lesson/VideoPlayer', () => ({
-  VideoPlayer: ({ videoUrl, duration }: { videoUrl: string; duration: number }) => (
-    <div data-testid="video-player">Video: {videoUrl} ({duration}min)</div>
-  ),
-}));
+## End State
 
-vi.mock('../../../components/lesson/Callout', () => ({
-  Callout: ({ variant, content }: { variant: string; content: string }) => (
-    <div data-testid="callout" data-variant={variant}>{content}</div>
-  ),
-}));
+The product is considered a complete online textbook only when all of the following are true:
 
-vi.mock('../../../components/lesson/MarkdownRenderer', () => ({
-  MarkdownRenderer: ({ content }: { content: string }) => (
- succeeded in 52ms:
-import { beforeEach, describe, expect, it, vi } from 'vitest';
-import { render, screen } from '@testing-library/react';
-import { notFound, redirect } from 'next/navigation';
+- all **88 instructional lessons** plus **capstone** exist as published Convex-backed runtime content
+- every lesson archetype renders correctly in the student experience
+- student progress works from phase -> lesson -> unit -> course
+- teacher monitoring works from course -> unit -> lesson -> student
+- the app is deployable and verified on **Cloudflare Workers**
+- active docs, seeds, and runtime behavior all describe the same curriculum
 
-import LessonPage from '../../../../../app/student/lesson/[lessonSlug]/page';
+## Roadmap Principles
 
-const { mockGetServerSessionClaims } = vi.hoisted(() => ({
-  mockGetServerSessionClaims: vi.fn(),
-}));
-const { mockFetchQuery } = vi.hoisted(() => ({
-  mockFetchQuery: vi.fn(),
-}));
-const { mockFetchInternalQuery } = vi.hoisted(() => ({
-  mockFetchInternalQuery: vi.fn(),
-}));
+1. Build the platform contracts once, then scale content.
+2. Get one textbook-quality vertical slice working before mass authoring.
+3. Ship teacher monitoring from the same progress model the student runtime uses.
+4. Treat content rollout as product work, not as an afterthought after the platform is “done”.
 
-vi.mock('next/navigation', () => ({
-  notFound: vi.fn(),
-  redirect: vi.fn(),
-}));
-2026-03-11T08:32:54.855935Z ERROR codex_core::models_manager::manager: failed to refresh available models: timeout waiting for child process to exit
-exec
-/bin/bash -lc "rg -n \"PhaseHeader|PhaseFooter\" . -g '"'!node_modules/**'"' -g '"'!dist/**'"'" in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
-./fix_tests.log:1036: [32m✓[39m __tests__/components/student/PhaseHeader.test.tsx [2m([22m[2m2 tests[22m[2m)[22m[33m 580[2mms[22m[39m
-./fix_tests.log:1052: [32m✓[39m __tests__/components/student/PhaseFooter.test.tsx [2m([22m[2m2 tests[22m[2m)[22m[33m 1565[2mms[22m[39m
-./components/student/PhaseFooter.tsx:28:export interface PhaseFooterNavigationOverrides {
-./components/student/PhaseFooter.tsx:43:export interface PhaseFooterProps {
-./components/student/PhaseFooter.tsx:47:  navigationOverrides?: PhaseFooterNavigationOverrides;
-./components/student/PhaseFooter.tsx:60:export function PhaseFooter({ lesson, phase, phases, navigationOverrides }: PhaseFooterProps) {
-./components/student/Lesson01Phase1.tsx:8:import { PhaseHeader } from './PhaseHeader';
-./components/student/Lesson01Phase1.tsx:9:import { PhaseFooter } from './PhaseFooter';
-./components/student/Lesson01Phase1.tsx:132:      <PhaseHeader lesson={lesson} unit={{ title: unit.title }} phase={phase} phases={phases} />
-./components/student/Lesson01Phase1.tsx:153:      <PhaseFooter lesson={lesson} phase={phase} phases={phases} />
-./components/student/PhaseHeader.tsx:76:export interface PhaseHeaderNavigationOverrides {
-./components/student/PhaseHeader.tsx:83:export interface PhaseHeaderProps {
-./components/student/PhaseHeader.tsx:90:  navigationOverrides?: PhaseHeaderNavigationOverrides;
-./components/student/PhaseHeader.tsx:93:export function PhaseHeader({ lesson, phase, phases, unit, navigationOverrides }: PhaseHeaderProps) {
-./__tests__/components/student/PhaseHeader.test.tsx:4:import { PhaseHeader } from '../../../components/student/PhaseHeader';
-./__tests__/components/student/PhaseHeader.test.tsx:13:describe('PhaseHeader', () => {
-./__tests__/components/student/PhaseHeader.test.tsx:27:    render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
-./__tests__/components/student/PhaseHeader.test.tsx:38:    render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
-./__tests__/components/student/PhaseFooter.test.tsx:4:import { PhaseFooter } from '../../../components/student/PhaseFooter';
-./__tests__/components/student/PhaseFooter.test.tsx:13:describe('PhaseFooter', () => {
+codex
+One exploratory read missed because `conductor/docs/curriculum/status.md` does not exist in this repo; that isn’t a blocker, so I’m using the existing roadmap/runtime Conductor docs as the authoritative curriculum status surface instead.
 exec
-/bin/bash -lc "sed -n '220,520p' app/student/lesson/[lessonSlug]/page.tsx" in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
-      createdAt: number;
-      sections: Array<{ _id: string; sectionType: string; content: unknown }>;
-    }) => ({
-      id: phase._id,
-      lessonId: lesson._id,
-      phaseNumber: phase.phaseNumber,
-      title: phase.title ?? fallbackPhaseTitle(phase.phaseNumber),
-      contentBlocks: phase.sections.map((s, idx) => toContentBlock(s, idx + 1)) as ContentBlock[],
-      estimatedMinutes: phase.estimatedMinutes,
-      metadata: {
-        phaseType:
-          PHASE_TYPE_BY_NUMBER[phase.phaseNumber as keyof typeof PHASE_TYPE_BY_NUMBER],
-      },
-      createdAt: new Date(phase.createdAt),
-      updatedAt: new Date(lesson.updatedAt),
-    }),
-  );
-
-  // Fetch user profile from Convex to check role
-  let userProfile: { role?: string } | null = null;
-2026-03-11T08:33:05.898961Z ERROR codex_core::models_manager::manager: failed to refresh available models: timeout waiting for child process to exit
+/bin/bash -lc "rg -n \"<UnitOverview|UnitOverview\\(|<UnitLessonPlan|UnitLessonPlan\\(\" app components lib __tests__" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+__tests__/components/teacher/UnitOverview.test.tsx:90:    render(<UnitOverview lesson={mockLesson} />);
+__tests__/components/teacher/UnitOverview.test.tsx:96:    render(<UnitOverview lesson={mockLesson} />);
+__tests__/components/teacher/UnitOverview.test.tsx:102:    render(<UnitOverview lesson={mockLesson} gradeLevel="9-12" course="Business Operations" />);
+__tests__/components/teacher/UnitOverview.test.tsx:110:    render(<UnitOverview lesson={mockLesson} />);
+__tests__/components/teacher/UnitOverview.test.tsx:117:    render(<UnitOverview lesson={mockLesson} />);
+__tests__/components/teacher/UnitOverview.test.tsx:125:    render(<UnitOverview lesson={mockLesson} />);
+__tests__/components/teacher/UnitOverview.test.tsx:133:    render(<UnitOverview lesson={mockLesson} />);
+__tests__/components/teacher/UnitOverview.test.tsx:141:    render(<UnitOverview lesson={mockLesson} />);
+__tests__/components/teacher/UnitOverview.test.tsx:151:    render(<UnitOverview lesson={mockLesson} capstoneConnection={customCapstone} />);
+__tests__/components/teacher/UnitOverview.test.tsx:164:    render(<UnitOverview lesson={minimalLesson} />);
+__tests__/components/teacher/UnitOverview.test.tsx:172:    render(<UnitOverview lesson={mockLesson} />);
+__tests__/components/teacher/UnitOverview.test.tsx:180:    render(<UnitOverview lesson={mockLesson} />);
+__tests__/components/teacher/UnitOverview.test.tsx:191:    render(<UnitOverview lesson={mockLesson} />);
+__tests__/components/teacher/UnitLessonPlan.test.tsx:78:    render(<UnitLessonPlan lesson={mockLesson} />);
+__tests__/components/teacher/UnitLessonPlan.test.tsx:85:    render(<UnitLessonPlan lesson={mockLesson} />);
+__tests__/components/teacher/UnitLessonPlan.test.tsx:91:    render(<UnitLessonPlan lesson={mockLesson} />);
+__tests__/components/teacher/UnitLessonPlan.test.tsx:98:    render(<UnitLessonPlan lesson={mockLesson} />);
+__tests__/components/teacher/UnitLessonPlan.test.tsx:108:    render(<UnitLessonPlan lesson={mockLesson} />);
+__tests__/components/teacher/UnitLessonPlan.test.tsx:119:    render(<UnitLessonPlan lesson={mockLesson} />);
+__tests__/components/teacher/UnitLessonPlan.test.tsx:125:    render(<UnitLessonPlan lesson={mockLesson} />);
 exec
-/bin/bash -lc "sed -n '1,260p' lib/db/schema/lessons.ts" in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc "rg -n \"type LessonMetadata|export const lessonMetadata|lessonType|learningObjectives|metadata:\" lib/db/schema app components __tests__ types -g '"'!node_modules/**'"'" in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
-import { integer, jsonb, pgTable, text, timestamp, uuid } from 'drizzle-orm/pg-core';
-import { z } from 'zod';
-
-const stringArray = z.array(z.string());
-
-export const unitResourceSchema = z.object({
-  title: z.string(),
-  url: z.string().url().optional(),
-  type: z.enum(['download', 'link', 'external'])
-});
-
-export const unitDrivingQuestionSchema = z.object({
-  question: z.string(),
-  context: z.string(),
-  scenario: z.string().optional()
-});
-
-export const unitObjectivesSchema = z.object({
-  content: stringArray,
-  skills: stringArray,
- succeeded in 50ms:
-lib/db/schema/validators.ts:51:  learningObjectives: z.array(z.string()).nullable().optional(),
-lib/db/schema/validators.ts:52:  metadata: lessonMetadataSchema.nullable().optional()
-lib/db/schema/validators.ts:56:  learningObjectives: z.array(z.string()).nullable(),
-lib/db/schema/validators.ts:57:  metadata: lessonMetadataSchema.nullable()
-lib/db/schema/validators.ts:70:  metadata: phaseMetadataSchema,
-lib/db/schema/validators.ts:99:  metadata: resourceMetadataSchema.nullable().optional()
-lib/db/schema/validators.ts:103:  metadata: resourceMetadataSchema.nullable()
-lib/db/schema/validators.ts:110:  metadata: profileMetadataSchema.nullable().optional()
-lib/db/schema/validators.ts:114:  metadata: profileMetadataSchema.nullable()
-lib/db/schema/validators.ts:138:  metadata: classMetadataSchema.nullable().optional()
-lib/db/schema/validators.ts:142:  metadata: classMetadataSchema.nullable()
-lib/db/schema/activity-submissions.ts:10:  metadata: z.record(z.string(), z.unknown()).optional(),
-lib/db/schema/profiles.ts:39:  metadata: jsonb('metadata').$type<ProfileMetadata | null>(),
-components/auth/AuthProvider.tsx:19:  metadata: Record<string, unknown> | null;
-lib/db/schema/lessons.ts:119:  learningObjectives: unitObjectivesSchema,
-lib/db/schema/lessons.ts:147:export const lessonMetadataSchema = z.object({
-lib/db/schema/lessons.ts:155:export type LessonMetadata = z.infer<typeof lessonMetadataSchema>;
-lib/db/schema/lessons.ts:163:  learningObjectives: jsonb('learning_objectives').$type<string[] | null>(),
-lib/db/schema/lessons.ts:165:  metadata: jsonb('metadata').$type<LessonMetadata | null>(),
-lib/db/schema/resources.ts:27:  metadata: jsonb('metadata').$type<ResourceMetadata | null>(),
+/bin/bash -lc "rg -n \"TeacherLessonPlan\\(|<TeacherLessonPlan|UnitLessonPlan\\(|<UnitLessonPlan|UnitOverview\\(|<UnitOverview\" app components lib __tests__" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+__tests__/components/teacher/UnitOverview.test.tsx:90:    render(<UnitOverview lesson={mockLesson} />);
+__tests__/components/teacher/UnitOverview.test.tsx:96:    render(<UnitOverview lesson={mockLesson} />);
+__tests__/components/teacher/UnitOverview.test.tsx:102:    render(<UnitOverview lesson={mockLesson} gradeLevel="9-12" course="Business Operations" />);
+__tests__/components/teacher/UnitOverview.test.tsx:110:    render(<UnitOverview lesson={mockLesson} />);
+__tests__/components/teacher/UnitOverview.test.tsx:117:    render(<UnitOverview lesson={mockLesson} />);
+__tests__/components/teacher/UnitOverview.test.tsx:125:    render(<UnitOverview lesson={mockLesson} />);
+__tests__/components/teacher/UnitOverview.test.tsx:133:    render(<UnitOverview lesson={mockLesson} />);
+__tests__/components/teacher/UnitOverview.test.tsx:141:    render(<UnitOverview lesson={mockLesson} />);
+__tests__/components/teacher/UnitOverview.test.tsx:151:    render(<UnitOverview lesson={mockLesson} capstoneConnection={customCapstone} />);
+__tests__/components/teacher/UnitOverview.test.tsx:164:    render(<UnitOverview lesson={minimalLesson} />);
+__tests__/components/teacher/UnitOverview.test.tsx:172:    render(<UnitOverview lesson={mockLesson} />);
+__tests__/components/teacher/UnitOverview.test.tsx:180:    render(<UnitOverview lesson={mockLesson} />);
+__tests__/components/teacher/UnitOverview.test.tsx:191:    render(<UnitOverview lesson={mockLesson} />);
+__tests__/components/teacher/TeacherLessonPlan.test.tsx:81:    render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+__tests__/components/teacher/TeacherLessonPlan.test.tsx:88:    render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+__tests__/components/teacher/TeacherLessonPlan.test.tsx:94:    render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+__tests__/components/teacher/TeacherLessonPlan.test.tsx:102:    render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+__tests__/components/teacher/TeacherLessonPlan.test.tsx:110:    render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+__tests__/components/teacher/TeacherLessonPlan.test.tsx:118:    render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+__tests__/components/teacher/TeacherLessonPlan.test.tsx:125:    render(<TeacherLessonPlan lesson={mockLesson} phases={mockPhases} lessonNumber={1} />);
 exec
-/bin/bash -lc "sed -n '1,260p' lib/db/schema/validators.ts" in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc "sed -n '1,220p' __tests__/curriculum/lesson-type-specs.test.ts" in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
-import { createInsertSchema, createSelectSchema } from 'drizzle-zod';
-import { z } from 'zod';
-
-import { resolveActivityComponentKey } from '@/lib/activities/component-keys';
-import { activities, activityPropsSchemas, gradingConfigSchema } from './activities';
-import { activitySubmissions, submissionDataSchema } from './activity-submissions';
-import { classEnrollments } from './class-enrollments';
-import { classes, classMetadataSchema } from './classes';
-import { contentRevisions, validationErrorSchema } from './content-revisions';
-import { lessons, lessonMetadataSchema } from './lessons';
-import { liveResponses, liveResponseAnswerSchema } from './live-responses';
-import { liveSessions, sessionSettingsSchema } from './live-sessions';
-import { contentBlockSchema, phaseMetadataSchema } from './phase-content';
-import { profiles, profileMetadataSchema } from './profiles';
-import { resources, resourceMetadataSchema } from './resources';
-import { sessionLeaderboard } from './session-leaderboard';
-import { studentProgress } from './student-progress';
-
-const jsonRecordSchema = z.record(z.string(), z.unknown());
-
- succeeded in 51ms:
-import { describe, expect, it } from 'vitest';
-
-import {
-  CURRICULUM_LESSON_TYPE_SPECS,
-  getLessonTypeFromOrderIndex,
-} from '@/lib/curriculum/types';
-
-describe('curriculum/lesson-type-specs', () => {
-  it('maps Unit 1 lesson order ranges to lesson types', () => {
-    expect(getLessonTypeFromOrderIndex(1)).toBe('accounting');
-    expect(getLessonTypeFromOrderIndex(5)).toBe('excel');
-    expect(getLessonTypeFromOrderIndex(8)).toBe('project');
-    expect(getLessonTypeFromOrderIndex(11)).toBe('assessment');
-  });
+/bin/bash -lc "sed -n '1,260p' __tests__/components/teacher/UnitOverview.test.tsx" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+import { describe, it, expect } from 'vitest';
+import { render, screen } from '@testing-library/react';
+import { UnitOverview } from '../../../components/teacher/UnitOverview';
+import { type Lesson } from '@/lib/db/schema/validators';
 
-  it('defines required phase rules for each lesson type', () => {
-    expect(CURRICULUM_LESSON_TYPE_SPECS.accounting.phaseRequirements).toHaveLength(6);
-    expect(CURRICULUM_LESSON_TYPE_SPECS.excel.phaseRequirements).toHaveLength(6);
-    expect(CURRICULUM_LESSON_TYPE_SPECS.project.phaseRequirements).toHaveLength(1);
-    expect(CURRICULUM_LESSON_TYPE_SPECS.assessment.phaseRequirements.length).toBeGreaterThanOrEqual(4);
-2026-03-11T08:33:21.843565Z ERROR codex_core::models_manager::manager: failed to refresh available models: timeout waiting for child process to exit
+describe('UnitOverview', () => {
+  const mockLesson: Lesson = {
+    id: '123e4567-e89b-12d3-a456-426614174000',
+    unitNumber: 1,
+    title: 'Balance by Design',
+    slug: 'balance-by-design',
+    description: 'Learn the fundamental accounting equation and how assets, liabilities, and equity work together.',
+    learningObjectives: [
+      'Understand the accounting equation',
+      'Identify assets, liabilities, and equity',
+      'Apply the accounting equation to real-world scenarios'
+    ],
+    orderIndex: 1,
+    metadata: {
+      duration: 120,
 exec
-/bin/bash -lc "sed -n '1,220p' conductor/archive/activity_component_contract_refactor_20260311/plan.md" in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
-# Implementation Plan
-
-## Phase 1: Contract Coverage Tests
-- [x] Task: Add failing tests for centralized activity catalog behavior
-  - [x] Add runtime registry tests for canonical key + alias resolution
-  - [x] Add validation/schema tests that cover components currently missing from the manual union
-  - [x] Add a docs-catalog regression test for `docs/components.yaml` coverage
-
-## Phase 2: Catalog Consolidation
-- [x] Task: Centralize activity component metadata
-  - [x] Implement canonical key + alias metadata in the activity registry layer
-  - [x] Refactor runtime resolution helpers to use the centralized metadata
-- [x] Task: Remove schema drift
-  - [x] Refactor activity validation/schema utilities to derive supported props from the centralized catalog
-  - [x] Preserve existing canonical seed keys and documented aliases
-
-## Phase 3: Verification and Closeout
-- [x] Task: Run lint, automated tests, and production build
-- [x] Task: Update README and Conductor memory files with the refactor outcome
-- [x] Task: Archive the track and finalize registry/metadata updates
+/bin/bash -lc 'rg -n "gradebook|CourseOverviewGrid|student detail|Teacher student detail|CurriculumUnitCard|NextLessonCard|PhaseHeader" __tests__ app components' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 72ms:
+components/dashboard/NextLessonCard.tsx:15:interface NextLessonCardProps {
+components/dashboard/NextLessonCard.tsx:22:export function NextLessonCard({
+components/dashboard/NextLessonCard.tsx:27:}: NextLessonCardProps) {
+__tests__/components/dashboard/NextLessonCard.test.tsx:4:import { NextLessonCard } from '@/components/dashboard/NextLessonCard';
+__tests__/components/dashboard/NextLessonCard.test.tsx:6:describe('NextLessonCard', () => {
+__tests__/components/dashboard/NextLessonCard.test.tsx:9:      <NextLessonCard
+__tests__/components/dashboard/NextLessonCard.test.tsx:34:      <NextLessonCard
+__tests__/components/dashboard/NextLessonCard.test.tsx:49:      <NextLessonCard
+components/teacher/CourseOverviewGrid.tsx:6:import { cellBgClass, cellColorLabel, sortRowsByName, applyStudentRowUpdate } from '@/lib/teacher/gradebook';
+components/teacher/CourseOverviewGrid.tsx:14:interface CourseOverviewGridProps {
+components/teacher/CourseOverviewGrid.tsx:21:export function CourseOverviewGrid({ rows, units }: CourseOverviewGridProps) {
+components/teacher/CourseOverviewGrid.tsx:32:        No students found in this gradebook.
+components/teacher/CourseOverviewGrid.tsx:80:            {/* Unit column headers — each links to the unit gradebook */}
+components/teacher/CourseOverviewGrid.tsx:123:                {/* Unit cells — each links to the Level 2 unit gradebook (spec: clicking a cell
+app/teacher/gradebook/page.tsx:6:import { CourseOverviewGrid } from '@/components/teacher/CourseOverviewGrid';
+app/teacher/gradebook/page.tsx:10:  const claims = await requireTeacherSessionClaims('/teacher/gradebook');
+app/teacher/gradebook/page.tsx:39:            Click a unit header to view the lesson-level gradebook.
+app/teacher/gradebook/page.tsx:43:        <section aria-label="Course overview gradebook">
+app/teacher/gradebook/page.tsx:44:          <CourseOverviewGrid rows={rows} units={units} />
+components/teacher/TeacherDashboardContent.tsx:30:import { CourseOverviewGrid } from "./CourseOverviewGrid";
+codex
+I have enough context to start verification-driven edits. I’m running the capstone-focused test slice now to see whether the current implementation already satisfies the new contract or if there are still gaps to patch.
 exec
-/bin/bash -lc "sed -n '1,220p' conductor/archive/student_lesson_access_boundary_cleanup_20260311/plan.md" in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc "sed -n '1,220p' conductor/archive/published_progress_viewmodel_consolidation_20260311/plan.md" in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
-# Implementation Plan: Student Write Boundary Hardening
-
-## Phase 1: Boundary Spec And Red Tests
+/bin/bash -lc 'npm test -- --runInBand __tests__/curriculum/published-manifest.test.ts __tests__/app/curriculum/page.test.tsx __tests__/app/student/dashboard/page.test.tsx __tests__/app/teacher/students/[studentId]/page.test.tsx __tests__/components/dashboard/NextLessonCard.test.tsx __tests__/components/teacher/CourseOverviewGrid.test.tsx __tests__/components/teacher/TeacherLessonPlan.test.tsx __tests__/components/teacher/UnitOverview.test.tsx __tests__/components/teacher/UnitLessonPlan.test.tsx __tests__/components/student/PhaseHeader.test.tsx __tests__/app/teacher/units/[unitNumber]/page.test.tsx' in /home/daniel-bo/Desktop/bus-math-v2 exited 1 in 1.20s:
 
-- [x] Add failing negative tests that prove teacher/admin request claims are rejected by student-write routes before any learner-data mutation runs.
-- [x] Extend route coverage for student-write endpoints to pin the expected `403` response shape and keep student success paths intact.
+> test
+> vitest run --runInBand __tests__/curriculum/published-manifest.test.ts __tests__/app/curriculum/page.test.tsx __tests__/app/student/dashboard/page.test.tsx __tests__/app/teacher/students/[studentId]/page.test.tsx __tests__/components/dashboard/NextLessonCard.test.tsx __tests__/components/teacher/CourseOverviewGrid.test.tsx __tests__/components/teacher/TeacherLessonPlan.test.tsx __tests__/components/teacher/UnitOverview.test.tsx __tests__/components/teacher/UnitLessonPlan.test.tsx __tests__/components/student/PhaseHeader.test.tsx __tests__/app/teacher/units/[unitNumber]/page.test.tsx
 
-## Phase 2: Hardening And Integration
+file:///home/daniel-bo/Desktop/bus-math-v2/node_modules/vitest/dist/chunks/cac.BGonGPac.js:404
+          throw new CACError(`Unknown option \`${name.length > 1 ? `--${name}` : `-${name}`}\``);
+                ^
 
-- [x] Add shared request-level auth helpers in `lib/auth/server.ts` for student-only API writes.
-- [x] Apply the new student-only guard to `app/api/phases/complete/route.ts`, `app/api/progress/assessment/route.ts`, `app/api/activities/spreadsheet/[activityId]/draft/route.ts`, and `app/api/activities/spreadsheet/[activityId]/submit/route.ts`.
-- [x] Keep teacher/admin spreadsheet replay reads working while preventing teacher/admin writes.
+CACError: Unknown option `--runInBand`
+    at Command.checkUnknownOptions (file:///home/daniel-bo/Desktop/bus-math-v2/node_modules/vitest/dist/chunks/cac.BGonGPac.js:404:17)
+    at CAC.runMatchedCommand (file:///home/daniel-bo/Desktop/bus-math-v2/node_modules/vitest/dist/chunks/cac.BGonGPac.js:604:13)
+    at CAC.parse (file:///home/daniel-bo/Desktop/bus-math-v2/node_modules/vitest/dist/chunks/cac.BGonGPac.js:545:12)
+    at file:///home/daniel-bo/Desktop/bus-math-v2/node_modules/vitest/dist/cli.js:28:13
+    at ModuleJob.run (node:internal/modules/esm/module_job:370:25)
+    at async onImport.tracePromise.__proto__ (node:internal/modules/esm/loader:665:26)
+    at async asyncRunEntryPointWithESMLoader (node:internal/modules/run_main:99:5)
 
-## Phase 3: Docs, Verification, And Closeout
+Node.js v24.4.0
+codex
+The first test invocation failed because this repo’s Vitest CLI does not accept `--runInBand`. I’m rerunning the same capstone-focused slice with the repo’s supported flags only, then I’ll patch whatever actually breaks.
+exec
+/bin/bash -lc 'npm test -- __tests__/curriculum/published-manifest.test.ts __tests__/app/curriculum/page.test.tsx __tests__/app/student/dashboard/page.test.tsx __tests__/app/teacher/students/[studentId]/page.test.tsx __tests__/components/dashboard/NextLessonCard.test.tsx __tests__/components/teacher/CourseOverviewGrid.test.tsx __tests__/components/teacher/TeacherLessonPlan.test.tsx __tests__/components/teacher/UnitOverview.test.tsx __tests__/components/teacher/UnitLessonPlan.test.tsx __tests__/components/student/PhaseHeader.test.tsx __tests__/app/teacher/units/[unitNumber]/page.test.tsx' in /home/daniel-bo/Desktop/bus-math-v2 exited 1 in 43.42s:
 
-- [x] Update README plus impacted security/Conductor memory docs to document the student-write boundary hardening and recorded follow-on auth debt.
-- [x] Run `CI=true npm run lint`, `CI=true npm test`, and `CI=true npm run build`, then resolve any issues.
-- [x] Archive the completed track, update `conductor/tracks.md`, commit with the model name/version in the message, and push the branch.
- succeeded in 51ms:
-# Implementation Plan
+> test
+> vitest run __tests__/curriculum/published-manifest.test.ts __tests__/app/curriculum/page.test.tsx __tests__/app/student/dashboard/page.test.tsx __tests__/app/teacher/students/[studentId]/page.test.tsx __tests__/components/dashboard/NextLessonCard.test.tsx __tests__/components/teacher/CourseOverviewGrid.test.tsx __tests__/components/teacher/TeacherLessonPlan.test.tsx __tests__/components/teacher/UnitOverview.test.tsx __tests__/components/teacher/UnitLessonPlan.test.tsx __tests__/components/student/PhaseHeader.test.tsx __tests__/app/teacher/units/[unitNumber]/page.test.tsx
 
-## Phase 1: Shared Progress Regression Tests
-- [x] Task: Add failing tests for published unit progress row assembly
-  - [x] Add helper-level tests that cover versioned titles/descriptions, published-only phase counts, and completed-phase percentage math
-  - [x] Add a regression test that proves teacher and student consumers can share the same unit row builder contract
-- [x] Task: Add failing tests for lesson phase status assembly
-  - [x] Add helper-level tests for `completed`, `current`, `available`, and `locked` transitions
-  - [x] Add a regression test that covers in-progress timestamps and ordered phase output
 
-## Phase 2: Shared Helper Refactor
-- [x] Task: Centralize published unit progress row assembly
-  - [x] Implement shared helper(s) in the published progress layer
-  - [x] Refactor `convex/student.ts` dashboard aggregation and `convex/teacher.ts` student-detail aggregation to use the shared helper(s)
-- [x] Task: Centralize lesson phase status assembly
-  - [x] Implement a reusable phase-status helper for published lesson phases
-  - [x] Refactor `convex/student.ts` lesson-progress query to use the shared helper
+ RUN  v4.0.16 /home/daniel-bo/Desktop/bus-math-v2
 
-## Phase 3: Verification and Closeout
-- [x] Task: Run lint, automated tests, and production build
+[baseline-browser-mapping] The data in this module is over two months old.  To ensure accurate Baseline data, please update: `npm i baseline-browser-mapping@latest -D`
+ ✓ __tests__/app/teacher/students/[studentId]/page.test.tsx (7 tests) 4423ms
+     ✓ redirects unauthenticated users to login  2904ms
+     ✓ renders org-scoped student details  1150ms
+ ✓ __tests__/components/teacher/TeacherLessonPlan.test.tsx (19 tests) 4804ms
+     ✓ renders lesson title and number  369ms
+     ✓ renders teaching strategies  345ms
+     ✓ displays phase estimated minutes  366ms
+     ✓ calls onNavigate when navigation buttons clicked  940ms
+     ✓ renders teacher guidance for phases  385ms
+     ✓ renders all six phase numbers with correct icons  558ms
+ ✓ __tests__/components/teacher/CourseOverviewGrid.test.tsx (15 tests) 3647ms
+     ✓ renders student names  537ms
 codex
-I have a concrete track now: tighten the student lesson phase shell so each phase shows phase-appropriate guidance and success criteria derived from the curriculum model, instead of rendering as generic content blocks. I’m checking the existing lesson-renderer tests before I write the new red tests.
+The focused slice mostly passes. The one current failure is a stale `PhaseHeader` expectation from the new shared label formatting, and I’ve also confirmed two teacher-facing components still hardcode `Unit {n}` and should be brought onto the same helper before I run the full gates.
 exec
-/bin/bash -lc "sed -n '1,360p' __tests__/components/student/LessonRenderer.test.tsx" in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc "sed -n '1,220p' __tests__/utils/lessonBuilders.ts" in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
-/* eslint-disable @typescript-eslint/no-explicit-any */
+/bin/bash -lc "sed -n '1,260p' __tests__/components/teacher/TeacherLessonPlan.test.tsx" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
 import { describe, it, expect, vi } from 'vitest';
 import { render, screen } from '@testing-library/react';
-import { LessonRenderer } from '../../../components/student/LessonRenderer';
-import { usePhaseProgress } from '../../../hooks/usePhaseProgress';
-import { usePhaseCompletion } from '../../../hooks/usePhaseCompletion';
-import type { ContentBlock } from '@/types/curriculum';
-
-vi.mock('next/navigation', () => ({
-  useRouter: () => ({
-    push: vi.fn(),
-  }),
-}));
-
-// Mock usePhaseProgress hook
-vi.mock('@/hooks/usePhaseProgress', () => ({
-  usePhaseProgress: vi.fn(() => ({
-    data: null,
-    isLoading: false,
-    refetch: vi.fn(),
- succeeded in 51ms:
-import type { Lesson, Phase } from '@/lib/db/schema/validators';
-import type { ContentBlock, PhaseMetadata } from '@/types/curriculum';
-
-type PartialDeep<T> = {
-  [K in keyof T]?: T[K] extends object ? PartialDeep<T[K]> : T[K];
-};
-
-const baseDate = new Date('2024-01-01T00:00:00Z');
-
-export const createLesson = (overrides: PartialDeep<Lesson> = {}): Lesson => {
-  const {
-    metadata: metadataOverrides,
-    createdAt,
-    updatedAt,
-    ...rest
-  } = overrides;
+import userEvent from '@testing-library/user-event';
+import { TeacherLessonPlan } from '../../../components/teacher/TeacherLessonPlan';
+import { type Lesson, type Phase } from '@/lib/db/schema/validators';
 
-  return {
-    id: 'lesson-001',
+describe('TeacherLessonPlan', () => {
+  const mockLesson: Lesson = {
+    id: '123e4567-e89b-12d3-a456-426614174000',
     unitNumber: 1,
+    title: 'Introduction to Accounting Equation',
+    slug: 'intro-to-accounting-equation',
+    description: 'Students will learn the fundamental accounting equation and how to apply it to real business scenarios.',
+    learningObjectives: [
+      'Identify the key components of the accounting equation',
+      'Explain why accurate financial records are essential',
+      'Apply the accounting equation to business transactions'
+    ],
+    orderIndex: 1,
+    metadata: {
 exec
-/bin/bash -lc "sed -n '1,260p' __tests__/components/student/PhaseHeader.test.tsx" in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
+/bin/bash -lc "sed -n '1,220p' __tests__/components/student/PhaseHeader.test.tsx" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
 import { render, screen } from '@testing-library/react';
 
 import { type Lesson, type Phase } from '@/lib/db/schema/validators';
 import { PhaseHeader } from '../../../components/student/PhaseHeader';
 import { createLesson, createPhase } from '@/__tests__/utils/lessonBuilders';
@@ -896,31749 +789,23935 @@ describe('PhaseHeader', () => {
     unitNumber: 3,
     orderIndex: 2,
     metadata: {
       duration: 120,
       tags: ['ledger', 'workflow', 'quality review']
-codex
-I’m creating the single active track now, with a scope that stays inside the directive: student lesson phase guidance and rendering alignment. After that I’ll start in the red phase with tests before touching implementation.
 exec
-/bin/bash -lc 'date -u +"%Y-%m-%dT%H:%M:%SZ"' in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
-2026-03-11T08:34:26Z
-file update
-A /home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-# Specification
-
-## Overview
-
-Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-
-## Functional Requirements
-
-1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-5. Existing student phase progress, completion, and navigation behavior must remain intact.
-
-## Non-Functional Requirements
-
-1. New curriculum guidance logic must be covered by automated tests.
-2. The student lesson rendering changes must be regression-tested at the component level.
-3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-
-## Acceptance Criteria
-
-- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-
-## Out of Scope
-
-- New interactive activity types or Convex persistence changes.
-- Curriculum seed rewrites beyond what is required to support the guidance shell.
-- Teacher-facing lesson-plan changes.
-A /home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-# Implementation Plan
-
-## Phase 1: Phase Guidance Red Tests
-- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-- [ ] Task: Add failing tests for the student phase shell
-  - [ ] Add a focused component test for the reusable phase guidance card
-  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-
-## Phase 2: Objective-Aware Student Phase Shell
-- [ ] Task: Implement shared curriculum phase-guidance helpers
-  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-- [ ] Task: Render the reusable student phase guidance shell
-  - [ ] Add a dedicated student-facing phase guidance component
-  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
+/bin/bash -lc "sed -n '1,260p' __tests__/components/teacher/UnitLessonPlan.test.tsx" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+import { describe, it, expect } from 'vitest';
+import { render, screen } from '@testing-library/react';
+import { UnitLessonPlan } from '../../../components/teacher/UnitLessonPlan';
+import { type Lesson } from '@/lib/db/schema/validators';
 
-## Phase 3: Verification and Closeout
-- [ ] Task: Run lint and automated tests for the track scope
-- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-- [ ] Task: Archive the track and finalize registry/metadata updates
-A /home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-{
-  "track_id": "lesson_phase_objective_shell_20260311",
-  "type": "feature",
-  "status": "in_progress",
-  "created_at": "2026-03-11T08:34:26Z",
-  "updated_at": "2026-03-11T08:34:26Z",
-  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-  "estimated_tasks": 7,
-  "actual_tasks": null,
-  "deviation_notes": ""
-}
-A /home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-# Track Index
+describe('UnitLessonPlan', () => {
+  const mockLesson: Lesson = {
+    id: '123e4567-e89b-12d3-a456-426614174000',
+    unitNumber: 1,
+    title: 'Balance by Design',
+    slug: 'balance-by-design',
+    description: 'Learn the fundamental accounting equation',
+    learningObjectives: null,
+    orderIndex: 1,
+    metadata: {
+      duration: 120,
+      durationLabel: '3-4 weeks',
+      difficulty: 'beginner',
+      tags: ['accounting'],
+      unitContent: {
+exec
+/bin/bash -lc "sed -n '1,260p' __tests__/app/teacher/units/[unitNumber]/page.test.tsx" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+import { beforeEach, describe, expect, it, vi } from 'vitest';
+import { notFound, redirect } from 'next/navigation';
 
-- [Specification](./spec.md)
-- [Implementation Plan](./plan.md)
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/tracks.md
-@@ -8,2 +8,6 @@
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
-apply_patch(auto_approved=true) exited 0 in 20ms:
-Success. Updated the following files:
-A /home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-A /home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-A /home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-A /home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/tracks.md
-file update:
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
+const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+  mockRequireTeacherSessionClaims: vi.fn(),
+  mockFetchInternalQuery: vi.fn(),
+}));
 
-file update:
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
+vi.mock('next/navigation', () => ({
+  redirect: vi.fn((path: string) => {
+    throw new Error(`NEXT_REDIRECT:${path}`);
+  }),
+  notFound: vi.fn(() => {
+    throw new Error('NEXT_NOT_FOUND');
+  }),
+}));
 
+vi.mock('@/lib/auth/server', () => ({
+  requireTeacherSessionClaims: mockRequireTeacherSessionClaims,
+}));
 codex
-I’ve created the track and marked it active. Now I’m in the red phase: adding failing tests for the new curriculum guidance helper and the updated student lesson shell before I change application code.
+I’m patching the remaining hardcoded unit labels now. The edits are limited to the shared teacher/unit presentation components plus the affected tests, so the capstone label contract is enforced consistently before I rerun the slice.
 file update
-A /home/daniel-bo/Desktop/bus-math-v2-experimental/__tests__/components/student/PhaseGuidanceCard.test.tsx
-import { render, screen } from '@testing-library/react';
-
-import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
-
-describe('PhaseGuidanceCard', () => {
-  it('renders phase goal, success criteria, and lesson objectives', () => {
-    render(
-      <PhaseGuidanceCard
-        guidance={{
-          lessonType: 'accounting',
-          phaseNumber: 3,
-          phaseLabel: 'Guided Practice',
-          goal: 'Practice the accounting move with teacher support.',
-          successCriteria: [
-            'Complete the interactive task with your group.',
-            'Explain why the accounting move is correct.',
-          ],
-        }}
-        learningObjectives={[
-          'Classify transaction effects on the accounting equation.',
-          'Use evidence from Sarah Chen’s store records.',
-        ]}
-      />,
-    );
-
-    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
-  });
-
-  it('hides the lesson-objectives section when none are provided', () => {
-    render(
-      <PhaseGuidanceCard
-        guidance={{
-          lessonType: 'excel',
-          phaseNumber: 2,
-          phaseLabel: 'Intro',
-          goal: 'Demonstrate the spreadsheet workflow.',
-          successCriteria: ['Name the quality checks before practicing.'],
-        }}
-        learningObjectives={null}
-      />,
-    );
-
-    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
-  });
-});
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/__tests__/components/student/LessonRenderer.test.tsx
-@@ -47,2 +47,18 @@
+M /home/daniel-bo/Desktop/bus-math-v2/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -83,2 +83,17 @@
  
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
 +
- describe('LessonRenderer', () => {
-@@ -161,3 +177,5 @@
+   it('renders Stage 1: Desired Results section', () => {
+M /home/daniel-bo/Desktop/bus-math-v2/__tests__/components/student/PhaseHeader.test.tsx
+@@ -29,3 +29,3 @@
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+@@ -34,2 +34,16 @@
  
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-   });
-@@ -185,4 +203,5 @@
- 
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-   });
-@@ -212,4 +231,5 @@
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-   });
-@@ -239,4 +259,5 @@
- 
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-   });
-@@ -264,4 +285,4 @@
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-     );
-@@ -269,2 +290,34 @@
- 
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
 +
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+M /home/daniel-bo/Desktop/bus-math-v2/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -86,2 +86,18 @@
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-   it('renders lesson without description', () => {
-A /home/daniel-bo/Desktop/bus-math-v2-experimental/__tests__/lib/curriculum/phase-guidance.test.ts
-import { describe, expect, it } from 'vitest';
-
-import {
-  getLessonPhaseGuidance,
-  getPhaseGuidance,
-} from '@/lib/curriculum/phase-guidance';
-
-describe('curriculum/phase-guidance', () => {
-  it('returns accounting guidance for the concept-intro phase', () => {
-    const guidance = getPhaseGuidance('accounting', 2);
-
-    expect(guidance).toMatchObject({
-      lessonType: 'accounting',
-      phaseNumber: 2,
-      phaseLabel: 'Concept Intro',
-    });
-    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-    expect(guidance?.successCriteria).toEqual(
-      expect.arrayContaining([
-        expect.stringMatching(/guided/i),
-        expect.stringMatching(/independent/i),
-      ]),
-    );
-  });
-
-  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-    const guidance = getPhaseGuidance('excel', 3);
-
-    expect(guidance).toMatchObject({
-      lessonType: 'excel',
-      phaseNumber: 3,
-      phaseLabel: 'Guided Practice',
-    });
-    expect(guidance?.successCriteria).toEqual(
-      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-    );
-  });
-
-  it('maps lesson order index ranges to the correct phase guidance', () => {
-    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-  });
-
-  it('returns null when the phase number is not defined for the lesson type', () => {
-    expect(getPhaseGuidance('project', 6)).toBeNull();
-  });
-});
-apply_patch(auto_approved=true) exited 0 in 36ms:
-Success. Updated the following files:
-A /home/daniel-bo/Desktop/bus-math-v2-experimental/__tests__/lib/curriculum/phase-guidance.test.ts
-A /home/daniel-bo/Desktop/bus-math-v2-experimental/__tests__/components/student/PhaseGuidanceCard.test.tsx
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/__tests__/components/student/LessonRenderer.test.tsx
-file update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..87b2e6fa89eb98fa88e0eee19faa6c6eaeba02e8
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
+   it('renders lesson duration', () => {
+M /home/daniel-bo/Desktop/bus-math-v2/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,2 +1,3 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+@@ -75,2 +76,14 @@
  
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+M /home/daniel-bo/Desktop/bus-math-v2/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -94,2 +94,17 @@
  
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+M /home/daniel-bo/Desktop/bus-math-v2/components/teacher/UnitOverview.tsx
+@@ -12,2 +12,3 @@
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+@@ -59,3 +60,3 @@
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+M /home/daniel-bo/Desktop/bus-math-v2/components/teacher/UnitLessonPlan.tsx
+@@ -21,2 +21,3 @@
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+@@ -127,3 +128,3 @@
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+apply_patch(auto_approved=true) exited 0 in 117ms:
+Success. Updated the following files:
+M /home/daniel-bo/Desktop/bus-math-v2/components/teacher/UnitOverview.tsx
+M /home/daniel-bo/Desktop/bus-math-v2/components/teacher/UnitLessonPlan.tsx
+M /home/daniel-bo/Desktop/bus-math-v2/__tests__/components/student/PhaseHeader.test.tsx
+M /home/daniel-bo/Desktop/bus-math-v2/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+M /home/daniel-bo/Desktop/bus-math-v2/__tests__/components/teacher/UnitOverview.test.tsx
+M /home/daniel-bo/Desktop/bus-math-v2/__tests__/components/teacher/UnitLessonPlan.test.tsx
+M /home/daniel-bo/Desktop/bus-math-v2/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..57942477b7e921bd85d3e26e4c9aa9a28e244d8c
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,25 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
  
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,11 +283,43 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-     );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
-+    );
-+
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
-+  });
-+
-   it('renders lesson without description', () => {
-     const lessonWithoutDescription = {
-       ...mockLesson,
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
-+
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
-+
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
-+  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
++    render(page);
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
 +  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..57942477b7e921bd85d3e26e4c9aa9a28e244d8c
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,25 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-
-file update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..87b2e6fa89eb98fa88e0eee19faa6c6eaeba02e8
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+exec
+/bin/bash -lc 'npm test -- __tests__/curriculum/published-manifest.test.ts __tests__/app/curriculum/page.test.tsx __tests__/app/student/dashboard/page.test.tsx __tests__/app/teacher/students/[studentId]/page.test.tsx __tests__/components/dashboard/NextLessonCard.test.tsx __tests__/components/teacher/CourseOverviewGrid.test.tsx __tests__/components/teacher/TeacherLessonPlan.test.tsx __tests__/components/teacher/UnitOverview.test.tsx __tests__/components/teacher/UnitLessonPlan.test.tsx __tests__/components/student/PhaseHeader.test.tsx __tests__/app/teacher/units/[unitNumber]/page.test.tsx' in /home/daniel-bo/Desktop/bus-math-v2file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
-   it('renders activity content blocks', () => {
-@@ -262,11 +283,43 @@
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..57942477b7e921bd85d3e26e4c9aa9a28e244d8c
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,25 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
  
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-     );
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
-+    );
-+
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
-+  });
-+
-   it('renders lesson without description', () => {
-     const lessonWithoutDescription = {
-       ...mockLesson,
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
 +
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
-+
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(page);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-
-exec
-/bin/bash -lc 'CI=true npm test -- __tests__/lib/curriculum/phase-guidance.test.ts __tests__/components/student/PhaseGuidanceCard.test.tsx __tests__/components/student/LessonRenderer.test.tsx' in /home/daniel-bo/Desktop/bus-math-v2-experimentalfile update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..87b2e6fa89eb98fa88e0eee19faa6c6eaeba02e8
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -159,7 +175,9 @@
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..57942477b7e921bd85d3e26e4c9aa9a28e244d8c
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,25 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
-   it('renders activity content blocks', () => {
-@@ -262,11 +283,43 @@
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..57942477b7e921bd85d3e26e4c9aa9a28e244d8c
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,25 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
  
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-     );
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
-+    );
-+
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
-+  });
-+
-   it('renders lesson without description', () => {
-     const lessonWithoutDescription = {
-       ...mockLesson,
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
 +
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
-+
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
 +    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
-+  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
++    render(page);
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
-+    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
 +  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..57942477b7e921bd85d3e26e4c9aa9a28e244d8c
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,25 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-
- exited 1 in 3.51s:
-
-> test
-> vitest run __tests__/lib/curriculum/phase-guidance.test.ts __tests__/components/student/PhaseGuidanceCard.test.tsx __tests__/components/student/LessonRenderer.test.tsx
-
-
- RUN  v4.0.16 /home/daniel-bo/Desktop/bus-math-v2-experimental
-
- ❯ __tests__/components/student/LessonRenderer.test.tsx (15 tests | 6 failed) 492ms
-     ✓ renders lesson header with title and description 89ms
-     ✓ renders learning objectives 23ms
-     ✓ renders empty state when no phases 5ms
-     ✓ renders phases with titles and numbers 27ms
-     × renders markdown content blocks 34ms
-     × renders callout content blocks 21ms
-     × renders video content blocks 21ms
-     × renders image content blocks 19ms
-     × renders activity content blocks 19ms
-     × renders a phase guidance shell for the current lesson phase 21ms
-     ✓ renders lesson without description 15ms
-     ✓ automatically triggers completion for Read phases 11ms
-codex
-The red tests are running now against just the new track scope. I expect failures at this point because neither the curriculum guidance helper nor the student guidance card exists yet, and `LessonRenderer` still uses the old inline block renderer.
-file update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..87b2e6fa89eb98fa88e0eee19faa6c6eaeba02e8
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
-   it('renders activity content blocks', () => {
-@@ -262,11 +283,43 @@
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..57942477b7e921bd85d3e26e4c9aa9a28e244d8c
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,25 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
  
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-     );
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
-+    );
-+
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
-+  });
-+
-   it('renders lesson without description', () => {
-     const lessonWithoutDescription = {
-       ...mockLesson,
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
-+  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
-+
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
-+    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
-+
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
-+  });
-+});
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
 
-codex
-The red phase failed exactly where expected: the new guidance modules don’t exist, and `LessonRenderer` is still bypassing the shared phase renderer. I’m implementing the curriculum guidance helper and the student phase guidance card first, then I’ll refactor `LessonRenderer` onto the shared rendering path.
-exec
-/bin/bash -lc "sed -n '1,360p' components/lesson/ActivityRenderer.tsx" in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc "sed -n '1,260p' __tests__/components/lesson/ActivityRenderer.test.tsx" in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
-'use client';
-
-import { useCallback, useEffect, useState } from 'react';
-import { Loader2 } from 'lucide-react';
-
-import { getActivityComponent } from '@/lib/activities/registry';
-import type { Activity } from '@/lib/db/schema/validators';
-import { Badge } from '@/components/ui/badge';
-import { Alert, AlertDescription, AlertTitle } from '@/components/ui/alert';
-import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
-import type { CompletePhaseResponse } from '@/types/api';
+ succeeded in 43.83s:
 
-interface ActivityRendererProps {
-  activityId: string;
-  lessonId: string;
-  phaseNumber: number;
-  required?: boolean;
-  initialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-  onStatusChange?: () => void;
- succeeded in 50ms:
-import { describe, it, expect, vi, beforeEach } from 'vitest';
-import { fireEvent, render, screen, waitFor } from '@testing-library/react';
-
-import { ActivityRenderer } from '../../../components/lesson/ActivityRenderer';
-import type { Activity } from '@/lib/db/schema/validators';
+> test
+> vitest run __tests__/curriculum/published-manifest.test.ts __tests__/app/curriculum/page.test.tsx __tests__/app/student/dashboard/page.test.tsx __tests__/app/teacher/students/[studentId]/page.test.tsx __tests__/components/dashboard/NextLessonCard.test.tsx __tests__/components/teacher/CourseOverviewGrid.test.tsx __tests__/components/teacher/TeacherLessonPlan.test.tsx __tests__/components/teacher/UnitOverview.test.tsx __tests__/components/teacher/UnitLessonPlan.test.tsx __tests__/components/student/PhaseHeader.test.tsx __tests__/app/teacher/units/[unitNumber]/page.test.tsx
 
-const mockFetch = vi.fn();
-global.fetch = mockFetch as unknown as typeof fetch;
-const mockCompletePhase = vi.fn();
-const mockUsePhaseCompletion = vi.fn();
 
-const { mockGetActivityComponent } = vi.hoisted(() => ({
-  mockGetActivityComponent: vi.fn(),
-}));
+ RUN  v4.0.16 /home/daniel-bo/Desktop/bus-math-v2
 
-vi.mock('@/lib/activities/registry', () => {
-  return {
-    getActivityComponent: mockGetActivityComponent,
-  };
-});
+[baseline-browser-mapping] The data in this module is over two months old.  To ensure accurate Baseline data, please update: `npm i baseline-browser-mapping@latest -D`
+ ✓ __tests__/components/student/PhaseHeader.test.tsx (3 tests) 958ms
+     ✓ renders breadcrumb labels and lesson metadata  680ms
+ ✓ __tests__/app/teacher/students/[studentId]/page.test.tsx (7 tests) 4915ms
+     ✓ redirects unauthenticated users to login  3472ms
+     ✓ renders org-scoped student details  1030ms
+ ✓ __tests__/components/teacher/TeacherLessonPlan.test.tsx (20 tests) 5058ms
+     ✓ calls onNavigate when navigation buttons clicked  1189ms
+     ✓ renders teacher guidance for phases  307ms
+     ✓ renders all six phase numbers with correct icons  342ms
+ ✓ __tests__/components/teacher/UnitLessonPlan.test.tsx (17 tests) 4939ms
+     ✓ renders unit title and description  771ms
+     ✓ renders essential question  335ms
 file update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..87b2e6fa89eb98fa88e0eee19faa6c6eaeba02e8
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
-+
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
    });
  
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..57942477b7e921bd85d3e26e4c9aa9a28e244d8c
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,25 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders activity content blocks', () => {
-@@ -262,11 +283,43 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-     );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
-+    );
-+
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
-+  });
-+
-   it('renders lesson without description', () => {
-     const lessonWithoutDescription = {
-       ...mockLesson,
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
-+
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
-+
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
-+  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+codex
+The capstone-focused slice is green now, including the added teacher/unit header coverage. I’m moving to the required repo-wide gates next: `npm run lint`, the full `npm test`, and `npm run build`.
+exec
+/bin/bash -lc 'npm run lint' in /home/daniel-bo/Desktop/bus-math-v2file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
++    render(page);
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
 +  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..57942477b7e921bd85d3e26e4c9aa9a28e244d8c
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,25 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-
-codex
-I found one integration detail before the refactor: `LessonRenderer` currently passes activity completion state into `ActivityRenderer`, while the shared `PhaseRenderer` does not. I’m threading those optional activity props through `PhaseRenderer` so the refactor doesn’t silently drop progress-refresh behavior.
-file update
-A /home/daniel-bo/Desktop/bus-math-v2-experimental/components/student/PhaseGuidanceCard.tsx
-import { BookCheck, Target } from 'lucide-react';
-
-import { Badge } from '@/components/ui/badge';
-import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-
-interface PhaseGuidanceCardProps {
-  guidance: PhaseGuidance;
-  learningObjectives: string[] | null;
-}
-
-export function PhaseGuidanceCard({
-  guidance,
-  learningObjectives,
-}: PhaseGuidanceCardProps) {
-  return (
-    <Card className="border-primary/20 bg-primary/5">
-      <CardHeader className="space-y-3">
-        <div className="flex flex-wrap items-center gap-2">
-          <Badge variant="secondary">Phase Focus</Badge>
-          <Badge variant="outline" className="capitalize">
-            {guidance.lessonType}
-          </Badge>
-        </div>
-        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-      </CardHeader>
-      <CardContent className="space-y-5">
-        <section className="space-y-2">
-          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-            <Target className="h-4 w-4 text-primary" />
-            What success looks like
-          </div>
-          <ul className="space-y-2 text-sm text-muted-foreground">
-            {guidance.successCriteria.map((criterion) => (
-              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-                {criterion}
-              </li>
-            ))}
-          </ul>
-        </section>
-
-        {learningObjectives && learningObjectives.length > 0 ? (
-          <section className="space-y-2">
-            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-              <BookCheck className="h-4 w-4 text-primary" />
-              This phase advances these lesson objectives
-            </div>
-            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-              {learningObjectives.map((objective) => (
-                <li key={objective}>{objective}</li>
-              ))}
-            </ul>
-          </section>
-        ) : null}
-      </CardContent>
-    </Card>
-  );
-}
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/components/lesson/PhaseRenderer.tsx
-@@ -14,2 +14,4 @@
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
-@@ -20,3 +22,9 @@
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-@@ -43,3 +51,9 @@
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-@@ -61,2 +75,4 @@
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-@@ -107,2 +123,4 @@
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-A /home/daniel-bo/Desktop/bus-math-v2-experimental/lib/curriculum/phase-guidance.ts
-import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-
-export interface PhaseGuidance {
-  lessonType: LessonType;
-  phaseNumber: number;
-  phaseLabel: string;
-  goal: string;
-  successCriteria: string[];
-}
-
-const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-  accounting: {
-    1: {
-      phaseLabel: 'Hook',
-      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-      successCriteria: [
-        'Explain why the accounting decision matters to the business.',
-        'Identify the main tension you will solve in this lesson.',
-      ],
-    },
-    2: {
-      phaseLabel: 'Concept Intro',
-      goal: 'Teach the concept with explicit A/L/E reasoning.',
-      successCriteria: [
-        'Use assets, liabilities, and equity language accurately.',
-        'Name what success looks like in the guided and independent phases.',
-      ],
-    },
-    3: {
-      phaseLabel: 'Guided Practice',
-      goal: 'Practice the accounting move with teacher-led interactive support.',
-      successCriteria: [
-        'Complete the interactive practice task, not just the reading.',
-        'Explain why each accounting move is correct before moving on.',
-      ],
-    },
-    4: {
-      phaseLabel: 'Independent Practice',
-      goal: 'Complete a graded accounting task independently.',
-      successCriteria: [
-        'Submit a fully completed task without teacher intervention.',
-        'Meet the passing-score target of at least 60%.',
-      ],
-    },
-    5: {
-      phaseLabel: 'Assessment',
-      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-      successCriteria: [
-        'Answer the auto-graded check independently.',
-        'Show that the lesson objective can be applied correctly.',
-      ],
-    },
-    6: {
-      phaseLabel: 'Reflection',
-      goal: 'Connect accounting accuracy to business trust and decision quality.',
-      successCriteria: [
-        'Describe how accuracy changes business decisions.',
-        'Reflect on one habit that improves financial reliability.',
-      ],
-    },
-  },
-  excel: {
-    1: {
-      phaseLabel: 'Hook',
-      goal: 'Frame the spreadsheet problem that needs to be solved.',
-      successCriteria: [
-        'Identify the spreadsheet problem in business terms.',
-        'Recognize what a successful workbook should accomplish.',
-      ],
-    },
-    2: {
-      phaseLabel: 'Intro',
-      goal: 'Demonstrate the Excel workflow and quality criteria.',
-      successCriteria: [
-        'Name the workflow steps before practice begins.',
-        'Describe the quality checks that keep the workbook reliable.',
-      ],
-    },
-    3: {
-      phaseLabel: 'Guided Practice',
-      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-      successCriteria: [
-        'Use the interactive practice component while following the workflow.',
-        'Explain which formula, formatting, or validation move you just applied.',
-      ],
-    },
-    4: {
-      phaseLabel: 'Independent Practice',
-      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-      successCriteria: [
-        'Produce the required workbook, screenshot, or PDF evidence.',
-        'Make sure the deliverable is ready for teacher review.',
-      ],
-    },
-    5: {
-      phaseLabel: 'Checkpoint Assessment',
-      goal: 'Complete an auto-graded checkpoint on your own.',
-      successCriteria: [
-        'Submit the checkpoint independently.',
-        'Meet the passing-score target of at least 80%.',
-      ],
-    },
-    6: {
-      phaseLabel: 'Reflection',
-      goal: 'Explain how spreadsheet discipline affects business decisions.',
-      successCriteria: [
-        'Describe one quality-control habit from the lesson.',
-        'Connect spreadsheet accuracy to business impact.',
-      ],
-    },
-  },
-  project: {
-    1: {
-      phaseLabel: 'Project Build',
-      goal: 'Advance the project deliverable through scaffolded team work.',
-      successCriteria: [
-        'Complete the assigned team deliverable for today’s project phase.',
-        'Use the project criteria to judge whether the work is presentation-ready.',
-      ],
-    },
-  },
-  assessment: {
-    1: {
-      phaseLabel: 'Instructions',
-      goal: 'Review the directions, expectations, and assessment setup.',
-      successCriteria: [
-        'Understand the assessment rules before starting.',
-        'Know how the assessment is divided across the upcoming sections.',
-      ],
-    },
-    2: {
-      phaseLabel: 'Knowledge',
-      goal: 'Demonstrate foundational knowledge without support.',
-      successCriteria: [
-        'Answer the knowledge questions independently.',
-        'Meet the minimum assessment performance target.',
-      ],
-    },
-    3: {
-      phaseLabel: 'Understanding',
-      goal: 'Show conceptual understanding of the business math ideas.',
-      successCriteria: [
-        'Explain or apply the concept correctly in the assessment task.',
-        'Meet the minimum assessment performance target.',
-      ],
-    },
-    4: {
-      phaseLabel: 'Application',
-      goal: 'Apply the concept in a realistic business scenario.',
-      successCriteria: [
-        'Use the concept accurately in the scenario-based task.',
-        'Meet the minimum assessment performance target.',
-      ],
-    },
-  },
-};
-
-export function getPhaseGuidance(
-  lessonType: LessonType,
-  phaseNumber: number,
-): PhaseGuidance | null {
-  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-  if (!guidance) {
-    return null;
-  }
-
-  return {
-    lessonType,
-    phaseNumber,
-    ...guidance,
-  };
-}
-
-export function getLessonPhaseGuidance(
-  orderIndex: number,
-  phaseNumber: number,
-): PhaseGuidance | null {
-  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-}
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/components/student/LessonRenderer.tsx
-@@ -6,2 +6,4 @@
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
-@@ -9,4 +11,4 @@
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
-@@ -40,2 +42,14 @@
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-@@ -61,2 +75,7 @@
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-@@ -188,47 +207,16 @@
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
-apply_patch(auto_approved=true) exited 0 in 49ms:
-Success. Updated the following files:
-A /home/daniel-bo/Desktop/bus-math-v2-experimental/lib/curriculum/phase-guidance.ts
-A /home/daniel-bo/Desktop/bus-math-v2-experimental/components/student/PhaseGuidanceCard.tsx
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/components/lesson/PhaseRenderer.tsx
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/components/student/LessonRenderer.tsx
-file update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..87b2e6fa89eb98fa88e0eee19faa6c6eaeba02e8
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -159,7 +175,9 @@
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
-   it('renders activity content blocks', () => {
-@@ -262,11 +283,43 @@
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..57942477b7e921bd85d3e26e4c9aa9a28e244d8c
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,25 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
  
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-     );
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
-+    );
-+
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
-+  });
-+
-   it('renders lesson without description', () => {
-     const lessonWithoutDescription = {
-       ...mockLesson,
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
-+
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(page);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..aeda41f084431e99e65fa20d541dd671a6f2c847
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..57942477b7e921bd85d3e26e4c9aa9a28e244d8c
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,25 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
  
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -59,6 +73,8 @@
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +121,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
  
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-file update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..87b2e6fa89eb98fa88e0eee19faa6c6eaeba02e8
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
-   it('renders activity content blocks', () => {
-@@ -262,11 +283,43 @@
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..57942477b7e921bd85d3e26e4c9aa9a28e244d8c
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,25 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
  
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-     );
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
-+    );
-+
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
-+  });
-+
-   it('renders lesson without description', () => {
-     const lessonWithoutDescription = {
-       ...mockLesson,
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
 +
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
-+
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(page);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..aeda41f084431e99e65fa20d541dd671a6f2c847
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -59,6 +73,8 @@
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +121,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-exec
-/bin/bash -lc 'CI=true npm test -- __tests__/lib/curriculum/phase-guidance.test.ts __tests__/components/student/PhaseGuidanceCard.test.tsx __tests__/components/student/LessonRenderer.test.tsx __tests__/components/lesson/PhaseRenderer.test.tsx' in /home/daniel-bo/Desktop/bus-math-v2-experimentalfile update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..87b2e6fa89eb98fa88e0eee19faa6c6eaeba02e8
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..57942477b7e921bd85d3e26e4c9aa9a28e244d8c
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,25 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders activity content blocks', () => {
-@@ -262,11 +283,43 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-     );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
-+    );
-+
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
-+  });
-+
-   it('renders lesson without description', () => {
-     const lessonWithoutDescription = {
-       ...mockLesson,
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
-+
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
-+
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
-+  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
++    render(page);
 +
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..57942477b7e921bd85d3e26e4c9aa9a28e244d8c
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,25 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
 +    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
-+  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
-+    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..aeda41f084431e99e65fa20d541dd671a6f2c847
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -59,6 +73,8 @@
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +121,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
- exited 1 in 4.55s:
-
-> test
-> vitest run __tests__/lib/curriculum/phase-guidance.test.ts __tests__/components/student/PhaseGuidanceCard.test.tsx __tests__/components/student/LessonRenderer.test.tsx __tests__/components/lesson/PhaseRenderer.test.tsx
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
 
+ succeeded in 49.53s:
 
- RUN  v4.0.16 /home/daniel-bo/Desktop/bus-math-v2-experimental
+> lint
+> vinext lint
 
- ✓ __tests__/lib/curriculum/phase-guidance.test.ts (4 tests) 12ms
- ✓ __tests__/components/student/PhaseGuidanceCard.test.tsx (2 tests) 137ms
- ❯ __tests__/components/student/LessonRenderer.test.tsx (15 tests | 1 failed) 621ms
-     ✓ renders lesson header with title and description 113ms
-     × renders learning objectives 62ms
-     ✓ renders empty state when no phases 5ms
-     ✓ renders phases with titles and numbers 46ms
-     ✓ renders markdown content blocks 28ms
-     ✓ renders callout content blocks 22ms
-     ✓ renders video content blocks 22ms
-     ✓ renders image content blocks 20ms
-     ✓ renders activity content blocks 21ms
-     ✓ renders a phase guidance shell for the current lesson phase 28ms
 file update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..87b2e6fa89eb98fa88e0eee19faa6c6eaeba02e8
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
-+
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
    });
  
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..57942477b7e921bd85d3e26e4c9aa9a28e244d8c
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,25 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
  
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+exec
+/bin/bash -lc 'npm test' in /home/daniel-bo/Desktop/bus-math-v2file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
-   it('renders activity content blocks', () => {
-@@ -262,11 +283,43 @@
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..57942477b7e921bd85d3e26e4c9aa9a28e244d8c
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,25 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
  
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-     );
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
-+    );
-+
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
-+  });
-+
-   it('renders lesson without description', () => {
-     const lessonWithoutDescription = {
-       ...mockLesson,
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
 +
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
-+
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
 +    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
-+  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
++    render(page);
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
-+    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
 +  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
-+
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
-+  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..aeda41f084431e99e65fa20d541dd671a6f2c847
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -59,6 +73,8 @@
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +121,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..57942477b7e921bd85d3e26e4c9aa9a28e244d8c
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,25 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
  
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-codex
-The implementation is mostly in place; the remaining failures are straightforward cleanup. One is a `PhaseRenderer` prop-threading bug, and the other is a test assertion that now sees the objective text in both the lesson summary and the new phase guidance card.
-file update
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/components/lesson/PhaseRenderer.tsx
-@@ -70,3 +70,5 @@
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/__tests__/components/student/LessonRenderer.test.tsx
-@@ -118,5 +118,5 @@
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
-   });
-apply_patch(auto_approved=true) exited 0 in 42ms:
-Success. Updated the following files:
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/components/lesson/PhaseRenderer.tsx
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/__tests__/components/student/LessonRenderer.test.tsx
-file update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
  
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+codex
+The targeted capstone work is stable. The full suite is running now; if there’s a repo-wide regression outside the capstone track, I’ll fix it before moving on to the build and Conductor cleanup.
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..57942477b7e921bd85d3e26e4c9aa9a28e244d8c
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,25 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
  
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
-     );
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
-+
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
-+
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
-+  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
++    render(page);
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
 +  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..57942477b7e921bd85d3e26e4c9aa9a28e244d8c
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,25 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-file update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..57942477b7e921bd85d3e26e4c9aa9a28e244d8c
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,25 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
  
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
-     );
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
-+
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
-+
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
-+  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
++    render(page);
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
 +  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..57942477b7e921bd85d3e26e4c9aa9a28e244d8c
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,25 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-exec
-/bin/bash -lc 'CI=true npm test -- __tests__/lib/curriculum/phase-guidance.test.ts __tests__/components/student/PhaseGuidanceCard.test.tsx __tests__/components/student/LessonRenderer.test.tsx __tests__/components/lesson/PhaseRenderer.test.tsx' in /home/daniel-bo/Desktop/bus-math-v2-experimentalfile update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
- 
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..57942477b7e921bd85d3e26e4c9aa9a28e244d8c
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,25 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(screen.getByText(/Unit 3 • Lesson 02/)).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
 +
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
- succeeded in 4.32s:
-
-> test
-> vitest run __tests__/lib/curriculum/phase-guidance.test.ts __tests__/components/student/PhaseGuidanceCard.test.tsx __tests__/components/student/LessonRenderer.test.tsx __tests__/components/lesson/PhaseRenderer.test.tsx
-
-
- RUN  v4.0.16 /home/daniel-bo/Desktop/bus-math-v2-experimental
-
- ✓ __tests__/components/student/PhaseGuidanceCard.test.tsx (2 tests) 125ms
- ✓ __tests__/components/lesson/PhaseRenderer.test.tsx (15 tests) 180ms
- ✓ __tests__/components/student/LessonRenderer.test.tsx (15 tests) 683ms
- ✓ __tests__/lib/curriculum/phase-guidance.test.ts (4 tests) 9ms
-
- Test Files  4 passed (4)
-      Tests  36 passed (36)
-   Start at  16:37:51
-   Duration  3.60s (transform 959ms, setup 1.53s, import 1.67s, tests 997ms, environment 3.69s)
-
-file update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
- 
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
 +
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-codex
-The feature work is green on the focused test slice. I’m switching to track verification now with repo-standard checks: lint first, then the full non-interactive test suite, before I update docs and Conductor state.
-exec
-/bin/bash -lc 'CI=true npm run lint' in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc 'CI=true npm test' in /home/daniel-bo/Desktop/bus-math-v2-experimentalfile update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
- 
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+codex
+The full suite is still progressing normally. I haven’t hit a repo-wide blocker so far; I’m staying on it until I have a clean result or a concrete failure to fix.
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
-+
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-codex
-Verification is in flight. I’m letting lint and the full Vitest suite run in parallel; if either exposes a regression, I’ll fix it before I touch README or closeout artifacts.
-file update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
- 
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
-+  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
-+
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
-+    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
-+
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
-+
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
-+  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
 
 file update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
-+
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
 +      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
 +
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
- succeeded in 40.76s:
-
-> lint
-> vinext lint
-
-
-  vinext lint
-
-  Using eslint (with existing config)
-
-
-  Lint passed.
-
-file update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
- 
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
 +
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-file update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
- 
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
-+
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-file update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
- 
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
 +
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-file update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
- 
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
 +
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
-+
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-file update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
- 
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
 +
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-file update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
- 
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
-+
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-file update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
- 
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
 +
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
- succeeded in 4m 11s:
-
-> test
-> vitest run
-
-
- RUN  v4.0.16 /home/daniel-bo/Desktop/bus-math-v2-experimental
-
- ✓ __tests__/components/business-simulations/PitchPresentationBuilder.test.tsx (18 tests) 4763ms
-     ✓ shows all three presentation modes  343ms
-     ✓ switches between build, practice, and review modes  389ms
-     ✓ allows editing business name  477ms
-     ✓ starts and pauses practice timer  436ms
-     ✓ resets practice timer  348ms
-     ✓ exports pitch deck in review mode  353ms
-     ✓ calls onSubmit when submit results is clicked in review mode  496ms
-     ✓ shows section completeness in review mode  329ms
- ✓ __tests__/components/login-form.test.tsx (17 tests) 6610ms
-     ✓ calls signIn with username and password on form submission  512ms
-     ✓ displays user-friendly error message on invalid credentials  458ms
-     ✓ attempts auto-provisioning for demo credentials before login  337ms
-file update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
- 
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
-+
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
-+    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
-+
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
-+
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
-+  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-codex
-Lint passed, and the full suite passed: `210` test files, `1141` tests. I’m in closeout now: updating the track plan/status, adding the outcome to README and project memory, then I’ll make the required non-interactive commits and archive the track.
-exec
-/bin/bash -lc "sed -n '1,240p' README.md" in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 50ms:
-# Math for Business Operations v2
-
-An interactive, Convex-backed digital textbook for teaching business mathematics to high school students. The app runs on Vinext/App Router, uses custom JWT username/password auth, and stores curriculum, activity, and progress data in Convex.
-
-## Features
-
-- **Interactive Lessons**: Six-phase learning structure with hands-on activities
-- **Progress Tracking**: Student progress monitoring and analytics
-- **Student Progress Hub**: Guided dashboard with overall course progress, resume/start recommendations, and unit-by-unit completion cards
-- **Teacher Intervention Queue**: At-risk and inactive student triage with status filters and CSV-aligned intervention exports
-- **Teacher Student Detail Analytics**: Teacher student detail pages now surface intervention status, unit-by-unit progress, and the next best published lesson for follow-up
-- **Student-Only Dashboard Boundary**: Student dashboard routes now enforce the student role explicitly and redirect teacher/admin sessions to their own dashboard surfaces
-- **Student-Only Write APIs**: Phase completion, assessment submission, spreadsheet drafts, and spreadsheet submissions now reject teacher/admin sessions so preview access cannot mutate learner records
-- **Published Curriculum Progress Guarantees**: Student dashboards, teacher snapshots, lesson delivery, and phase-completion checks all resolve against the latest published lesson version so drafts do not leak into classroom progress
-- **Convex-Backed Teacher Views**: Teacher dashboard, course gradebook, unit gradebook, and student detail pages now all read through internal Convex queries instead of legacy Drizzle runtime paths
-- **Shared Server Role Guards**: Teacher/admin App Router pages now use shared server-side claim guards, and the admin dashboard rejects non-admin sessions
-- **Shared Dashboard Recommendation Cards**: Student and teacher progress surfaces now reuse one next-lesson card and shared unit-status presentation helpers to keep resume/start guidance aligned
-- **Shared Published Progress View-Models**: Student dashboard units, teacher student-detail progress rows, and lesson phase-status responses now derive from the same published-progress helpers so progress math and locking rules stay aligned
-- **Canonical Activity Component Catalog**: Activity rendering and validation now resolve documented aliases such as `spreadsheet-activity` and `journal-entry-activity` through one canonical runtime contract, reducing curriculum authoring drift
-- **Deterministic Simulation Runtime IDs**: Inventory simulation notifications and market events now keep stable unique ids during rapid updates, preventing duplicate-key rendering regressions
-exec
-/bin/bash -lc 'wc -l conductor/lessons-learned.md conductor/tech-debt.md' in /home/daniel-bo/Desktop/bus-math-v2-experimentalexec
-/bin/bash -lc "sed -n '1,220p' conductor/tracks/lesson_phase_objective_shell_20260311/plan.md" in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
-   45 conductor/lessons-learned.md
-   21 conductor/tech-debt.md
-   66 total
- succeeded in 51ms:
-# Implementation Plan
-
-## Phase 1: Phase Guidance Red Tests
-- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-- [ ] Task: Add failing tests for the student phase shell
-  - [ ] Add a focused component test for the reusable phase guidance card
-  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
 
-## Phase 2: Objective-Aware Student Phase Shell
-- [ ] Task: Implement shared curriculum phase-guidance helpers
-  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-- [ ] Task: Render the reusable student phase guidance shell
-  - [ ] Add a dedicated student-facing phase guidance component
-  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-
-## Phase 3: Verification and Closeout
-- [ ] Task: Run lint and automated tests for the track scope
 file update:
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
-+
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
-   });
- 
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
-     );
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
-+
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
-+
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
-+  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
-+
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
 +    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
-+  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..a2ab324478dc6468121ef70a8849dd9aa6e4ece6
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": null,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..6d4a4eac8b817437622e92a77879bca317e65f9f
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [ ] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [ ] Assert guided-practice and graded-phase success criteria are exposed
-+- [ ] Task: Add failing tests for the student phase shell
-+  - [ ] Add a focused component test for the reusable phase guidance card
-+  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [ ] Task: Implement shared curriculum phase-guidance helpers
-+  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [ ] Task: Render the reusable student phase guidance shell
-+  - [ ] Add a dedicated student-facing phase guidance component
-+  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [ ] Task: Run lint and automated tests for the track scope
-+- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-codex
-I’m making the closeout edits now: documenting the new phase-guidance behavior, recording one reusable lesson in project memory, and updating the track plan/metadata so the archive commit reflects the completed work accurately.
-file update
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -3,20 +3,20 @@
- ## Phase 1: Phase Guidance Red Tests
--- [ ] Task: Add failing tests for curriculum phase-guidance metadata
--  - [ ] Cover accounting, excel, project, and assessment phase guidance lookup
--  - [ ] Assert guided-practice and graded-phase success criteria are exposed
--- [ ] Task: Add failing tests for the student phase shell
--  - [ ] Add a focused component test for the reusable phase guidance card
--  - [ ] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+- [x] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [x] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [x] Assert guided-practice and graded-phase success criteria are exposed
-+- [x] Task: Add failing tests for the student phase shell
-+  - [x] Add a focused component test for the reusable phase guidance card
-+  - [x] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
- 
- ## Phase 2: Objective-Aware Student Phase Shell
--- [ ] Task: Implement shared curriculum phase-guidance helpers
--  - [ ] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
--  - [ ] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
--- [ ] Task: Render the reusable student phase guidance shell
--  - [ ] Add a dedicated student-facing phase guidance component
--  - [ ] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+- [x] Task: Implement shared curriculum phase-guidance helpers
-+  - [x] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [x] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [x] Task: Render the reusable student phase guidance shell
-+  - [x] Add a dedicated student-facing phase guidance component
-+  - [x] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
- 
- ## Phase 3: Verification and Closeout
--- [ ] Task: Run lint and automated tests for the track scope
--- [ ] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [x] Task: Run lint and automated tests for the track scope
-+- [x] Task: Update README and Conductor memory files with the phase-shell alignment outcome
- - [ ] Task: Archive the track and finalize registry/metadata updates
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/README.md
-@@ -19,2 +19,3 @@
- - **Canonical Activity Component Catalog**: Activity rendering and validation now resolve documented aliases such as `spreadsheet-activity` and `journal-entry-activity` through one canonical runtime contract, reducing curriculum authoring drift
-+- **Objective-Aware Lesson Phase Shell**: Student lesson phases now show curriculum-aligned phase guidance and success criteria while reusing the shared phase content renderer
- - **Deterministic Simulation Runtime IDs**: Inventory simulation notifications and market events now keep stable unique ids during rapid updates, preventing duplicate-key rendering regressions
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/lessons-learned.md
-@@ -16,2 +16,3 @@
- - (2026-03-11, student_lesson_access_boundary_cleanup_20260311) Student-owned API writes need an explicit request-level student guard even when teacher/admin lesson preview is intentionally allowed elsewhere; auth presence alone is not a safe mutation boundary.
-+- (2026-03-11, lesson_phase_objective_shell_20260311) Student lesson pages should derive phase guidance from curriculum lesson-type metadata and reuse the shared `PhaseRenderer`, or pedagogy copy and content rendering drift separately.
- 
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -8,3 +8,3 @@
-   "estimated_tasks": 7,
--  "actual_tasks": null,
-+  "actual_tasks": 7,
-   "deviation_notes": ""
-apply_patch(auto_approved=true) exited 0 in 46ms:
-Success. Updated the following files:
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/lessons-learned.md
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/README.md
-file update:
-diff --git a/README.md b/README.md
-index 9ee7fe58cfca200160c7dcd63527376b6a6e3202..d15e181d7fdd88c0d5393d28b496fdbcdc953762
---- a/README.md
-+++ b/README.md
-@@ -17,6 +17,7 @@
- - **Shared Dashboard Recommendation Cards**: Student and teacher progress surfaces now reuse one next-lesson card and shared unit-status presentation helpers to keep resume/start guidance aligned
- - **Shared Published Progress View-Models**: Student dashboard units, teacher student-detail progress rows, and lesson phase-status responses now derive from the same published-progress helpers so progress math and locking rules stay aligned
- - **Canonical Activity Component Catalog**: Activity rendering and validation now resolve documented aliases such as `spreadsheet-activity` and `journal-entry-activity` through one canonical runtime contract, reducing curriculum authoring drift
-+- **Objective-Aware Lesson Phase Shell**: Student lesson phases now show curriculum-aligned phase guidance and success criteria while reusing the shared phase content renderer
- - **Deterministic Simulation Runtime IDs**: Inventory simulation notifications and market events now keep stable unique ids during rapid updates, preventing duplicate-key rendering regressions
- - **Account Settings & Self-Service Password Changes**: Authenticated users can review account context and update their own password without leaving the session
- - **Multi-tenant Architecture**: Organization-based access control
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
- 
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
 +
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
-+
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
-index 5a1f9d47c9c577b016380d41f806bcb496b3903e..d8b4b634818b45cfc7f4d67cb6059d6c008a677c
---- a/conductor/lessons-learned.md
-+++ b/conductor/lessons-learned.md
-@@ -14,6 +14,7 @@
- - (2026-03-11, activity_component_contract_refactor_20260311) Activity contracts drift quickly when alias handling lives separately from the runtime registry and schema validators; documented curriculum keys need one canonical resolver.
- - (2026-03-11, published_progress_viewmodel_consolidation_20260311) Student dashboard rows, teacher student-detail rows, and lesson phase-status payloads should derive from the same published-progress helpers or published-version/locking fixes will diverge again.
- - (2026-03-11, student_lesson_access_boundary_cleanup_20260311) Student-owned API writes need an explicit request-level student guard even when teacher/admin lesson preview is intentionally allowed elsewhere; auth presence alone is not a safe mutation boundary.
-+- (2026-03-11, lesson_phase_objective_shell_20260311) Student lesson pages should derive phase guidance from curriculum lesson-type metadata and reuse the shared `PhaseRenderer`, or pedagogy copy and content rendering drift separately.
- 
- ### Recurring Gotchas
- <!-- Problems encountered repeatedly; save future tracks from the same pain -->
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..364eb86a46b01b017fb851065b622f944b094de2
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": 7,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..2c93ed93a25fc6373d8d1f962121f21b751f077d
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [x] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [x] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [x] Assert guided-practice and graded-phase success criteria are exposed
-+- [x] Task: Add failing tests for the student phase shell
-+  - [x] Add a focused component test for the reusable phase guidance card
-+  - [x] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [x] Task: Implement shared curriculum phase-guidance helpers
-+  - [x] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [x] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [x] Task: Render the reusable student phase guidance shell
-+  - [x] Add a dedicated student-facing phase guidance component
-+  - [x] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [x] Task: Run lint and automated tests for the track scope
-+- [x] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-file update:
-diff --git a/README.md b/README.md
-index 9ee7fe58cfca200160c7dcd63527376b6a6e3202..d15e181d7fdd88c0d5393d28b496fdbcdc953762
---- a/README.md
-+++ b/README.md
-@@ -17,6 +17,7 @@
- - **Shared Dashboard Recommendation Cards**: Student and teacher progress surfaces now reuse one next-lesson card and shared unit-status presentation helpers to keep resume/start guidance aligned
- - **Shared Published Progress View-Models**: Student dashboard units, teacher student-detail progress rows, and lesson phase-status responses now derive from the same published-progress helpers so progress math and locking rules stay aligned
- - **Canonical Activity Component Catalog**: Activity rendering and validation now resolve documented aliases such as `spreadsheet-activity` and `journal-entry-activity` through one canonical runtime contract, reducing curriculum authoring drift
-+- **Objective-Aware Lesson Phase Shell**: Student lesson phases now show curriculum-aligned phase guidance and success criteria while reusing the shared phase content renderer
- - **Deterministic Simulation Runtime IDs**: Inventory simulation notifications and market events now keep stable unique ids during rapid updates, preventing duplicate-key rendering regressions
- - **Account Settings & Self-Service Password Changes**: Authenticated users can review account context and update their own password without leaving the session
- - **Multi-tenant Architecture**: Organization-based access control
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
- 
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
 +
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
-+
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
-+    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
-+
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
-+
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
-+  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
-index 5a1f9d47c9c577b016380d41f806bcb496b3903e..d8b4b634818b45cfc7f4d67cb6059d6c008a677c
---- a/conductor/lessons-learned.md
-+++ b/conductor/lessons-learned.md
-@@ -14,6 +14,7 @@
- - (2026-03-11, activity_component_contract_refactor_20260311) Activity contracts drift quickly when alias handling lives separately from the runtime registry and schema validators; documented curriculum keys need one canonical resolver.
- - (2026-03-11, published_progress_viewmodel_consolidation_20260311) Student dashboard rows, teacher student-detail rows, and lesson phase-status payloads should derive from the same published-progress helpers or published-version/locking fixes will diverge again.
- - (2026-03-11, student_lesson_access_boundary_cleanup_20260311) Student-owned API writes need an explicit request-level student guard even when teacher/admin lesson preview is intentionally allowed elsewhere; auth presence alone is not a safe mutation boundary.
-+- (2026-03-11, lesson_phase_objective_shell_20260311) Student lesson pages should derive phase guidance from curriculum lesson-type metadata and reuse the shared `PhaseRenderer`, or pedagogy copy and content rendering drift separately.
- 
- ### Recurring Gotchas
- <!-- Problems encountered repeatedly; save future tracks from the same pain -->
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..364eb86a46b01b017fb851065b622f944b094de2
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": 7,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..2c93ed93a25fc6373d8d1f962121f21b751f077d
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [x] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [x] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [x] Assert guided-practice and graded-phase success criteria are exposed
-+- [x] Task: Add failing tests for the student phase shell
-+  - [x] Add a focused component test for the reusable phase guidance card
-+  - [x] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [x] Task: Implement shared curriculum phase-guidance helpers
-+  - [x] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [x] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [x] Task: Render the reusable student phase guidance shell
-+  - [x] Add a dedicated student-facing phase guidance component
-+  - [x] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [x] Task: Run lint and automated tests for the track scope
-+- [x] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
 
-exec
-/bin/bash -lc 'git status --short' in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
- M README.md
- M __tests__/components/student/LessonRenderer.test.tsx
- M components/lesson/PhaseRenderer.tsx
- M components/student/LessonRenderer.tsx
- M conductor/autonomous_prompt.md
- M conductor/codex-cron.log
- M conductor/codex-last.txt
- M conductor/lessons-learned.md
- M conductor/tracks.md
- M conductor/workflow.md
 file update:
-diff --git a/README.md b/README.md
-index 9ee7fe58cfca200160c7dcd63527376b6a6e3202..d15e181d7fdd88c0d5393d28b496fdbcdc953762
---- a/README.md
-+++ b/README.md
-@@ -17,6 +17,7 @@
- - **Shared Dashboard Recommendation Cards**: Student and teacher progress surfaces now reuse one next-lesson card and shared unit-status presentation helpers to keep resume/start guidance aligned
- - **Shared Published Progress View-Models**: Student dashboard units, teacher student-detail progress rows, and lesson phase-status responses now derive from the same published-progress helpers so progress math and locking rules stay aligned
- - **Canonical Activity Component Catalog**: Activity rendering and validation now resolve documented aliases such as `spreadsheet-activity` and `journal-entry-activity` through one canonical runtime contract, reducing curriculum authoring drift
-+- **Objective-Aware Lesson Phase Shell**: Student lesson phases now show curriculum-aligned phase guidance and success criteria while reusing the shared phase content renderer
- - **Deterministic Simulation Runtime IDs**: Inventory simulation notifications and market events now keep stable unique ids during rapid updates, preventing duplicate-key rendering regressions
- - **Account Settings & Self-Service Password Changes**: Authenticated users can review account context and update their own password without leaving the session
- - **Multi-tenant Architecture**: Organization-based access control
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
-+
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
-   });
- 
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
-     );
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
-+
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
-+
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
-+  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
-+
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
 +    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
-+  });
-+
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
-index 5a1f9d47c9c577b016380d41f806bcb496b3903e..d8b4b634818b45cfc7f4d67cb6059d6c008a677c
---- a/conductor/lessons-learned.md
-+++ b/conductor/lessons-learned.md
-@@ -14,6 +14,7 @@
- - (2026-03-11, activity_component_contract_refactor_20260311) Activity contracts drift quickly when alias handling lives separately from the runtime registry and schema validators; documented curriculum keys need one canonical resolver.
- - (2026-03-11, published_progress_viewmodel_consolidation_20260311) Student dashboard rows, teacher student-detail rows, and lesson phase-status payloads should derive from the same published-progress helpers or published-version/locking fixes will diverge again.
- - (2026-03-11, student_lesson_access_boundary_cleanup_20260311) Student-owned API writes need an explicit request-level student guard even when teacher/admin lesson preview is intentionally allowed elsewhere; auth presence alone is not a safe mutation boundary.
-+- (2026-03-11, lesson_phase_objective_shell_20260311) Student lesson pages should derive phase guidance from curriculum lesson-type metadata and reuse the shared `PhaseRenderer`, or pedagogy copy and content rendering drift separately.
- 
- ### Recurring Gotchas
- <!-- Problems encountered repeatedly; save future tracks from the same pain -->
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..364eb86a46b01b017fb851065b622f944b094de2
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": 7,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..2c93ed93a25fc6373d8d1f962121f21b751f077d
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [x] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [x] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [x] Assert guided-practice and graded-phase success criteria are exposed
-+- [x] Task: Add failing tests for the student phase shell
-+  - [x] Add a focused component test for the reusable phase guidance card
-+  - [x] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [x] Task: Implement shared curriculum phase-guidance helpers
-+  - [x] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [x] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [x] Task: Render the reusable student phase guidance shell
-+  - [x] Add a dedicated student-facing phase guidance component
-+  - [x] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [x] Task: Run lint and automated tests for the track scope
-+- [x] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-exec
-/bin/bash -lc 'git status --short --untracked-files=all' in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
- M README.md
- M __tests__/components/student/LessonRenderer.test.tsx
- M components/lesson/PhaseRenderer.tsx
- M components/student/LessonRenderer.tsx
- M conductor/autonomous_prompt.md
- M conductor/codex-cron.log
- M conductor/codex-last.txt
- M conductor/lessons-learned.md
- M conductor/tracks.md
- M conductor/workflow.md
-?? __tests__/components/student/PhaseGuidanceCard.test.tsx
-?? __tests__/lib/curriculum/phase-guidance.test.ts
-?? components/student/PhaseGuidanceCard.tsx
-?? conductor/current_directive.md
-?? conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-?? conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-?? conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-?? conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-?? lib/curriculum/phase-guidance.ts
-file update:
-diff --git a/README.md b/README.md
-index 9ee7fe58cfca200160c7dcd63527376b6a6e3202..d15e181d7fdd88c0d5393d28b496fdbcdc953762
---- a/README.md
-+++ b/README.md
-@@ -17,6 +17,7 @@
- - **Shared Dashboard Recommendation Cards**: Student and teacher progress surfaces now reuse one next-lesson card and shared unit-status presentation helpers to keep resume/start guidance aligned
- - **Shared Published Progress View-Models**: Student dashboard units, teacher student-detail progress rows, and lesson phase-status responses now derive from the same published-progress helpers so progress math and locking rules stay aligned
- - **Canonical Activity Component Catalog**: Activity rendering and validation now resolve documented aliases such as `spreadsheet-activity` and `journal-entry-activity` through one canonical runtime contract, reducing curriculum authoring drift
-+- **Objective-Aware Lesson Phase Shell**: Student lesson phases now show curriculum-aligned phase guidance and success criteria while reusing the shared phase content renderer
- - **Deterministic Simulation Runtime IDs**: Inventory simulation notifications and market events now keep stable unique ids during rapid updates, preventing duplicate-key rendering regressions
- - **Account Settings & Self-Service Password Changes**: Authenticated users can review account context and update their own password without leaving the session
- - **Multi-tenant Architecture**: Organization-based access control
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
- 
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
 +
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
-index 5a1f9d47c9c577b016380d41f806bcb496b3903e..d8b4b634818b45cfc7f4d67cb6059d6c008a677c
---- a/conductor/lessons-learned.md
-+++ b/conductor/lessons-learned.md
-@@ -14,6 +14,7 @@
- - (2026-03-11, activity_component_contract_refactor_20260311) Activity contracts drift quickly when alias handling lives separately from the runtime registry and schema validators; documented curriculum keys need one canonical resolver.
- - (2026-03-11, published_progress_viewmodel_consolidation_20260311) Student dashboard rows, teacher student-detail rows, and lesson phase-status payloads should derive from the same published-progress helpers or published-version/locking fixes will diverge again.
- - (2026-03-11, student_lesson_access_boundary_cleanup_20260311) Student-owned API writes need an explicit request-level student guard even when teacher/admin lesson preview is intentionally allowed elsewhere; auth presence alone is not a safe mutation boundary.
-+- (2026-03-11, lesson_phase_objective_shell_20260311) Student lesson pages should derive phase guidance from curriculum lesson-type metadata and reuse the shared `PhaseRenderer`, or pedagogy copy and content rendering drift separately.
- 
- ### Recurring Gotchas
- <!-- Problems encountered repeatedly; save future tracks from the same pain -->
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..364eb86a46b01b017fb851065b622f944b094de2
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": 7,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..2c93ed93a25fc6373d8d1f962121f21b751f077d
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [x] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [x] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [x] Assert guided-practice and graded-phase success criteria are exposed
-+- [x] Task: Add failing tests for the student phase shell
-+  - [x] Add a focused component test for the reusable phase guidance card
-+  - [x] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [x] Task: Implement shared curriculum phase-guidance helpers
-+  - [x] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [x] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [x] Task: Render the reusable student phase guidance shell
-+  - [x] Add a dedicated student-facing phase guidance component
-+  - [x] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [x] Task: Run lint and automated tests for the track scope
-+- [x] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-codex
-I’ve isolated the implementation changes from unrelated working-tree edits. Next I’m creating the first commit for the track work itself, then I’ll archive the track in a separate cleanup commit as requested.
-exec
-/bin/bash -lc 'git add README.md __tests__/components/student/LessonRenderer.test.tsx __tests__/components/student/PhaseGuidanceCard.test.tsx __tests__/lib/curriculum/phase-guidance.test.ts components/lesson/PhaseRenderer.tsx components/student/LessonRenderer.tsx components/student/PhaseGuidanceCard.tsx lib/curriculum/phase-guidance.ts conductor/lessons-learned.md conductor/tracks.md conductor/tracks/lesson_phase_objective_shell_20260311/index.md conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json conductor/tracks/lesson_phase_objective_shell_20260311/plan.md conductor/tracks/lesson_phase_objective_shell_20260311/spec.md' in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
-
-file update:
-diff --git a/README.md b/README.md
-index 9ee7fe58cfca200160c7dcd63527376b6a6e3202..d15e181d7fdd88c0d5393d28b496fdbcdc953762
---- a/README.md
-+++ b/README.md
-@@ -17,6 +17,7 @@
- - **Shared Dashboard Recommendation Cards**: Student and teacher progress surfaces now reuse one next-lesson card and shared unit-status presentation helpers to keep resume/start guidance aligned
- - **Shared Published Progress View-Models**: Student dashboard units, teacher student-detail progress rows, and lesson phase-status responses now derive from the same published-progress helpers so progress math and locking rules stay aligned
- - **Canonical Activity Component Catalog**: Activity rendering and validation now resolve documented aliases such as `spreadsheet-activity` and `journal-entry-activity` through one canonical runtime contract, reducing curriculum authoring drift
-+- **Objective-Aware Lesson Phase Shell**: Student lesson phases now show curriculum-aligned phase guidance and success criteria while reusing the shared phase content renderer
- - **Deterministic Simulation Runtime IDs**: Inventory simulation notifications and market events now keep stable unique ids during rapid updates, preventing duplicate-key rendering regressions
- - **Account Settings & Self-Service Password Changes**: Authenticated users can review account context and update their own password without leaving the session
- - **Multi-tenant Architecture**: Organization-based access control
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
- 
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
 +
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
-+  });
-+
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
-+    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
-+
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
-+
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
-+  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
-index 5a1f9d47c9c577b016380d41f806bcb496b3903e..d8b4b634818b45cfc7f4d67cb6059d6c008a677c
---- a/conductor/lessons-learned.md
-+++ b/conductor/lessons-learned.md
-@@ -14,6 +14,7 @@
- - (2026-03-11, activity_component_contract_refactor_20260311) Activity contracts drift quickly when alias handling lives separately from the runtime registry and schema validators; documented curriculum keys need one canonical resolver.
- - (2026-03-11, published_progress_viewmodel_consolidation_20260311) Student dashboard rows, teacher student-detail rows, and lesson phase-status payloads should derive from the same published-progress helpers or published-version/locking fixes will diverge again.
- - (2026-03-11, student_lesson_access_boundary_cleanup_20260311) Student-owned API writes need an explicit request-level student guard even when teacher/admin lesson preview is intentionally allowed elsewhere; auth presence alone is not a safe mutation boundary.
-+- (2026-03-11, lesson_phase_objective_shell_20260311) Student lesson pages should derive phase guidance from curriculum lesson-type metadata and reuse the shared `PhaseRenderer`, or pedagogy copy and content rendering drift separately.
- 
- ### Recurring Gotchas
- <!-- Problems encountered repeatedly; save future tracks from the same pain -->
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..364eb86a46b01b017fb851065b622f944b094de2
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": 7,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..2c93ed93a25fc6373d8d1f962121f21b751f077d
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [x] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [x] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [x] Assert guided-practice and graded-phase success criteria are exposed
-+- [x] Task: Add failing tests for the student phase shell
-+  - [x] Add a focused component test for the reusable phase guidance card
-+  - [x] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [x] Task: Implement shared curriculum phase-guidance helpers
-+  - [x] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [x] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [x] Task: Render the reusable student phase guidance shell
-+  - [x] Add a dedicated student-facing phase guidance component
-+  - [x] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [x] Task: Run lint and automated tests for the track scope
-+- [x] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-exec
-/bin/bash -lc 'git commit -m "feat(lesson): align phase shell with curriculum guidance"' in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
-[experimental-rewrite f9a651e] feat(lesson): align phase shell with curriculum guidance
- 14 files changed, 532 insertions(+), 60 deletions(-)
- create mode 100644 __tests__/components/student/PhaseGuidanceCard.test.tsx
- create mode 100644 __tests__/lib/curriculum/phase-guidance.test.ts
- create mode 100644 components/student/PhaseGuidanceCard.tsx
- create mode 100644 conductor/tracks/lesson_phase_objective_shell_20260311/index.md
- create mode 100644 conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
- create mode 100644 conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
- create mode 100644 conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
- create mode 100644 lib/curriculum/phase-guidance.ts
-file update:
-diff --git a/README.md b/README.md
-index 9ee7fe58cfca200160c7dcd63527376b6a6e3202..d15e181d7fdd88c0d5393d28b496fdbcdc953762
---- a/README.md
-+++ b/README.md
-@@ -17,6 +17,7 @@
- - **Shared Dashboard Recommendation Cards**: Student and teacher progress surfaces now reuse one next-lesson card and shared unit-status presentation helpers to keep resume/start guidance aligned
- - **Shared Published Progress View-Models**: Student dashboard units, teacher student-detail progress rows, and lesson phase-status responses now derive from the same published-progress helpers so progress math and locking rules stay aligned
- - **Canonical Activity Component Catalog**: Activity rendering and validation now resolve documented aliases such as `spreadsheet-activity` and `journal-entry-activity` through one canonical runtime contract, reducing curriculum authoring drift
-+- **Objective-Aware Lesson Phase Shell**: Student lesson phases now show curriculum-aligned phase guidance and success criteria while reusing the shared phase content renderer
- - **Deterministic Simulation Runtime IDs**: Inventory simulation notifications and market events now keep stable unique ids during rapid updates, preventing duplicate-key rendering regressions
- - **Account Settings & Self-Service Password Changes**: Authenticated users can review account context and update their own password without leaving the session
- - **Multi-tenant Architecture**: Organization-based access control
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
-+
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
-     );
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
-+
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
-+
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
-+  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
 +
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
-+    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
-+  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
-index 5a1f9d47c9c577b016380d41f806bcb496b3903e..d8b4b634818b45cfc7f4d67cb6059d6c008a677c
---- a/conductor/lessons-learned.md
-+++ b/conductor/lessons-learned.md
-@@ -14,6 +14,7 @@
- - (2026-03-11, activity_component_contract_refactor_20260311) Activity contracts drift quickly when alias handling lives separately from the runtime registry and schema validators; documented curriculum keys need one canonical resolver.
- - (2026-03-11, published_progress_viewmodel_consolidation_20260311) Student dashboard rows, teacher student-detail rows, and lesson phase-status payloads should derive from the same published-progress helpers or published-version/locking fixes will diverge again.
- - (2026-03-11, student_lesson_access_boundary_cleanup_20260311) Student-owned API writes need an explicit request-level student guard even when teacher/admin lesson preview is intentionally allowed elsewhere; auth presence alone is not a safe mutation boundary.
-+- (2026-03-11, lesson_phase_objective_shell_20260311) Student lesson pages should derive phase guidance from curriculum lesson-type metadata and reuse the shared `PhaseRenderer`, or pedagogy copy and content rendering drift separately.
- 
- ### Recurring Gotchas
- <!-- Problems encountered repeatedly; save future tracks from the same pain -->
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..364eb86a46b01b017fb851065b622f944b094de2
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": 7,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..2c93ed93a25fc6373d8d1f962121f21b751f077d
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [x] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [x] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [x] Assert guided-practice and graded-phase success criteria are exposed
-+- [x] Task: Add failing tests for the student phase shell
-+  - [x] Add a focused component test for the reusable phase guidance card
-+  - [x] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [x] Task: Implement shared curriculum phase-guidance helpers
-+  - [x] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [x] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [x] Task: Render the reusable student phase guidance shell
-+  - [x] Add a dedicated student-facing phase guidance component
-+  - [x] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [x] Task: Run lint and automated tests for the track scope
-+- [x] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-exec
-/bin/bash -lc 'git notes add -m "Track: lesson_phase_objective_shell_20260311
-Summary: Added canonical curriculum phase-guidance metadata and a student-facing phase guidance card, then refactored LessonRenderer onto the shared PhaseRenderer path while preserving activity completion refresh behavior.
-Files: README.md, components/student/LessonRenderer.tsx, components/student/PhaseGuidanceCard.tsx, components/lesson/PhaseRenderer.tsx, lib/curriculum/phase-guidance.ts, __tests__/components/student/LessonRenderer.test.tsx, __tests__/components/student/PhaseGuidanceCard.test.tsx, __tests__/lib/curriculum/phase-guidance.test.ts, conductor/lessons-learned.md, conductor/tracks.md, conductor/tracks/lesson_phase_objective_shell_20260311/{index.md,metadata.json,plan.md,spec.md}
-Why: Student lesson phases needed curriculum-aligned guidance and a shared rendering path so lesson pedagogy and phase content presentation do not drift separately." f9a651e' in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
-
-file update:
-diff --git a/README.md b/README.md
-index 9ee7fe58cfca200160c7dcd63527376b6a6e3202..d15e181d7fdd88c0d5393d28b496fdbcdc953762
---- a/README.md
-+++ b/README.md
-@@ -17,6 +17,7 @@
- - **Shared Dashboard Recommendation Cards**: Student and teacher progress surfaces now reuse one next-lesson card and shared unit-status presentation helpers to keep resume/start guidance aligned
- - **Shared Published Progress View-Models**: Student dashboard units, teacher student-detail progress rows, and lesson phase-status responses now derive from the same published-progress helpers so progress math and locking rules stay aligned
- - **Canonical Activity Component Catalog**: Activity rendering and validation now resolve documented aliases such as `spreadsheet-activity` and `journal-entry-activity` through one canonical runtime contract, reducing curriculum authoring drift
-+- **Objective-Aware Lesson Phase Shell**: Student lesson phases now show curriculum-aligned phase guidance and success criteria while reusing the shared phase content renderer
- - **Deterministic Simulation Runtime IDs**: Inventory simulation notifications and market events now keep stable unique ids during rapid updates, preventing duplicate-key rendering regressions
- - **Account Settings & Self-Service Password Changes**: Authenticated users can review account context and update their own password without leaving the session
- - **Multi-tenant Architecture**: Organization-based access control
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
  
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
-     );
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
-+
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
-+
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
-+  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
 +
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
-+    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
-+  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
-index 5a1f9d47c9c577b016380d41f806bcb496b3903e..d8b4b634818b45cfc7f4d67cb6059d6c008a677c
---- a/conductor/lessons-learned.md
-+++ b/conductor/lessons-learned.md
-@@ -14,6 +14,7 @@
- - (2026-03-11, activity_component_contract_refactor_20260311) Activity contracts drift quickly when alias handling lives separately from the runtime registry and schema validators; documented curriculum keys need one canonical resolver.
- - (2026-03-11, published_progress_viewmodel_consolidation_20260311) Student dashboard rows, teacher student-detail rows, and lesson phase-status payloads should derive from the same published-progress helpers or published-version/locking fixes will diverge again.
- - (2026-03-11, student_lesson_access_boundary_cleanup_20260311) Student-owned API writes need an explicit request-level student guard even when teacher/admin lesson preview is intentionally allowed elsewhere; auth presence alone is not a safe mutation boundary.
-+- (2026-03-11, lesson_phase_objective_shell_20260311) Student lesson pages should derive phase guidance from curriculum lesson-type metadata and reuse the shared `PhaseRenderer`, or pedagogy copy and content rendering drift separately.
- 
- ### Recurring Gotchas
- <!-- Problems encountered repeatedly; save future tracks from the same pain -->
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..364eb86a46b01b017fb851065b622f944b094de2
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": 7,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..2c93ed93a25fc6373d8d1f962121f21b751f077d
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [x] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [x] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [x] Assert guided-practice and graded-phase success criteria are exposed
-+- [x] Task: Add failing tests for the student phase shell
-+  - [x] Add a focused component test for the reusable phase guidance card
-+  - [x] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [x] Task: Implement shared curriculum phase-guidance helpers
-+  - [x] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [x] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [x] Task: Render the reusable student phase guidance shell
-+  - [x] Add a dedicated student-facing phase guidance component
-+  - [x] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [x] Task: Run lint and automated tests for the track scope
-+- [x] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-exec
-/bin/bash -lc 'date -u +"%Y-%m-%dT%H:%M:%SZ"' in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
-2026-03-11T08:44:39Z
-file update:
-diff --git a/README.md b/README.md
-index 9ee7fe58cfca200160c7dcd63527376b6a6e3202..d15e181d7fdd88c0d5393d28b496fdbcdc953762
---- a/README.md
-+++ b/README.md
-@@ -17,6 +17,7 @@
- - **Shared Dashboard Recommendation Cards**: Student and teacher progress surfaces now reuse one next-lesson card and shared unit-status presentation helpers to keep resume/start guidance aligned
- - **Shared Published Progress View-Models**: Student dashboard units, teacher student-detail progress rows, and lesson phase-status responses now derive from the same published-progress helpers so progress math and locking rules stay aligned
- - **Canonical Activity Component Catalog**: Activity rendering and validation now resolve documented aliases such as `spreadsheet-activity` and `journal-entry-activity` through one canonical runtime contract, reducing curriculum authoring drift
-+- **Objective-Aware Lesson Phase Shell**: Student lesson phases now show curriculum-aligned phase guidance and success criteria while reusing the shared phase content renderer
- - **Deterministic Simulation Runtime IDs**: Inventory simulation notifications and market events now keep stable unique ids during rapid updates, preventing duplicate-key rendering regressions
- - **Account Settings & Self-Service Password Changes**: Authenticated users can review account context and update their own password without leaving the session
- - **Multi-tenant Architecture**: Organization-based access control
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
  
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
-     );
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
-+
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
-+
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
-+  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
-+
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
 +    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
-+  });
-+
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
- 
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
- 
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
-index 5a1f9d47c9c577b016380d41f806bcb496b3903e..d8b4b634818b45cfc7f4d67cb6059d6c008a677c
---- a/conductor/lessons-learned.md
-+++ b/conductor/lessons-learned.md
-@@ -14,6 +14,7 @@
- - (2026-03-11, activity_component_contract_refactor_20260311) Activity contracts drift quickly when alias handling lives separately from the runtime registry and schema validators; documented curriculum keys need one canonical resolver.
- - (2026-03-11, published_progress_viewmodel_consolidation_20260311) Student dashboard rows, teacher student-detail rows, and lesson phase-status payloads should derive from the same published-progress helpers or published-version/locking fixes will diverge again.
- - (2026-03-11, student_lesson_access_boundary_cleanup_20260311) Student-owned API writes need an explicit request-level student guard even when teacher/admin lesson preview is intentionally allowed elsewhere; auth presence alone is not a safe mutation boundary.
-+- (2026-03-11, lesson_phase_objective_shell_20260311) Student lesson pages should derive phase guidance from curriculum lesson-type metadata and reuse the shared `PhaseRenderer`, or pedagogy copy and content rendering drift separately.
- 
- ### Recurring Gotchas
- <!-- Problems encountered repeatedly; save future tracks from the same pain -->
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/index.md b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..4d352ed1cbc4bdfae1ad5964f2110e6352c164ca
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/index.md
-@@ -0,0 +1,4 @@
-+# Track Index
-+
-+- [Specification](./spec.md)
-+- [Implementation Plan](./plan.md)
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-new file mode 100644
-index 0000000000000000000000000000000000000000..364eb86a46b01b017fb851065b622f944b094de2
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
-@@ -0,0 +1,11 @@
-+{
-+  "track_id": "lesson_phase_objective_shell_20260311",
-+  "type": "feature",
-+  "status": "in_progress",
-+  "created_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:34:26Z",
-+  "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-+  "estimated_tasks": 7,
-+  "actual_tasks": 7,
-+  "deviation_notes": ""
-+}
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..2c93ed93a25fc6373d8d1f962121f21b751f077d
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
-@@ -0,0 +1,22 @@
-+# Implementation Plan
-+
-+## Phase 1: Phase Guidance Red Tests
-+- [x] Task: Add failing tests for curriculum phase-guidance metadata
-+  - [x] Cover accounting, excel, project, and assessment phase guidance lookup
-+  - [x] Assert guided-practice and graded-phase success criteria are exposed
-+- [x] Task: Add failing tests for the student phase shell
-+  - [x] Add a focused component test for the reusable phase guidance card
-+  - [x] Add lesson-renderer regression tests that expect the current phase guidance shell and shared `PhaseRenderer`
-+
-+## Phase 2: Objective-Aware Student Phase Shell
-+- [x] Task: Implement shared curriculum phase-guidance helpers
-+  - [x] Add canonical phase goal and success-criteria metadata keyed by lesson type and phase number
-+  - [x] Expose a helper that resolves the current lesson type and phase guidance with safe fallback behavior
-+- [x] Task: Render the reusable student phase guidance shell
-+  - [x] Add a dedicated student-facing phase guidance component
-+  - [x] Refactor `LessonRenderer` to render the guidance shell and shared `PhaseRenderer` while preserving progress/navigation behavior
-+
-+## Phase 3: Verification and Closeout
-+- [x] Task: Run lint and automated tests for the track scope
-+- [x] Task: Update README and Conductor memory files with the phase-shell alignment outcome
-+- [ ] Task: Archive the track and finalize registry/metadata updates
-diff --git a/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-new file mode 100644
-index 0000000000000000000000000000000000000000..3206316cdff30053726fe021f3e9f7ebd0f2c885
---- /dev/null
-+++ b/conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
-@@ -0,0 +1,33 @@
-+# Specification
-+
-+## Overview
-+
-+Student lesson phases currently render through a generic shell that shows the lesson title and raw content blocks, but it does not communicate the curriculum intent of the current phase. To support coherent curriculum implementation, the student lesson view should present phase-specific guidance that matches the lesson type, the current phase objective, and the lesson's learning objectives while reusing the shared lesson content renderer instead of duplicating block presentation.
-+
-+## Functional Requirements
-+
-+1. The curriculum layer must expose canonical phase-guidance metadata for each lesson type and phase number used in the six-phase lesson model.
-+2. The student lesson phase view must render a reusable guidance component that shows the current phase purpose and practical success criteria derived from the curriculum guidance plus the lesson's learning objectives.
-+3. The guidance component must work for at least accounting, excel, project, and assessment lesson types, with safe fallback behavior if a phase configuration is incomplete.
-+4. The student lesson renderer must use the shared `PhaseRenderer` content-block renderer instead of maintaining a second inline renderer for markdown, callouts, media, and activities.
-+5. Existing student phase progress, completion, and navigation behavior must remain intact.
-+
-+## Non-Functional Requirements
-+
-+1. New curriculum guidance logic must be covered by automated tests.
-+2. The student lesson rendering changes must be regression-tested at the component level.
-+3. The implementation must stay within the existing Vinext/React/TypeScript stack and avoid dependency changes.
-+
-+## Acceptance Criteria
-+
-+- Accounting and Excel lesson phases render guidance that clearly states the current phase goal and what successful completion looks like.
-+- Guided-practice phases explicitly communicate the interactive requirement through the rendered student shell.
-+- Lesson-specific learning objectives are surfaced in the current phase guidance without replacing the lesson-wide objectives section.
-+- Student lessons continue to render content blocks and activities through the shared lesson renderer path.
-+- Automated tests cover the new phase-guidance model and the updated student lesson rendering behavior.
-+
-+## Out of Scope
-+
-+- New interactive activity types or Convex persistence changes.
-+- Curriculum seed rewrites beyond what is required to support the guidance shell.
-+- Teacher-facing lesson-plan changes.
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-exec
-/bin/bash -lc 'mv conductor/tracks/lesson_phase_objective_shell_20260311 conductor/archive/lesson_phase_objective_shell_20260311' in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
-
-file update:
-diff --git a/README.md b/README.md
-index 9ee7fe58cfca200160c7dcd63527376b6a6e3202..d15e181d7fdd88c0d5393d28b496fdbcdc953762
---- a/README.md
-+++ b/README.md
-@@ -17,6 +17,7 @@
- - **Shared Dashboard Recommendation Cards**: Student and teacher progress surfaces now reuse one next-lesson card and shared unit-status presentation helpers to keep resume/start guidance aligned
- - **Shared Published Progress View-Models**: Student dashboard units, teacher student-detail progress rows, and lesson phase-status responses now derive from the same published-progress helpers so progress math and locking rules stay aligned
- - **Canonical Activity Component Catalog**: Activity rendering and validation now resolve documented aliases such as `spreadsheet-activity` and `journal-entry-activity` through one canonical runtime contract, reducing curriculum authoring drift
-+- **Objective-Aware Lesson Phase Shell**: Student lesson phases now show curriculum-aligned phase guidance and success criteria while reusing the shared phase content renderer
- - **Deterministic Simulation Runtime IDs**: Inventory simulation notifications and market events now keep stable unique ids during rapid updates, preventing duplicate-key rendering regressions
- - **Account Settings & Self-Service Password Changes**: Authenticated users can review account context and update their own password without leaving the session
- - **Multi-tenant Architecture**: Organization-based access control
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
- 
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
 +
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
 +    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
-+  });
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
  
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
-index 5a1f9d47c9c577b016380d41f806bcb496b3903e..d8b4b634818b45cfc7f4d67cb6059d6c008a677c
---- a/conductor/lessons-learned.md
-+++ b/conductor/lessons-learned.md
-@@ -14,6 +14,7 @@
- - (2026-03-11, activity_component_contract_refactor_20260311) Activity contracts drift quickly when alias handling lives separately from the runtime registry and schema validators; documented curriculum keys need one canonical resolver.
- - (2026-03-11, published_progress_viewmodel_consolidation_20260311) Student dashboard rows, teacher student-detail rows, and lesson phase-status payloads should derive from the same published-progress helpers or published-version/locking fixes will diverge again.
- - (2026-03-11, student_lesson_access_boundary_cleanup_20260311) Student-owned API writes need an explicit request-level student guard even when teacher/admin lesson preview is intentionally allowed elsewhere; auth presence alone is not a safe mutation boundary.
-+- (2026-03-11, lesson_phase_objective_shell_20260311) Student lesson pages should derive phase guidance from curriculum lesson-type metadata and reuse the shared `PhaseRenderer`, or pedagogy copy and content rendering drift separately.
- 
- ### Recurring Gotchas
- <!-- Problems encountered repeatedly; save future tracks from the same pain -->
-diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..52fd09f42994229b46d4c191f1f51cb6dd4f3388
---- a/conductor/tracks.md
-+++ b/conductor/tracks.md
-@@ -6,6 +6,10 @@
- 
- ## Active Execution Queue (progressive order)
- 
-+- [~] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
-+  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
-+
- ## Upcoming / Unplanned
- 
- 1. Convex Auth Runtime Verification & Closeout
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-file update
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/tracks.md
-@@ -8,6 +8,2 @@
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
--- [~] **Track: Lesson Phase Objective Shell**
--  *Link: [./conductor/tracks/lesson_phase_objective_shell_20260311/](./conductor/tracks/lesson_phase_objective_shell_20260311/)*
--  *Goal: align the student lesson phase shell with curriculum phase objectives by rendering reusable phase guidance and shared block rendering.*
--
- ## Upcoming / Unplanned
-@@ -38,2 +34,6 @@
- 
-+- [x] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/archive/lesson_phase_objective_shell_20260311/](./conductor/archive/lesson_phase_objective_shell_20260311/)*
-+  *Checkpoint: `f9a651e` (2026-03-11 implementation complete; archived during closeout)*
-+
- - [x] **Track: Student Write Boundary Hardening**
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-@@ -21,2 +21,2 @@
- - [x] Task: Update README and Conductor memory files with the phase-shell alignment outcome
--- [ ] Task: Archive the track and finalize registry/metadata updates
-+- [x] Task: Archive the track and finalize registry/metadata updates
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-@@ -3,5 +3,5 @@
-   "type": "feature",
--  "status": "in_progress",
-+  "status": "archived",
-   "created_at": "2026-03-11T08:34:26Z",
--  "updated_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:44:39Z",
-   "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-apply_patch(auto_approved=true) exited 0 in 42ms:
-Success. Updated the following files:
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-M /home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/tracks.md
-file update:
-diff --git a/README.md b/README.md
-index 9ee7fe58cfca200160c7dcd63527376b6a6e3202..d15e181d7fdd88c0d5393d28b496fdbcdc953762
---- a/README.md
-+++ b/README.md
-@@ -17,6 +17,7 @@
- - **Shared Dashboard Recommendation Cards**: Student and teacher progress surfaces now reuse one next-lesson card and shared unit-status presentation helpers to keep resume/start guidance aligned
- - **Shared Published Progress View-Models**: Student dashboard units, teacher student-detail progress rows, and lesson phase-status responses now derive from the same published-progress helpers so progress math and locking rules stay aligned
- - **Canonical Activity Component Catalog**: Activity rendering and validation now resolve documented aliases such as `spreadsheet-activity` and `journal-entry-activity` through one canonical runtime contract, reducing curriculum authoring drift
-+- **Objective-Aware Lesson Phase Shell**: Student lesson phases now show curriculum-aligned phase guidance and success criteria while reusing the shared phase content renderer
- - **Deterministic Simulation Runtime IDs**: Inventory simulation notifications and market events now keep stable unique ids during rapid updates, preventing duplicate-key rendering regressions
- - **Account Settings & Self-Service Password Changes**: Authenticated users can review account context and update their own password without leaving the session
- - **Multi-tenant Architecture**: Organization-based access control
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
  
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
 +
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+ succeeded in 11m 18s:
+
+> test
+> vitest run
+
+
+ RUN  v4.0.16 /home/daniel-bo/Desktop/bus-math-v2
+
+[baseline-browser-mapping] The data in this module is over two months old.  To ensure accurate Baseline data, please update: `npm i baseline-browser-mapping@latest -D`
+ ✓ __tests__/components/teacher/UnitOverview.test.tsx (14 tests) 2328ms
+     ✓ renders unit title and number  479ms
+ ✓ __tests__/components/login-form.test.tsx (17 tests) 13855ms
+     ✓ renders login form with username and password fields  1253ms
+     ✓ calls signIn with username and password on form submission  1544ms
+     ✓ displays user-friendly error message on invalid credentials  1419ms
+     ✓ attempts auto-provisioning for demo credentials before login  1317ms
+     ✓ displays generic error message for unknown errors  1034ms
+     ✓ disables submit button while loading  1084ms
+     ✓ redirects to redirect query param after successful login  1187ms
+     ✓ redirects to student dashboard for student role without redirect param  731ms
+     ✓ redirects to teacher dashboard for teacher role  1012ms
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
  
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+exec
+/bin/bash -lc 'npm run build' in /home/daniel-bo/Desktop/bus-math-v2file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
    });
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
  
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
 +      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+codex
+`npm test` is clean across the full suite. I’m on the last required gate now: production build. After that I’ll update the Conductor artifacts, archive the track, and prepare the two required commits.
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
 +
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
 +    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
-+  });
 +
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+codex
+The build has compiled successfully and is in the type-check/finalization phase now. If it exits cleanly, the remaining work is Conductor metadata cleanup plus the requested commits and push.
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+ succeeded in 4m 09s:
+
+> build
+> vinext build
+
+[baseline-browser-mapping] The data in this module is over two months old.  To ensure accurate Baseline data, please update: `npm i baseline-browser-mapping@latest -D`
+▲ Next.js 16.1.1 (Turbopack)
+- Environments: .env.local
+
+  Creating an optimized production build ...
+[baseline-browser-mapping] The data in this module is over two months old.  To ensure accurate Baseline data, please update: `npm i baseline-browser-mapping@latest -D`
+[baseline-browser-mapping] The data in this module is over two months old.  To ensure accurate Baseline data, please update: `npm i baseline-browser-mapping@latest -D`
+✓ Compiled successfully in 112s
+  Running TypeScript ...
+  Collecting page data using 3 workers ...
+[baseline-browser-mapping] The data in this module is over two months old.  To ensure accurate Baseline data, please update: `npm i baseline-browser-mapping@latest -D`
+[baseline-browser-mapping] The data in this module is over two months old.  To ensure accurate Baseline data, please update: `npm i baseline-browser-mapping@latest -D`
+[baseline-browser-mapping] The data in this module is over two months old.  To ensure accurate Baseline data, please update: `npm i baseline-browser-mapping@latest -D`
+  Generating static pages using 3 workers (0/32) ...
+  Generating static pages using 3 workers (8/32) 
+  Generating static pages using 3 workers (16/32) 
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+codex
+All verification gates are green: lint, the full 215-file Vitest suite, and the production build. I’m in the final Conductor pass now, updating the active track artifacts and project memory before I create the required implementation commit and separate archive commit.
+exec
+/bin/bash -lc 'wc -l conductor/tech-debt.md conductor/lessons-learned.md' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+  10 conductor/tech-debt.md
+  38 conductor/lessons-learned.md
+  48 total
+exec
+/bin/bash -lc 'date -u +"%Y-%m-%dT%H:%M:%SZ"' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+2026-03-14T00:51:55Z
+exec
+/bin/bash -lc 'git status --short' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+ M .env.example
+ M README.md
+ M __tests__/app/curriculum/page.test.tsx
+ M __tests__/app/student/dashboard/page.test.tsx
+ M __tests__/app/teacher/students/[studentId]/page.test.tsx
+ M __tests__/app/teacher/units/[unitNumber]/page.test.tsx
+ M __tests__/components/dashboard/NextLessonCard.test.tsx
+ M __tests__/components/exercises/JournalEntryBuilding.test.tsx
+ M __tests__/components/financial-reports/CashFlowStatementDetailed.test.tsx
+ M __tests__/components/student/PhaseHeader.test.tsx
+ M __tests__/components/teacher/CourseOverviewGrid.test.tsx
+ M __tests__/components/teacher/TeacherLessonPlan.test.tsx
+ M __tests__/components/teacher/UnitLessonPlan.test.tsx
+ M __tests__/components/teacher/UnitOverview.test.tsx
+ M __tests__/curriculum/published-manifest.test.ts
+ M __tests__/lib/convex/config.test.ts
+ M __tests__/proxy.test.ts
+ M __tests__/setup/dev-stack-script.test.ts
+ M app/curriculum/CurriculumUnitCard.tsx
+ M app/student/dashboard/page.tsx
+exec
+/bin/bash -lc 'rg -n "capstone still relies|remaining scope gap|generated capstone scaffold|full phase-1 textbook|authored capstone" conductor docs README.md app lib __tests__' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 55ms:
+conductor/tracks/capstone_textbook_completion_20260314/spec.md:9:1. The capstone must publish from canonical repository-authored lesson content rather than the generic generated capstone scaffold.
+conductor/tracks/capstone_textbook_completion_20260314/spec.md:10:2. The authored capstone runtime content must define explicit milestone checkpoints, final presentation expectations, and evidence requirements aligned to the active curriculum narrative and roadmap deliverables.
+conductor/tracks/capstone_textbook_completion_20260314/spec.md:20:- Active Conductor docs must reflect that the full phase-1 textbook now includes authored capstone runtime content.
+conductor/tracks/capstone_textbook_completion_20260314/metadata.json:7:  "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+conductor/tracks/capstone_textbook_completion_20260314/plan.md:5:- [ ] Task: Add regression tests for authored capstone curriculum coverage
+conductor/tracks/capstone_textbook_completion_20260314/plan.md:7:  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+conductor/tracks/capstone_textbook_completion_20260314/plan.md:16:- [ ] Task: Create canonical authored capstone runtime content
+conductor/tracks/capstone_textbook_completion_20260314/plan.md:17:  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+conductor/tracks/capstone_textbook_completion_20260314/plan.md:18:  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+conductor/tracks/capstone_textbook_completion_20260314/plan.md:26:  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+conductor/tech-debt.md:7:| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+conductor/codex-cron.log:42:| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+conductor/codex-cron.log:156:- [ ] Task: Add regression tests for authored capstone curriculum coverage
+conductor/codex-cron.log:158:  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+conductor/codex-cron.log:167:- [ ] Task: Create canonical authored capstone runtime content
+conductor/codex-cron.log:168:  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+conductor/codex-cron.log:169:  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+conductor/codex-cron.log:182:1. The capstone must publish from canonical repository-authored lesson content rather than the generic generated capstone scaffold.
+conductor/codex-cron.log:183:2. The authored capstone runtime content must define explicit milestone checkpoints, final presentation expectations, and evidence requirements aligned to the active curriculum narrative and roadmap deliverables.
+conductor/codex-cron.log:193:- Active Conductor docs must reflect that the full phase-1 textbook now includes authored capstone runtime content.
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+exec
+/bin/bash -lc 'git diff -- convex/teacher.ts lib/db/schema/phase-content.ts lib/db/schema/validators.ts lib/activities/component-keys.ts types/curriculum.ts' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+diff --git a/convex/teacher.ts b/convex/teacher.ts
+index a5a10e6..d7bbd49 100644
+--- a/convex/teacher.ts
++++ b/convex/teacher.ts
+@@ -1,4 +1,5 @@
+ import { internalQuery, type QueryCtx } from "./_generated/server";
++import type { Doc, Id } from "./_generated/dataModel";
+ import { v } from "convex/values";
+ import { assembleCourseOverviewRows } from "../lib/teacher/course-overview";
+ import { assembleGradebookRows } from "../lib/teacher/gradebook";
+@@ -58,9 +59,9 @@ function sortStudentsByName<
+ 
+ async function getAuthorizedTeacher(
+   ctx: QueryCtx,
+-  userId: string,
+-) {
+-  const teacher = await ctx.db.get(userId as never);
++  userId: Id<"profiles">,
++): Promise<Doc<"profiles"> | null> {
++  const teacher = await ctx.db.get(userId);
+exec
+/bin/bash -lc 'git diff -- __tests__/components/exercises/JournalEntryBuilding.test.tsx __tests__/components/financial-reports/CashFlowStatementDetailed.test.tsx' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+diff --git a/__tests__/components/exercises/JournalEntryBuilding.test.tsx b/__tests__/components/exercises/JournalEntryBuilding.test.tsx
+index a85f3f5..34e5e30 100644
+--- a/__tests__/components/exercises/JournalEntryBuilding.test.tsx
++++ b/__tests__/components/exercises/JournalEntryBuilding.test.tsx
+@@ -69,5 +69,5 @@ describe('JournalEntryBuilding', () => {
+         })
+       )
+     })
+-  })
++  }, 15_000)
+ })
+diff --git a/__tests__/components/financial-reports/CashFlowStatementDetailed.test.tsx b/__tests__/components/financial-reports/CashFlowStatementDetailed.test.tsx
+index c98ef4a..7cb6a16 100644
+--- a/__tests__/components/financial-reports/CashFlowStatementDetailed.test.tsx
++++ b/__tests__/components/financial-reports/CashFlowStatementDetailed.test.tsx
+@@ -99,5 +99,5 @@ describe('CashFlowStatementDetailed', () => {
+     await user.click(screen.getByRole('button', { name: /show details/i }));
+ 
+     expect(screen.getByText(/Total Adjustments/i)).toBeInTheDocument();
+-  });
+exec
+/bin/bash -lc 'git diff -- lib/convex/admin.ts __tests__/lib/convex/config.test.ts scripts/dev-stack.mjs __tests__/setup/dev-stack-script.test.ts README.md .env.example conductor/docs/architecture/runtime.md docs/security-api-route-matrix.md proxy.ts __tests__/proxy.test.ts' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+diff --git a/.env.example b/.env.example
+index 16cb66f..3ba4885 100644
+--- a/.env.example
++++ b/.env.example
+@@ -4,7 +4,7 @@ NEXT_PUBLIC_CONVEX_URL=http://127.0.0.1:3210
+ 
+ # Required for server-side internal Convex calls in cloud/production environments.
+ # Keep this private (never expose via NEXT_PUBLIC_*).
+-# Local development can fall back to `.convex/local/*/config.json`.
++# Local development can fall back to Convex local state under `~/.convex/`.
+ CONVEX_DEPLOY_KEY=
+ 
+ # JWT signing secret for session cookies.
+diff --git a/README.md b/README.md
+index 22af126..f8ded20 100644
+--- a/README.md
++++ b/README.md
+@@ -60,7 +60,7 @@ Both accounts are intended for local development and test environments. Preview
+ 
+ 3. Start Convex locally:
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+
+codex
+I’ve separated the capstone track artifacts from unrelated dirty files in the worktree. Now I’m updating only the Conductor state for this track: completed plan markers, metadata, memory files, and a concise roadmap status note before I stage the implementation commit.
+file update
+M /home/daniel-bo/Desktop/bus-math-v2/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+@@ -3,9 +3,9 @@
+   "type": "feature",
+-  "status": "in_progress",
++  "status": "completed",
+   "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
++  "updated_at": "2026-03-14T00:51:55Z",
+   "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+   "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
++  "actual_tasks": 6,
++  "deviation_notes": "Capstone labeling normalization was extended into shared teacher planning and student lesson-header components so generic Unit 9 labels could not survive in secondary textbook surfaces."
+ }
+M /home/daniel-bo/Desktop/bus-math-v2/conductor/lessons-learned.md
+@@ -25,2 +25,3 @@
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+@@ -38 +39,2 @@
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+M /home/daniel-bo/Desktop/bus-math-v2/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+@@ -4,10 +4,10 @@
+ 
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
++- [x] Task: Add regression tests for authored capstone curriculum coverage
++  - [x] Write tests that fail if the published capstone lesson is still generated
++  - [x] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
++- [x] Task: Add regression tests for textbook capstone labeling
++  - [x] Write tests that fail if the public curriculum card labels the capstone as a generic unit
++  - [x] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
++- [x] Task: Mark the new track active in Conductor and capture implementation status
++  - [x] Update track/task status markers as execution progresses
+ 
+@@ -15,8 +15,8 @@
+ 
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
++- [x] Task: Create canonical authored capstone runtime content
++  - [x] Add a repository-authored capstone source module aligned to the active curriculum narrative
++  - [x] Replace the generated capstone manifest builder with authored capstone integration
++- [x] Task: Normalize capstone labeling across key textbook surfaces
++  - [x] Add a shared capstone label helper for published curriculum surfaces
++  - [x] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+ 
+@@ -24,8 +24,8 @@
+ 
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
++- [x] Task: Synchronize active Conductor docs and project memory for textbook completion
++  - [x] Update impacted Conductor docs to reflect authored capstone completion
++  - [x] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
++- [x] Task: Run required verification and finalize the track
++  - [x] Run `npm run lint`
++  - [x] Run `npm test`
++  - [x] Run `npm run build`
+M /home/daniel-bo/Desktop/bus-math-v2/conductor/tech-debt.md
+@@ -6,3 +6,2 @@
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+M /home/daniel-bo/Desktop/bus-math-v2/conductor/roadmap.md
+@@ -107,2 +107,4 @@
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+apply_patch(auto_approved=true) exited 0 in 46ms:
+Success. Updated the following files:
+M /home/daniel-bo/Desktop/bus-math-v2/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+M /home/daniel-bo/Desktop/bus-math-v2/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+M /home/daniel-bo/Desktop/bus-math-v2/conductor/tech-debt.md
+M /home/daniel-bo/Desktop/bus-math-v2/conductor/lessons-learned.md
+M /home/daniel-bo/Desktop/bus-math-v2/conductor/roadmap.md
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
+index c6744e6265e1305f9e62ae2ff35179c8e2a6a46c..cfcfcc73b61799c67dee30ab713d6cfa49742d13
+--- a/conductor/lessons-learned.md
++++ b/conductor/lessons-learned.md
+@@ -23,6 +23,7 @@
+ - (2026-03-13, unit1_canonicalization_archetypes) Canonicalizing authored curriculum at the source is safer than accumulating manifest exceptions because both validation drift and role-specific rendering bugs disappear together.
+ - (2026-03-13, auth bootstrap hotfix) JWT-protected proxy rules must explicitly allow unauthenticated auth bootstrap endpoints like `/api/auth/login` and `/api/auth/session`, or the login page will redirect its own API calls back to itself.
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+ ### Patterns Worth Repeating
+ 
+@@ -36,3 +37,4 @@
+ - (2026-03-13, curriculum_rollout_wave1) Lesson-planning matrices are a workable source for authored rollout waves when they are converted into canonical lesson blueprints and validated through manifest regression tests before runtime wiring.
+ - (2026-03-13, unit1_redesign_lesson_contract) Old infrastructure tests should validate canonical phase numbers and contract outcomes rather than stale phase labels, or redesign tracks will appear broken even when the authored curriculum and published runtime agree.
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+diff --git a/conductor/roadmap.md b/conductor/roadmap.md
+index 11d3802482df109f7afa63e1f8e06a29b281e2de..1ba34cd74dce8269c6b81c2e0e4a298afca7f180
+--- a/conductor/roadmap.md
++++ b/conductor/roadmap.md
+@@ -105,6 +105,8 @@
+ 
+ **Goal**: finish the book with the culminating experience and final coherence pass.
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+ 
+ - Capstone and Textbook Completion
+diff --git a/conductor/tech-debt.md b/conductor/tech-debt.md
+index 9bac252a4eba200df1bc57aede467e480d1d1f2a..640e1a1e5f5401852a117c3e7cf478b2cdbc1dff
+--- a/conductor/tech-debt.md
++++ b/conductor/tech-debt.md
+@@ -4,7 +4,6 @@
+ 
+ | Date | Track | Item | Severity | Status | Notes |
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+ | 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+ | 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+index e53f588834f06d46a36ab82106566ba908bb6cbc..d0163977afd242fdc5af0413ba1bf01af34a4964
+--- a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
++++ b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+@@ -1,11 +1,11 @@
+ {
+   "track_id": "capstone_textbook_completion_20260314",
+   "type": "feature",
+-  "status": "in_progress",
++  "status": "completed",
+   "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
++  "updated_at": "2026-03-14T00:51:55Z",
+   "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+   "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
++  "actual_tasks": 6,
++  "deviation_notes": "Capstone labeling normalization was extended into shared teacher planning and student lesson-header components so generic Unit 9 labels could not survive in secondary textbook surfaces."
+ }
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/plan.md b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+index 48b611e5d419a88dad2df9bf2d2a717532f66631..b6ea5b8820bf3add0cda7adf573ab7d766ea9a58
+--- a/conductor/tracks/capstone_textbook_completion_20260314/plan.md
++++ b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+@@ -2,30 +2,30 @@
+ 
+ ## Phase 1: Capstone Contract Guards
+ 
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
++- [x] Task: Add regression tests for authored capstone curriculum coverage
++  - [x] Write tests that fail if the published capstone lesson is still generated
++  - [x] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
++- [x] Task: Add regression tests for textbook capstone labeling
++  - [x] Write tests that fail if the public curriculum card labels the capstone as a generic unit
++  - [x] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
++- [x] Task: Mark the new track active in Conductor and capture implementation status
++  - [x] Update track/task status markers as execution progresses
+ 
+ ## Phase 2: Capstone Runtime Completion
+ 
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
++- [x] Task: Create canonical authored capstone runtime content
++  - [x] Add a repository-authored capstone source module aligned to the active curriculum narrative
++  - [x] Replace the generated capstone manifest builder with authored capstone integration
++- [x] Task: Normalize capstone labeling across key textbook surfaces
++  - [x] Add a shared capstone label helper for published curriculum surfaces
++  - [x] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+ 
+ ## Phase 3: Verification and Documentation
+ 
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
++- [x] Task: Synchronize active Conductor docs and project memory for textbook completion
++  - [x] Update impacted Conductor docs to reflect authored capstone completion
++  - [x] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
++- [x] Task: Run required verification and finalize the track
++  - [x] Run `npm run lint`
++  - [x] Run `npm test`
++  - [x] Run `npm run build`
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
+index c6744e6265e1305f9e62ae2ff35179c8e2a6a46c..cfcfcc73b61799c67dee30ab713d6cfa49742d13
+--- a/conductor/lessons-learned.md
++++ b/conductor/lessons-learned.md
+@@ -23,6 +23,7 @@
+ - (2026-03-13, unit1_canonicalization_archetypes) Canonicalizing authored curriculum at the source is safer than accumulating manifest exceptions because both validation drift and role-specific rendering bugs disappear together.
+ - (2026-03-13, auth bootstrap hotfix) JWT-protected proxy rules must explicitly allow unauthenticated auth bootstrap endpoints like `/api/auth/login` and `/api/auth/session`, or the login page will redirect its own API calls back to itself.
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+ ### Patterns Worth Repeating
+ 
+@@ -36,3 +37,4 @@
+ - (2026-03-13, curriculum_rollout_wave1) Lesson-planning matrices are a workable source for authored rollout waves when they are converted into canonical lesson blueprints and validated through manifest regression tests before runtime wiring.
+ - (2026-03-13, unit1_redesign_lesson_contract) Old infrastructure tests should validate canonical phase numbers and contract outcomes rather than stale phase labels, or redesign tracks will appear broken even when the authored curriculum and published runtime agree.
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+diff --git a/conductor/roadmap.md b/conductor/roadmap.md
+index 11d3802482df109f7afa63e1f8e06a29b281e2de..1ba34cd74dce8269c6b81c2e0e4a298afca7f180
+--- a/conductor/roadmap.md
++++ b/conductor/roadmap.md
+@@ -105,6 +105,8 @@
+ 
+ **Goal**: finish the book with the culminating experience and final coherence pass.
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+ 
+ - Capstone and Textbook Completion
+diff --git a/conductor/tech-debt.md b/conductor/tech-debt.md
+index 9bac252a4eba200df1bc57aede467e480d1d1f2a..640e1a1e5f5401852a117c3e7cf478b2cdbc1dff
+--- a/conductor/tech-debt.md
++++ b/conductor/tech-debt.md
+@@ -4,7 +4,6 @@
+ 
+ | Date | Track | Item | Severity | Status | Notes |
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+ | 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+ | 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+index e53f588834f06d46a36ab82106566ba908bb6cbc..d0163977afd242fdc5af0413ba1bf01af34a4964
+--- a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
++++ b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+@@ -1,11 +1,11 @@
+ {
+   "track_id": "capstone_textbook_completion_20260314",
+   "type": "feature",
+-  "status": "in_progress",
++  "status": "completed",
+   "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
++  "updated_at": "2026-03-14T00:51:55Z",
+   "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+   "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
++  "actual_tasks": 6,
++  "deviation_notes": "Capstone labeling normalization was extended into shared teacher planning and student lesson-header components so generic Unit 9 labels could not survive in secondary textbook surfaces."
+ }
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/plan.md b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+index 48b611e5d419a88dad2df9bf2d2a717532f66631..b6ea5b8820bf3add0cda7adf573ab7d766ea9a58
+--- a/conductor/tracks/capstone_textbook_completion_20260314/plan.md
++++ b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+@@ -2,30 +2,30 @@
+ 
+ ## Phase 1: Capstone Contract Guards
+ 
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
++- [x] Task: Add regression tests for authored capstone curriculum coverage
++  - [x] Write tests that fail if the published capstone lesson is still generated
++  - [x] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
++- [x] Task: Add regression tests for textbook capstone labeling
++  - [x] Write tests that fail if the public curriculum card labels the capstone as a generic unit
++  - [x] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
++- [x] Task: Mark the new track active in Conductor and capture implementation status
++  - [x] Update track/task status markers as execution progresses
+ 
+ ## Phase 2: Capstone Runtime Completion
+ 
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
++- [x] Task: Create canonical authored capstone runtime content
++  - [x] Add a repository-authored capstone source module aligned to the active curriculum narrative
++  - [x] Replace the generated capstone manifest builder with authored capstone integration
++- [x] Task: Normalize capstone labeling across key textbook surfaces
++  - [x] Add a shared capstone label helper for published curriculum surfaces
++  - [x] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+ 
+ ## Phase 3: Verification and Documentation
+ 
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
++- [x] Task: Synchronize active Conductor docs and project memory for textbook completion
++  - [x] Update impacted Conductor docs to reflect authored capstone completion
++  - [x] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
++- [x] Task: Run required verification and finalize the track
++  - [x] Run `npm run lint`
++  - [x] Run `npm test`
++  - [x] Run `npm run build`
+
+exec
+/bin/bash -lc 'wc -l conductor/tech-debt.md conductor/lessons-learned.md' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+   9 conductor/tech-debt.md
+  40 conductor/lessons-learned.md
+  49 total
+exec
+/bin/bash -lc "sed -n '1,220p' conductor/tracks/capstone_textbook_completion_20260314/plan.md" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+# Implementation Plan
+
+## Phase 1: Capstone Contract Guards
+
+- [x] Task: Add regression tests for authored capstone curriculum coverage
+  - [x] Write tests that fail if the published capstone lesson is still generated
+  - [x] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+- [x] Task: Add regression tests for textbook capstone labeling
+  - [x] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+  - [x] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+- [x] Task: Mark the new track active in Conductor and capture implementation status
+  - [x] Update track/task status markers as execution progresses
+
+## Phase 2: Capstone Runtime Completion
+
+- [x] Task: Create canonical authored capstone runtime content
+  - [x] Add a repository-authored capstone source module aligned to the active curriculum narrative
+  - [x] Replace the generated capstone manifest builder with authored capstone integration
+- [x] Task: Normalize capstone labeling across key textbook surfaces
+  - [x] Add a shared capstone label helper for published curriculum surfaces
+exec
+/bin/bash -lc "sed -n '1,220p' conductor/roadmap.md" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+# Product Roadmap: Complete Online Textbook
+
+## End State
+
+The product is considered a complete online textbook only when all of the following are true:
+
+- all **88 instructional lessons** plus **capstone** exist as published Convex-backed runtime content
+- every lesson archetype renders correctly in the student experience
+- student progress works from phase -> lesson -> unit -> course
+- teacher monitoring works from course -> unit -> lesson -> student
+- the app is deployable and verified on **Cloudflare Workers**
+- active docs, seeds, and runtime behavior all describe the same curriculum
+
+## Roadmap Principles
+
+1. Build the platform contracts once, then scale content.
+2. Get one textbook-quality vertical slice working before mass authoring.
+3. Ship teacher monitoring from the same progress model the student runtime uses.
+4. Treat content rollout as product work, not as an afterthought after the platform is “done”.
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
+index c6744e6265e1305f9e62ae2ff35179c8e2a6a46c..cfcfcc73b61799c67dee30ab713d6cfa49742d13
+--- a/conductor/lessons-learned.md
++++ b/conductor/lessons-learned.md
+@@ -23,6 +23,7 @@
+ - (2026-03-13, unit1_canonicalization_archetypes) Canonicalizing authored curriculum at the source is safer than accumulating manifest exceptions because both validation drift and role-specific rendering bugs disappear together.
+ - (2026-03-13, auth bootstrap hotfix) JWT-protected proxy rules must explicitly allow unauthenticated auth bootstrap endpoints like `/api/auth/login` and `/api/auth/session`, or the login page will redirect its own API calls back to itself.
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+ ### Patterns Worth Repeating
+ 
+@@ -36,3 +37,4 @@
+ - (2026-03-13, curriculum_rollout_wave1) Lesson-planning matrices are a workable source for authored rollout waves when they are converted into canonical lesson blueprints and validated through manifest regression tests before runtime wiring.
+ - (2026-03-13, unit1_redesign_lesson_contract) Old infrastructure tests should validate canonical phase numbers and contract outcomes rather than stale phase labels, or redesign tracks will appear broken even when the authored curriculum and published runtime agree.
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+diff --git a/conductor/roadmap.md b/conductor/roadmap.md
+index 11d3802482df109f7afa63e1f8e06a29b281e2de..1ba34cd74dce8269c6b81c2e0e4a298afca7f180
+--- a/conductor/roadmap.md
++++ b/conductor/roadmap.md
+@@ -105,6 +105,8 @@
+ 
+ **Goal**: finish the book with the culminating experience and final coherence pass.
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+ 
+ - Capstone and Textbook Completion
+diff --git a/conductor/tech-debt.md b/conductor/tech-debt.md
+index 9bac252a4eba200df1bc57aede467e480d1d1f2a..640e1a1e5f5401852a117c3e7cf478b2cdbc1dff
+--- a/conductor/tech-debt.md
++++ b/conductor/tech-debt.md
+@@ -4,7 +4,6 @@
+ 
+ | Date | Track | Item | Severity | Status | Notes |
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+ | 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+ | 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+index e53f588834f06d46a36ab82106566ba908bb6cbc..d0163977afd242fdc5af0413ba1bf01af34a4964
+--- a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
++++ b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+@@ -1,11 +1,11 @@
+ {
+   "track_id": "capstone_textbook_completion_20260314",
+   "type": "feature",
+-  "status": "in_progress",
++  "status": "completed",
+   "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
++  "updated_at": "2026-03-14T00:51:55Z",
+   "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+   "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
++  "actual_tasks": 6,
++  "deviation_notes": "Capstone labeling normalization was extended into shared teacher planning and student lesson-header components so generic Unit 9 labels could not survive in secondary textbook surfaces."
+ }
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/plan.md b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+index 48b611e5d419a88dad2df9bf2d2a717532f66631..b6ea5b8820bf3add0cda7adf573ab7d766ea9a58
+--- a/conductor/tracks/capstone_textbook_completion_20260314/plan.md
++++ b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+@@ -2,30 +2,30 @@
+ 
+ ## Phase 1: Capstone Contract Guards
+ 
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
++- [x] Task: Add regression tests for authored capstone curriculum coverage
++  - [x] Write tests that fail if the published capstone lesson is still generated
++  - [x] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
++- [x] Task: Add regression tests for textbook capstone labeling
++  - [x] Write tests that fail if the public curriculum card labels the capstone as a generic unit
++  - [x] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
++- [x] Task: Mark the new track active in Conductor and capture implementation status
++  - [x] Update track/task status markers as execution progresses
+ 
+ ## Phase 2: Capstone Runtime Completion
+ 
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
++- [x] Task: Create canonical authored capstone runtime content
++  - [x] Add a repository-authored capstone source module aligned to the active curriculum narrative
++  - [x] Replace the generated capstone manifest builder with authored capstone integration
++- [x] Task: Normalize capstone labeling across key textbook surfaces
++  - [x] Add a shared capstone label helper for published curriculum surfaces
++  - [x] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+ 
+ ## Phase 3: Verification and Documentation
+ 
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
++- [x] Task: Synchronize active Conductor docs and project memory for textbook completion
++  - [x] Update impacted Conductor docs to reflect authored capstone completion
++  - [x] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
++- [x] Task: Run required verification and finalize the track
++  - [x] Run `npm run lint`
++  - [x] Run `npm test`
++  - [x] Run `npm run build`
+
+exec
+/bin/bash -lc 'git ls-files --error-unmatch lib/curriculum/generated/capstone-authored.ts' in /home/daniel-bo/Desktop/bus-math-v2 exited 1 in 0ms:
+error: pathspec 'lib/curriculum/generated/capstone-authored.ts' did not match any file(s) known to git
+Did you forget to 'git add'?
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
+index c6744e6265e1305f9e62ae2ff35179c8e2a6a46c..cfcfcc73b61799c67dee30ab713d6cfa49742d13
+--- a/conductor/lessons-learned.md
++++ b/conductor/lessons-learned.md
+@@ -23,6 +23,7 @@
+ - (2026-03-13, unit1_canonicalization_archetypes) Canonicalizing authored curriculum at the source is safer than accumulating manifest exceptions because both validation drift and role-specific rendering bugs disappear together.
+ - (2026-03-13, auth bootstrap hotfix) JWT-protected proxy rules must explicitly allow unauthenticated auth bootstrap endpoints like `/api/auth/login` and `/api/auth/session`, or the login page will redirect its own API calls back to itself.
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+ ### Patterns Worth Repeating
+ 
+@@ -36,3 +37,4 @@
+ - (2026-03-13, curriculum_rollout_wave1) Lesson-planning matrices are a workable source for authored rollout waves when they are converted into canonical lesson blueprints and validated through manifest regression tests before runtime wiring.
+ - (2026-03-13, unit1_redesign_lesson_contract) Old infrastructure tests should validate canonical phase numbers and contract outcomes rather than stale phase labels, or redesign tracks will appear broken even when the authored curriculum and published runtime agree.
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+diff --git a/conductor/roadmap.md b/conductor/roadmap.md
+index 11d3802482df109f7afa63e1f8e06a29b281e2de..1ba34cd74dce8269c6b81c2e0e4a298afca7f180
+--- a/conductor/roadmap.md
++++ b/conductor/roadmap.md
+@@ -105,6 +105,8 @@
+ 
+ **Goal**: finish the book with the culminating experience and final coherence pass.
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+ 
+ - Capstone and Textbook Completion
+diff --git a/conductor/tech-debt.md b/conductor/tech-debt.md
+index 9bac252a4eba200df1bc57aede467e480d1d1f2a..640e1a1e5f5401852a117c3e7cf478b2cdbc1dff
+--- a/conductor/tech-debt.md
++++ b/conductor/tech-debt.md
+@@ -4,7 +4,6 @@
+ 
+ | Date | Track | Item | Severity | Status | Notes |
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+ | 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+ | 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+index e53f588834f06d46a36ab82106566ba908bb6cbc..d0163977afd242fdc5af0413ba1bf01af34a4964
+--- a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
++++ b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+@@ -1,11 +1,11 @@
+ {
+   "track_id": "capstone_textbook_completion_20260314",
+   "type": "feature",
+-  "status": "in_progress",
++  "status": "completed",
+   "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
++  "updated_at": "2026-03-14T00:51:55Z",
+   "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+   "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
++  "actual_tasks": 6,
++  "deviation_notes": "Capstone labeling normalization was extended into shared teacher planning and student lesson-header components so generic Unit 9 labels could not survive in secondary textbook surfaces."
+ }
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/plan.md b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+index 48b611e5d419a88dad2df9bf2d2a717532f66631..b6ea5b8820bf3add0cda7adf573ab7d766ea9a58
+--- a/conductor/tracks/capstone_textbook_completion_20260314/plan.md
++++ b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+@@ -2,30 +2,30 @@
+ 
+ ## Phase 1: Capstone Contract Guards
+ 
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
++- [x] Task: Add regression tests for authored capstone curriculum coverage
++  - [x] Write tests that fail if the published capstone lesson is still generated
++  - [x] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
++- [x] Task: Add regression tests for textbook capstone labeling
++  - [x] Write tests that fail if the public curriculum card labels the capstone as a generic unit
++  - [x] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
++- [x] Task: Mark the new track active in Conductor and capture implementation status
++  - [x] Update track/task status markers as execution progresses
+ 
+ ## Phase 2: Capstone Runtime Completion
+ 
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
++- [x] Task: Create canonical authored capstone runtime content
++  - [x] Add a repository-authored capstone source module aligned to the active curriculum narrative
++  - [x] Replace the generated capstone manifest builder with authored capstone integration
++- [x] Task: Normalize capstone labeling across key textbook surfaces
++  - [x] Add a shared capstone label helper for published curriculum surfaces
++  - [x] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+ 
+ ## Phase 3: Verification and Documentation
+ 
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
++- [x] Task: Synchronize active Conductor docs and project memory for textbook completion
++  - [x] Update impacted Conductor docs to reflect authored capstone completion
++  - [x] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
++- [x] Task: Run required verification and finalize the track
++  - [x] Run `npm run lint`
++  - [x] Run `npm test`
++  - [x] Run `npm run build`
+
+codex
+I found one important gap in the worktree state: the authored capstone blueprint file exists on disk but is not tracked by Git yet. I’m adding it to the implementation commit along with the capstone/runtime/test/doc files only, while leaving the unrelated dirty files untouched.
+exec
+/bin/bash -lc 'git status --short --untracked-files=all lib/curriculum/generated/capstone-authored.ts' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+?? lib/curriculum/generated/capstone-authored.ts
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
+index c6744e6265e1305f9e62ae2ff35179c8e2a6a46c..cfcfcc73b61799c67dee30ab713d6cfa49742d13
+--- a/conductor/lessons-learned.md
++++ b/conductor/lessons-learned.md
+@@ -23,6 +23,7 @@
+ - (2026-03-13, unit1_canonicalization_archetypes) Canonicalizing authored curriculum at the source is safer than accumulating manifest exceptions because both validation drift and role-specific rendering bugs disappear together.
+ - (2026-03-13, auth bootstrap hotfix) JWT-protected proxy rules must explicitly allow unauthenticated auth bootstrap endpoints like `/api/auth/login` and `/api/auth/session`, or the login page will redirect its own API calls back to itself.
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+ ### Patterns Worth Repeating
+ 
+@@ -36,3 +37,4 @@
+ - (2026-03-13, curriculum_rollout_wave1) Lesson-planning matrices are a workable source for authored rollout waves when they are converted into canonical lesson blueprints and validated through manifest regression tests before runtime wiring.
+ - (2026-03-13, unit1_redesign_lesson_contract) Old infrastructure tests should validate canonical phase numbers and contract outcomes rather than stale phase labels, or redesign tracks will appear broken even when the authored curriculum and published runtime agree.
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+diff --git a/conductor/roadmap.md b/conductor/roadmap.md
+index 11d3802482df109f7afa63e1f8e06a29b281e2de..1ba34cd74dce8269c6b81c2e0e4a298afca7f180
+--- a/conductor/roadmap.md
++++ b/conductor/roadmap.md
+@@ -105,6 +105,8 @@
+ 
+ **Goal**: finish the book with the culminating experience and final coherence pass.
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+ 
+ - Capstone and Textbook Completion
+diff --git a/conductor/tech-debt.md b/conductor/tech-debt.md
+index 9bac252a4eba200df1bc57aede467e480d1d1f2a..640e1a1e5f5401852a117c3e7cf478b2cdbc1dff
+--- a/conductor/tech-debt.md
++++ b/conductor/tech-debt.md
+@@ -4,7 +4,6 @@
+ 
+ | Date | Track | Item | Severity | Status | Notes |
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+ | 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+ | 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+index e53f588834f06d46a36ab82106566ba908bb6cbc..d0163977afd242fdc5af0413ba1bf01af34a4964
+--- a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
++++ b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+@@ -1,11 +1,11 @@
+ {
+   "track_id": "capstone_textbook_completion_20260314",
+   "type": "feature",
+-  "status": "in_progress",
++  "status": "completed",
+   "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
++  "updated_at": "2026-03-14T00:51:55Z",
+   "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+   "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
++  "actual_tasks": 6,
++  "deviation_notes": "Capstone labeling normalization was extended into shared teacher planning and student lesson-header components so generic Unit 9 labels could not survive in secondary textbook surfaces."
+ }
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/plan.md b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+index 48b611e5d419a88dad2df9bf2d2a717532f66631..b6ea5b8820bf3add0cda7adf573ab7d766ea9a58
+--- a/conductor/tracks/capstone_textbook_completion_20260314/plan.md
++++ b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+@@ -2,30 +2,30 @@
+ 
+ ## Phase 1: Capstone Contract Guards
+ 
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
++- [x] Task: Add regression tests for authored capstone curriculum coverage
++  - [x] Write tests that fail if the published capstone lesson is still generated
++  - [x] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
++- [x] Task: Add regression tests for textbook capstone labeling
++  - [x] Write tests that fail if the public curriculum card labels the capstone as a generic unit
++  - [x] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
++- [x] Task: Mark the new track active in Conductor and capture implementation status
++  - [x] Update track/task status markers as execution progresses
+ 
+ ## Phase 2: Capstone Runtime Completion
+ 
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
++- [x] Task: Create canonical authored capstone runtime content
++  - [x] Add a repository-authored capstone source module aligned to the active curriculum narrative
++  - [x] Replace the generated capstone manifest builder with authored capstone integration
++- [x] Task: Normalize capstone labeling across key textbook surfaces
++  - [x] Add a shared capstone label helper for published curriculum surfaces
++  - [x] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+ 
+ ## Phase 3: Verification and Documentation
+ 
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
++- [x] Task: Synchronize active Conductor docs and project memory for textbook completion
++  - [x] Update impacted Conductor docs to reflect authored capstone completion
++  - [x] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
++- [x] Task: Run required verification and finalize the track
++  - [x] Run `npm run lint`
++  - [x] Run `npm test`
++  - [x] Run `npm run build`
+
+exec
+/bin/bash -lc 'git add -- lib/curriculum/generated/capstone-authored.ts lib/curriculum/published-manifest.ts app/curriculum/CurriculumUnitCard.tsx app/student/dashboard/page.tsx app/teacher/students/[studentId]/page.tsx app/teacher/units/[unitNumber]/page.tsx components/dashboard/NextLessonCard.tsx components/student/LessonRenderer.tsx components/student/PhaseHeader.tsx components/teacher/CourseOverviewGrid.tsx components/teacher/TeacherLessonPlan.tsx components/teacher/UnitLessonPlan.tsx components/teacher/UnitOverview.tsx __tests__/curriculum/published-manifest.test.ts __tests__/app/curriculum/page.test.tsx __tests__/app/student/dashboard/page.test.tsx __tests__/app/teacher/students/[studentId]/page.test.tsx __tests__/app/teacher/units/[unitNumber]/page.test.tsx __tests__/components/dashboard/NextLessonCard.test.tsx __tests__/components/student/PhaseHeader.test.tsx __tests__/components/teacher/CourseOverviewGrid.test.tsx __tests__/components/teacher/TeacherLessonPlan.test.tsx __tests__/components/teacher/UnitLessonPlan.test.tsx __tests__/components/teacher/UnitOverview.test.tsx conductor/tracks/capstone_textbook_completion_20260314/plan.md conductor/tracks/capstone_textbook_completion_20260314/metadata.json conductor/tech-debt.md conductor/lessons-learned.md conductor/roadmap.md' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
+index c6744e6265e1305f9e62ae2ff35179c8e2a6a46c..cfcfcc73b61799c67dee30ab713d6cfa49742d13
+--- a/conductor/lessons-learned.md
++++ b/conductor/lessons-learned.md
+@@ -23,6 +23,7 @@
+ - (2026-03-13, unit1_canonicalization_archetypes) Canonicalizing authored curriculum at the source is safer than accumulating manifest exceptions because both validation drift and role-specific rendering bugs disappear together.
+ - (2026-03-13, auth bootstrap hotfix) JWT-protected proxy rules must explicitly allow unauthenticated auth bootstrap endpoints like `/api/auth/login` and `/api/auth/session`, or the login page will redirect its own API calls back to itself.
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+ ### Patterns Worth Repeating
+ 
+@@ -36,3 +37,4 @@
+ - (2026-03-13, curriculum_rollout_wave1) Lesson-planning matrices are a workable source for authored rollout waves when they are converted into canonical lesson blueprints and validated through manifest regression tests before runtime wiring.
+ - (2026-03-13, unit1_redesign_lesson_contract) Old infrastructure tests should validate canonical phase numbers and contract outcomes rather than stale phase labels, or redesign tracks will appear broken even when the authored curriculum and published runtime agree.
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+diff --git a/conductor/roadmap.md b/conductor/roadmap.md
+index 11d3802482df109f7afa63e1f8e06a29b281e2de..1ba34cd74dce8269c6b81c2e0e4a298afca7f180
+--- a/conductor/roadmap.md
++++ b/conductor/roadmap.md
+@@ -105,6 +105,8 @@
+ 
+ **Goal**: finish the book with the culminating experience and final coherence pass.
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+ 
+ - Capstone and Textbook Completion
+diff --git a/conductor/tech-debt.md b/conductor/tech-debt.md
+index 9bac252a4eba200df1bc57aede467e480d1d1f2a..640e1a1e5f5401852a117c3e7cf478b2cdbc1dff
+--- a/conductor/tech-debt.md
++++ b/conductor/tech-debt.md
+@@ -4,7 +4,6 @@
+ 
+ | Date | Track | Item | Severity | Status | Notes |
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+ | 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+ | 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+index e53f588834f06d46a36ab82106566ba908bb6cbc..d0163977afd242fdc5af0413ba1bf01af34a4964
+--- a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
++++ b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+@@ -1,11 +1,11 @@
+ {
+   "track_id": "capstone_textbook_completion_20260314",
+   "type": "feature",
+-  "status": "in_progress",
++  "status": "completed",
+   "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
++  "updated_at": "2026-03-14T00:51:55Z",
+   "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+   "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
++  "actual_tasks": 6,
++  "deviation_notes": "Capstone labeling normalization was extended into shared teacher planning and student lesson-header components so generic Unit 9 labels could not survive in secondary textbook surfaces."
+ }
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/plan.md b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+index 48b611e5d419a88dad2df9bf2d2a717532f66631..b6ea5b8820bf3add0cda7adf573ab7d766ea9a58
+--- a/conductor/tracks/capstone_textbook_completion_20260314/plan.md
++++ b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+@@ -2,30 +2,30 @@
+ 
+ ## Phase 1: Capstone Contract Guards
+ 
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
++- [x] Task: Add regression tests for authored capstone curriculum coverage
++  - [x] Write tests that fail if the published capstone lesson is still generated
++  - [x] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
++- [x] Task: Add regression tests for textbook capstone labeling
++  - [x] Write tests that fail if the public curriculum card labels the capstone as a generic unit
++  - [x] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
++- [x] Task: Mark the new track active in Conductor and capture implementation status
++  - [x] Update track/task status markers as execution progresses
+ 
+ ## Phase 2: Capstone Runtime Completion
+ 
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
++- [x] Task: Create canonical authored capstone runtime content
++  - [x] Add a repository-authored capstone source module aligned to the active curriculum narrative
++  - [x] Replace the generated capstone manifest builder with authored capstone integration
++- [x] Task: Normalize capstone labeling across key textbook surfaces
++  - [x] Add a shared capstone label helper for published curriculum surfaces
++  - [x] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+ 
+ ## Phase 3: Verification and Documentation
+ 
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
++- [x] Task: Synchronize active Conductor docs and project memory for textbook completion
++  - [x] Update impacted Conductor docs to reflect authored capstone completion
++  - [x] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
++- [x] Task: Run required verification and finalize the track
++  - [x] Run `npm run lint`
++  - [x] Run `npm test`
++  - [x] Run `npm run build`
+
+exec
+/bin/bash -lc 'git status --short' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+ M .env.example
+ M README.md
+M  __tests__/app/curriculum/page.test.tsx
+M  __tests__/app/student/dashboard/page.test.tsx
+M  __tests__/app/teacher/students/[studentId]/page.test.tsx
+M  __tests__/app/teacher/units/[unitNumber]/page.test.tsx
+M  __tests__/components/dashboard/NextLessonCard.test.tsx
+ M __tests__/components/exercises/JournalEntryBuilding.test.tsx
+ M __tests__/components/financial-reports/CashFlowStatementDetailed.test.tsx
+M  __tests__/components/student/PhaseHeader.test.tsx
+M  __tests__/components/teacher/CourseOverviewGrid.test.tsx
+M  __tests__/components/teacher/TeacherLessonPlan.test.tsx
+M  __tests__/components/teacher/UnitLessonPlan.test.tsx
+M  __tests__/components/teacher/UnitOverview.test.tsx
+M  __tests__/curriculum/published-manifest.test.ts
+ M __tests__/lib/convex/config.test.ts
+ M __tests__/proxy.test.ts
+ M __tests__/setup/dev-stack-script.test.ts
+M  app/curriculum/CurriculumUnitCard.tsx
+M  app/student/dashboard/page.tsx
+exec
+/bin/bash -lc 'git diff --cached --stat' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+ __tests__/app/curriculum/page.test.tsx             | 27 ++++++++
+ __tests__/app/student/dashboard/page.test.tsx      | 31 +++++++++
+ .../app/teacher/students/[studentId]/page.test.tsx | 46 +++++++++++++
+ .../app/teacher/units/[unitNumber]/page.test.tsx   | 13 ++++
+ .../components/dashboard/NextLessonCard.test.tsx   | 19 ++++++
+ __tests__/components/student/PhaseHeader.test.tsx  | 18 +++++-
+ .../components/teacher/CourseOverviewGrid.test.tsx | 22 +++++++
+ .../components/teacher/TeacherLessonPlan.test.tsx  | 16 +++++
+ .../components/teacher/UnitLessonPlan.test.tsx     | 15 +++++
+ __tests__/components/teacher/UnitOverview.test.tsx | 15 +++++
+ __tests__/curriculum/published-manifest.test.ts    | 29 ++++++++-
+ app/curriculum/CurriculumUnitCard.tsx              |  3 +-
+ app/student/dashboard/page.tsx                     |  5 +-
+ app/teacher/students/[studentId]/page.tsx          |  3 +-
+ app/teacher/units/[unitNumber]/page.tsx            |  5 +-
+ components/dashboard/NextLessonCard.tsx            |  5 +-
+ components/student/LessonRenderer.tsx              |  3 +-
+ components/student/PhaseHeader.tsx                 | 11 +++-
+ components/teacher/CourseOverviewGrid.tsx          |  5 +-
+ components/teacher/TeacherLessonPlan.tsx           |  3 +-
+exec
+/bin/bash -lc 'git diff --stat' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 12ms:
+ .env.example                                       |     2 +-
+ README.md                                          |    13 +-
+ .../exercises/JournalEntryBuilding.test.tsx        |     2 +-
+ .../CashFlowStatementDetailed.test.tsx             |     2 +-
+ __tests__/lib/convex/config.test.ts                |    40 +-
+ __tests__/proxy.test.ts                            |    10 +
+ __tests__/setup/dev-stack-script.test.ts           |    32 +
+ conductor/codex-cron.log                           | 48547 +++++++------------
+ conductor/codex-last.txt                           |    10 +-
+ conductor/docs/architecture/runtime.md             |     3 +-
+ conductor/tracks.md                                |     7 +-
+ convex/teacher.ts                                  |    47 +-
+ docs/security-api-route-matrix.md                  |     3 +
+ lib/activities/component-keys.ts                   |     2 +-
+ lib/convex/admin.ts                                |    38 +-
+ lib/db/schema/phase-content.ts                     |     2 +-
+ lib/db/schema/validators.ts                        |     2 +-
+ proxy.ts                                           |     2 +
+ scripts/dev-stack.mjs                              |   155 +-
+ types/curriculum.ts                                |    10 +-
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
+index c6744e6265e1305f9e62ae2ff35179c8e2a6a46c..cfcfcc73b61799c67dee30ab713d6cfa49742d13
+--- a/conductor/lessons-learned.md
++++ b/conductor/lessons-learned.md
+@@ -23,6 +23,7 @@
+ - (2026-03-13, unit1_canonicalization_archetypes) Canonicalizing authored curriculum at the source is safer than accumulating manifest exceptions because both validation drift and role-specific rendering bugs disappear together.
+ - (2026-03-13, auth bootstrap hotfix) JWT-protected proxy rules must explicitly allow unauthenticated auth bootstrap endpoints like `/api/auth/login` and `/api/auth/session`, or the login page will redirect its own API calls back to itself.
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+ ### Patterns Worth Repeating
+ 
+@@ -36,3 +37,4 @@
+ - (2026-03-13, curriculum_rollout_wave1) Lesson-planning matrices are a workable source for authored rollout waves when they are converted into canonical lesson blueprints and validated through manifest regression tests before runtime wiring.
+ - (2026-03-13, unit1_redesign_lesson_contract) Old infrastructure tests should validate canonical phase numbers and contract outcomes rather than stale phase labels, or redesign tracks will appear broken even when the authored curriculum and published runtime agree.
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+diff --git a/conductor/roadmap.md b/conductor/roadmap.md
+index 11d3802482df109f7afa63e1f8e06a29b281e2de..1ba34cd74dce8269c6b81c2e0e4a298afca7f180
+--- a/conductor/roadmap.md
++++ b/conductor/roadmap.md
+@@ -105,6 +105,8 @@
+ 
+ **Goal**: finish the book with the culminating experience and final coherence pass.
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+ 
+ - Capstone and Textbook Completion
+diff --git a/conductor/tech-debt.md b/conductor/tech-debt.md
+index 9bac252a4eba200df1bc57aede467e480d1d1f2a..640e1a1e5f5401852a117c3e7cf478b2cdbc1dff
+--- a/conductor/tech-debt.md
++++ b/conductor/tech-debt.md
+@@ -4,7 +4,6 @@
+ 
+ | Date | Track | Item | Severity | Status | Notes |
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+ | 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+ | 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+index e53f588834f06d46a36ab82106566ba908bb6cbc..d0163977afd242fdc5af0413ba1bf01af34a4964
+--- a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
++++ b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+@@ -1,11 +1,11 @@
+ {
+   "track_id": "capstone_textbook_completion_20260314",
+   "type": "feature",
+-  "status": "in_progress",
++  "status": "completed",
+   "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
++  "updated_at": "2026-03-14T00:51:55Z",
+   "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+   "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
++  "actual_tasks": 6,
++  "deviation_notes": "Capstone labeling normalization was extended into shared teacher planning and student lesson-header components so generic Unit 9 labels could not survive in secondary textbook surfaces."
+ }
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/plan.md b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+index 48b611e5d419a88dad2df9bf2d2a717532f66631..b6ea5b8820bf3add0cda7adf573ab7d766ea9a58
+--- a/conductor/tracks/capstone_textbook_completion_20260314/plan.md
++++ b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+@@ -2,30 +2,30 @@
+ 
+ ## Phase 1: Capstone Contract Guards
+ 
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
++- [x] Task: Add regression tests for authored capstone curriculum coverage
++  - [x] Write tests that fail if the published capstone lesson is still generated
++  - [x] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
++- [x] Task: Add regression tests for textbook capstone labeling
++  - [x] Write tests that fail if the public curriculum card labels the capstone as a generic unit
++  - [x] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
++- [x] Task: Mark the new track active in Conductor and capture implementation status
++  - [x] Update track/task status markers as execution progresses
+ 
+ ## Phase 2: Capstone Runtime Completion
+ 
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
++- [x] Task: Create canonical authored capstone runtime content
++  - [x] Add a repository-authored capstone source module aligned to the active curriculum narrative
++  - [x] Replace the generated capstone manifest builder with authored capstone integration
++- [x] Task: Normalize capstone labeling across key textbook surfaces
++  - [x] Add a shared capstone label helper for published curriculum surfaces
++  - [x] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+ 
+ ## Phase 3: Verification and Documentation
+ 
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
++- [x] Task: Synchronize active Conductor docs and project memory for textbook completion
++  - [x] Update impacted Conductor docs to reflect authored capstone completion
++  - [x] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
++- [x] Task: Run required verification and finalize the track
++  - [x] Run `npm run lint`
++  - [x] Run `npm test`
++  - [x] Run `npm run build`
+
+exec
+/bin/bash -lc 'git commit -m "feat(curriculum): complete capstone textbook flow (Codex GPT-5)"' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+[main 7f860c4] feat(curriculum): complete capstone textbook flow (Codex GPT-5)
+ 29 files changed, 439 insertions(+), 31 deletions(-)
+ create mode 100644 conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+ create mode 100644 conductor/tracks/capstone_textbook_completion_20260314/plan.md
+ create mode 100644 lib/curriculum/generated/capstone-authored.ts
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
+index c6744e6265e1305f9e62ae2ff35179c8e2a6a46c..cfcfcc73b61799c67dee30ab713d6cfa49742d13
+--- a/conductor/lessons-learned.md
++++ b/conductor/lessons-learned.md
+@@ -23,6 +23,7 @@
+ - (2026-03-13, unit1_canonicalization_archetypes) Canonicalizing authored curriculum at the source is safer than accumulating manifest exceptions because both validation drift and role-specific rendering bugs disappear together.
+ - (2026-03-13, auth bootstrap hotfix) JWT-protected proxy rules must explicitly allow unauthenticated auth bootstrap endpoints like `/api/auth/login` and `/api/auth/session`, or the login page will redirect its own API calls back to itself.
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+ ### Patterns Worth Repeating
+ 
+@@ -36,3 +37,4 @@
+ - (2026-03-13, curriculum_rollout_wave1) Lesson-planning matrices are a workable source for authored rollout waves when they are converted into canonical lesson blueprints and validated through manifest regression tests before runtime wiring.
+ - (2026-03-13, unit1_redesign_lesson_contract) Old infrastructure tests should validate canonical phase numbers and contract outcomes rather than stale phase labels, or redesign tracks will appear broken even when the authored curriculum and published runtime agree.
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+diff --git a/conductor/roadmap.md b/conductor/roadmap.md
+index 11d3802482df109f7afa63e1f8e06a29b281e2de..1ba34cd74dce8269c6b81c2e0e4a298afca7f180
+--- a/conductor/roadmap.md
++++ b/conductor/roadmap.md
+@@ -105,6 +105,8 @@
+ 
+ **Goal**: finish the book with the culminating experience and final coherence pass.
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+ 
+ - Capstone and Textbook Completion
+diff --git a/conductor/tech-debt.md b/conductor/tech-debt.md
+index 9bac252a4eba200df1bc57aede467e480d1d1f2a..640e1a1e5f5401852a117c3e7cf478b2cdbc1dff
+--- a/conductor/tech-debt.md
++++ b/conductor/tech-debt.md
+@@ -4,7 +4,6 @@
+ 
+ | Date | Track | Item | Severity | Status | Notes |
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+ | 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+ | 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+index e53f588834f06d46a36ab82106566ba908bb6cbc..d0163977afd242fdc5af0413ba1bf01af34a4964
+--- a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
++++ b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+@@ -1,11 +1,11 @@
+ {
+   "track_id": "capstone_textbook_completion_20260314",
+   "type": "feature",
+-  "status": "in_progress",
++  "status": "completed",
+   "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
++  "updated_at": "2026-03-14T00:51:55Z",
+   "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+   "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
++  "actual_tasks": 6,
++  "deviation_notes": "Capstone labeling normalization was extended into shared teacher planning and student lesson-header components so generic Unit 9 labels could not survive in secondary textbook surfaces."
+ }
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/plan.md b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+index 48b611e5d419a88dad2df9bf2d2a717532f66631..b6ea5b8820bf3add0cda7adf573ab7d766ea9a58
+--- a/conductor/tracks/capstone_textbook_completion_20260314/plan.md
++++ b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+@@ -2,30 +2,30 @@
+ 
+ ## Phase 1: Capstone Contract Guards
+ 
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
++- [x] Task: Add regression tests for authored capstone curriculum coverage
++  - [x] Write tests that fail if the published capstone lesson is still generated
++  - [x] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
++- [x] Task: Add regression tests for textbook capstone labeling
++  - [x] Write tests that fail if the public curriculum card labels the capstone as a generic unit
++  - [x] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
++- [x] Task: Mark the new track active in Conductor and capture implementation status
++  - [x] Update track/task status markers as execution progresses
+ 
+ ## Phase 2: Capstone Runtime Completion
+ 
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
++- [x] Task: Create canonical authored capstone runtime content
++  - [x] Add a repository-authored capstone source module aligned to the active curriculum narrative
++  - [x] Replace the generated capstone manifest builder with authored capstone integration
++- [x] Task: Normalize capstone labeling across key textbook surfaces
++  - [x] Add a shared capstone label helper for published curriculum surfaces
++  - [x] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+ 
+ ## Phase 3: Verification and Documentation
+ 
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
++- [x] Task: Synchronize active Conductor docs and project memory for textbook completion
++  - [x] Update impacted Conductor docs to reflect authored capstone completion
++  - [x] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
++- [x] Task: Run required verification and finalize the track
++  - [x] Run `npm run lint`
++  - [x] Run `npm test`
++  - [x] Run `npm run build`
+
+codex
+The implementation commit is in. I’m doing the separate Conductor cleanup commit now: archive the completed track folder and rewrite `conductor/tracks.md` to remove it from the active list and record the closeout in the archive ledger.
+exec
+/bin/bash -lc "sed -n '1,220p' conductor/tracks.md" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+# Project Tracks
+
+This file is the source of truth for active execution order. Archived tracks live in `conductor/archive/`. The milestone-level delivery sequence lives in [./roadmap.md](./roadmap.md).
+
+---
+
+## Active Track
+
+- [~] **Track: Capstone and Textbook Completion**
+  *Link: [./tracks/capstone_textbook_completion_20260314/](./tracks/capstone_textbook_completion_20260314/)*
+
+## Planned Queue
+
+1. **Cloudflare Production Hardening and Launch**
+  Finalize Worker deployment, runtime verification, auth hardening, and launch quality gates.
+
+## Archive Ledger
+
+- [x] **Track: Curriculum Rollout Wave 2 (Units 5-8)**
+  *Link: [./archive/curriculum_rollout_wave2_20260314/](./archive/curriculum_rollout_wave2_20260314/)*
+exec
+/bin/bash -lc 'git status --short' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+ M .env.example
+ M README.md
+ M __tests__/components/exercises/JournalEntryBuilding.test.tsx
+ M __tests__/components/financial-reports/CashFlowStatementDetailed.test.tsx
+ M __tests__/lib/convex/config.test.ts
+ M __tests__/proxy.test.ts
+ M __tests__/setup/dev-stack-script.test.ts
+ M conductor/codex-cron.log
+ M conductor/codex-last.txt
+ M conductor/docs/architecture/runtime.md
+ M conductor/tracks.md
+ M convex/teacher.ts
+ M docs/security-api-route-matrix.md
+ M lib/activities/component-keys.ts
+ M lib/convex/admin.ts
+ M lib/db/schema/phase-content.ts
+ M lib/db/schema/validators.ts
+ M proxy.ts
+ M scripts/dev-stack.mjs
+ M types/curriculum.ts
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
+index c6744e6265e1305f9e62ae2ff35179c8e2a6a46c..cfcfcc73b61799c67dee30ab713d6cfa49742d13
+--- a/conductor/lessons-learned.md
++++ b/conductor/lessons-learned.md
+@@ -23,6 +23,7 @@
+ - (2026-03-13, unit1_canonicalization_archetypes) Canonicalizing authored curriculum at the source is safer than accumulating manifest exceptions because both validation drift and role-specific rendering bugs disappear together.
+ - (2026-03-13, auth bootstrap hotfix) JWT-protected proxy rules must explicitly allow unauthenticated auth bootstrap endpoints like `/api/auth/login` and `/api/auth/session`, or the login page will redirect its own API calls back to itself.
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+ ### Patterns Worth Repeating
+ 
+@@ -36,3 +37,4 @@
+ - (2026-03-13, curriculum_rollout_wave1) Lesson-planning matrices are a workable source for authored rollout waves when they are converted into canonical lesson blueprints and validated through manifest regression tests before runtime wiring.
+ - (2026-03-13, unit1_redesign_lesson_contract) Old infrastructure tests should validate canonical phase numbers and contract outcomes rather than stale phase labels, or redesign tracks will appear broken even when the authored curriculum and published runtime agree.
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+diff --git a/conductor/roadmap.md b/conductor/roadmap.md
+index 11d3802482df109f7afa63e1f8e06a29b281e2de..1ba34cd74dce8269c6b81c2e0e4a298afca7f180
+--- a/conductor/roadmap.md
++++ b/conductor/roadmap.md
+@@ -105,6 +105,8 @@
+ 
+ **Goal**: finish the book with the culminating experience and final coherence pass.
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+ 
+ - Capstone and Textbook Completion
+diff --git a/conductor/tech-debt.md b/conductor/tech-debt.md
+index 9bac252a4eba200df1bc57aede467e480d1d1f2a..640e1a1e5f5401852a117c3e7cf478b2cdbc1dff
+--- a/conductor/tech-debt.md
++++ b/conductor/tech-debt.md
+@@ -4,7 +4,6 @@
+ 
+ | Date | Track | Item | Severity | Status | Notes |
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+ | 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+ | 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+index e53f588834f06d46a36ab82106566ba908bb6cbc..d0163977afd242fdc5af0413ba1bf01af34a4964
+--- a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
++++ b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+@@ -1,11 +1,11 @@
+ {
+   "track_id": "capstone_textbook_completion_20260314",
+   "type": "feature",
+-  "status": "in_progress",
++  "status": "completed",
+   "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
++  "updated_at": "2026-03-14T00:51:55Z",
+   "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+   "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
++  "actual_tasks": 6,
++  "deviation_notes": "Capstone labeling normalization was extended into shared teacher planning and student lesson-header components so generic Unit 9 labels could not survive in secondary textbook surfaces."
+ }
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/plan.md b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+index 48b611e5d419a88dad2df9bf2d2a717532f66631..b6ea5b8820bf3add0cda7adf573ab7d766ea9a58
+--- a/conductor/tracks/capstone_textbook_completion_20260314/plan.md
++++ b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+@@ -2,30 +2,30 @@
+ 
+ ## Phase 1: Capstone Contract Guards
+ 
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
++- [x] Task: Add regression tests for authored capstone curriculum coverage
++  - [x] Write tests that fail if the published capstone lesson is still generated
++  - [x] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
++- [x] Task: Add regression tests for textbook capstone labeling
++  - [x] Write tests that fail if the public curriculum card labels the capstone as a generic unit
++  - [x] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
++- [x] Task: Mark the new track active in Conductor and capture implementation status
++  - [x] Update track/task status markers as execution progresses
+ 
+ ## Phase 2: Capstone Runtime Completion
+ 
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
++- [x] Task: Create canonical authored capstone runtime content
++  - [x] Add a repository-authored capstone source module aligned to the active curriculum narrative
++  - [x] Replace the generated capstone manifest builder with authored capstone integration
++- [x] Task: Normalize capstone labeling across key textbook surfaces
++  - [x] Add a shared capstone label helper for published curriculum surfaces
++  - [x] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+ 
+ ## Phase 3: Verification and Documentation
+ 
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
++- [x] Task: Synchronize active Conductor docs and project memory for textbook completion
++  - [x] Update impacted Conductor docs to reflect authored capstone completion
++  - [x] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
++- [x] Task: Run required verification and finalize the track
++  - [x] Run `npm run lint`
++  - [x] Run `npm test`
++  - [x] Run `npm run build`
+
+exec
+/bin/bash -lc 'mv conductor/tracks/capstone_textbook_completion_20260314 conductor/archive/capstone_textbook_completion_20260314' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
+ 
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
+index c6744e6265e1305f9e62ae2ff35179c8e2a6a46c..cfcfcc73b61799c67dee30ab713d6cfa49742d13
+--- a/conductor/lessons-learned.md
++++ b/conductor/lessons-learned.md
+@@ -23,6 +23,7 @@
+ - (2026-03-13, unit1_canonicalization_archetypes) Canonicalizing authored curriculum at the source is safer than accumulating manifest exceptions because both validation drift and role-specific rendering bugs disappear together.
+ - (2026-03-13, auth bootstrap hotfix) JWT-protected proxy rules must explicitly allow unauthenticated auth bootstrap endpoints like `/api/auth/login` and `/api/auth/session`, or the login page will redirect its own API calls back to itself.
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+ ### Patterns Worth Repeating
+ 
+@@ -36,3 +37,4 @@
+ - (2026-03-13, curriculum_rollout_wave1) Lesson-planning matrices are a workable source for authored rollout waves when they are converted into canonical lesson blueprints and validated through manifest regression tests before runtime wiring.
+ - (2026-03-13, unit1_redesign_lesson_contract) Old infrastructure tests should validate canonical phase numbers and contract outcomes rather than stale phase labels, or redesign tracks will appear broken even when the authored curriculum and published runtime agree.
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+diff --git a/conductor/roadmap.md b/conductor/roadmap.md
+index 11d3802482df109f7afa63e1f8e06a29b281e2de..1ba34cd74dce8269c6b81c2e0e4a298afca7f180
+--- a/conductor/roadmap.md
++++ b/conductor/roadmap.md
+@@ -105,6 +105,8 @@
+ 
+ **Goal**: finish the book with the culminating experience and final coherence pass.
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+ 
+ - Capstone and Textbook Completion
+diff --git a/conductor/tech-debt.md b/conductor/tech-debt.md
+index 9bac252a4eba200df1bc57aede467e480d1d1f2a..640e1a1e5f5401852a117c3e7cf478b2cdbc1dff
+--- a/conductor/tech-debt.md
++++ b/conductor/tech-debt.md
+@@ -4,7 +4,6 @@
+ 
+ | Date | Track | Item | Severity | Status | Notes |
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+ | 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+ | 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+deleted file mode 100644
+index e53f588834f06d46a36ab82106566ba908bb6cbc..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
++++ /dev/null
+@@ -1,11 +0,0 @@
+-{
+-  "track_id": "capstone_textbook_completion_20260314",
+-  "type": "feature",
+-  "status": "in_progress",
+-  "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
+-  "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+-  "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
+-}
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/plan.md b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+deleted file mode 100644
+index 48b611e5d419a88dad2df9bf2d2a717532f66631..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/plan.md
++++ /dev/null
+@@ -1,31 +0,0 @@
+-# Implementation Plan
+-
+-## Phase 1: Capstone Contract Guards
+-
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
+-
+-## Phase 2: Capstone Runtime Completion
+-
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+-
+-## Phase 3: Verification and Documentation
+-
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
+
+file update
+M /home/daniel-bo/Desktop/bus-math-v2/conductor/tracks.md
+@@ -8,4 +8,3 @@
+ 
+-- [~] **Track: Capstone and Textbook Completion**
+-  *Link: [./tracks/capstone_textbook_completion_20260314/](./tracks/capstone_textbook_completion_20260314/)*
++- No active track. The next `/conductor` run should take the first item from the planned queue.
+ 
+@@ -18,2 +17,6 @@
+ 
++- [x] **Track: Capstone and Textbook Completion**
++  *Link: [./archive/capstone_textbook_completion_20260314/](./archive/capstone_textbook_completion_20260314/)*
++  *Closeout: archived on 2026-03-14 after replacing the generated capstone scaffold with authored runtime content, locking milestone/workbook/final-presentation coverage into manifest and UI regressions, and normalizing capstone labeling across the core public, student, and teacher textbook flow.*
++
+ - [x] **Track: Curriculum Rollout Wave 2 (Units 5-8)**
+apply_patch(auto_approved=true) exited 0 in 87ms:
+Success. Updated the following files:
+M /home/daniel-bo/Desktop/bus-math-v2/conductor/tracks.md
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
++    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
 +
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
-+    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-+  });
 +
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
-+  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json b/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-index 364eb86a46b01b017fb851065b622f944b094de2..c26bc753dc608b14cfc91af840979997116e6d4e
---- a/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-+++ b/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-@@ -1,9 +1,9 @@
- {
-   "track_id": "lesson_phase_objective_shell_20260311",
-   "type": "feature",
--  "status": "in_progress",
-+  "status": "archived",
-   "created_at": "2026-03-11T08:34:26Z",
--  "updated_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:44:39Z",
-   "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-   "estimated_tasks": 7,
-   "actual_tasks": 7,
-diff --git a/conductor/archive/lesson_phase_objective_shell_20260311/plan.md b/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-index 2c93ed93a25fc6373d8d1f962121f21b751f077d..504954d0bd54aa9b155a27df0bdb25d02fbe44eb
---- a/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-+++ b/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-@@ -19,4 +19,4 @@
- ## Phase 3: Verification and Closeout
- - [x] Task: Run lint and automated tests for the track scope
- - [x] Task: Update README and Conductor memory files with the phase-shell alignment outcome
--- [ ] Task: Archive the track and finalize registry/metadata updates
-+- [x] Task: Archive the track and finalize registry/metadata updates
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
 diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
-index 5a1f9d47c9c577b016380d41f806bcb496b3903e..d8b4b634818b45cfc7f4d67cb6059d6c008a677c
+index c6744e6265e1305f9e62ae2ff35179c8e2a6a46c..cfcfcc73b61799c67dee30ab713d6cfa49742d13
 --- a/conductor/lessons-learned.md
 +++ b/conductor/lessons-learned.md
-@@ -14,6 +14,7 @@
- - (2026-03-11, activity_component_contract_refactor_20260311) Activity contracts drift quickly when alias handling lives separately from the runtime registry and schema validators; documented curriculum keys need one canonical resolver.
- - (2026-03-11, published_progress_viewmodel_consolidation_20260311) Student dashboard rows, teacher student-detail rows, and lesson phase-status payloads should derive from the same published-progress helpers or published-version/locking fixes will diverge again.
- - (2026-03-11, student_lesson_access_boundary_cleanup_20260311) Student-owned API writes need an explicit request-level student guard even when teacher/admin lesson preview is intentionally allowed elsewhere; auth presence alone is not a safe mutation boundary.
-+- (2026-03-11, lesson_phase_objective_shell_20260311) Student lesson pages should derive phase guidance from curriculum lesson-type metadata and reuse the shared `PhaseRenderer`, or pedagogy copy and content rendering drift separately.
- 
- ### Recurring Gotchas
- <!-- Problems encountered repeatedly; save future tracks from the same pain -->
+@@ -23,6 +23,7 @@
+ - (2026-03-13, unit1_canonicalization_archetypes) Canonicalizing authored curriculum at the source is safer than accumulating manifest exceptions because both validation drift and role-specific rendering bugs disappear together.
+ - (2026-03-13, auth bootstrap hotfix) JWT-protected proxy rules must explicitly allow unauthenticated auth bootstrap endpoints like `/api/auth/login` and `/api/auth/session`, or the login page will redirect its own API calls back to itself.
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+ ### Patterns Worth Repeating
+ 
+@@ -36,3 +37,4 @@
+ - (2026-03-13, curriculum_rollout_wave1) Lesson-planning matrices are a workable source for authored rollout waves when they are converted into canonical lesson blueprints and validated through manifest regression tests before runtime wiring.
+ - (2026-03-13, unit1_redesign_lesson_contract) Old infrastructure tests should validate canonical phase numbers and contract outcomes rather than stale phase labels, or redesign tracks will appear broken even when the authored curriculum and published runtime agree.
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+diff --git a/conductor/roadmap.md b/conductor/roadmap.md
+index 11d3802482df109f7afa63e1f8e06a29b281e2de..1ba34cd74dce8269c6b81c2e0e4a298afca7f180
+--- a/conductor/roadmap.md
++++ b/conductor/roadmap.md
+@@ -105,6 +105,8 @@
+ 
+ **Goal**: finish the book with the culminating experience and final coherence pass.
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+ 
+ - Capstone and Textbook Completion
+diff --git a/conductor/tech-debt.md b/conductor/tech-debt.md
+index 9bac252a4eba200df1bc57aede467e480d1d1f2a..640e1a1e5f5401852a117c3e7cf478b2cdbc1dff
+--- a/conductor/tech-debt.md
++++ b/conductor/tech-debt.md
+@@ -4,7 +4,6 @@
+ 
+ | Date | Track | Item | Severity | Status | Notes |
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+ | 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+ | 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
 diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..a94965df8c673cc029927782a3d791909b4e2091
+index 6704b229976c3e143989c878a6d0db8b29221c6c..8adc26ec3d1fcbe497a6f50ee5d48079589ac882
 --- a/conductor/tracks.md
 +++ b/conductor/tracks.md
-@@ -32,6 +32,10 @@
- 
- ## Archive Ledger
- 
-+- [x] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/archive/lesson_phase_objective_shell_20260311/](./conductor/archive/lesson_phase_objective_shell_20260311/)*
-+  *Checkpoint: `f9a651e` (2026-03-11 implementation complete; archived during closeout)*
-+
- - [x] **Track: Student Write Boundary Hardening**
-   *Link: [./conductor/archive/student_lesson_access_boundary_cleanup_20260311/](./conductor/archive/student_lesson_access_boundary_cleanup_20260311/)*
-   *Checkpoint: `pending` (2026-03-11 closeout)*
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-file update:
-diff --git a/README.md b/README.md
-index 9ee7fe58cfca200160c7dcd63527376b6a6e3202..d15e181d7fdd88c0d5393d28b496fdbcdc953762
---- a/README.md
-+++ b/README.md
-@@ -17,6 +17,7 @@
- - **Shared Dashboard Recommendation Cards**: Student and teacher progress surfaces now reuse one next-lesson card and shared unit-status presentation helpers to keep resume/start guidance aligned
- - **Shared Published Progress View-Models**: Student dashboard units, teacher student-detail progress rows, and lesson phase-status responses now derive from the same published-progress helpers so progress math and locking rules stay aligned
- - **Canonical Activity Component Catalog**: Activity rendering and validation now resolve documented aliases such as `spreadsheet-activity` and `journal-entry-activity` through one canonical runtime contract, reducing curriculum authoring drift
-+- **Objective-Aware Lesson Phase Shell**: Student lesson phases now show curriculum-aligned phase guidance and success criteria while reusing the shared phase content renderer
- - **Deterministic Simulation Runtime IDs**: Inventory simulation notifications and market events now keep stable unique ids during rapid updates, preventing duplicate-key rendering regressions
- - **Account Settings & Self-Service Password Changes**: Authenticated users can review account context and update their own password without leaving the session
- - **Multi-tenant Architecture**: Organization-based access control
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
-+
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
-   });
+@@ -6,8 +6,7 @@
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
+ ## Active Track
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+-- [~] **Track: Capstone and Textbook Completion**
+-  *Link: [./tracks/capstone_textbook_completion_20260314/](./tracks/capstone_textbook_completion_20260314/)*
++- No active track. The next `/conductor` run should take the first item from the planned queue.
  
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+ ## Planned Queue
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-   });
+@@ -16,6 +15,10 @@
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
+ ## Archive Ledger
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++- [x] **Track: Capstone and Textbook Completion**
++  *Link: [./archive/capstone_textbook_completion_20260314/](./archive/capstone_textbook_completion_20260314/)*
++  *Closeout: archived on 2026-03-14 after replacing the generated capstone scaffold with authored runtime content, locking milestone/workbook/final-presentation coverage into manifest and UI regressions, and normalizing capstone labeling across the core public, student, and teacher textbook flow.*
++
+ - [x] **Track: Curriculum Rollout Wave 2 (Units 5-8)**
+   *Link: [./archive/curriculum_rollout_wave2_20260314/](./archive/curriculum_rollout_wave2_20260314/)*
+   *Closeout: archived on 2026-03-14 after replacing Units 5-8 generated curriculum scaffolds with canonical authored published lessons, locking Lesson 7-11 dataset/workbook/presentation contract details into manifest regressions, and updating the active Conductor curriculum status to show all eight instructional units as authored.*
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+deleted file mode 100644
+index e53f588834f06d46a36ab82106566ba908bb6cbc..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
++++ /dev/null
+@@ -1,11 +0,0 @@
+-{
+-  "track_id": "capstone_textbook_completion_20260314",
+-  "type": "feature",
+-  "status": "in_progress",
+-  "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
+-  "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+-  "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
+-}
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/plan.md b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+deleted file mode 100644
+index 48b611e5d419a88dad2df9bf2d2a717532f66631..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/plan.md
++++ /dev/null
+@@ -1,31 +0,0 @@
+-# Implementation Plan
+-
+-## Phase 1: Capstone Contract Guards
+-
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
+-
+-## Phase 2: Capstone Runtime Completion
+-
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+-
+-## Phase 3: Verification and Documentation
+-
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
    });
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-+  });
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
-     );
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
-+
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
-+
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
-+  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
-+
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
-+  });
-+
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
-+    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
-+
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
-+  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json b/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-index 364eb86a46b01b017fb851065b622f944b094de2..c26bc753dc608b14cfc91af840979997116e6d4e
---- a/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-+++ b/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-@@ -1,9 +1,9 @@
- {
-   "track_id": "lesson_phase_objective_shell_20260311",
-   "type": "feature",
--  "status": "in_progress",
-+  "status": "archived",
-   "created_at": "2026-03-11T08:34:26Z",
--  "updated_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:44:39Z",
-   "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-   "estimated_tasks": 7,
-   "actual_tasks": 7,
-diff --git a/conductor/archive/lesson_phase_objective_shell_20260311/plan.md b/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-index 2c93ed93a25fc6373d8d1f962121f21b751f077d..504954d0bd54aa9b155a27df0bdb25d02fbe44eb
---- a/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-+++ b/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-@@ -19,4 +19,4 @@
- ## Phase 3: Verification and Closeout
- - [x] Task: Run lint and automated tests for the track scope
- - [x] Task: Update README and Conductor memory files with the phase-shell alignment outcome
--- [ ] Task: Archive the track and finalize registry/metadata updates
-+- [x] Task: Archive the track and finalize registry/metadata updates
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
 diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
-index 5a1f9d47c9c577b016380d41f806bcb496b3903e..d8b4b634818b45cfc7f4d67cb6059d6c008a677c
+index c6744e6265e1305f9e62ae2ff35179c8e2a6a46c..cfcfcc73b61799c67dee30ab713d6cfa49742d13
 --- a/conductor/lessons-learned.md
 +++ b/conductor/lessons-learned.md
-@@ -14,6 +14,7 @@
- - (2026-03-11, activity_component_contract_refactor_20260311) Activity contracts drift quickly when alias handling lives separately from the runtime registry and schema validators; documented curriculum keys need one canonical resolver.
- - (2026-03-11, published_progress_viewmodel_consolidation_20260311) Student dashboard rows, teacher student-detail rows, and lesson phase-status payloads should derive from the same published-progress helpers or published-version/locking fixes will diverge again.
- - (2026-03-11, student_lesson_access_boundary_cleanup_20260311) Student-owned API writes need an explicit request-level student guard even when teacher/admin lesson preview is intentionally allowed elsewhere; auth presence alone is not a safe mutation boundary.
-+- (2026-03-11, lesson_phase_objective_shell_20260311) Student lesson pages should derive phase guidance from curriculum lesson-type metadata and reuse the shared `PhaseRenderer`, or pedagogy copy and content rendering drift separately.
- 
- ### Recurring Gotchas
- <!-- Problems encountered repeatedly; save future tracks from the same pain -->
+@@ -23,6 +23,7 @@
+ - (2026-03-13, unit1_canonicalization_archetypes) Canonicalizing authored curriculum at the source is safer than accumulating manifest exceptions because both validation drift and role-specific rendering bugs disappear together.
+ - (2026-03-13, auth bootstrap hotfix) JWT-protected proxy rules must explicitly allow unauthenticated auth bootstrap endpoints like `/api/auth/login` and `/api/auth/session`, or the login page will redirect its own API calls back to itself.
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+ ### Patterns Worth Repeating
+ 
+@@ -36,3 +37,4 @@
+ - (2026-03-13, curriculum_rollout_wave1) Lesson-planning matrices are a workable source for authored rollout waves when they are converted into canonical lesson blueprints and validated through manifest regression tests before runtime wiring.
+ - (2026-03-13, unit1_redesign_lesson_contract) Old infrastructure tests should validate canonical phase numbers and contract outcomes rather than stale phase labels, or redesign tracks will appear broken even when the authored curriculum and published runtime agree.
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+diff --git a/conductor/roadmap.md b/conductor/roadmap.md
+index 11d3802482df109f7afa63e1f8e06a29b281e2de..1ba34cd74dce8269c6b81c2e0e4a298afca7f180
+--- a/conductor/roadmap.md
++++ b/conductor/roadmap.md
+@@ -105,6 +105,8 @@
+ 
+ **Goal**: finish the book with the culminating experience and final coherence pass.
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+ 
+ - Capstone and Textbook Completion
+diff --git a/conductor/tech-debt.md b/conductor/tech-debt.md
+index 9bac252a4eba200df1bc57aede467e480d1d1f2a..640e1a1e5f5401852a117c3e7cf478b2cdbc1dff
+--- a/conductor/tech-debt.md
++++ b/conductor/tech-debt.md
+@@ -4,7 +4,6 @@
+ 
+ | Date | Track | Item | Severity | Status | Notes |
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+ | 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+ | 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
 diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..a94965df8c673cc029927782a3d791909b4e2091
+index 6704b229976c3e143989c878a6d0db8b29221c6c..8adc26ec3d1fcbe497a6f50ee5d48079589ac882
 --- a/conductor/tracks.md
 +++ b/conductor/tracks.md
-@@ -32,6 +32,10 @@
+@@ -6,8 +6,7 @@
+ 
+ ## Active Track
+ 
+-- [~] **Track: Capstone and Textbook Completion**
+-  *Link: [./tracks/capstone_textbook_completion_20260314/](./tracks/capstone_textbook_completion_20260314/)*
++- No active track. The next `/conductor` run should take the first item from the planned queue.
+ 
+ ## Planned Queue
+ 
+@@ -16,6 +15,10 @@
  
  ## Archive Ledger
  
-+- [x] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/archive/lesson_phase_objective_shell_20260311/](./conductor/archive/lesson_phase_objective_shell_20260311/)*
-+  *Checkpoint: `f9a651e` (2026-03-11 implementation complete; archived during closeout)*
-+
- - [x] **Track: Student Write Boundary Hardening**
-   *Link: [./conductor/archive/student_lesson_access_boundary_cleanup_20260311/](./conductor/archive/student_lesson_access_boundary_cleanup_20260311/)*
-   *Checkpoint: `pending` (2026-03-11 closeout)*
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
++- [x] **Track: Capstone and Textbook Completion**
++  *Link: [./archive/capstone_textbook_completion_20260314/](./archive/capstone_textbook_completion_20260314/)*
++  *Closeout: archived on 2026-03-14 after replacing the generated capstone scaffold with authored runtime content, locking milestone/workbook/final-presentation coverage into manifest and UI regressions, and normalizing capstone labeling across the core public, student, and teacher textbook flow.*
++
+ - [x] **Track: Curriculum Rollout Wave 2 (Units 5-8)**
+   *Link: [./archive/curriculum_rollout_wave2_20260314/](./archive/curriculum_rollout_wave2_20260314/)*
+   *Closeout: archived on 2026-03-14 after replacing Units 5-8 generated curriculum scaffolds with canonical authored published lessons, locking Lesson 7-11 dataset/workbook/presentation contract details into manifest regressions, and updating the active Conductor curriculum status to show all eight instructional units as authored.*
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+deleted file mode 100644
+index e53f588834f06d46a36ab82106566ba908bb6cbc..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
++++ /dev/null
+@@ -1,11 +0,0 @@
+-{
+-  "track_id": "capstone_textbook_completion_20260314",
+-  "type": "feature",
+-  "status": "in_progress",
+-  "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
+-  "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+-  "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
+-}
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/plan.md b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+deleted file mode 100644
+index 48b611e5d419a88dad2df9bf2d2a717532f66631..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/plan.md
++++ /dev/null
+@@ -1,31 +0,0 @@
+-# Implementation Plan
+-
+-## Phase 1: Capstone Contract Guards
+-
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
+-
+-## Phase 2: Capstone Runtime Completion
+-
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+-
+-## Phase 3: Verification and Documentation
+-
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
 
 exec
-/bin/bash -lc 'git status --short --untracked-files=all' in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
- M conductor/autonomous_prompt.md
+/bin/bash -lc 'git status --short' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+ M .env.example
+ M README.md
+ M __tests__/components/exercises/JournalEntryBuilding.test.tsx
+ M __tests__/components/financial-reports/CashFlowStatementDetailed.test.tsx
+ M __tests__/lib/convex/config.test.ts
+ M __tests__/proxy.test.ts
+ M __tests__/setup/dev-stack-script.test.ts
  M conductor/codex-cron.log
  M conductor/codex-last.txt
+ M conductor/docs/architecture/runtime.md
  M conductor/tracks.md
- D conductor/tracks/lesson_phase_objective_shell_20260311/index.md
- D conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json
- D conductor/tracks/lesson_phase_objective_shell_20260311/plan.md
- D conductor/tracks/lesson_phase_objective_shell_20260311/spec.md
- M conductor/workflow.md
-?? conductor/archive/lesson_phase_objective_shell_20260311/index.md
-?? conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-?? conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-?? conductor/archive/lesson_phase_objective_shell_20260311/spec.md
-?? conductor/current_directive.md
+ D conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+ D conductor/tracks/capstone_textbook_completion_20260314/plan.md
+ M convex/teacher.ts
+ M docs/security-api-route-matrix.md
+ M lib/activities/component-keys.ts
+ M lib/convex/admin.ts
+ M lib/db/schema/phase-content.ts
+ M lib/db/schema/validators.ts
+ M proxy.ts
+exec
+/bin/bash -lc "sed -n '1,220p' conductor/tracks.md" in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+# Project Tracks
+
+This file is the source of truth for active execution order. Archived tracks live in `conductor/archive/`. The milestone-level delivery sequence lives in [./roadmap.md](./roadmap.md).
+
+---
+
+## Active Track
+
+- No active track. The next `/conductor` run should take the first item from the planned queue.
+
+## Planned Queue
+
+1. **Cloudflare Production Hardening and Launch**
+  Finalize Worker deployment, runtime verification, auth hardening, and launch quality gates.
+
+## Archive Ledger
+
+- [x] **Track: Capstone and Textbook Completion**
+  *Link: [./archive/capstone_textbook_completion_20260314/](./archive/capstone_textbook_completion_20260314/)*
+  *Closeout: archived on 2026-03-14 after replacing the generated capstone scaffold with authored runtime content, locking milestone/workbook/final-presentation coverage into manifest and UI regressions, and normalizing capstone labeling across the core public, student, and teacher textbook flow.*
 file update:
-diff --git a/README.md b/README.md
-index 9ee7fe58cfca200160c7dcd63527376b6a6e3202..d15e181d7fdd88c0d5393d28b496fdbcdc953762
---- a/README.md
-+++ b/README.md
-@@ -17,6 +17,7 @@
- - **Shared Dashboard Recommendation Cards**: Student and teacher progress surfaces now reuse one next-lesson card and shared unit-status presentation helpers to keep resume/start guidance aligned
- - **Shared Published Progress View-Models**: Student dashboard units, teacher student-detail progress rows, and lesson phase-status responses now derive from the same published-progress helpers so progress math and locking rules stay aligned
- - **Canonical Activity Component Catalog**: Activity rendering and validation now resolve documented aliases such as `spreadsheet-activity` and `journal-entry-activity` through one canonical runtime contract, reducing curriculum authoring drift
-+- **Objective-Aware Lesson Phase Shell**: Student lesson phases now show curriculum-aligned phase guidance and success criteria while reusing the shared phase content renderer
- - **Deterministic Simulation Runtime IDs**: Inventory simulation notifications and market events now keep stable unique ids during rapid updates, preventing duplicate-key rendering regressions
- - **Account Settings & Self-Service Password Changes**: Authenticated users can review account context and update their own password without leaving the session
- - **Multi-tenant Architecture**: Organization-based access control
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
-+
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
+index c6744e6265e1305f9e62ae2ff35179c8e2a6a46c..cfcfcc73b61799c67dee30ab713d6cfa49742d13
+--- a/conductor/lessons-learned.md
++++ b/conductor/lessons-learned.md
+@@ -23,6 +23,7 @@
+ - (2026-03-13, unit1_canonicalization_archetypes) Canonicalizing authored curriculum at the source is safer than accumulating manifest exceptions because both validation drift and role-specific rendering bugs disappear together.
+ - (2026-03-13, auth bootstrap hotfix) JWT-protected proxy rules must explicitly allow unauthenticated auth bootstrap endpoints like `/api/auth/login` and `/api/auth/session`, or the login page will redirect its own API calls back to itself.
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+ ### Patterns Worth Repeating
+ 
+@@ -36,3 +37,4 @@
+ - (2026-03-13, curriculum_rollout_wave1) Lesson-planning matrices are a workable source for authored rollout waves when they are converted into canonical lesson blueprints and validated through manifest regression tests before runtime wiring.
+ - (2026-03-13, unit1_redesign_lesson_contract) Old infrastructure tests should validate canonical phase numbers and contract outcomes rather than stale phase labels, or redesign tracks will appear broken even when the authored curriculum and published runtime agree.
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+diff --git a/conductor/roadmap.md b/conductor/roadmap.md
+index 11d3802482df109f7afa63e1f8e06a29b281e2de..1ba34cd74dce8269c6b81c2e0e4a298afca7f180
+--- a/conductor/roadmap.md
++++ b/conductor/roadmap.md
+@@ -105,6 +105,8 @@
+ 
+ **Goal**: finish the book with the culminating experience and final coherence pass.
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+ 
+ - Capstone and Textbook Completion
+diff --git a/conductor/tech-debt.md b/conductor/tech-debt.md
+index 9bac252a4eba200df1bc57aede467e480d1d1f2a..640e1a1e5f5401852a117c3e7cf478b2cdbc1dff
+--- a/conductor/tech-debt.md
++++ b/conductor/tech-debt.md
+@@ -4,7 +4,6 @@
+ 
+ | Date | Track | Item | Severity | Status | Notes |
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+ | 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+ | 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
+diff --git a/conductor/tracks.md b/conductor/tracks.md
+index 6704b229976c3e143989c878a6d0db8b29221c6c..8adc26ec3d1fcbe497a6f50ee5d48079589ac882
+--- a/conductor/tracks.md
++++ b/conductor/tracks.md
+@@ -6,8 +6,7 @@
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
+ ## Active Track
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+-- [~] **Track: Capstone and Textbook Completion**
+-  *Link: [./tracks/capstone_textbook_completion_20260314/](./tracks/capstone_textbook_completion_20260314/)*
++- No active track. The next `/conductor` run should take the first item from the planned queue.
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-   });
+ ## Planned Queue
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
+@@ -16,6 +15,10 @@
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+ ## Archive Ledger
  
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-+  });
-+
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
++- [x] **Track: Capstone and Textbook Completion**
++  *Link: [./archive/capstone_textbook_completion_20260314/](./archive/capstone_textbook_completion_20260314/)*
++  *Closeout: archived on 2026-03-14 after replacing the generated capstone scaffold with authored runtime content, locking milestone/workbook/final-presentation coverage into manifest and UI regressions, and normalizing capstone labeling across the core public, student, and teacher textbook flow.*
++
+ - [x] **Track: Curriculum Rollout Wave 2 (Units 5-8)**
+   *Link: [./archive/curriculum_rollout_wave2_20260314/](./archive/curriculum_rollout_wave2_20260314/)*
+   *Closeout: archived on 2026-03-14 after replacing Units 5-8 generated curriculum scaffolds with canonical authored published lessons, locking Lesson 7-11 dataset/workbook/presentation contract details into manifest regressions, and updating the active Conductor curriculum status to show all eight instructional units as authored.*
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+deleted file mode 100644
+index e53f588834f06d46a36ab82106566ba908bb6cbc..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
++++ /dev/null
+@@ -1,11 +0,0 @@
+-{
+-  "track_id": "capstone_textbook_completion_20260314",
+-  "type": "feature",
+-  "status": "in_progress",
+-  "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
+-  "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+-  "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
+-}
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/plan.md b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+deleted file mode 100644
+index 48b611e5d419a88dad2df9bf2d2a717532f66631..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/plan.md
++++ /dev/null
+@@ -1,31 +0,0 @@
+-# Implementation Plan
+-
+-## Phase 1: Capstone Contract Guards
+-
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
+-
+-## Phase 2: Capstone Runtime Completion
+-
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+-
+-## Phase 3: Verification and Documentation
+-
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
+
+exec
+/bin/bash -lc 'git add -- conductor/tracks.md conductor/archive/capstone_textbook_completion_20260314 conductor/tracks/capstone_textbook_completion_20260314' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
-+  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
-+
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
-+  });
-+
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
-+    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
-+
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
-+  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json b/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-index 364eb86a46b01b017fb851065b622f944b094de2..c26bc753dc608b14cfc91af840979997116e6d4e
---- a/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-+++ b/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-@@ -1,9 +1,9 @@
- {
-   "track_id": "lesson_phase_objective_shell_20260311",
-   "type": "feature",
--  "status": "in_progress",
-+  "status": "archived",
-   "created_at": "2026-03-11T08:34:26Z",
--  "updated_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:44:39Z",
-   "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-   "estimated_tasks": 7,
-   "actual_tasks": 7,
-diff --git a/conductor/archive/lesson_phase_objective_shell_20260311/plan.md b/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-index 2c93ed93a25fc6373d8d1f962121f21b751f077d..504954d0bd54aa9b155a27df0bdb25d02fbe44eb
---- a/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-+++ b/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-@@ -19,4 +19,4 @@
- ## Phase 3: Verification and Closeout
- - [x] Task: Run lint and automated tests for the track scope
- - [x] Task: Update README and Conductor memory files with the phase-shell alignment outcome
--- [ ] Task: Archive the track and finalize registry/metadata updates
-+- [x] Task: Archive the track and finalize registry/metadata updates
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
 diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
-index 5a1f9d47c9c577b016380d41f806bcb496b3903e..d8b4b634818b45cfc7f4d67cb6059d6c008a677c
+index c6744e6265e1305f9e62ae2ff35179c8e2a6a46c..cfcfcc73b61799c67dee30ab713d6cfa49742d13
 --- a/conductor/lessons-learned.md
 +++ b/conductor/lessons-learned.md
-@@ -14,6 +14,7 @@
- - (2026-03-11, activity_component_contract_refactor_20260311) Activity contracts drift quickly when alias handling lives separately from the runtime registry and schema validators; documented curriculum keys need one canonical resolver.
- - (2026-03-11, published_progress_viewmodel_consolidation_20260311) Student dashboard rows, teacher student-detail rows, and lesson phase-status payloads should derive from the same published-progress helpers or published-version/locking fixes will diverge again.
- - (2026-03-11, student_lesson_access_boundary_cleanup_20260311) Student-owned API writes need an explicit request-level student guard even when teacher/admin lesson preview is intentionally allowed elsewhere; auth presence alone is not a safe mutation boundary.
-+- (2026-03-11, lesson_phase_objective_shell_20260311) Student lesson pages should derive phase guidance from curriculum lesson-type metadata and reuse the shared `PhaseRenderer`, or pedagogy copy and content rendering drift separately.
- 
- ### Recurring Gotchas
- <!-- Problems encountered repeatedly; save future tracks from the same pain -->
+@@ -23,6 +23,7 @@
+ - (2026-03-13, unit1_canonicalization_archetypes) Canonicalizing authored curriculum at the source is safer than accumulating manifest exceptions because both validation drift and role-specific rendering bugs disappear together.
+ - (2026-03-13, auth bootstrap hotfix) JWT-protected proxy rules must explicitly allow unauthenticated auth bootstrap endpoints like `/api/auth/login` and `/api/auth/session`, or the login page will redirect its own API calls back to itself.
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+ ### Patterns Worth Repeating
+ 
+@@ -36,3 +37,4 @@
+ - (2026-03-13, curriculum_rollout_wave1) Lesson-planning matrices are a workable source for authored rollout waves when they are converted into canonical lesson blueprints and validated through manifest regression tests before runtime wiring.
+ - (2026-03-13, unit1_redesign_lesson_contract) Old infrastructure tests should validate canonical phase numbers and contract outcomes rather than stale phase labels, or redesign tracks will appear broken even when the authored curriculum and published runtime agree.
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+diff --git a/conductor/roadmap.md b/conductor/roadmap.md
+index 11d3802482df109f7afa63e1f8e06a29b281e2de..1ba34cd74dce8269c6b81c2e0e4a298afca7f180
+--- a/conductor/roadmap.md
++++ b/conductor/roadmap.md
+@@ -105,6 +105,8 @@
+ 
+ **Goal**: finish the book with the culminating experience and final coherence pass.
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+ 
+ - Capstone and Textbook Completion
+diff --git a/conductor/tech-debt.md b/conductor/tech-debt.md
+index 9bac252a4eba200df1bc57aede467e480d1d1f2a..640e1a1e5f5401852a117c3e7cf478b2cdbc1dff
+--- a/conductor/tech-debt.md
++++ b/conductor/tech-debt.md
+@@ -4,7 +4,6 @@
+ 
+ | Date | Track | Item | Severity | Status | Notes |
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+ | 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+ | 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
 diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..a94965df8c673cc029927782a3d791909b4e2091
+index 6704b229976c3e143989c878a6d0db8b29221c6c..8adc26ec3d1fcbe497a6f50ee5d48079589ac882
 --- a/conductor/tracks.md
 +++ b/conductor/tracks.md
-@@ -32,6 +32,10 @@
- 
- ## Archive Ledger
- 
-+- [x] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/archive/lesson_phase_objective_shell_20260311/](./conductor/archive/lesson_phase_objective_shell_20260311/)*
-+  *Checkpoint: `f9a651e` (2026-03-11 implementation complete; archived during closeout)*
-+
- - [x] **Track: Student Write Boundary Hardening**
-   *Link: [./conductor/archive/student_lesson_access_boundary_cleanup_20260311/](./conductor/archive/student_lesson_access_boundary_cleanup_20260311/)*
-   *Checkpoint: `pending` (2026-03-11 closeout)*
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
-
-exec
-/bin/bash -lc 'git add conductor/tracks.md conductor/tracks/lesson_phase_objective_shell_20260311/index.md conductor/tracks/lesson_phase_objective_shell_20260311/metadata.json conductor/tracks/lesson_phase_objective_shell_20260311/plan.md conductor/tracks/lesson_phase_objective_shell_20260311/spec.md conductor/archive/lesson_phase_objective_shell_20260311/index.md conductor/archive/lesson_phase_objective_shell_20260311/metadata.json conductor/archive/lesson_phase_objective_shell_20260311/plan.md conductor/archive/lesson_phase_objective_shell_20260311/spec.md' in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
-
-file update:
-diff --git a/README.md b/README.md
-index 9ee7fe58cfca200160c7dcd63527376b6a6e3202..d15e181d7fdd88c0d5393d28b496fdbcdc953762
---- a/README.md
-+++ b/README.md
-@@ -17,6 +17,7 @@
- - **Shared Dashboard Recommendation Cards**: Student and teacher progress surfaces now reuse one next-lesson card and shared unit-status presentation helpers to keep resume/start guidance aligned
- - **Shared Published Progress View-Models**: Student dashboard units, teacher student-detail progress rows, and lesson phase-status responses now derive from the same published-progress helpers so progress math and locking rules stay aligned
- - **Canonical Activity Component Catalog**: Activity rendering and validation now resolve documented aliases such as `spreadsheet-activity` and `journal-entry-activity` through one canonical runtime contract, reducing curriculum authoring drift
-+- **Objective-Aware Lesson Phase Shell**: Student lesson phases now show curriculum-aligned phase guidance and success criteria while reusing the shared phase content renderer
- - **Deterministic Simulation Runtime IDs**: Inventory simulation notifications and market events now keep stable unique ids during rapid updates, preventing duplicate-key rendering regressions
- - **Account Settings & Self-Service Password Changes**: Authenticated users can review account context and update their own password without leaving the session
- - **Multi-tenant Architecture**: Organization-based access control
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
-+
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
-   });
- 
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
+@@ -6,8 +6,7 @@
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+ ## Active Track
  
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-   });
+-- [~] **Track: Capstone and Textbook Completion**
+-  *Link: [./tracks/capstone_textbook_completion_20260314/](./tracks/capstone_textbook_completion_20260314/)*
++- No active track. The next `/conductor` run should take the first item from the planned queue.
  
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
+ ## Planned Queue
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-   });
+@@ -16,6 +15,10 @@
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
+ ## Archive Ledger
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
++- [x] **Track: Capstone and Textbook Completion**
++  *Link: [./archive/capstone_textbook_completion_20260314/](./archive/capstone_textbook_completion_20260314/)*
++  *Closeout: archived on 2026-03-14 after replacing the generated capstone scaffold with authored runtime content, locking milestone/workbook/final-presentation coverage into manifest and UI regressions, and normalizing capstone labeling across the core public, student, and teacher textbook flow.*
++
+ - [x] **Track: Curriculum Rollout Wave 2 (Units 5-8)**
+   *Link: [./archive/curriculum_rollout_wave2_20260314/](./archive/curriculum_rollout_wave2_20260314/)*
+   *Closeout: archived on 2026-03-14 after replacing Units 5-8 generated curriculum scaffolds with canonical authored published lessons, locking Lesson 7-11 dataset/workbook/presentation contract details into manifest regressions, and updating the active Conductor curriculum status to show all eight instructional units as authored.*
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+deleted file mode 100644
+index e53f588834f06d46a36ab82106566ba908bb6cbc..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
++++ /dev/null
+@@ -1,11 +0,0 @@
+-{
+-  "track_id": "capstone_textbook_completion_20260314",
+-  "type": "feature",
+-  "status": "in_progress",
+-  "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
+-  "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+-  "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
+-}
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/plan.md b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+deleted file mode 100644
+index 48b611e5d419a88dad2df9bf2d2a717532f66631..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/plan.md
++++ /dev/null
+@@ -1,31 +0,0 @@
+-# Implementation Plan
+-
+-## Phase 1: Capstone Contract Guards
+-
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
+-
+-## Phase 2: Capstone Runtime Completion
+-
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+-
+-## Phase 3: Verification and Documentation
+-
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
+
+exec
+/bin/bash -lc 'git status --short' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+ M .env.example
+ M README.md
+ M __tests__/components/exercises/JournalEntryBuilding.test.tsx
+ M __tests__/components/financial-reports/CashFlowStatementDetailed.test.tsx
+ M __tests__/lib/convex/config.test.ts
+ M __tests__/proxy.test.ts
+ M __tests__/setup/dev-stack-script.test.ts
+A  conductor/archive/capstone_textbook_completion_20260314/index.md
+R  conductor/tracks/capstone_textbook_completion_20260314/metadata.json -> conductor/archive/capstone_textbook_completion_20260314/metadata.json
+R  conductor/tracks/capstone_textbook_completion_20260314/plan.md -> conductor/archive/capstone_textbook_completion_20260314/plan.md
+A  conductor/archive/capstone_textbook_completion_20260314/spec.md
+ M conductor/codex-cron.log
+ M conductor/codex-last.txt
+ M conductor/docs/architecture/runtime.md
+M  conductor/tracks.md
+ M convex/teacher.ts
+ M docs/security-api-route-matrix.md
+ M lib/activities/component-keys.ts
+ M lib/convex/admin.ts
+ M lib/db/schema/phase-content.ts
+exec
+/bin/bash -lc 'git diff --cached --stat' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+ .../capstone_textbook_completion_20260314/index.md |  4 +++
+ .../metadata.json                                  |  0
+ .../capstone_textbook_completion_20260314/plan.md  |  0
+ .../capstone_textbook_completion_20260314/spec.md  | 34 ++++++++++++++++++++++
+ conductor/tracks.md                                |  8 +++--
+ 5 files changed, 43 insertions(+), 3 deletions(-)
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
    });
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-+  });
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
-     );
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
-+
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
-+
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
-+  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
-+
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
-+  });
-+
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
-+    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
-+
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
-+  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json b/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-index 364eb86a46b01b017fb851065b622f944b094de2..c26bc753dc608b14cfc91af840979997116e6d4e
---- a/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-+++ b/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-@@ -1,9 +1,9 @@
- {
-   "track_id": "lesson_phase_objective_shell_20260311",
-   "type": "feature",
--  "status": "in_progress",
-+  "status": "archived",
-   "created_at": "2026-03-11T08:34:26Z",
--  "updated_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:44:39Z",
-   "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-   "estimated_tasks": 7,
-   "actual_tasks": 7,
-diff --git a/conductor/archive/lesson_phase_objective_shell_20260311/plan.md b/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-index 2c93ed93a25fc6373d8d1f962121f21b751f077d..504954d0bd54aa9b155a27df0bdb25d02fbe44eb
---- a/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-+++ b/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-@@ -19,4 +19,4 @@
- ## Phase 3: Verification and Closeout
- - [x] Task: Run lint and automated tests for the track scope
- - [x] Task: Update README and Conductor memory files with the phase-shell alignment outcome
--- [ ] Task: Archive the track and finalize registry/metadata updates
-+- [x] Task: Archive the track and finalize registry/metadata updates
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
 diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
-index 5a1f9d47c9c577b016380d41f806bcb496b3903e..d8b4b634818b45cfc7f4d67cb6059d6c008a677c
+index c6744e6265e1305f9e62ae2ff35179c8e2a6a46c..cfcfcc73b61799c67dee30ab713d6cfa49742d13
 --- a/conductor/lessons-learned.md
 +++ b/conductor/lessons-learned.md
-@@ -14,6 +14,7 @@
- - (2026-03-11, activity_component_contract_refactor_20260311) Activity contracts drift quickly when alias handling lives separately from the runtime registry and schema validators; documented curriculum keys need one canonical resolver.
- - (2026-03-11, published_progress_viewmodel_consolidation_20260311) Student dashboard rows, teacher student-detail rows, and lesson phase-status payloads should derive from the same published-progress helpers or published-version/locking fixes will diverge again.
- - (2026-03-11, student_lesson_access_boundary_cleanup_20260311) Student-owned API writes need an explicit request-level student guard even when teacher/admin lesson preview is intentionally allowed elsewhere; auth presence alone is not a safe mutation boundary.
-+- (2026-03-11, lesson_phase_objective_shell_20260311) Student lesson pages should derive phase guidance from curriculum lesson-type metadata and reuse the shared `PhaseRenderer`, or pedagogy copy and content rendering drift separately.
- 
- ### Recurring Gotchas
- <!-- Problems encountered repeatedly; save future tracks from the same pain -->
+@@ -23,6 +23,7 @@
+ - (2026-03-13, unit1_canonicalization_archetypes) Canonicalizing authored curriculum at the source is safer than accumulating manifest exceptions because both validation drift and role-specific rendering bugs disappear together.
+ - (2026-03-13, auth bootstrap hotfix) JWT-protected proxy rules must explicitly allow unauthenticated auth bootstrap endpoints like `/api/auth/login` and `/api/auth/session`, or the login page will redirect its own API calls back to itself.
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+ ### Patterns Worth Repeating
+ 
+@@ -36,3 +37,4 @@
+ - (2026-03-13, curriculum_rollout_wave1) Lesson-planning matrices are a workable source for authored rollout waves when they are converted into canonical lesson blueprints and validated through manifest regression tests before runtime wiring.
+ - (2026-03-13, unit1_redesign_lesson_contract) Old infrastructure tests should validate canonical phase numbers and contract outcomes rather than stale phase labels, or redesign tracks will appear broken even when the authored curriculum and published runtime agree.
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+diff --git a/conductor/roadmap.md b/conductor/roadmap.md
+index 11d3802482df109f7afa63e1f8e06a29b281e2de..1ba34cd74dce8269c6b81c2e0e4a298afca7f180
+--- a/conductor/roadmap.md
++++ b/conductor/roadmap.md
+@@ -105,6 +105,8 @@
+ 
+ **Goal**: finish the book with the culminating experience and final coherence pass.
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+ 
+ - Capstone and Textbook Completion
+diff --git a/conductor/tech-debt.md b/conductor/tech-debt.md
+index 9bac252a4eba200df1bc57aede467e480d1d1f2a..640e1a1e5f5401852a117c3e7cf478b2cdbc1dff
+--- a/conductor/tech-debt.md
++++ b/conductor/tech-debt.md
+@@ -4,7 +4,6 @@
+ 
+ | Date | Track | Item | Severity | Status | Notes |
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+ | 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+ | 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
 diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..a94965df8c673cc029927782a3d791909b4e2091
+index 6704b229976c3e143989c878a6d0db8b29221c6c..8adc26ec3d1fcbe497a6f50ee5d48079589ac882
 --- a/conductor/tracks.md
 +++ b/conductor/tracks.md
-@@ -32,6 +32,10 @@
+@@ -6,8 +6,7 @@
+ 
+ ## Active Track
+ 
+-- [~] **Track: Capstone and Textbook Completion**
+-  *Link: [./tracks/capstone_textbook_completion_20260314/](./tracks/capstone_textbook_completion_20260314/)*
++- No active track. The next `/conductor` run should take the first item from the planned queue.
+ 
+ ## Planned Queue
+ 
+@@ -16,6 +15,10 @@
  
  ## Archive Ledger
  
-+- [x] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/archive/lesson_phase_objective_shell_20260311/](./conductor/archive/lesson_phase_objective_shell_20260311/)*
-+  *Checkpoint: `f9a651e` (2026-03-11 implementation complete; archived during closeout)*
-+
- - [x] **Track: Student Write Boundary Hardening**
-   *Link: [./conductor/archive/student_lesson_access_boundary_cleanup_20260311/](./conductor/archive/student_lesson_access_boundary_cleanup_20260311/)*
-   *Checkpoint: `pending` (2026-03-11 closeout)*
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
++- [x] **Track: Capstone and Textbook Completion**
++  *Link: [./archive/capstone_textbook_completion_20260314/](./archive/capstone_textbook_completion_20260314/)*
++  *Closeout: archived on 2026-03-14 after replacing the generated capstone scaffold with authored runtime content, locking milestone/workbook/final-presentation coverage into manifest and UI regressions, and normalizing capstone labeling across the core public, student, and teacher textbook flow.*
++
+ - [x] **Track: Curriculum Rollout Wave 2 (Units 5-8)**
+   *Link: [./archive/curriculum_rollout_wave2_20260314/](./archive/curriculum_rollout_wave2_20260314/)*
+   *Closeout: archived on 2026-03-14 after replacing Units 5-8 generated curriculum scaffolds with canonical authored published lessons, locking Lesson 7-11 dataset/workbook/presentation contract details into manifest regressions, and updating the active Conductor curriculum status to show all eight instructional units as authored.*
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+deleted file mode 100644
+index e53f588834f06d46a36ab82106566ba908bb6cbc..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
++++ /dev/null
+@@ -1,11 +0,0 @@
+-{
+-  "track_id": "capstone_textbook_completion_20260314",
+-  "type": "feature",
+-  "status": "in_progress",
+-  "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
+-  "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+-  "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
+-}
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/plan.md b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+deleted file mode 100644
+index 48b611e5d419a88dad2df9bf2d2a717532f66631..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/plan.md
++++ /dev/null
+@@ -1,31 +0,0 @@
+-# Implementation Plan
+-
+-## Phase 1: Capstone Contract Guards
+-
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
+-
+-## Phase 2: Capstone Runtime Completion
+-
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+-
+-## Phase 3: Verification and Documentation
+-
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
 
 exec
-/bin/bash -lc 'git commit -m "chore(conductor): archive lesson phase objective shell track"' in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
-[experimental-rewrite 3c4a121] chore(conductor): archive lesson phase objective shell track
- 5 files changed, 7 insertions(+), 7 deletions(-)
- rename conductor/{tracks => archive}/lesson_phase_objective_shell_20260311/index.md (100%)
- rename conductor/{tracks => archive}/lesson_phase_objective_shell_20260311/metadata.json (84%)
- rename conductor/{tracks => archive}/lesson_phase_objective_shell_20260311/plan.md (95%)
- rename conductor/{tracks => archive}/lesson_phase_objective_shell_20260311/spec.md (100%)
+/bin/bash -lc 'git commit -m "chore(conductor): Archive completed track"' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+[main a5c333a] chore(conductor): Archive completed track
+ 5 files changed, 43 insertions(+), 3 deletions(-)
+ create mode 100644 conductor/archive/capstone_textbook_completion_20260314/index.md
+ rename conductor/{tracks => archive}/capstone_textbook_completion_20260314/metadata.json (100%)
+ rename conductor/{tracks => archive}/capstone_textbook_completion_20260314/plan.md (100%)
+ create mode 100644 conductor/archive/capstone_textbook_completion_20260314/spec.md
 file update:
-diff --git a/README.md b/README.md
-index 9ee7fe58cfca200160c7dcd63527376b6a6e3202..d15e181d7fdd88c0d5393d28b496fdbcdc953762
---- a/README.md
-+++ b/README.md
-@@ -17,6 +17,7 @@
- - **Shared Dashboard Recommendation Cards**: Student and teacher progress surfaces now reuse one next-lesson card and shared unit-status presentation helpers to keep resume/start guidance aligned
- - **Shared Published Progress View-Models**: Student dashboard units, teacher student-detail progress rows, and lesson phase-status responses now derive from the same published-progress helpers so progress math and locking rules stay aligned
- - **Canonical Activity Component Catalog**: Activity rendering and validation now resolve documented aliases such as `spreadsheet-activity` and `journal-entry-activity` through one canonical runtime contract, reducing curriculum authoring drift
-+- **Objective-Aware Lesson Phase Shell**: Student lesson phases now show curriculum-aligned phase guidance and success criteria while reusing the shared phase content renderer
- - **Deterministic Simulation Runtime IDs**: Inventory simulation notifications and market events now keep stable unique ids during rapid updates, preventing duplicate-key rendering regressions
- - **Account Settings & Self-Service Password Changes**: Authenticated users can review account context and update their own password without leaving the session
- - **Multi-tenant Architecture**: Organization-based access control
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
-+
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
+index c6744e6265e1305f9e62ae2ff35179c8e2a6a46c..cfcfcc73b61799c67dee30ab713d6cfa49742d13
+--- a/conductor/lessons-learned.md
++++ b/conductor/lessons-learned.md
+@@ -23,6 +23,7 @@
+ - (2026-03-13, unit1_canonicalization_archetypes) Canonicalizing authored curriculum at the source is safer than accumulating manifest exceptions because both validation drift and role-specific rendering bugs disappear together.
+ - (2026-03-13, auth bootstrap hotfix) JWT-protected proxy rules must explicitly allow unauthenticated auth bootstrap endpoints like `/api/auth/login` and `/api/auth/session`, or the login page will redirect its own API calls back to itself.
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+ ### Patterns Worth Repeating
+ 
+@@ -36,3 +37,4 @@
+ - (2026-03-13, curriculum_rollout_wave1) Lesson-planning matrices are a workable source for authored rollout waves when they are converted into canonical lesson blueprints and validated through manifest regression tests before runtime wiring.
+ - (2026-03-13, unit1_redesign_lesson_contract) Old infrastructure tests should validate canonical phase numbers and contract outcomes rather than stale phase labels, or redesign tracks will appear broken even when the authored curriculum and published runtime agree.
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+diff --git a/conductor/roadmap.md b/conductor/roadmap.md
+index 11d3802482df109f7afa63e1f8e06a29b281e2de..1ba34cd74dce8269c6b81c2e0e4a298afca7f180
+--- a/conductor/roadmap.md
++++ b/conductor/roadmap.md
+@@ -105,6 +105,8 @@
+ 
+ **Goal**: finish the book with the culminating experience and final coherence pass.
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+ 
+ - Capstone and Textbook Completion
+diff --git a/conductor/tech-debt.md b/conductor/tech-debt.md
+index 9bac252a4eba200df1bc57aede467e480d1d1f2a..640e1a1e5f5401852a117c3e7cf478b2cdbc1dff
+--- a/conductor/tech-debt.md
++++ b/conductor/tech-debt.md
+@@ -4,7 +4,6 @@
+ 
+ | Date | Track | Item | Severity | Status | Notes |
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+ | 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+ | 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
+diff --git a/conductor/tracks.md b/conductor/tracks.md
+index 6704b229976c3e143989c878a6d0db8b29221c6c..8adc26ec3d1fcbe497a6f50ee5d48079589ac882
+--- a/conductor/tracks.md
++++ b/conductor/tracks.md
+@@ -6,8 +6,7 @@
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
+ ## Active Track
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+-- [~] **Track: Capstone and Textbook Completion**
+-  *Link: [./tracks/capstone_textbook_completion_20260314/](./tracks/capstone_textbook_completion_20260314/)*
++- No active track. The next `/conductor` run should take the first item from the planned queue.
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-   });
+ ## Planned Queue
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
+@@ -16,6 +15,10 @@
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+ ## Archive Ledger
  
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-+  });
-+
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
++- [x] **Track: Capstone and Textbook Completion**
++  *Link: [./archive/capstone_textbook_completion_20260314/](./archive/capstone_textbook_completion_20260314/)*
++  *Closeout: archived on 2026-03-14 after replacing the generated capstone scaffold with authored runtime content, locking milestone/workbook/final-presentation coverage into manifest and UI regressions, and normalizing capstone labeling across the core public, student, and teacher textbook flow.*
++
+ - [x] **Track: Curriculum Rollout Wave 2 (Units 5-8)**
+   *Link: [./archive/curriculum_rollout_wave2_20260314/](./archive/curriculum_rollout_wave2_20260314/)*
+   *Closeout: archived on 2026-03-14 after replacing Units 5-8 generated curriculum scaffolds with canonical authored published lessons, locking Lesson 7-11 dataset/workbook/presentation contract details into manifest regressions, and updating the active Conductor curriculum status to show all eight instructional units as authored.*
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+deleted file mode 100644
+index e53f588834f06d46a36ab82106566ba908bb6cbc..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
++++ /dev/null
+@@ -1,11 +0,0 @@
+-{
+-  "track_id": "capstone_textbook_completion_20260314",
+-  "type": "feature",
+-  "status": "in_progress",
+-  "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
+-  "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+-  "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
+-}
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/plan.md b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+deleted file mode 100644
+index 48b611e5d419a88dad2df9bf2d2a717532f66631..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/plan.md
++++ /dev/null
+@@ -1,31 +0,0 @@
+-# Implementation Plan
+-
+-## Phase 1: Capstone Contract Guards
+-
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
+-
+-## Phase 2: Capstone Runtime Completion
+-
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+-
+-## Phase 3: Verification and Documentation
+-
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
+
+codex
+Both required commits are created. I’m doing a final Git check now, then I’ll push the branch with those two commits while leaving the unrelated pre-existing worktree changes untouched.
+exec
+/bin/bash -lc 'git log --oneline -2' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+a5c333a chore(conductor): Archive completed track
+7f860c4 feat(curriculum): complete capstone textbook flow (Codex GPT-5)
+exec
+/bin/bash -lc 'git status --short --branch' in /home/daniel-bo/Desktop/bus-math-v2 succeeded in 0ms:
+## main...origin/main [ahead 2]
+ M .env.example
+ M README.md
+ M __tests__/components/exercises/JournalEntryBuilding.test.tsx
+ M __tests__/components/financial-reports/CashFlowStatementDetailed.test.tsx
+ M __tests__/lib/convex/config.test.ts
+ M __tests__/proxy.test.ts
+ M __tests__/setup/dev-stack-script.test.ts
+ M conductor/codex-cron.log
+ M conductor/codex-last.txt
+ M conductor/docs/architecture/runtime.md
+ M convex/teacher.ts
+ M docs/security-api-route-matrix.md
+ M lib/activities/component-keys.ts
+ M lib/convex/admin.ts
+ M lib/db/schema/phase-content.ts
+ M lib/db/schema/validators.ts
+ M proxy.ts
+ M scripts/dev-stack.mjs
+ M types/curriculum.ts
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
-+  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
-+
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
-+  });
-+
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
-+    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
-+
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
-+  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json b/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-index 364eb86a46b01b017fb851065b622f944b094de2..c26bc753dc608b14cfc91af840979997116e6d4e
---- a/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-+++ b/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-@@ -1,9 +1,9 @@
- {
-   "track_id": "lesson_phase_objective_shell_20260311",
-   "type": "feature",
--  "status": "in_progress",
-+  "status": "archived",
-   "created_at": "2026-03-11T08:34:26Z",
--  "updated_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:44:39Z",
-   "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-   "estimated_tasks": 7,
-   "actual_tasks": 7,
-diff --git a/conductor/archive/lesson_phase_objective_shell_20260311/plan.md b/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-index 2c93ed93a25fc6373d8d1f962121f21b751f077d..504954d0bd54aa9b155a27df0bdb25d02fbe44eb
---- a/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-+++ b/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-@@ -19,4 +19,4 @@
- ## Phase 3: Verification and Closeout
- - [x] Task: Run lint and automated tests for the track scope
- - [x] Task: Update README and Conductor memory files with the phase-shell alignment outcome
--- [ ] Task: Archive the track and finalize registry/metadata updates
-+- [x] Task: Archive the track and finalize registry/metadata updates
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
 diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
-index 5a1f9d47c9c577b016380d41f806bcb496b3903e..d8b4b634818b45cfc7f4d67cb6059d6c008a677c
+index c6744e6265e1305f9e62ae2ff35179c8e2a6a46c..cfcfcc73b61799c67dee30ab713d6cfa49742d13
 --- a/conductor/lessons-learned.md
 +++ b/conductor/lessons-learned.md
-@@ -14,6 +14,7 @@
- - (2026-03-11, activity_component_contract_refactor_20260311) Activity contracts drift quickly when alias handling lives separately from the runtime registry and schema validators; documented curriculum keys need one canonical resolver.
- - (2026-03-11, published_progress_viewmodel_consolidation_20260311) Student dashboard rows, teacher student-detail rows, and lesson phase-status payloads should derive from the same published-progress helpers or published-version/locking fixes will diverge again.
- - (2026-03-11, student_lesson_access_boundary_cleanup_20260311) Student-owned API writes need an explicit request-level student guard even when teacher/admin lesson preview is intentionally allowed elsewhere; auth presence alone is not a safe mutation boundary.
-+- (2026-03-11, lesson_phase_objective_shell_20260311) Student lesson pages should derive phase guidance from curriculum lesson-type metadata and reuse the shared `PhaseRenderer`, or pedagogy copy and content rendering drift separately.
- 
- ### Recurring Gotchas
- <!-- Problems encountered repeatedly; save future tracks from the same pain -->
+@@ -23,6 +23,7 @@
+ - (2026-03-13, unit1_canonicalization_archetypes) Canonicalizing authored curriculum at the source is safer than accumulating manifest exceptions because both validation drift and role-specific rendering bugs disappear together.
+ - (2026-03-13, auth bootstrap hotfix) JWT-protected proxy rules must explicitly allow unauthenticated auth bootstrap endpoints like `/api/auth/login` and `/api/auth/session`, or the login page will redirect its own API calls back to itself.
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+ ### Patterns Worth Repeating
+ 
+@@ -36,3 +37,4 @@
+ - (2026-03-13, curriculum_rollout_wave1) Lesson-planning matrices are a workable source for authored rollout waves when they are converted into canonical lesson blueprints and validated through manifest regression tests before runtime wiring.
+ - (2026-03-13, unit1_redesign_lesson_contract) Old infrastructure tests should validate canonical phase numbers and contract outcomes rather than stale phase labels, or redesign tracks will appear broken even when the authored curriculum and published runtime agree.
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+diff --git a/conductor/roadmap.md b/conductor/roadmap.md
+index 11d3802482df109f7afa63e1f8e06a29b281e2de..1ba34cd74dce8269c6b81c2e0e4a298afca7f180
+--- a/conductor/roadmap.md
++++ b/conductor/roadmap.md
+@@ -105,6 +105,8 @@
+ 
+ **Goal**: finish the book with the culminating experience and final coherence pass.
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+ 
+ - Capstone and Textbook Completion
+diff --git a/conductor/tech-debt.md b/conductor/tech-debt.md
+index 9bac252a4eba200df1bc57aede467e480d1d1f2a..640e1a1e5f5401852a117c3e7cf478b2cdbc1dff
+--- a/conductor/tech-debt.md
++++ b/conductor/tech-debt.md
+@@ -4,7 +4,6 @@
+ 
+ | Date | Track | Item | Severity | Status | Notes |
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+ | 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+ | 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
 diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..a94965df8c673cc029927782a3d791909b4e2091
+index 6704b229976c3e143989c878a6d0db8b29221c6c..8adc26ec3d1fcbe497a6f50ee5d48079589ac882
 --- a/conductor/tracks.md
 +++ b/conductor/tracks.md
-@@ -32,6 +32,10 @@
+@@ -6,8 +6,7 @@
+ 
+ ## Active Track
+ 
+-- [~] **Track: Capstone and Textbook Completion**
+-  *Link: [./tracks/capstone_textbook_completion_20260314/](./tracks/capstone_textbook_completion_20260314/)*
++- No active track. The next `/conductor` run should take the first item from the planned queue.
+ 
+ ## Planned Queue
+ 
+@@ -16,6 +15,10 @@
  
  ## Archive Ledger
  
-+- [x] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/archive/lesson_phase_objective_shell_20260311/](./conductor/archive/lesson_phase_objective_shell_20260311/)*
-+  *Checkpoint: `f9a651e` (2026-03-11 implementation complete; archived during closeout)*
-+
- - [x] **Track: Student Write Boundary Hardening**
-   *Link: [./conductor/archive/student_lesson_access_boundary_cleanup_20260311/](./conductor/archive/student_lesson_access_boundary_cleanup_20260311/)*
-   *Checkpoint: `pending` (2026-03-11 closeout)*
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
++- [x] **Track: Capstone and Textbook Completion**
++  *Link: [./archive/capstone_textbook_completion_20260314/](./archive/capstone_textbook_completion_20260314/)*
++  *Closeout: archived on 2026-03-14 after replacing the generated capstone scaffold with authored runtime content, locking milestone/workbook/final-presentation coverage into manifest and UI regressions, and normalizing capstone labeling across the core public, student, and teacher textbook flow.*
++
+ - [x] **Track: Curriculum Rollout Wave 2 (Units 5-8)**
+   *Link: [./archive/curriculum_rollout_wave2_20260314/](./archive/curriculum_rollout_wave2_20260314/)*
+   *Closeout: archived on 2026-03-14 after replacing Units 5-8 generated curriculum scaffolds with canonical authored published lessons, locking Lesson 7-11 dataset/workbook/presentation contract details into manifest regressions, and updating the active Conductor curriculum status to show all eight instructional units as authored.*
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+deleted file mode 100644
+index e53f588834f06d46a36ab82106566ba908bb6cbc..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
++++ /dev/null
+@@ -1,11 +0,0 @@
+-{
+-  "track_id": "capstone_textbook_completion_20260314",
+-  "type": "feature",
+-  "status": "in_progress",
+-  "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
+-  "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+-  "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
+-}
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/plan.md b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+deleted file mode 100644
+index 48b611e5d419a88dad2df9bf2d2a717532f66631..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/plan.md
++++ /dev/null
+@@ -1,31 +0,0 @@
+-# Implementation Plan
+-
+-## Phase 1: Capstone Contract Guards
+-
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
+-
+-## Phase 2: Capstone Runtime Completion
+-
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+-
+-## Phase 3: Verification and Documentation
+-
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
 
 exec
-/bin/bash -lc 'git notes add -m "Track cleanup: lesson_phase_objective_shell_20260311
-Summary: Archived the completed track, marked its final closeout task complete, updated metadata status to archived, and removed the active queue entry while adding the archive ledger reference.
-Files: conductor/tracks.md, conductor/archive/lesson_phase_objective_shell_20260311/{index.md,metadata.json,plan.md,spec.md}
-Why: The user requested end-to-end Conductor finalization, including archival and registry cleanup after implementation verification." 3c4a121' in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
-
-file update:
-diff --git a/README.md b/README.md
-index 9ee7fe58cfca200160c7dcd63527376b6a6e3202..d15e181d7fdd88c0d5393d28b496fdbcdc953762
---- a/README.md
-+++ b/README.md
-@@ -17,6 +17,7 @@
- - **Shared Dashboard Recommendation Cards**: Student and teacher progress surfaces now reuse one next-lesson card and shared unit-status presentation helpers to keep resume/start guidance aligned
- - **Shared Published Progress View-Models**: Student dashboard units, teacher student-detail progress rows, and lesson phase-status responses now derive from the same published-progress helpers so progress math and locking rules stay aligned
- - **Canonical Activity Component Catalog**: Activity rendering and validation now resolve documented aliases such as `spreadsheet-activity` and `journal-entry-activity` through one canonical runtime contract, reducing curriculum authoring drift
-+- **Objective-Aware Lesson Phase Shell**: Student lesson phases now show curriculum-aligned phase guidance and success criteria while reusing the shared phase content renderer
- - **Deterministic Simulation Runtime IDs**: Inventory simulation notifications and market events now keep stable unique ids during rapid updates, preventing duplicate-key rendering regressions
- - **Account Settings & Self-Service Password Changes**: Authenticated users can review account context and update their own password without leaving the session
- - **Multi-tenant Architecture**: Organization-based access control
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
-+
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+/bin/bash -lc 'git push origin main' in /home/daniel-bo/Desktop/bus-math-v2file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
+index c6744e6265e1305f9e62ae2ff35179c8e2a6a46c..cfcfcc73b61799c67dee30ab713d6cfa49742d13
+--- a/conductor/lessons-learned.md
++++ b/conductor/lessons-learned.md
+@@ -23,6 +23,7 @@
+ - (2026-03-13, unit1_canonicalization_archetypes) Canonicalizing authored curriculum at the source is safer than accumulating manifest exceptions because both validation drift and role-specific rendering bugs disappear together.
+ - (2026-03-13, auth bootstrap hotfix) JWT-protected proxy rules must explicitly allow unauthenticated auth bootstrap endpoints like `/api/auth/login` and `/api/auth/session`, or the login page will redirect its own API calls back to itself.
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+ ### Patterns Worth Repeating
+ 
+@@ -36,3 +37,4 @@
+ - (2026-03-13, curriculum_rollout_wave1) Lesson-planning matrices are a workable source for authored rollout waves when they are converted into canonical lesson blueprints and validated through manifest regression tests before runtime wiring.
+ - (2026-03-13, unit1_redesign_lesson_contract) Old infrastructure tests should validate canonical phase numbers and contract outcomes rather than stale phase labels, or redesign tracks will appear broken even when the authored curriculum and published runtime agree.
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+diff --git a/conductor/roadmap.md b/conductor/roadmap.md
+index 11d3802482df109f7afa63e1f8e06a29b281e2de..1ba34cd74dce8269c6b81c2e0e4a298afca7f180
+--- a/conductor/roadmap.md
++++ b/conductor/roadmap.md
+@@ -105,6 +105,8 @@
+ 
+ **Goal**: finish the book with the culminating experience and final coherence pass.
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+ 
+ - Capstone and Textbook Completion
+diff --git a/conductor/tech-debt.md b/conductor/tech-debt.md
+index 9bac252a4eba200df1bc57aede467e480d1d1f2a..640e1a1e5f5401852a117c3e7cf478b2cdbc1dff
+--- a/conductor/tech-debt.md
++++ b/conductor/tech-debt.md
+@@ -4,7 +4,6 @@
+ 
+ | Date | Track | Item | Severity | Status | Notes |
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+ | 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+ | 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
+diff --git a/conductor/tracks.md b/conductor/tracks.md
+index 6704b229976c3e143989c878a6d0db8b29221c6c..8adc26ec3d1fcbe497a6f50ee5d48079589ac882
+--- a/conductor/tracks.md
++++ b/conductor/tracks.md
+@@ -6,8 +6,7 @@
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
+ ## Active Track
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+-- [~] **Track: Capstone and Textbook Completion**
+-  *Link: [./tracks/capstone_textbook_completion_20260314/](./tracks/capstone_textbook_completion_20260314/)*
++- No active track. The next `/conductor` run should take the first item from the planned queue.
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-   });
+ ## Planned Queue
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
+@@ -16,6 +15,10 @@
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+ ## Archive Ledger
  
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-+  });
-+
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
++- [x] **Track: Capstone and Textbook Completion**
++  *Link: [./archive/capstone_textbook_completion_20260314/](./archive/capstone_textbook_completion_20260314/)*
++  *Closeout: archived on 2026-03-14 after replacing the generated capstone scaffold with authored runtime content, locking milestone/workbook/final-presentation coverage into manifest and UI regressions, and normalizing capstone labeling across the core public, student, and teacher textbook flow.*
++
+ - [x] **Track: Curriculum Rollout Wave 2 (Units 5-8)**
+   *Link: [./archive/curriculum_rollout_wave2_20260314/](./archive/curriculum_rollout_wave2_20260314/)*
+   *Closeout: archived on 2026-03-14 after replacing Units 5-8 generated curriculum scaffolds with canonical authored published lessons, locking Lesson 7-11 dataset/workbook/presentation contract details into manifest regressions, and updating the active Conductor curriculum status to show all eight instructional units as authored.*
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+deleted file mode 100644
+index e53f588834f06d46a36ab82106566ba908bb6cbc..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
++++ /dev/null
+@@ -1,11 +0,0 @@
+-{
+-  "track_id": "capstone_textbook_completion_20260314",
+-  "type": "feature",
+-  "status": "in_progress",
+-  "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
+-  "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+-  "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
+-}
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/plan.md b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+deleted file mode 100644
+index 48b611e5d419a88dad2df9bf2d2a717532f66631..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/plan.md
++++ /dev/null
+@@ -1,31 +0,0 @@
+-# Implementation Plan
+-
+-## Phase 1: Capstone Contract Guards
+-
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
+-
+-## Phase 2: Capstone Runtime Completion
+-
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+-
+-## Phase 3: Verification and Documentation
+-
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
+
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
-+  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
-+
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
-+  });
-+
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
-+    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
-+
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
-+  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json b/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-index 364eb86a46b01b017fb851065b622f944b094de2..c26bc753dc608b14cfc91af840979997116e6d4e
---- a/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-+++ b/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-@@ -1,9 +1,9 @@
- {
-   "track_id": "lesson_phase_objective_shell_20260311",
-   "type": "feature",
--  "status": "in_progress",
-+  "status": "archived",
-   "created_at": "2026-03-11T08:34:26Z",
--  "updated_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:44:39Z",
-   "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-   "estimated_tasks": 7,
-   "actual_tasks": 7,
-diff --git a/conductor/archive/lesson_phase_objective_shell_20260311/plan.md b/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-index 2c93ed93a25fc6373d8d1f962121f21b751f077d..504954d0bd54aa9b155a27df0bdb25d02fbe44eb
---- a/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-+++ b/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-@@ -19,4 +19,4 @@
- ## Phase 3: Verification and Closeout
- - [x] Task: Run lint and automated tests for the track scope
- - [x] Task: Update README and Conductor memory files with the phase-shell alignment outcome
--- [ ] Task: Archive the track and finalize registry/metadata updates
-+- [x] Task: Archive the track and finalize registry/metadata updates
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
 diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
-index 5a1f9d47c9c577b016380d41f806bcb496b3903e..d8b4b634818b45cfc7f4d67cb6059d6c008a677c
+index c6744e6265e1305f9e62ae2ff35179c8e2a6a46c..cfcfcc73b61799c67dee30ab713d6cfa49742d13
 --- a/conductor/lessons-learned.md
 +++ b/conductor/lessons-learned.md
-@@ -14,6 +14,7 @@
- - (2026-03-11, activity_component_contract_refactor_20260311) Activity contracts drift quickly when alias handling lives separately from the runtime registry and schema validators; documented curriculum keys need one canonical resolver.
- - (2026-03-11, published_progress_viewmodel_consolidation_20260311) Student dashboard rows, teacher student-detail rows, and lesson phase-status payloads should derive from the same published-progress helpers or published-version/locking fixes will diverge again.
- - (2026-03-11, student_lesson_access_boundary_cleanup_20260311) Student-owned API writes need an explicit request-level student guard even when teacher/admin lesson preview is intentionally allowed elsewhere; auth presence alone is not a safe mutation boundary.
-+- (2026-03-11, lesson_phase_objective_shell_20260311) Student lesson pages should derive phase guidance from curriculum lesson-type metadata and reuse the shared `PhaseRenderer`, or pedagogy copy and content rendering drift separately.
- 
- ### Recurring Gotchas
- <!-- Problems encountered repeatedly; save future tracks from the same pain -->
+@@ -23,6 +23,7 @@
+ - (2026-03-13, unit1_canonicalization_archetypes) Canonicalizing authored curriculum at the source is safer than accumulating manifest exceptions because both validation drift and role-specific rendering bugs disappear together.
+ - (2026-03-13, auth bootstrap hotfix) JWT-protected proxy rules must explicitly allow unauthenticated auth bootstrap endpoints like `/api/auth/login` and `/api/auth/session`, or the login page will redirect its own API calls back to itself.
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+ ### Patterns Worth Repeating
+ 
+@@ -36,3 +37,4 @@
+ - (2026-03-13, curriculum_rollout_wave1) Lesson-planning matrices are a workable source for authored rollout waves when they are converted into canonical lesson blueprints and validated through manifest regression tests before runtime wiring.
+ - (2026-03-13, unit1_redesign_lesson_contract) Old infrastructure tests should validate canonical phase numbers and contract outcomes rather than stale phase labels, or redesign tracks will appear broken even when the authored curriculum and published runtime agree.
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+diff --git a/conductor/roadmap.md b/conductor/roadmap.md
+index 11d3802482df109f7afa63e1f8e06a29b281e2de..1ba34cd74dce8269c6b81c2e0e4a298afca7f180
+--- a/conductor/roadmap.md
++++ b/conductor/roadmap.md
+@@ -105,6 +105,8 @@
+ 
+ **Goal**: finish the book with the culminating experience and final coherence pass.
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+ 
+ - Capstone and Textbook Completion
+diff --git a/conductor/tech-debt.md b/conductor/tech-debt.md
+index 9bac252a4eba200df1bc57aede467e480d1d1f2a..640e1a1e5f5401852a117c3e7cf478b2cdbc1dff
+--- a/conductor/tech-debt.md
++++ b/conductor/tech-debt.md
+@@ -4,7 +4,6 @@
+ 
+ | Date | Track | Item | Severity | Status | Notes |
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+ | 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+ | 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
 diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..a94965df8c673cc029927782a3d791909b4e2091
+index 6704b229976c3e143989c878a6d0db8b29221c6c..8adc26ec3d1fcbe497a6f50ee5d48079589ac882
 --- a/conductor/tracks.md
 +++ b/conductor/tracks.md
-@@ -32,6 +32,10 @@
+@@ -6,8 +6,7 @@
+ 
+ ## Active Track
+ 
+-- [~] **Track: Capstone and Textbook Completion**
+-  *Link: [./tracks/capstone_textbook_completion_20260314/](./tracks/capstone_textbook_completion_20260314/)*
++- No active track. The next `/conductor` run should take the first item from the planned queue.
+ 
+ ## Planned Queue
+ 
+@@ -16,6 +15,10 @@
  
  ## Archive Ledger
  
-+- [x] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/archive/lesson_phase_objective_shell_20260311/](./conductor/archive/lesson_phase_objective_shell_20260311/)*
-+  *Checkpoint: `f9a651e` (2026-03-11 implementation complete; archived during closeout)*
-+
- - [x] **Track: Student Write Boundary Hardening**
-   *Link: [./conductor/archive/student_lesson_access_boundary_cleanup_20260311/](./conductor/archive/student_lesson_access_boundary_cleanup_20260311/)*
-   *Checkpoint: `pending` (2026-03-11 closeout)*
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
++- [x] **Track: Capstone and Textbook Completion**
++  *Link: [./archive/capstone_textbook_completion_20260314/](./archive/capstone_textbook_completion_20260314/)*
++  *Closeout: archived on 2026-03-14 after replacing the generated capstone scaffold with authored runtime content, locking milestone/workbook/final-presentation coverage into manifest and UI regressions, and normalizing capstone labeling across the core public, student, and teacher textbook flow.*
++
+ - [x] **Track: Curriculum Rollout Wave 2 (Units 5-8)**
+   *Link: [./archive/curriculum_rollout_wave2_20260314/](./archive/curriculum_rollout_wave2_20260314/)*
+   *Closeout: archived on 2026-03-14 after replacing Units 5-8 generated curriculum scaffolds with canonical authored published lessons, locking Lesson 7-11 dataset/workbook/presentation contract details into manifest regressions, and updating the active Conductor curriculum status to show all eight instructional units as authored.*
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+deleted file mode 100644
+index e53f588834f06d46a36ab82106566ba908bb6cbc..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
++++ /dev/null
+@@ -1,11 +0,0 @@
+-{
+-  "track_id": "capstone_textbook_completion_20260314",
+-  "type": "feature",
+-  "status": "in_progress",
+-  "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
+-  "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+-  "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
+-}
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/plan.md b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+deleted file mode 100644
+index 48b611e5d419a88dad2df9bf2d2a717532f66631..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/plan.md
++++ /dev/null
+@@ -1,31 +0,0 @@
+-# Implementation Plan
+-
+-## Phase 1: Capstone Contract Guards
+-
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
+-
+-## Phase 2: Capstone Runtime Completion
+-
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+-
+-## Phase 3: Verification and Documentation
+-
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
 
-exec
-/bin/bash -lc 'git status --short --untracked-files=all' in /home/daniel-bo/Desktop/bus-math-v2-experimental succeeded in 51ms:
- M conductor/autonomous_prompt.md
- M conductor/codex-cron.log
- M conductor/codex-last.txt
- M conductor/workflow.md
-?? conductor/current_directive.md
+ succeeded in 13.88s:
+To https://github.com/bodangren/bus-math-v2.git
+   e2dbc4a..a5c333a  main -> main
 file update:
-diff --git a/README.md b/README.md
-index 9ee7fe58cfca200160c7dcd63527376b6a6e3202..d15e181d7fdd88c0d5393d28b496fdbcdc953762
---- a/README.md
-+++ b/README.md
-@@ -17,6 +17,7 @@
- - **Shared Dashboard Recommendation Cards**: Student and teacher progress surfaces now reuse one next-lesson card and shared unit-status presentation helpers to keep resume/start guidance aligned
- - **Shared Published Progress View-Models**: Student dashboard units, teacher student-detail progress rows, and lesson phase-status responses now derive from the same published-progress helpers so progress math and locking rules stay aligned
- - **Canonical Activity Component Catalog**: Activity rendering and validation now resolve documented aliases such as `spreadsheet-activity` and `journal-entry-activity` through one canonical runtime contract, reducing curriculum authoring drift
-+- **Objective-Aware Lesson Phase Shell**: Student lesson phases now show curriculum-aligned phase guidance and success criteria while reusing the shared phase content renderer
- - **Deterministic Simulation Runtime IDs**: Inventory simulation notifications and market events now keep stable unique ids during rapid updates, preventing duplicate-key rendering regressions
- - **Account Settings & Self-Service Password Changes**: Authenticated users can review account context and update their own password without leaving the session
- - **Multi-tenant Architecture**: Organization-based access control
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
-+
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
    });
  
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
++
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
  
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone label for unit 9 lessons', () => {
++    render(
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++        lessonNumber={1}
++      />,
 +    );
++
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
++  });
++
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
    });
  
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
 +    );
-   });
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
+index c6744e6265e1305f9e62ae2ff35179c8e2a6a46c..cfcfcc73b61799c67dee30ab713d6cfa49742d13
+--- a/conductor/lessons-learned.md
++++ b/conductor/lessons-learned.md
+@@ -23,6 +23,7 @@
+ - (2026-03-13, unit1_canonicalization_archetypes) Canonicalizing authored curriculum at the source is safer than accumulating manifest exceptions because both validation drift and role-specific rendering bugs disappear together.
+ - (2026-03-13, auth bootstrap hotfix) JWT-protected proxy rules must explicitly allow unauthenticated auth bootstrap endpoints like `/api/auth/login` and `/api/auth/session`, or the login page will redirect its own API calls back to itself.
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+ ### Patterns Worth Repeating
+ 
+@@ -36,3 +37,4 @@
+ - (2026-03-13, curriculum_rollout_wave1) Lesson-planning matrices are a workable source for authored rollout waves when they are converted into canonical lesson blueprints and validated through manifest regression tests before runtime wiring.
+ - (2026-03-13, unit1_redesign_lesson_contract) Old infrastructure tests should validate canonical phase numbers and contract outcomes rather than stale phase labels, or redesign tracks will appear broken even when the authored curriculum and published runtime agree.
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+diff --git a/conductor/roadmap.md b/conductor/roadmap.md
+index 11d3802482df109f7afa63e1f8e06a29b281e2de..1ba34cd74dce8269c6b81c2e0e4a298afca7f180
+--- a/conductor/roadmap.md
++++ b/conductor/roadmap.md
+@@ -105,6 +105,8 @@
+ 
+ **Goal**: finish the book with the culminating experience and final coherence pass.
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+ 
+ - Capstone and Textbook Completion
+diff --git a/conductor/tech-debt.md b/conductor/tech-debt.md
+index 9bac252a4eba200df1bc57aede467e480d1d1f2a..640e1a1e5f5401852a117c3e7cf478b2cdbc1dff
+--- a/conductor/tech-debt.md
++++ b/conductor/tech-debt.md
+@@ -4,7 +4,6 @@
+ 
+ | Date | Track | Item | Severity | Status | Notes |
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+ | 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+ | 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
+diff --git a/conductor/tracks.md b/conductor/tracks.md
+index 6704b229976c3e143989c878a6d0db8b29221c6c..8adc26ec3d1fcbe497a6f50ee5d48079589ac882
+--- a/conductor/tracks.md
++++ b/conductor/tracks.md
+@@ -6,8 +6,7 @@
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
+ ## Active Track
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+-- [~] **Track: Capstone and Textbook Completion**
+-  *Link: [./tracks/capstone_textbook_completion_20260314/](./tracks/capstone_textbook_completion_20260314/)*
++- No active track. The next `/conductor` run should take the first item from the planned queue.
  
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-   });
+ ## Planned Queue
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
+@@ -16,6 +15,10 @@
  
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+ ## Archive Ledger
  
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-+  });
-+
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
++- [x] **Track: Capstone and Textbook Completion**
++  *Link: [./archive/capstone_textbook_completion_20260314/](./archive/capstone_textbook_completion_20260314/)*
++  *Closeout: archived on 2026-03-14 after replacing the generated capstone scaffold with authored runtime content, locking milestone/workbook/final-presentation coverage into manifest and UI regressions, and normalizing capstone labeling across the core public, student, and teacher textbook flow.*
++
+ - [x] **Track: Curriculum Rollout Wave 2 (Units 5-8)**
+   *Link: [./archive/curriculum_rollout_wave2_20260314/](./archive/curriculum_rollout_wave2_20260314/)*
+   *Closeout: archived on 2026-03-14 after replacing Units 5-8 generated curriculum scaffolds with canonical authored published lessons, locking Lesson 7-11 dataset/workbook/presentation contract details into manifest regressions, and updating the active Conductor curriculum status to show all eight instructional units as authored.*
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+deleted file mode 100644
+index e53f588834f06d46a36ab82106566ba908bb6cbc..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
++++ /dev/null
+@@ -1,11 +0,0 @@
+-{
+-  "track_id": "capstone_textbook_completion_20260314",
+-  "type": "feature",
+-  "status": "in_progress",
+-  "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
+-  "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+-  "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
+-}
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/plan.md b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+deleted file mode 100644
+index 48b611e5d419a88dad2df9bf2d2a717532f66631..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/plan.md
++++ /dev/null
+@@ -1,31 +0,0 @@
+-# Implementation Plan
+-
+-## Phase 1: Capstone Contract Guards
+-
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
+-
+-## Phase 2: Capstone Runtime Completion
+-
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+-
+-## Phase 3: Verification and Documentation
+-
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
+
+Plan update
+Capstone track implementation, full verification, Conductor finalization, and push are complete.
+  ✓ Inspect active track diffs and determine remaining implementation gaps against spec and plan
+  ✓ Complete any missing code/tests/docs for capstone authored content and labeling
+  ✓ Run lint, full test suite, and production build; fix regressions until green
+  ✓ Finalize Conductor artifacts, archive the completed track, and commit/push the changes
+file update:
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
+ 
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
      );
+   });
+ 
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
++
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
 +
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
-+  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
-+
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
-+  });
-+
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
-+    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
-+
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
-+  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json b/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-index 364eb86a46b01b017fb851065b622f944b094de2..c26bc753dc608b14cfc91af840979997116e6d4e
---- a/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-+++ b/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-@@ -1,9 +1,9 @@
- {
-   "track_id": "lesson_phase_objective_shell_20260311",
-   "type": "feature",
--  "status": "in_progress",
-+  "status": "archived",
-   "created_at": "2026-03-11T08:34:26Z",
--  "updated_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:44:39Z",
-   "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-   "estimated_tasks": 7,
-   "actual_tasks": 7,
-diff --git a/conductor/archive/lesson_phase_objective_shell_20260311/plan.md b/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-index 2c93ed93a25fc6373d8d1f962121f21b751f077d..504954d0bd54aa9b155a27df0bdb25d02fbe44eb
---- a/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-+++ b/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-@@ -19,4 +19,4 @@
- ## Phase 3: Verification and Closeout
- - [x] Task: Run lint and automated tests for the track scope
- - [x] Task: Update README and Conductor memory files with the phase-shell alignment outcome
--- [ ] Task: Archive the track and finalize registry/metadata updates
-+- [x] Task: Archive the track and finalize registry/metadata updates
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
 diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
-index 5a1f9d47c9c577b016380d41f806bcb496b3903e..d8b4b634818b45cfc7f4d67cb6059d6c008a677c
+index c6744e6265e1305f9e62ae2ff35179c8e2a6a46c..cfcfcc73b61799c67dee30ab713d6cfa49742d13
 --- a/conductor/lessons-learned.md
 +++ b/conductor/lessons-learned.md
-@@ -14,6 +14,7 @@
- - (2026-03-11, activity_component_contract_refactor_20260311) Activity contracts drift quickly when alias handling lives separately from the runtime registry and schema validators; documented curriculum keys need one canonical resolver.
- - (2026-03-11, published_progress_viewmodel_consolidation_20260311) Student dashboard rows, teacher student-detail rows, and lesson phase-status payloads should derive from the same published-progress helpers or published-version/locking fixes will diverge again.
- - (2026-03-11, student_lesson_access_boundary_cleanup_20260311) Student-owned API writes need an explicit request-level student guard even when teacher/admin lesson preview is intentionally allowed elsewhere; auth presence alone is not a safe mutation boundary.
-+- (2026-03-11, lesson_phase_objective_shell_20260311) Student lesson pages should derive phase guidance from curriculum lesson-type metadata and reuse the shared `PhaseRenderer`, or pedagogy copy and content rendering drift separately.
- 
- ### Recurring Gotchas
- <!-- Problems encountered repeatedly; save future tracks from the same pain -->
+@@ -23,6 +23,7 @@
+ - (2026-03-13, unit1_canonicalization_archetypes) Canonicalizing authored curriculum at the source is safer than accumulating manifest exceptions because both validation drift and role-specific rendering bugs disappear together.
+ - (2026-03-13, auth bootstrap hotfix) JWT-protected proxy rules must explicitly allow unauthenticated auth bootstrap endpoints like `/api/auth/login` and `/api/auth/session`, or the login page will redirect its own API calls back to itself.
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+ ### Patterns Worth Repeating
+ 
+@@ -36,3 +37,4 @@
+ - (2026-03-13, curriculum_rollout_wave1) Lesson-planning matrices are a workable source for authored rollout waves when they are converted into canonical lesson blueprints and validated through manifest regression tests before runtime wiring.
+ - (2026-03-13, unit1_redesign_lesson_contract) Old infrastructure tests should validate canonical phase numbers and contract outcomes rather than stale phase labels, or redesign tracks will appear broken even when the authored curriculum and published runtime agree.
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+diff --git a/conductor/roadmap.md b/conductor/roadmap.md
+index 11d3802482df109f7afa63e1f8e06a29b281e2de..1ba34cd74dce8269c6b81c2e0e4a298afca7f180
+--- a/conductor/roadmap.md
++++ b/conductor/roadmap.md
+@@ -105,6 +105,8 @@
+ 
+ **Goal**: finish the book with the culminating experience and final coherence pass.
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+ 
+ - Capstone and Textbook Completion
+diff --git a/conductor/tech-debt.md b/conductor/tech-debt.md
+index 9bac252a4eba200df1bc57aede467e480d1d1f2a..640e1a1e5f5401852a117c3e7cf478b2cdbc1dff
+--- a/conductor/tech-debt.md
++++ b/conductor/tech-debt.md
+@@ -4,7 +4,6 @@
+ 
+ | Date | Track | Item | Severity | Status | Notes |
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+ | 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+ | 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
 diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..a94965df8c673cc029927782a3d791909b4e2091
+index 6704b229976c3e143989c878a6d0db8b29221c6c..8adc26ec3d1fcbe497a6f50ee5d48079589ac882
 --- a/conductor/tracks.md
 +++ b/conductor/tracks.md
-@@ -32,6 +32,10 @@
+@@ -6,8 +6,7 @@
+ 
+ ## Active Track
+ 
+-- [~] **Track: Capstone and Textbook Completion**
+-  *Link: [./tracks/capstone_textbook_completion_20260314/](./tracks/capstone_textbook_completion_20260314/)*
++- No active track. The next `/conductor` run should take the first item from the planned queue.
+ 
+ ## Planned Queue
+ 
+@@ -16,6 +15,10 @@
  
  ## Archive Ledger
  
-+- [x] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/archive/lesson_phase_objective_shell_20260311/](./conductor/archive/lesson_phase_objective_shell_20260311/)*
-+  *Checkpoint: `f9a651e` (2026-03-11 implementation complete; archived during closeout)*
-+
- - [x] **Track: Student Write Boundary Hardening**
-   *Link: [./conductor/archive/student_lesson_access_boundary_cleanup_20260311/](./conductor/archive/student_lesson_access_boundary_cleanup_20260311/)*
-   *Checkpoint: `pending` (2026-03-11 closeout)*
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
++- [x] **Track: Capstone and Textbook Completion**
++  *Link: [./archive/capstone_textbook_completion_20260314/](./archive/capstone_textbook_completion_20260314/)*
++  *Closeout: archived on 2026-03-14 after replacing the generated capstone scaffold with authored runtime content, locking milestone/workbook/final-presentation coverage into manifest and UI regressions, and normalizing capstone labeling across the core public, student, and teacher textbook flow.*
++
+ - [x] **Track: Curriculum Rollout Wave 2 (Units 5-8)**
+   *Link: [./archive/curriculum_rollout_wave2_20260314/](./archive/curriculum_rollout_wave2_20260314/)*
+   *Closeout: archived on 2026-03-14 after replacing Units 5-8 generated curriculum scaffolds with canonical authored published lessons, locking Lesson 7-11 dataset/workbook/presentation contract details into manifest regressions, and updating the active Conductor curriculum status to show all eight instructional units as authored.*
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+deleted file mode 100644
+index e53f588834f06d46a36ab82106566ba908bb6cbc..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
++++ /dev/null
+@@ -1,11 +0,0 @@
+-{
+-  "track_id": "capstone_textbook_completion_20260314",
+-  "type": "feature",
+-  "status": "in_progress",
+-  "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
+-  "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+-  "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
+-}
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/plan.md b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+deleted file mode 100644
+index 48b611e5d419a88dad2df9bf2d2a717532f66631..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/plan.md
++++ /dev/null
+@@ -1,31 +0,0 @@
+-# Implementation Plan
+-
+-## Phase 1: Capstone Contract Guards
+-
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
+-
+-## Phase 2: Capstone Runtime Completion
+-
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+-
+-## Phase 3: Verification and Documentation
+-
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
 
 codex
-Implemented one Conductor track aligned to the current directive and archived it end-to-end.
+Implemented and finalized the active Conductor track. The capstone now publishes from authored runtime content in [published-manifest.ts](/home/daniel-bo/Desktop/bus-math-v2/lib/curriculum/published-manifest.ts) backed by the new authored blueprint in [capstone-authored.ts](/home/daniel-bo/Desktop/bus-math-v2/lib/curriculum/generated/capstone-authored.ts), and the main public, student, and teacher surfaces now use distinct capstone labeling instead of leaking `Unit 9`. I also extended the label normalization into shared teacher planning and student header components so the regression coverage is broader than the minimum route set.
 
-The student lesson phase shell now shows curriculum-aligned phase guidance and success criteria via [PhaseGuidanceCard.tsx](/home/daniel-bo/Desktop/bus-math-v2-experimental/components/student/PhaseGuidanceCard.tsx) and [phase-guidance.ts](/home/daniel-bo/Desktop/bus-math-v2-experimental/lib/curriculum/phase-guidance.ts). [LessonRenderer.tsx](/home/daniel-bo/Desktop/bus-math-v2-experimental/components/student/LessonRenderer.tsx) now uses the shared [PhaseRenderer.tsx](/home/daniel-bo/Desktop/bus-math-v2-experimental/components/lesson/PhaseRenderer.tsx) path instead of its own inline block renderer, while still preserving activity-status refresh behavior.
+Conductor is updated and archived: the track plan/metadata were completed, project memory was refreshed, the roadmap now records Milestone 5 as complete, and the archived track lives at [capstone_textbook_completion_20260314](/home/daniel-bo/Desktop/bus-math-v2/conductor/archive/capstone_textbook_completion_20260314). [tracks.md](/home/daniel-bo/Desktop/bus-math-v2/conductor/tracks.md) now has no active track and points to the next planned queue item.
 
-Verification passed with `CI=true npm run lint` and `CI=true npm test` (`210` files, `1141` tests). I also added regression coverage for the new guidance model and student shell behavior. The track was committed and archived in two non-interactive commits: `f9a651e` and `3c4a121`. The archived track lives at [lesson_phase_objective_shell_20260311](/home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/archive/lesson_phase_objective_shell_20260311).
+Verification passed:
+- `npm run lint`
+- `npm test` with 215 test files / 1193 tests passing
+- `npm run build`
 
-Unrelated pre-existing working tree changes remain in `conductor/autonomous_prompt.md`, `conductor/codex-cron.log`, `conductor/codex-last.txt`, `conductor/workflow.md`, and untracked `conductor/current_directive.md`; I left them untouched.
+Commits pushed to `origin/main`:
+- `7f860c4` `feat(curriculum): complete capstone textbook flow (Codex GPT-5)`
+- `a5c333a` `chore(conductor): Archive completed track`
+
+The worktree still has unrelated pre-existing unstaged changes outside this track. I left those untouched and out of the two pushed commits.
 file update:
-diff --git a/README.md b/README.md
-index 9ee7fe58cfca200160c7dcd63527376b6a6e3202..d15e181d7fdd88c0d5393d28b496fdbcdc953762
---- a/README.md
-+++ b/README.md
-@@ -17,6 +17,7 @@
- - **Shared Dashboard Recommendation Cards**: Student and teacher progress surfaces now reuse one next-lesson card and shared unit-status presentation helpers to keep resume/start guidance aligned
- - **Shared Published Progress View-Models**: Student dashboard units, teacher student-detail progress rows, and lesson phase-status responses now derive from the same published-progress helpers so progress math and locking rules stay aligned
- - **Canonical Activity Component Catalog**: Activity rendering and validation now resolve documented aliases such as `spreadsheet-activity` and `journal-entry-activity` through one canonical runtime contract, reducing curriculum authoring drift
-+- **Objective-Aware Lesson Phase Shell**: Student lesson phases now show curriculum-aligned phase guidance and success criteria while reusing the shared phase content renderer
- - **Deterministic Simulation Runtime IDs**: Inventory simulation notifications and market events now keep stable unique ids during rapid updates, preventing duplicate-key rendering regressions
- - **Account Settings & Self-Service Password Changes**: Authenticated users can review account context and update their own password without leaving the session
- - **Multi-tenant Architecture**: Organization-based access control
-diff --git a/__tests__/components/student/LessonRenderer.test.tsx b/__tests__/components/student/LessonRenderer.test.tsx
-index b59e19d2cf01f704196f4e0870643e81732e868e..52fea6a13aec14dd08aa3b065096bcbdb8690ac2
---- a/__tests__/components/student/LessonRenderer.test.tsx
-+++ b/__tests__/components/student/LessonRenderer.test.tsx
-@@ -45,6 +45,22 @@
-   ),
- }));
- 
-+vi.mock('@/components/lesson/PhaseRenderer', () => ({
-+  PhaseRenderer: ({
-+    contentBlocks,
-+    lessonId,
-+    phaseNumber,
-+  }: {
-+    contentBlocks: ContentBlock[];
-+    lessonId: string;
-+    phaseNumber: number;
-+  }) => (
-+    <div data-testid="phase-renderer">
-+      Shared renderer for {lessonId} phase {phaseNumber} with {contentBlocks.length} blocks
-+    </div>
-+  ),
-+}));
-+
- describe('LessonRenderer', () => {
-   const mockLesson = {
-     id: '123',
-@@ -100,9 +116,9 @@
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
-     expect(screen.getByText('Learning Objectives')).toBeInTheDocument();
--    expect(screen.getByText('Understand balance sheets')).toBeInTheDocument();
--    expect(screen.getByText('Read income statements')).toBeInTheDocument();
--    expect(screen.getByText('Analyze cash flow statements')).toBeInTheDocument();
-+    expect(screen.getAllByText('Understand balance sheets').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Read income statements').length).toBeGreaterThan(0);
-+    expect(screen.getAllByText('Analyze cash flow statements').length).toBeGreaterThan(0);
-   });
- 
-   it('renders empty state when no phases', () => {
-@@ -159,7 +175,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('This is markdown content')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-   });
- 
-   it('renders callout content blocks', () => {
-@@ -183,8 +201,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('why this matters')).toBeInTheDocument();
--    expect(screen.getByText('This is important information')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-   });
- 
-   it('renders video content blocks', () => {
-@@ -210,8 +229,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
+diff --git a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+index 8562c54481bb9ded71574b2a309f9cb59f97838e..8a53bbf6572dbb7e1bb5361e9acb142b151c11f8
+--- a/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
++++ b/__tests__/app/teacher/units/[unitNumber]/page.test.tsx
+@@ -1,4 +1,5 @@
+ import { beforeEach, describe, expect, it, vi } from 'vitest';
++import { render, screen } from '@testing-library/react';
+ import { notFound, redirect } from 'next/navigation';
  
--    expect(screen.getByText('Video')).toBeInTheDocument();
--    expect(screen.getByText('https://example.com/video.mp4')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
+ const { mockRequireTeacherSessionClaims, mockFetchInternalQuery } = vi.hoisted(() => ({
+@@ -73,6 +74,18 @@
+     );
    });
  
-   it('renders image content blocks', () => {
-@@ -237,8 +257,9 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByText('Image')).toBeInTheDocument();
--    expect(screen.getByText('Example diagram')).toBeInTheDocument();
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
++  it('labels unit 9 as the capstone on the gradebook page', async () => {
++    const page = await UnitGradebookPage({
++      params: Promise.resolve({ unitNumber: '9' }),
++    });
++
++    render(page);
++
++    expect(screen.getByRole('heading', { name: 'Capstone — Gradebook' })).toBeInTheDocument();
++    expect(screen.getByRole('region', { name: 'Capstone gradebook' })).toBeInTheDocument();
++    expect(screen.queryByRole('heading', { name: 'Unit 9 — Gradebook' })).not.toBeInTheDocument();
++  });
++
+   it('redirects unauthenticated users to login', async () => {
+     mockRequireTeacherSessionClaims.mockRejectedValue(
+       new Error('NEXT_REDIRECT:/auth/login?redirect=/teacher/units/2'),
+diff --git a/__tests__/components/student/PhaseHeader.test.tsx b/__tests__/components/student/PhaseHeader.test.tsx
+index 40b1918d201fd18965dc3019bff89df1931e8c13..548e672719c0389304610660373632db50ca928f
+--- a/__tests__/components/student/PhaseHeader.test.tsx
++++ b/__tests__/components/student/PhaseHeader.test.tsx
+@@ -27,11 +27,27 @@
+     render(<PhaseHeader lesson={lesson} phase={phases[1]} phases={phases} />);
+ 
+     expect(screen.getByRole('link', { name: /^Student$/ })).toBeInTheDocument();
+-    expect(screen.getByText(/Unit 03 • Lesson 02/)).toBeInTheDocument();
++    expect(
++      screen.getByText((_, node) => node?.textContent === 'Unit 3 • Lesson 02'),
++    ).toBeInTheDocument();
+     expect(screen.getByRole('link', { name: /Lesson 2/ })).toBeInTheDocument();
+     expect(screen.getByText(/Ledger Launch Lab/)).toBeInTheDocument();
    });
  
-   it('renders activity content blocks', () => {
-@@ -262,9 +283,41 @@
- 
-     render(<LessonRenderer lesson={mockLesson} phases={mockPhases} {...defaultProps} />);
- 
--    expect(screen.getByTestId('activity-renderer')).toHaveTextContent(
--      /activity renderer for activity-123 \(required\)/i,
-+    expect(screen.getByTestId('phase-renderer')).toHaveTextContent(
-+      'Shared renderer for intro-to-financial-statements phase 1 with 1 blocks',
-+    );
-+  });
++  it('uses the capstone label for unit 9 lessons', () => {
++    const phases = buildPhases();
++    const capstoneLesson: Lesson = createLesson({
++      title: 'Capstone: Investor-Ready Plan',
++      unitNumber: 9,
++      orderIndex: 1,
++    });
 +
-+  it('renders a phase guidance shell for the current lesson phase', () => {
-+    const mockPhases = [
-+      {
-+        id: 'phase-2',
-+        phaseNumber: 2,
-+        title: 'Concept Intro',
-+        contentBlocks: [
-+          {
-+            id: 'block-1',
-+            type: 'markdown' as const,
-+            content: 'Work through the accounting logic.',
-+          },
-+        ],
-+        estimatedMinutes: 20,
-+        metadata: { phaseType: 'example' as const },
-+      },
-+    ];
-+
-+    render(
-+      <LessonRenderer
-+        lesson={mockLesson}
-+        phases={mockPhases}
-+        currentPhaseNumber={2}
-+        lessonSlug="intro-to-financial-statements"
-+      />,
-     );
++    render(<PhaseHeader lesson={capstoneLesson} phase={phases[0]} phases={phases} />);
++
++    expect(screen.getByText(/Capstone • Lesson 01/)).toBeInTheDocument();
++    expect(screen.queryByText(/Unit 9 • Lesson 01/)).not.toBeInTheDocument();
++  });
 +
-+    expect(screen.getByText('Phase Focus')).toBeInTheDocument();
-+    expect(screen.getByText(/explicit a\/l\/e reasoning/i)).toBeInTheDocument();
-+    expect(screen.getByText('This phase advances these lesson objectives')).toBeInTheDocument();
+   it('indicates progress for the current phase', () => {
+     const phases = buildPhases();
+ 
+diff --git a/__tests__/components/teacher/TeacherLessonPlan.test.tsx b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+index 4b3b3c8cd3edf9da9931e98d32ffd54467c8bc47..b104bf8587e514600e7c43a7f27d091d12f379c6
+--- a/__tests__/components/teacher/TeacherLessonPlan.test.tsx
++++ b/__tests__/components/teacher/TeacherLessonPlan.test.tsx
+@@ -84,6 +84,22 @@
+     expect(screen.getByText('Introduction to Accounting Equation')).toBeInTheDocument();
    });
  
-   it('renders lesson without description', () => {
-diff --git a/__tests__/components/student/PhaseGuidanceCard.test.tsx b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..8867b903d322992de4c44a99bf9b9b32e603c31c
---- /dev/null
-+++ b/__tests__/components/student/PhaseGuidanceCard.test.tsx
-@@ -0,0 +1,48 @@
-+import { render, screen } from '@testing-library/react';
-+
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
-+
-+describe('PhaseGuidanceCard', () => {
-+  it('renders phase goal, success criteria, and lesson objectives', () => {
++  it('uses the capstone label for unit 9 lessons', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'accounting',
-+          phaseNumber: 3,
-+          phaseLabel: 'Guided Practice',
-+          goal: 'Practice the accounting move with teacher support.',
-+          successCriteria: [
-+            'Complete the interactive task with your group.',
-+            'Explain why the accounting move is correct.',
-+          ],
++      <TeacherLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={[
-+          'Classify transaction effects on the accounting equation.',
-+          'Use evidence from Sarah Chen’s store records.',
-+        ]}
++        lessonNumber={1}
 +      />,
 +    );
 +
-+    expect(screen.getByText('Guided Practice')).toBeInTheDocument();
-+    expect(screen.getByText(/teacher support/i)).toBeInTheDocument();
-+    expect(screen.getByText(/complete the interactive task/i)).toBeInTheDocument();
-+    expect(screen.getByText(/classify transaction effects/i)).toBeInTheDocument();
++    expect(screen.getByText('Capstone - Lesson 1')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9 - Lesson 1')).not.toBeInTheDocument();
 +  });
 +
-+  it('hides the lesson-objectives section when none are provided', () => {
+   it('renders lesson duration', () => {
+     render(<TeacherLessonPlan lesson={mockLesson} lessonNumber={1} />);
+ 
+diff --git a/__tests__/components/teacher/UnitLessonPlan.test.tsx b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+index dfccc17717bbaafc92ff7c3153020239edccceb5..d93e0135b531a12d75122190f96260eec480ee8f
+--- a/__tests__/components/teacher/UnitLessonPlan.test.tsx
++++ b/__tests__/components/teacher/UnitLessonPlan.test.tsx
+@@ -81,6 +81,21 @@
+     expect(screen.getByText(/fundamental accounting equation/)).toBeInTheDocument();
+   });
+ 
++  it('uses the capstone title without a Unit 9 prefix', () => {
 +    render(
-+      <PhaseGuidanceCard
-+        guidance={{
-+          lessonType: 'excel',
-+          phaseNumber: 2,
-+          phaseLabel: 'Intro',
-+          goal: 'Demonstrate the spreadsheet workflow.',
-+          successCriteria: ['Name the quality checks before practicing.'],
++      <UnitLessonPlan
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
 +        }}
-+        learningObjectives={null}
 +      />,
 +    );
 +
-+    expect(screen.queryByText('This phase advances these lesson objectives')).not.toBeInTheDocument();
-+  });
-+});
-diff --git a/__tests__/lib/curriculum/phase-guidance.test.ts b/__tests__/lib/curriculum/phase-guidance.test.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..17082d565438c06f031014b610ed72ae9aad5342
---- /dev/null
-+++ b/__tests__/lib/curriculum/phase-guidance.test.ts
-@@ -0,0 +1,49 @@
-+import { describe, expect, it } from 'vitest';
-+
-+import {
-+  getLessonPhaseGuidance,
-+  getPhaseGuidance,
-+} from '@/lib/curriculum/phase-guidance';
-+
-+describe('curriculum/phase-guidance', () => {
-+  it('returns accounting guidance for the concept-intro phase', () => {
-+    const guidance = getPhaseGuidance('accounting', 2);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'accounting',
-+      phaseNumber: 2,
-+      phaseLabel: 'Concept Intro',
-+    });
-+    expect(guidance?.goal).toMatch(/explicit A\/L\/E reasoning/i);
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([
-+        expect.stringMatching(/guided/i),
-+        expect.stringMatching(/independent/i),
-+      ]),
-+    );
-+  });
-+
-+  it('returns excel guided-practice guidance with an interactive success criterion', () => {
-+    const guidance = getPhaseGuidance('excel', 3);
-+
-+    expect(guidance).toMatchObject({
-+      lessonType: 'excel',
-+      phaseNumber: 3,
-+      phaseLabel: 'Guided Practice',
-+    });
-+    expect(guidance?.successCriteria).toEqual(
-+      expect.arrayContaining([expect.stringMatching(/interactive/i)]),
-+    );
-+  });
-+
-+  it('maps lesson order index ranges to the correct phase guidance', () => {
-+    expect(getLessonPhaseGuidance(1, 3)?.lessonType).toBe('accounting');
-+    expect(getLessonPhaseGuidance(5, 3)?.lessonType).toBe('excel');
-+    expect(getLessonPhaseGuidance(8, 1)?.lessonType).toBe('project');
-+    expect(getLessonPhaseGuidance(11, 2)?.lessonType).toBe('assessment');
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
 +  });
 +
-+  it('returns null when the phase number is not defined for the lesson type', () => {
-+    expect(getPhaseGuidance('project', 6)).toBeNull();
-+  });
-+});
-diff --git a/components/lesson/PhaseRenderer.tsx b/components/lesson/PhaseRenderer.tsx
-index ed8e72dcef1b1ab6d52e0ab7899c32b12eca3e19..ba3517a79a2486740c43022e98286fa73fd3442a
---- a/components/lesson/PhaseRenderer.tsx
-+++ b/components/lesson/PhaseRenderer.tsx
-@@ -12,13 +12,21 @@
-   contentBlocks: ContentBlock[];
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }
+   it('renders Stage 1: Desired Results section', () => {
+     render(<UnitLessonPlan lesson={mockLesson} />);
  
- /**
-  * PhaseRenderer iterates over phase.contentBlocks and renders each block
-  * according to its type using discriminated union pattern.
-  */
--export function PhaseRenderer({ contentBlocks, lessonId, phaseNumber }: PhaseRendererProps) {
-+export function PhaseRenderer({
-+  contentBlocks,
-+  lessonId,
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
-+}: PhaseRendererProps) {
-   // Validate content blocks with Zod
-   const validatedBlocks = contentBlocks.map((block, index) => {
-     try {
-@@ -41,7 +49,13 @@
-     <div className="space-y-4">
-       {validatedBlocks.map((block) => (
-         <ContentBlockErrorBoundary key={block.id}>
--          <ContentBlockRenderer block={block} lessonId={lessonId} phaseNumber={phaseNumber} />
-+          <ContentBlockRenderer
-+            block={block}
-+            lessonId={lessonId}
-+            phaseNumber={phaseNumber}
-+            activityInitialStatus={activityInitialStatus}
-+            onActivityStatusChange={onActivityStatusChange}
-+          />
-         </ContentBlockErrorBoundary>
-       ))}
-     </div>
-@@ -54,11 +68,15 @@
- function ContentBlockRenderer({
-   block,
-   lessonId,
--  phaseNumber
-+  phaseNumber,
-+  activityInitialStatus,
-+  onActivityStatusChange,
- }: {
-   block: ContentBlock;
-   lessonId: string;
-   phaseNumber: number;
-+  activityInitialStatus?: 'not_started' | 'in_progress' | 'completed' | 'current' | 'available' | 'locked';
-+  onActivityStatusChange?: () => void;
- }) {
-   switch (block.type) {
-     case 'markdown':
-@@ -105,6 +123,8 @@
-           lessonId={lessonId}
-           phaseNumber={phaseNumber}
-           required={block.required}
-+          initialStatus={activityInitialStatus}
-+          onStatusChange={onActivityStatusChange}
-           linkedStandardId={block.linkedStandardId}
-         />
-       );
-diff --git a/components/student/LessonRenderer.tsx b/components/student/LessonRenderer.tsx
-index 26013096b1a02fbd0a60f297feecf78f88272e19..7e900e990af7e374a5eb6ac925c8199f3fd2964d
---- a/components/student/LessonRenderer.tsx
-+++ b/components/student/LessonRenderer.tsx
-@@ -4,11 +4,13 @@
- import { useRouter } from 'next/navigation';
- import { ChevronLeft, ChevronRight, CheckCircle2 } from 'lucide-react';
- import { LessonStepper, type StepperPhase } from '@/components/lesson/LessonStepper';
-+import { PhaseRenderer } from '@/components/lesson/PhaseRenderer';
-+import { PhaseGuidanceCard } from '@/components/student/PhaseGuidanceCard';
- import { usePhaseProgress } from '@/hooks/usePhaseProgress';
- import { usePhaseCompletion } from '@/hooks/usePhaseCompletion';
- import { Button } from '@/components/ui/button';
-+import { getLessonPhaseGuidance, type PhaseGuidance } from '@/lib/curriculum/phase-guidance';
- import { cn } from '@/lib/utils';
--import { ActivityRenderer } from '@/components/lesson/ActivityRenderer';
- import type { ContentBlock, LessonMetadata, PhaseMetadata } from '@/types/curriculum';
- 
- interface Phase {
-@@ -38,6 +40,18 @@
-   lessonSlug: string;
- }
+diff --git a/__tests__/components/teacher/UnitOverview.test.tsx b/__tests__/components/teacher/UnitOverview.test.tsx
+index ae283a4263014f878be98c10d3da0cac642e36bb..632d91ab1313ee7f708198b9ffe8c95bcea1c99e
+--- a/__tests__/components/teacher/UnitOverview.test.tsx
++++ b/__tests__/components/teacher/UnitOverview.test.tsx
+@@ -92,6 +92,21 @@
+     expect(screen.getByText('Unit 1: Balance by Design')).toBeInTheDocument();
+   });
  
-+function buildFallbackPhaseGuidance(phase: Phase, lesson: Lesson): PhaseGuidance {
-+  return {
-+    lessonType: 'accounting',
-+    phaseNumber: phase.phaseNumber,
-+    phaseLabel: phase.title,
-+    goal: `Work through ${phase.title.toLowerCase()} in a way that advances the lesson objective.`,
-+    successCriteria: lesson.learningObjectives?.length
-+      ? [`Connect your work in this phase to at least one lesson objective.`]
-+      : ['Complete the current phase and be ready to explain your thinking.'],
-+  };
-+}
-+
- /**
-  * Renders a single phase of a lesson with navigation
-  * Integrates LessonStepper and enforces phase locking on the client side
-@@ -59,6 +73,11 @@
-   // Get current phase completion status (safe with optional chaining when phase is absent)
-   const currentPhaseProgress = progressData?.phases.find(p => p.phaseId === currentPhase?.id);
-   const isCurrentPhaseCompleted = currentPhaseProgress?.status === 'completed';
-+  const phaseGuidance =
-+    currentPhase
-+      ? getLessonPhaseGuidance(lesson.orderIndex, currentPhase.phaseNumber) ??
-+        buildFallbackPhaseGuidance(currentPhase, lesson)
-+      : null;
- 
-   // Hooks must be called unconditionally — placed before any early return.
-   const { completePhase: autoMarkComplete, isCompleting: isAutoCompleting } = usePhaseCompletion({
-@@ -186,51 +205,20 @@
-               )}
-             </div>
- 
--            {/* Content Blocks - Basic rendering */}
-             <div className="space-y-4">
--              {(currentPhase.contentBlocks ?? []).map((block) => (
--                <div key={block.id}>
--                  {block.type === 'markdown' && (
--                    <div className="prose prose-sm max-w-none">
--                      {block.content}
--                    </div>
--                  )}
--                  {block.type === 'callout' && (
--                    <div className="border-l-4 border-primary pl-4 py-2 bg-muted/50">
--                      <div className="font-semibold capitalize mb-1">
--                        {block.variant.replace(/-/g, ' ')}
--                      </div>
--                      <div className="text-sm">{block.content}</div>
--                    </div>
--                  )}
--                  {block.type === 'video' && (
--                    <div className="border rounded p-4 bg-muted/50">
--                      <div className="font-semibold mb-2">Video</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.videoUrl}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'image' && (
--                    <div className="border rounded p-4">
--                      <div className="font-semibold mb-2">Image</div>
--                      <div className="text-sm text-muted-foreground">
--                        {block.props.alt}
--                      </div>
--                    </div>
--                  )}
--                  {block.type === 'activity' && (
--                    <ActivityRenderer
--                      activityId={block.activityId}
--                      lessonId={lesson.slug}
--                      phaseNumber={currentPhase.phaseNumber}
--                      required={block.required}
--                      initialStatus={currentPhaseProgress?.status}
--                      onStatusChange={() => refetch()}
--                    />
--                  )}
--                </div>
--              ))}
-+              {phaseGuidance ? (
-+                <PhaseGuidanceCard
-+                  guidance={phaseGuidance}
-+                  learningObjectives={lesson.learningObjectives}
-+                />
-+              ) : null}
-+              <PhaseRenderer
-+                contentBlocks={currentPhase.contentBlocks ?? []}
-+                lessonId={lesson.slug}
-+                phaseNumber={currentPhase.phaseNumber}
-+                activityInitialStatus={currentPhaseProgress?.status}
-+                onActivityStatusChange={() => refetch()}
-+              />
-             </div>
- 
-             <div className="mt-6 flex justify-end">
-diff --git a/components/student/PhaseGuidanceCard.tsx b/components/student/PhaseGuidanceCard.tsx
-new file mode 100644
-index 0000000000000000000000000000000000000000..e5cd8d228bdc28b34614494d828f75c7a7994c47
---- /dev/null
-+++ b/components/student/PhaseGuidanceCard.tsx
-@@ -0,0 +1,59 @@
-+import { BookCheck, Target } from 'lucide-react';
-+
-+import { Badge } from '@/components/ui/badge';
-+import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
-+import type { PhaseGuidance } from '@/lib/curriculum/phase-guidance';
-+
-+interface PhaseGuidanceCardProps {
-+  guidance: PhaseGuidance;
-+  learningObjectives: string[] | null;
-+}
-+
-+export function PhaseGuidanceCard({
-+  guidance,
-+  learningObjectives,
-+}: PhaseGuidanceCardProps) {
-+  return (
-+    <Card className="border-primary/20 bg-primary/5">
-+      <CardHeader className="space-y-3">
-+        <div className="flex flex-wrap items-center gap-2">
-+          <Badge variant="secondary">Phase Focus</Badge>
-+          <Badge variant="outline" className="capitalize">
-+            {guidance.lessonType}
-+          </Badge>
-+        </div>
-+        <CardTitle className="text-xl">{guidance.phaseLabel}</CardTitle>
-+        <p className="text-sm text-muted-foreground">{guidance.goal}</p>
-+      </CardHeader>
-+      <CardContent className="space-y-5">
-+        <section className="space-y-2">
-+          <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+            <Target className="h-4 w-4 text-primary" />
-+            What success looks like
-+          </div>
-+          <ul className="space-y-2 text-sm text-muted-foreground">
-+            {guidance.successCriteria.map((criterion) => (
-+              <li key={criterion} className="rounded-md border border-border/50 bg-background/80 px-3 py-2">
-+                {criterion}
-+              </li>
-+            ))}
-+          </ul>
-+        </section>
-+
-+        {learningObjectives && learningObjectives.length > 0 ? (
-+          <section className="space-y-2">
-+            <div className="flex items-center gap-2 text-sm font-semibold text-foreground">
-+              <BookCheck className="h-4 w-4 text-primary" />
-+              This phase advances these lesson objectives
-+            </div>
-+            <ul className="list-disc space-y-1 pl-5 text-sm text-muted-foreground">
-+              {learningObjectives.map((objective) => (
-+                <li key={objective}>{objective}</li>
-+              ))}
-+            </ul>
-+          </section>
-+        ) : null}
-+      </CardContent>
-+    </Card>
-+  );
-+}
-diff --git a/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json b/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-index 364eb86a46b01b017fb851065b622f944b094de2..c26bc753dc608b14cfc91af840979997116e6d4e
---- a/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-+++ b/conductor/archive/lesson_phase_objective_shell_20260311/metadata.json
-@@ -1,9 +1,9 @@
- {
-   "track_id": "lesson_phase_objective_shell_20260311",
-   "type": "feature",
--  "status": "in_progress",
-+  "status": "archived",
-   "created_at": "2026-03-11T08:34:26Z",
--  "updated_at": "2026-03-11T08:34:26Z",
-+  "updated_at": "2026-03-11T08:44:39Z",
-   "description": "Align student lesson phase rendering with curriculum phase objectives by adding a reusable phase guidance shell and objective-aware student presentation.",
-   "estimated_tasks": 7,
-   "actual_tasks": 7,
-diff --git a/conductor/archive/lesson_phase_objective_shell_20260311/plan.md b/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-index 2c93ed93a25fc6373d8d1f962121f21b751f077d..504954d0bd54aa9b155a27df0bdb25d02fbe44eb
---- a/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-+++ b/conductor/archive/lesson_phase_objective_shell_20260311/plan.md
-@@ -19,4 +19,4 @@
- ## Phase 3: Verification and Closeout
- - [x] Task: Run lint and automated tests for the track scope
- - [x] Task: Update README and Conductor memory files with the phase-shell alignment outcome
--- [ ] Task: Archive the track and finalize registry/metadata updates
-+- [x] Task: Archive the track and finalize registry/metadata updates
++  it('uses the capstone title without a Unit 9 prefix', () => {
++    render(
++      <UnitOverview
++        lesson={{
++          ...mockLesson,
++          unitNumber: 9,
++          title: 'Capstone: Investor-Ready Plan',
++        }}
++      />,
++    );
++
++    expect(screen.getByText('Capstone: Investor-Ready Plan')).toBeInTheDocument();
++    expect(screen.queryByText('Unit 9: Capstone: Investor-Ready Plan')).not.toBeInTheDocument();
++  });
++
+   it('renders unit description', () => {
+     render(<UnitOverview lesson={mockLesson} />);
+ 
+diff --git a/components/teacher/UnitLessonPlan.tsx b/components/teacher/UnitLessonPlan.tsx
+index 9834fd74f1fd096246ecaa19f12355629dc161c6..3cb05c8910f4b28316e576b04ea7d0a353c1f433
+--- a/components/teacher/UnitLessonPlan.tsx
++++ b/components/teacher/UnitLessonPlan.tsx
+@@ -19,6 +19,7 @@
+   GraduationCap
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface DailyLessonActivity {
+   name: string;
+@@ -125,7 +126,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
+diff --git a/components/teacher/UnitOverview.tsx b/components/teacher/UnitOverview.tsx
+index 6e86237e663b3d9f242e05cc914720213074a673..35a3c273d30783f3020bae9aa220ba2e1318bcf9
+--- a/components/teacher/UnitOverview.tsx
++++ b/components/teacher/UnitOverview.tsx
+@@ -10,6 +10,7 @@
+   TrendingUp
+ } from "lucide-react"
+ import { type Lesson } from '@/lib/db/schema/validators';
++import { formatCurriculumSegmentTitle } from '@/lib/curriculum/segment-labels';
+ 
+ interface UnitOverviewProps {
+   lesson: Lesson;
+@@ -57,7 +58,7 @@
+       {/* Header */}
+       <header className="mb-8">
+         <h1 className="text-3xl font-bold mb-4">
+-          Unit {lesson.unitNumber}: {lesson.title}
++          {formatCurriculumSegmentTitle(lesson.unitNumber, lesson.title)}
+         </h1>
+         <p className="text-lg text-muted-foreground mb-4">
+           {lesson.description}
 diff --git a/conductor/lessons-learned.md b/conductor/lessons-learned.md
-index 5a1f9d47c9c577b016380d41f806bcb496b3903e..d8b4b634818b45cfc7f4d67cb6059d6c008a677c
+index c6744e6265e1305f9e62ae2ff35179c8e2a6a46c..cfcfcc73b61799c67dee30ab713d6cfa49742d13
 --- a/conductor/lessons-learned.md
 +++ b/conductor/lessons-learned.md
-@@ -14,6 +14,7 @@
- - (2026-03-11, activity_component_contract_refactor_20260311) Activity contracts drift quickly when alias handling lives separately from the runtime registry and schema validators; documented curriculum keys need one canonical resolver.
- - (2026-03-11, published_progress_viewmodel_consolidation_20260311) Student dashboard rows, teacher student-detail rows, and lesson phase-status payloads should derive from the same published-progress helpers or published-version/locking fixes will diverge again.
- - (2026-03-11, student_lesson_access_boundary_cleanup_20260311) Student-owned API writes need an explicit request-level student guard even when teacher/admin lesson preview is intentionally allowed elsewhere; auth presence alone is not a safe mutation boundary.
-+- (2026-03-11, lesson_phase_objective_shell_20260311) Student lesson pages should derive phase guidance from curriculum lesson-type metadata and reuse the shared `PhaseRenderer`, or pedagogy copy and content rendering drift separately.
- 
- ### Recurring Gotchas
- <!-- Problems encountered repeatedly; save future tracks from the same pain -->
+@@ -23,6 +23,7 @@
+ - (2026-03-13, unit1_canonicalization_archetypes) Canonicalizing authored curriculum at the source is safer than accumulating manifest exceptions because both validation drift and role-specific rendering bugs disappear together.
+ - (2026-03-13, auth bootstrap hotfix) JWT-protected proxy rules must explicitly allow unauthenticated auth bootstrap endpoints like `/api/auth/login` and `/api/auth/session`, or the login page will redirect its own API calls back to itself.
+ - (2026-03-14, curriculum_rollout_wave2) Later-unit project lessons regress back to generic placeholders unless Lesson 7-11 dataset, workbook, polish, and presentation obligations are asserted directly in manifest regression tests.
++- (2026-03-14, capstone_textbook_completion) Capstone regressions hide in shared unit-oriented components unless the distinct segment label is asserted from the public card layer through student headers and teacher planning views.
+ 
+ ### Patterns Worth Repeating
+ 
+@@ -36,3 +37,4 @@
+ - (2026-03-13, curriculum_rollout_wave1) Lesson-planning matrices are a workable source for authored rollout waves when they are converted into canonical lesson blueprints and validated through manifest regression tests before runtime wiring.
+ - (2026-03-13, unit1_redesign_lesson_contract) Old infrastructure tests should validate canonical phase numbers and contract outcomes rather than stale phase labels, or redesign tracks will appear broken even when the authored curriculum and published runtime agree.
+ - (2026-03-14, curriculum_rollout_wave2) Unit-level authored blueprint modules are a practical way to scale later curriculum waves because they keep matrix detail, contract resources, and published lesson text aligned without reintroducing runtime-only generators.
++- (2026-03-14, capstone_textbook_completion) A shared curriculum-segment label helper is the simplest way to keep capstone naming coherent across public, student, and teacher flows without creating capstone-only storage or route forks.
+diff --git a/conductor/roadmap.md b/conductor/roadmap.md
+index 11d3802482df109f7afa63e1f8e06a29b281e2de..1ba34cd74dce8269c6b81c2e0e4a298afca7f180
+--- a/conductor/roadmap.md
++++ b/conductor/roadmap.md
+@@ -105,6 +105,8 @@
+ 
+ **Goal**: finish the book with the culminating experience and final coherence pass.
+ 
++**Status**: completed on 2026-03-14 with authored capstone runtime content and shared capstone labeling across the textbook, student progress, and teacher monitoring surfaces.
++
+ **Tracks**
+ 
+ - Capstone and Textbook Completion
+diff --git a/conductor/tech-debt.md b/conductor/tech-debt.md
+index 9bac252a4eba200df1bc57aede467e480d1d1f2a..640e1a1e5f5401852a117c3e7cf478b2cdbc1dff
+--- a/conductor/tech-debt.md
++++ b/conductor/tech-debt.md
+@@ -4,7 +4,6 @@
+ 
+ | Date | Track | Item | Severity | Status | Notes |
+ |------|-------|------|----------|--------|-------|
+-| 2026-03-14 | curriculum_rollout_wave2 | Capstone still relies on a generated publish scaffold even though all eight instructional units are now authored and canonical | Critical | Open | Start the capstone completion track next so the textbook can close the remaining scope gap and teacher monitoring can cover the culminating experience. |
+ | 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
+ | 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
+ | 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
 diff --git a/conductor/tracks.md b/conductor/tracks.md
-index f2fd9692cb4e3564b792a0968dda5beef4e78192..a94965df8c673cc029927782a3d791909b4e2091
+index 6704b229976c3e143989c878a6d0db8b29221c6c..8adc26ec3d1fcbe497a6f50ee5d48079589ac882
 --- a/conductor/tracks.md
 +++ b/conductor/tracks.md
-@@ -32,6 +32,10 @@
+@@ -6,8 +6,7 @@
+ 
+ ## Active Track
+ 
+-- [~] **Track: Capstone and Textbook Completion**
+-  *Link: [./tracks/capstone_textbook_completion_20260314/](./tracks/capstone_textbook_completion_20260314/)*
++- No active track. The next `/conductor` run should take the first item from the planned queue.
+ 
+ ## Planned Queue
+ 
+@@ -16,6 +15,10 @@
  
  ## Archive Ledger
  
-+- [x] **Track: Lesson Phase Objective Shell**
-+  *Link: [./conductor/archive/lesson_phase_objective_shell_20260311/](./conductor/archive/lesson_phase_objective_shell_20260311/)*
-+  *Checkpoint: `f9a651e` (2026-03-11 implementation complete; archived during closeout)*
-+
- - [x] **Track: Student Write Boundary Hardening**
-   *Link: [./conductor/archive/student_lesson_access_boundary_cleanup_20260311/](./conductor/archive/student_lesson_access_boundary_cleanup_20260311/)*
-   *Checkpoint: `pending` (2026-03-11 closeout)*
-diff --git a/lib/curriculum/phase-guidance.ts b/lib/curriculum/phase-guidance.ts
-new file mode 100644
-index 0000000000000000000000000000000000000000..ec0abd20e67c3925f4ab2dfdec61f78413101851
---- /dev/null
-+++ b/lib/curriculum/phase-guidance.ts
-@@ -0,0 +1,179 @@
-+import { getLessonTypeFromOrderIndex, type LessonType } from '@/lib/curriculum/types';
-+
-+export interface PhaseGuidance {
-+  lessonType: LessonType;
-+  phaseNumber: number;
-+  phaseLabel: string;
-+  goal: string;
-+  successCriteria: string[];
-+}
-+
-+const PHASE_GUIDANCE_BY_TYPE: Record<LessonType, Record<number, Omit<PhaseGuidance, 'lessonType' | 'phaseNumber'>>> = {
-+  accounting: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Open with a business decision tension in Sarah Chen’s story.',
-+      successCriteria: [
-+        'Explain why the accounting decision matters to the business.',
-+        'Identify the main tension you will solve in this lesson.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Concept Intro',
-+      goal: 'Teach the concept with explicit A/L/E reasoning.',
-+      successCriteria: [
-+        'Use assets, liabilities, and equity language accurately.',
-+        'Name what success looks like in the guided and independent phases.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Practice the accounting move with teacher-led interactive support.',
-+      successCriteria: [
-+        'Complete the interactive practice task, not just the reading.',
-+        'Explain why each accounting move is correct before moving on.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete a graded accounting task independently.',
-+      successCriteria: [
-+        'Submit a fully completed task without teacher intervention.',
-+        'Meet the passing-score target of at least 60%.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Assessment',
-+      goal: 'Verify mastery of the lesson objective with an exit ticket.',
-+      successCriteria: [
-+        'Answer the auto-graded check independently.',
-+        'Show that the lesson objective can be applied correctly.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Connect accounting accuracy to business trust and decision quality.',
-+      successCriteria: [
-+        'Describe how accuracy changes business decisions.',
-+        'Reflect on one habit that improves financial reliability.',
-+      ],
-+    },
-+  },
-+  excel: {
-+    1: {
-+      phaseLabel: 'Hook',
-+      goal: 'Frame the spreadsheet problem that needs to be solved.',
-+      successCriteria: [
-+        'Identify the spreadsheet problem in business terms.',
-+        'Recognize what a successful workbook should accomplish.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Intro',
-+      goal: 'Demonstrate the Excel workflow and quality criteria.',
-+      successCriteria: [
-+        'Name the workflow steps before practice begins.',
-+        'Describe the quality checks that keep the workbook reliable.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Guided Practice',
-+      goal: 'Build the spreadsheet skill with live teacher facilitation.',
-+      successCriteria: [
-+        'Use the interactive practice component while following the workflow.',
-+        'Explain which formula, formatting, or validation move you just applied.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Independent Practice',
-+      goal: 'Complete real spreadsheet work and prepare a teacher submission.',
-+      successCriteria: [
-+        'Produce the required workbook, screenshot, or PDF evidence.',
-+        'Make sure the deliverable is ready for teacher review.',
-+      ],
-+    },
-+    5: {
-+      phaseLabel: 'Checkpoint Assessment',
-+      goal: 'Complete an auto-graded checkpoint on your own.',
-+      successCriteria: [
-+        'Submit the checkpoint independently.',
-+        'Meet the passing-score target of at least 80%.',
-+      ],
-+    },
-+    6: {
-+      phaseLabel: 'Reflection',
-+      goal: 'Explain how spreadsheet discipline affects business decisions.',
-+      successCriteria: [
-+        'Describe one quality-control habit from the lesson.',
-+        'Connect spreadsheet accuracy to business impact.',
-+      ],
-+    },
-+  },
-+  project: {
-+    1: {
-+      phaseLabel: 'Project Build',
-+      goal: 'Advance the project deliverable through scaffolded team work.',
-+      successCriteria: [
-+        'Complete the assigned team deliverable for today’s project phase.',
-+        'Use the project criteria to judge whether the work is presentation-ready.',
-+      ],
-+    },
-+  },
-+  assessment: {
-+    1: {
-+      phaseLabel: 'Instructions',
-+      goal: 'Review the directions, expectations, and assessment setup.',
-+      successCriteria: [
-+        'Understand the assessment rules before starting.',
-+        'Know how the assessment is divided across the upcoming sections.',
-+      ],
-+    },
-+    2: {
-+      phaseLabel: 'Knowledge',
-+      goal: 'Demonstrate foundational knowledge without support.',
-+      successCriteria: [
-+        'Answer the knowledge questions independently.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    3: {
-+      phaseLabel: 'Understanding',
-+      goal: 'Show conceptual understanding of the business math ideas.',
-+      successCriteria: [
-+        'Explain or apply the concept correctly in the assessment task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+    4: {
-+      phaseLabel: 'Application',
-+      goal: 'Apply the concept in a realistic business scenario.',
-+      successCriteria: [
-+        'Use the concept accurately in the scenario-based task.',
-+        'Meet the minimum assessment performance target.',
-+      ],
-+    },
-+  },
-+};
-+
-+export function getPhaseGuidance(
-+  lessonType: LessonType,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  const guidance = PHASE_GUIDANCE_BY_TYPE[lessonType][phaseNumber];
-+  if (!guidance) {
-+    return null;
-+  }
-+
-+  return {
-+    lessonType,
-+    phaseNumber,
-+    ...guidance,
-+  };
-+}
-+
-+export function getLessonPhaseGuidance(
-+  orderIndex: number,
-+  phaseNumber: number,
-+): PhaseGuidance | null {
-+  return getPhaseGuidance(getLessonTypeFromOrderIndex(orderIndex), phaseNumber);
-+}
++- [x] **Track: Capstone and Textbook Completion**
++  *Link: [./archive/capstone_textbook_completion_20260314/](./archive/capstone_textbook_completion_20260314/)*
++  *Closeout: archived on 2026-03-14 after replacing the generated capstone scaffold with authored runtime content, locking milestone/workbook/final-presentation coverage into manifest and UI regressions, and normalizing capstone labeling across the core public, student, and teacher textbook flow.*
++
+ - [x] **Track: Curriculum Rollout Wave 2 (Units 5-8)**
+   *Link: [./archive/curriculum_rollout_wave2_20260314/](./archive/curriculum_rollout_wave2_20260314/)*
+   *Closeout: archived on 2026-03-14 after replacing Units 5-8 generated curriculum scaffolds with canonical authored published lessons, locking Lesson 7-11 dataset/workbook/presentation contract details into manifest regressions, and updating the active Conductor curriculum status to show all eight instructional units as authored.*
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json b/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
+deleted file mode 100644
+index e53f588834f06d46a36ab82106566ba908bb6cbc..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/metadata.json
++++ /dev/null
+@@ -1,11 +0,0 @@
+-{
+-  "track_id": "capstone_textbook_completion_20260314",
+-  "type": "feature",
+-  "status": "in_progress",
+-  "created_at": "2026-03-14T00:00:00Z",
+-  "updated_at": "2026-03-14T00:00:00Z",
+-  "description": "Replace the generated capstone scaffold with canonical authored runtime content and finish the first end-to-end textbook experience from Unit 1 through capstone.",
+-  "estimated_tasks": 6,
+-  "actual_tasks": null,
+-  "deviation_notes": ""
+-}
+diff --git a/conductor/tracks/capstone_textbook_completion_20260314/plan.md b/conductor/tracks/capstone_textbook_completion_20260314/plan.md
+deleted file mode 100644
+index 48b611e5d419a88dad2df9bf2d2a717532f66631..0000000000000000000000000000000000000000
+--- a/conductor/tracks/capstone_textbook_completion_20260314/plan.md
++++ /dev/null
+@@ -1,31 +0,0 @@
+-# Implementation Plan
+-
+-## Phase 1: Capstone Contract Guards
+-
+-- [ ] Task: Add regression tests for authored capstone curriculum coverage
+-  - [ ] Write tests that fail if the published capstone lesson is still generated
+-  - [ ] Write tests that fail if authored capstone content loses milestone, workbook, or presentation guidance
+-- [ ] Task: Add regression tests for textbook capstone labeling
+-  - [ ] Write tests that fail if the public curriculum card labels the capstone as a generic unit
+-  - [ ] Write tests that fail if the main student and teacher progress surfaces regress to generic `Unit 9` labeling
+-- [ ] Task: Mark the new track active in Conductor and capture implementation status
+-  - [~] Update track/task status markers as execution progresses
+-
+-## Phase 2: Capstone Runtime Completion
+-
+-- [ ] Task: Create canonical authored capstone runtime content
+-  - [ ] Add a repository-authored capstone source module aligned to the active curriculum narrative
+-  - [ ] Replace the generated capstone manifest builder with authored capstone integration
+-- [ ] Task: Normalize capstone labeling across key textbook surfaces
+-  - [ ] Add a shared capstone label helper for published curriculum surfaces
+-  - [ ] Apply the helper to the main public, student, and teacher routes/components that currently leak `Unit 9`
+-
+-## Phase 3: Verification and Documentation
+-
+-- [ ] Task: Synchronize active Conductor docs and project memory for textbook completion
+-  - [ ] Update impacted Conductor docs to reflect authored capstone completion
+-  - [ ] Update `tech-debt.md` and `lessons-learned.md` with any remaining near-term implications
+-- [ ] Task: Run required verification and finalize the track
+-  - [ ] Run `npm run lint`
+-  - [ ] Run `npm test`
+-  - [ ] Run `npm run build`
 
 tokens used
-151,166
-Implemented one Conductor track aligned to the current directive and archived it end-to-end.
+237,362
+Implemented and finalized the active Conductor track. The capstone now publishes from authored runtime content in [published-manifest.ts](/home/daniel-bo/Desktop/bus-math-v2/lib/curriculum/published-manifest.ts) backed by the new authored blueprint in [capstone-authored.ts](/home/daniel-bo/Desktop/bus-math-v2/lib/curriculum/generated/capstone-authored.ts), and the main public, student, and teacher surfaces now use distinct capstone labeling instead of leaking `Unit 9`. I also extended the label normalization into shared teacher planning and student header components so the regression coverage is broader than the minimum route set.
+
+Conductor is updated and archived: the track plan/metadata were completed, project memory was refreshed, the roadmap now records Milestone 5 as complete, and the archived track lives at [capstone_textbook_completion_20260314](/home/daniel-bo/Desktop/bus-math-v2/conductor/archive/capstone_textbook_completion_20260314). [tracks.md](/home/daniel-bo/Desktop/bus-math-v2/conductor/tracks.md) now has no active track and points to the next planned queue item.
 
-The student lesson phase shell now shows curriculum-aligned phase guidance and success criteria via [PhaseGuidanceCard.tsx](/home/daniel-bo/Desktop/bus-math-v2-experimental/components/student/PhaseGuidanceCard.tsx) and [phase-guidance.ts](/home/daniel-bo/Desktop/bus-math-v2-experimental/lib/curriculum/phase-guidance.ts). [LessonRenderer.tsx](/home/daniel-bo/Desktop/bus-math-v2-experimental/components/student/LessonRenderer.tsx) now uses the shared [PhaseRenderer.tsx](/home/daniel-bo/Desktop/bus-math-v2-experimental/components/lesson/PhaseRenderer.tsx) path instead of its own inline block renderer, while still preserving activity-status refresh behavior.
+Verification passed:
+- `npm run lint`
+- `npm test` with 215 test files / 1193 tests passing
+- `npm run build`
 
-Verification passed with `CI=true npm run lint` and `CI=true npm test` (`210` files, `1141` tests). I also added regression coverage for the new guidance model and student shell behavior. The track was committed and archived in two non-interactive commits: `f9a651e` and `3c4a121`. The archived track lives at [lesson_phase_objective_shell_20260311](/home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/archive/lesson_phase_objective_shell_20260311).
+Commits pushed to `origin/main`:
+- `7f860c4` `feat(curriculum): complete capstone textbook flow (Codex GPT-5)`
+- `a5c333a` `chore(conductor): Archive completed track`
 
-Unrelated pre-existing working tree changes remain in `conductor/autonomous_prompt.md`, `conductor/codex-cron.log`, `conductor/codex-last.txt`, `conductor/workflow.md`, and untracked `conductor/current_directive.md`; I left them untouched.
+The worktree still has unrelated pre-existing unstaged changes outside this track. I left those untouched and out of the two pushed commits.
diff --git a/conductor/codex-last.txt b/conductor/codex-last.txt
index 955d7c3..2696877 100644
--- a/conductor/codex-last.txt
+++ b/conductor/codex-last.txt
@@ -1,7 +1,14 @@
-Implemented one Conductor track aligned to the current directive and archived it end-to-end.
+Implemented and finalized the active Conductor track. The capstone now publishes from authored runtime content in [published-manifest.ts](/home/daniel-bo/Desktop/bus-math-v2/lib/curriculum/published-manifest.ts) backed by the new authored blueprint in [capstone-authored.ts](/home/daniel-bo/Desktop/bus-math-v2/lib/curriculum/generated/capstone-authored.ts), and the main public, student, and teacher surfaces now use distinct capstone labeling instead of leaking `Unit 9`. I also extended the label normalization into shared teacher planning and student header components so the regression coverage is broader than the minimum route set.
 
-The student lesson phase shell now shows curriculum-aligned phase guidance and success criteria via [PhaseGuidanceCard.tsx](/home/daniel-bo/Desktop/bus-math-v2-experimental/components/student/PhaseGuidanceCard.tsx) and [phase-guidance.ts](/home/daniel-bo/Desktop/bus-math-v2-experimental/lib/curriculum/phase-guidance.ts). [LessonRenderer.tsx](/home/daniel-bo/Desktop/bus-math-v2-experimental/components/student/LessonRenderer.tsx) now uses the shared [PhaseRenderer.tsx](/home/daniel-bo/Desktop/bus-math-v2-experimental/components/lesson/PhaseRenderer.tsx) path instead of its own inline block renderer, while still preserving activity-status refresh behavior.
+Conductor is updated and archived: the track plan/metadata were completed, project memory was refreshed, the roadmap now records Milestone 5 as complete, and the archived track lives at [capstone_textbook_completion_20260314](/home/daniel-bo/Desktop/bus-math-v2/conductor/archive/capstone_textbook_completion_20260314). [tracks.md](/home/daniel-bo/Desktop/bus-math-v2/conductor/tracks.md) now has no active track and points to the next planned queue item.
 
-Verification passed with `CI=true npm run lint` and `CI=true npm test` (`210` files, `1141` tests). I also added regression coverage for the new guidance model and student shell behavior. The track was committed and archived in two non-interactive commits: `f9a651e` and `3c4a121`. The archived track lives at [lesson_phase_objective_shell_20260311](/home/daniel-bo/Desktop/bus-math-v2-experimental/conductor/archive/lesson_phase_objective_shell_20260311).
+Verification passed:
+- `npm run lint`
+- `npm test` with 215 test files / 1193 tests passing
+- `npm run build`
 
-Unrelated pre-existing working tree changes remain in `conductor/autonomous_prompt.md`, `conductor/codex-cron.log`, `conductor/codex-last.txt`, `conductor/workflow.md`, and untracked `conductor/current_directive.md`; I left them untouched.
\ No newline at end of file
+Commits pushed to `origin/main`:
+- `7f860c4` `feat(curriculum): complete capstone textbook flow (Codex GPT-5)`
+- `a5c333a` `chore(conductor): Archive completed track`
+
+The worktree still has unrelated pre-existing unstaged changes outside this track. I left those untouched and out of the two pushed commits.
\ No newline at end of file
diff --git a/conductor/docs/architecture/runtime.md b/conductor/docs/architecture/runtime.md
index cacfee6..f8eea4a 100644
--- a/conductor/docs/architecture/runtime.md
+++ b/conductor/docs/architecture/runtime.md
@@ -27,10 +27,11 @@ The following should disappear from the active runtime path:
 - Supabase test/debug routes and docs that still look active
 - Drizzle/Postgres runtime reads that duplicate Convex-backed behavior
 
 ## Deployment Notes
 
-- Local development uses `vinext dev` plus `npx convex dev`.
+- Local development should prefer `npm run dev:stack`, which runs `npx convex dev --local` and starts `vinext dev` through the Convex parent process so startup and shutdown stay coordinated.
+- `npx convex dev --local` writes local deployment state under `~/.convex/`.
 - Production planning must include Worker config, build verification, and secret management for:
   - `NEXT_PUBLIC_CONVEX_URL`
   - `CONVEX_DEPLOY_KEY`
   - `AUTH_JWT_SECRET`
diff --git a/convex/teacher.ts b/convex/teacher.ts
index a5a10e6..d7bbd49 100644
--- a/convex/teacher.ts
+++ b/convex/teacher.ts
@@ -1,6 +1,7 @@
 import { internalQuery, type QueryCtx } from "./_generated/server";
+import type { Doc, Id } from "./_generated/dataModel";
 import { v } from "convex/values";
 import { assembleCourseOverviewRows } from "../lib/teacher/course-overview";
 import { assembleGradebookRows } from "../lib/teacher/gradebook";
 import {
   buildLatestPublishedLessonVersionMap,
@@ -56,38 +57,38 @@ function sortStudentsByName<
   );
 }
 
 async function getAuthorizedTeacher(
   ctx: QueryCtx,
-  userId: string,
-) {
-  const teacher = await ctx.db.get(userId as never);
+  userId: Id<"profiles">,
+): Promise<Doc<"profiles"> | null> {
+  const teacher = await ctx.db.get(userId);
   if (!teacher || (teacher.role !== "teacher" && teacher.role !== "admin")) {
     return null;
   }
   return teacher;
 }
 
 async function listOrganizationStudents(
   ctx: QueryCtx,
-  organizationId: string,
+  organizationId: Id<"organizations">,
 ) {
   const allProfiles = await ctx.db
     .query("profiles")
-    .withIndex("by_organization", (q) => q.eq("organizationId", organizationId as never))
+    .withIndex("by_organization", (q) => q.eq("organizationId", organizationId))
     .collect();
 
   return sortStudentsByName(
     allProfiles.filter((profile) => profile.role === "student"),
   );
 }
 
 async function getOrganizationName(
   ctx: QueryCtx,
-  organizationId: string,
+  organizationId: Id<"organizations">,
 ) {
-  const organization = await ctx.db.get(organizationId as never);
+  const organization = await ctx.db.get(organizationId);
   return organization?.name ?? "Your organization";
 }
 
 async function listLatestPublishedLessonVersions(
   ctx: QueryCtx,
@@ -99,40 +100,40 @@ async function listLatestPublishedLessonVersions(
   ];
 }
 
 async function listActivePhaseIds(
   ctx: QueryCtx,
-) {
+): Promise<Set<Id<"phase_versions">>> {
   const lessonIds = (await ctx.db.query("lessons").collect()).map((lesson) => lesson._id);
   const lessonVersions = await ctx.db.query("lesson_versions").collect();
   const phaseVersions = await ctx.db.query("phase_versions").collect();
   return buildPublishedPhaseIdSet({
     lessonIds,
     lessonVersions,
     phaseVersions,
-  });
+  }) as Set<Id<"phase_versions">>;
 }
 
 async function buildStudentProgressSnapshot(
   ctx: QueryCtx,
-  studentId: string,
-  activePhaseIds: Set<string>,
+  studentId: Id<"profiles">,
+  activePhaseIds: Set<Id<"phase_versions">>,
 ): Promise<TeacherProgressSnapshot> {
   const progressRows = await ctx.db
     .query("student_progress")
-    .withIndex("by_user", (q) => q.eq("userId", studentId as never))
+    .withIndex("by_user", (q) => q.eq("userId", studentId))
     .collect();
 
   return buildPublishedProgressSnapshot({
     activePhaseIds,
     progressRows,
   });
 }
 
 async function listStudentDetailUnits(
   ctx: QueryCtx,
-  studentId: string,
+  studentId: Id<"profiles">,
 ): Promise<TeacherStudentDetailUnitRow[]> {
   const allLessons = await ctx.db.query("lessons").collect();
 
   if (allLessons.length === 0) {
     return [];
@@ -222,12 +223,18 @@ export const getTeacherCourseOverviewData = internalQuery({
     )).map((version) => ({
       id: version._id,
       lessonId: version.lessonId,
     }));
 
+    const rawStudents = students.map((student) => ({
+      id: student._id,
+      username: student.username,
+      displayName: student.displayName ?? null,
+    }));
+
     if (rawLessonVersions.length === 0) {
-      return assembleCourseOverviewRows(students, rawLessons, [], [], []);
+      return assembleCourseOverviewRows(rawStudents, rawLessons, [], [], []);
     }
 
     const lessonVersionIds = new Set(rawLessonVersions.map((version) => version.id));
     const rawPrimaryStandards = (await ctx.db.query("lesson_standards").collect())
       .filter(
@@ -239,11 +246,11 @@ export const getTeacherCourseOverviewData = internalQuery({
         standardId: standard.standardId,
         isPrimary: standard.isPrimary,
       }));
 
     if (rawPrimaryStandards.length === 0) {
-      return assembleCourseOverviewRows(students, rawLessons, rawLessonVersions, [], []);
+      return assembleCourseOverviewRows(rawStudents, rawLessons, rawLessonVersions, [], []);
     }
 
     const standardIds = new Set(rawPrimaryStandards.map((standard) => standard.standardId));
     const competencyRows = (
       await Promise.all(
@@ -262,11 +269,11 @@ export const getTeacherCourseOverviewData = internalQuery({
         standardId: row.standardId,
         masteryLevel: row.masteryLevel,
       }));
 
     return assembleCourseOverviewRows(
-      students,
+      rawStudents,
       rawLessons,
       rawLessonVersions,
       rawPrimaryStandards,
       competencyRows,
     );
@@ -329,10 +336,16 @@ export const getTeacherGradebookData = internalQuery({
         standardId: standard.standardId,
         isPrimary: standard.isPrimary,
       }));
 
     const students = await listOrganizationStudents(ctx, teacher.organizationId);
+    const rawStudents = students.map((student) => ({
+      id: student._id,
+      username: student.username,
+      displayName: student.displayName ?? null,
+    }));
+
     if (students.length === 0) {
       return assembleGradebookRows([], rawLessons, rawLessonVersions, rawPhaseVersions, rawPrimaryStandards, [], []);
     }
 
     const phaseIds = new Set(rawPhaseVersions.map((phase) => phase.id));
@@ -373,11 +386,11 @@ export const getTeacherGradebookData = internalQuery({
         standardId: row.standardId,
         masteryLevel: row.masteryLevel,
       }));
 
     return assembleGradebookRows(
-      students,
+      rawStudents,
       rawLessons,
       rawLessonVersions,
       rawPhaseVersions,
       rawPrimaryStandards,
       progressRows,
diff --git a/docs/security-api-route-matrix.md b/docs/security-api-route-matrix.md
index 1d06c3d..f35d6f5 100644
--- a/docs/security-api-route-matrix.md
+++ b/docs/security-api-route-matrix.md
@@ -6,10 +6,12 @@ It is the source of truth for the API hardening refactor track (`api_security_ha
 
 ## Route Classification
 
 | Route | Methods | Current State | Required State | Required Role |
 | --- | --- | --- | --- | --- |
+| `/api/auth/login` | `POST` | Proxy treated it as private, so unauthenticated login attempts were redirected back to `/auth/login` before the handler ran | Public bootstrap endpoint for username/password login; no proxy auth redirect | `public` |
+| `/api/auth/session` | `GET` | Proxy treated it as private, so session bootstrap checks were redirected before the handler could return unauthenticated state | Public session introspection endpoint; returns auth state instead of redirecting | `public` |
 | `/api/activities/[activityId]` | `GET` | Public via proxy, no auth in handler, raw activity payload | Private, auth required, student-safe payload redaction | `student`, `teacher` |
 | `/api/activities/complete` | `POST` | Deprecated compatibility shim forwarding to `/api/phases/complete` | Private while shim exists; migrate runtime clients to `/api/phases/complete` | `student` |
 | `/api/activities/spreadsheet/[activityId]/draft` | `GET`, `POST` | Auth checked in handler | Private, student-only draft ownership enforcement | `student` |
 | `/api/activities/spreadsheet/[activityId]/submit` | `GET`, `POST` | Auth checked in handler; `GET` supports read-only replay with teacher org-scoped student access | Private; `POST` is student-only, `GET` keeps teacher replay access | `student` for `POST`; `student`, `teacher` for `GET` |
 | `/api/lessons/[lessonId]/progress` | `GET` | Auth checked in handler | Private, unchanged auth requirement | `student`, `teacher` |
@@ -24,10 +26,11 @@ It is the source of truth for the API hardening refactor track (`api_security_ha
 | `/api/test/cleanup-e2e` | `POST` | Non-production guard only | Public test-only endpoint with non-production + optional secret token guard | `public` (guarded by env+secret) |
 
 ## Policy Decisions
 - Deny by default at proxy level for `/api/**`.
 - Keep public API allowlist explicit and minimal.
+- Public auth bootstrap routes are limited to `/api/auth/login` and `/api/auth/session`; authenticated auth-management routes stay private.
 - All lesson/progress/activity/student-management APIs are private unless explicitly listed otherwise.
 - Student-facing activity responses must never include answer keys (`correctAnswer`) or grading internals.
 - Test/debug endpoints must be unusable in production and optionally require `x-test-api-key` in non-production.
 - Demo bootstrap endpoints with known credentials must be explicitly environment-gated and covered by production-denial tests.
 
diff --git a/lib/activities/component-keys.ts b/lib/activities/component-keys.ts
index 4ccf70c..5a59cd3 100644
--- a/lib/activities/component-keys.ts
+++ b/lib/activities/component-keys.ts
@@ -1,6 +1,6 @@
-import type { ActivityComponentKey } from '@/lib/db/schema/activities';
+import type { ActivityComponentKey } from '../db/schema/activities-core';
 
 const activityComponentAliases = {
   'general-drag-and-drop': 'drag-and-drop',
   'journal-entry-activity': 'journal-entry-building',
   'spreadsheet-activity': 'spreadsheet',
diff --git a/lib/convex/admin.ts b/lib/convex/admin.ts
index 99e097f..039db14 100644
--- a/lib/convex/admin.ts
+++ b/lib/convex/admin.ts
@@ -1,10 +1,11 @@
 type EnvLike = Record<string, string | undefined>;
 
 interface ResolveConvexAdminAuthOptions {
   cwd?: string;
   env?: EnvLike;
+  homeDir?: string;
 }
 
 interface ConvexAdminAuth {
   source: 'deploy-key' | 'local-admin-key';
   token: string;
@@ -12,26 +13,24 @@ interface ConvexAdminAuth {
 
 function isProductionRuntime(env: EnvLike): boolean {
   return env.NODE_ENV === 'production' || env.VERCEL_ENV === 'production';
 }
 
-async function readLocalConvexAdminKey(cwd: string): Promise<string | null> {
+async function findAdminKeyInConfigRoot(configRoot: string): Promise<string | null> {
   const fs = await import('node:fs/promises');
   const path = await import('node:path');
-
-  const localRoot = path.join(cwd, '.convex', 'local');
   let directoryEntries;
 
   try {
-    directoryEntries = await fs.readdir(localRoot, { withFileTypes: true });
+    directoryEntries = await fs.readdir(configRoot, { withFileTypes: true });
   } catch {
     return null;
   }
 
   const configPaths = directoryEntries
     .filter((entry) => entry.isDirectory())
-    .map((entry) => path.join(localRoot, entry.name, 'config.json'))
+    .map((entry) => path.join(configRoot, entry.name, 'config.json'))
     .sort((left, right) => left.localeCompare(right));
 
   for (const configPath of configPaths) {
     try {
       const rawConfig = await fs.readFile(configPath, 'utf8');
@@ -45,10 +44,32 @@ async function readLocalConvexAdminKey(cwd: string): Promise<string | null> {
   }
 
   return null;
 }
 
+async function readLocalConvexAdminKey(
+  cwd: string,
+  homeDir?: string,
+): Promise<string | null> {
+  const path = await import('node:path');
+  const os = await import('node:os');
+
+  const configRoots = [
+    path.join(cwd, '.convex', 'local'),
+    path.join(homeDir ?? os.homedir(), '.convex'),
+  ];
+
+  for (const configRoot of configRoots) {
+    const adminKey = await findAdminKeyInConfigRoot(configRoot);
+    if (adminKey) {
+      return adminKey;
+    }
+  }
+
+  return null;
+}
+
 export async function resolveConvexAdminAuth(
   options: ResolveConvexAdminAuthOptions = {},
 ): Promise<ConvexAdminAuth> {
   const env = options.env ?? process.env;
   const deployKey = env.CONVEX_DEPLOY_KEY?.trim();
@@ -64,17 +85,20 @@ export async function resolveConvexAdminAuth(
     throw new Error(
       'Missing Convex admin auth. Set CONVEX_DEPLOY_KEY for production server-side internal function calls.',
     );
   }
 
-  const localAdminKey = await readLocalConvexAdminKey(options.cwd ?? process.cwd());
+  const localAdminKey = await readLocalConvexAdminKey(
+    options.cwd ?? process.cwd(),
+    options.homeDir,
+  );
   if (localAdminKey) {
     return {
       source: 'local-admin-key',
       token: localAdminKey,
     };
   }
 
   throw new Error(
-    'Missing Convex admin auth. Start `npx convex dev` locally or set CONVEX_DEPLOY_KEY for server-side internal function calls.',
+    'Missing Convex admin auth. Start `npm run dev:stack` or `npx convex dev --local` locally, or set CONVEX_DEPLOY_KEY for server-side internal function calls.',
   );
 }
diff --git a/lib/db/schema/phase-content.ts b/lib/db/schema/phase-content.ts
index ee9f7cd..7ee48de 100644
--- a/lib/db/schema/phase-content.ts
+++ b/lib/db/schema/phase-content.ts
@@ -1,7 +1,7 @@
 import { z } from 'zod';
-import { COMPETENCY_STANDARD_CODE_PATTERN } from '@/lib/curriculum/standards';
+import { COMPETENCY_STANDARD_CODE_PATTERN } from '../../curriculum/standards';
 
 export const contentBlockSchema = z.discriminatedUnion('type', [
   z.object({
     id: z.string(),
     type: z.literal('markdown'),
diff --git a/lib/db/schema/validators.ts b/lib/db/schema/validators.ts
index 8f5ff93..5147bfb 100644
--- a/lib/db/schema/validators.ts
+++ b/lib/db/schema/validators.ts
@@ -1,9 +1,9 @@
 import { createInsertSchema, createSelectSchema } from 'drizzle-zod';
 import { z } from 'zod';
 
-import { resolveActivityComponentKey } from '@/lib/activities/component-keys';
+import { resolveActivityComponentKey } from '../../activities/component-keys';
 import { activities, activityPropsSchemas, gradingConfigSchema } from './activities';
 import { activitySubmissions, submissionDataSchema } from './activity-submissions';
 import { classEnrollments } from './class-enrollments';
 import { classes, classMetadataSchema } from './classes';
 import { contentRevisions, validationErrorSchema } from './content-revisions';
diff --git a/proxy.ts b/proxy.ts
index 93c7ad1..6d31221 100644
--- a/proxy.ts
+++ b/proxy.ts
@@ -15,10 +15,12 @@ export async function proxy(request: NextRequest) {
     '/curriculum',
     '/login',
     '/auth',
   ];
   const publicApiRoutes = [
+    '/api/auth/login',
+    '/api/auth/session',
     '/api/test/seed-e2e',
     '/api/test/cleanup-e2e',
   ];
   if (isDemoProvisioningEnabled()) {
     publicApiRoutes.unshift('/api/users/ensure-demo');
diff --git a/scripts/dev-stack.mjs b/scripts/dev-stack.mjs
index 5638831..669b3b7 100644
--- a/scripts/dev-stack.mjs
+++ b/scripts/dev-stack.mjs
@@ -1,60 +1,137 @@
+import fs from 'node:fs';
+import path from 'node:path';
 import { spawn } from 'node:child_process';
+import { pathToFileURL } from 'node:url';
 
-const processes = [
-  { name: 'convex', command: 'npx', args: ['convex', 'dev'] },
-  { name: 'vinext', command: 'npx', args: ['vinext', 'dev'] },
-];
+function readJsonIfPresent(filePath) {
+  if (!fs.existsSync(filePath)) {
+    return null;
+  }
 
-const children = new Set();
-let shuttingDown = false;
+  return JSON.parse(fs.readFileSync(filePath, 'utf8'));
+}
 
-function stopChildren(signal = 'SIGTERM') {
-  for (const child of children) {
-    if (!child.killed) {
-      child.kill(signal);
-    }
+function resolvePinnedConvexVersion(projectRoot) {
+  const packageLock = readJsonIfPresent(path.join(projectRoot, 'package-lock.json'));
+  const lockedVersion = packageLock?.packages?.['node_modules/convex']?.version;
+
+  if (typeof lockedVersion === 'string' && lockedVersion.length > 0) {
+    return lockedVersion;
   }
-}
 
-function exitWith(code) {
-  if (shuttingDown) {
-    return;
+  const packageJson = readJsonIfPresent(path.join(projectRoot, 'package.json'));
+  const declaredVersion =
+    packageJson?.dependencies?.convex?.trim() ||
+    packageJson?.devDependencies?.convex?.trim();
+
+  if (!declaredVersion) {
+    return null;
   }
 
-  shuttingDown = true;
-  stopChildren('SIGTERM');
-  setTimeout(() => stopChildren('SIGKILL'), 3000).unref();
-  process.exit(code);
+  const normalizedVersion = declaredVersion.replace(/^[~^]/, '');
+  return /^\d+\.\d+\.\d+(?:[-+].+)?$/.test(normalizedVersion)
+    ? normalizedVersion
+    : null;
 }
 
-for (const processConfig of processes) {
-  const child = spawn(processConfig.command, processConfig.args, {
-    stdio: 'inherit',
-    env: process.env,
-  });
+export function resolveConvexCliPackageSpec(projectRoot = process.cwd()) {
+  const pinnedVersion = resolvePinnedConvexVersion(projectRoot);
+  return pinnedVersion ? `convex@${pinnedVersion}` : 'convex';
+}
 
-  children.add(child);
+export function createProcesses(projectRoot = process.cwd()) {
+  return [
+    {
+      name: 'convex',
+      command: 'npx',
+      // Force npm to execute the published CLI package rather than a stubbed local SDK install.
+      // Let Convex own the Vinext child process so startup stays ordered and shutdown is coordinated.
+      args: [
+        '--yes',
+        '--package',
+        resolveConvexCliPackageSpec(projectRoot),
+        'convex',
+        'dev',
+        '--local',
+        '--run-sh',
+        'npx vinext dev',
+      ],
+    },
+  ];
+}
 
-  child.on('exit', (code, signal) => {
-    children.delete(child);
+export function runDevStack(processes = createProcesses()) {
+  const children = new Set();
+  let shuttingDown = false;
 
-    if (shuttingDown) {
-      return;
+  function stopChildren(signal = 'SIGTERM') {
+    for (const child of children) {
+      if (child.killed) {
+        continue;
+      }
+
+      if (process.platform !== 'win32' && typeof child.pid === 'number') {
+        try {
+          process.kill(-child.pid, signal);
+          continue;
+        } catch {
+          // Fall through to direct child termination when the process group no longer exists.
+        }
+      }
+
+      child.kill(signal);
     }
+  }
 
-    if (signal) {
-      exitWith(1);
+  function exitWith(code) {
+    if (shuttingDown) {
       return;
     }
 
-    exitWith(code ?? 1);
-  });
+    shuttingDown = true;
+    stopChildren('SIGTERM');
+    setTimeout(() => stopChildren('SIGKILL'), 3000).unref();
+    process.exit(code);
+  }
+
+  for (const processConfig of processes) {
+    const child = spawn(processConfig.command, processConfig.args, {
+      stdio: 'inherit',
+      env: process.env,
+      detached: process.platform !== 'win32',
+    });
+
+    children.add(child);
+
+    child.on('exit', (code, signal) => {
+      children.delete(child);
+
+      if (shuttingDown) {
+        return;
+      }
 
-  child.on('error', (error) => {
-    console.error(`[dev:stack] Failed to start ${processConfig.name}:`, error);
-    exitWith(1);
-  });
+      if (signal) {
+        exitWith(1);
+        return;
+      }
+
+      exitWith(code ?? 1);
+    });
+
+    child.on('error', (error) => {
+      console.error(`[dev:stack] Failed to start ${processConfig.name}:`, error);
+      exitWith(1);
+    });
+  }
+
+  process.on('SIGINT', () => exitWith(130));
+  process.on('SIGTERM', () => exitWith(143));
 }
 
-process.on('SIGINT', () => exitWith(130));
-process.on('SIGTERM', () => exitWith(143));
+const isDirectExecution =
+  typeof process.argv[1] === 'string' &&
+  import.meta.url === pathToFileURL(path.resolve(process.argv[1])).href;
+
+if (isDirectExecution) {
+  runDevStack();
+}
diff --git a/skills-lock.json b/skills-lock.json
index 1386f9f..5e1ea6f 100644
--- a/skills-lock.json
+++ b/skills-lock.json
@@ -4,25 +4,15 @@
     "avoid-feature-creep": {
       "source": "waynesutton/convexskills",
       "sourceType": "github",
       "computedHash": "e63392840c3a80cbd9ec7b27888bf8d433b09bf6c9077ee3a6f882668057a390"
     },
-    "convex": {
-      "source": "waynesutton/convexskills",
-      "sourceType": "github",
-      "computedHash": "de7a20d081cd19bf8bce4038bd62d038a0a88f218f85afb35320d09b052fb534"
-    },
     "convex-agents": {
       "source": "waynesutton/convexskills",
       "sourceType": "github",
       "computedHash": "3054123dedc1b7ded9b6404c6c641023683fe509103fe7dc148acda831d53d36"
     },
-    "convex-best-practices": {
-      "source": "waynesutton/convexskills",
-      "sourceType": "github",
-      "computedHash": "3b24d2afbb5859e3a3640c1e47e2ac9ace81e9737ec76b891951999361c92ab1"
-    },
     "convex-component-authoring": {
       "source": "waynesutton/convexskills",
       "sourceType": "github",
       "computedHash": "dbb84d8c1996d9054958f07e67502ece523c49ff957bb3109397a6595e44f4ed"
     },
@@ -68,13 +58,8 @@
     },
     "convex-security-check": {
       "source": "waynesutton/convexskills",
       "sourceType": "github",
       "computedHash": "b53e061304bb81d037c6dd4e38762d27a596523bcbeab43c58e0b05140449802"
-    },
-    "migrate-to-vinext": {
-      "source": "cloudflare/vinext",
-      "sourceType": "github",
-      "computedHash": "19853b718a63cde339b1d5fb80ab8051738505b5940111b976888d6ab7351b32"
     }
   }
-}
+}
\ No newline at end of file
diff --git a/types/curriculum.ts b/types/curriculum.ts
index caf8b27..6ffd2be 100644
--- a/types/curriculum.ts
+++ b/types/curriculum.ts
@@ -1,8 +1,8 @@
 import type { InferInsertModel, InferSelectModel } from 'drizzle-orm';
-import { lessons } from '@/lib/db/schema';
-import type { Phase as SchemaPhase, NewPhase as SchemaNewPhase } from '@/lib/db/schema/validators';
+import { lessons } from '../lib/db/schema/lessons';
+import type { Phase as SchemaPhase, NewPhase as SchemaNewPhase } from '../lib/db/schema/validators';
 import type {
   LessonMetadata as SchemaLessonMetadata,
   UnitAssessment as SchemaUnitAssessment,
   UnitContent as SchemaUnitContent,
   UnitDifferentiation as SchemaUnitDifferentiation,
@@ -11,16 +11,16 @@ import type {
   UnitLearningSequence as SchemaUnitLearningSequence,
   UnitObjectives as SchemaUnitObjectives,
   UnitPrerequisites as SchemaUnitPrerequisites,
   UnitStudentChoices as SchemaUnitStudentChoices,
   UnitIntroduction as SchemaUnitIntroduction,
-} from '@/lib/db/schema/lessons';
+} from '../lib/db/schema/lessons';
 import type {
   ContentBlock as SchemaContentBlock,
   PhaseMetadata as SchemaPhaseMetadata,
-} from '@/lib/db/schema/phase-content';
-import { contentBlockSchema as schemaContentBlockSchema } from '@/lib/db/schema/phase-content';
+} from '../lib/db/schema/phase-content';
+import { contentBlockSchema as schemaContentBlockSchema } from '../lib/db/schema/phase-content';
 
 export const CURRICULUM_PHASE_COUNT = 6;
 export const contentBlockSchema = schemaContentBlockSchema;
 
 export type LessonRow = InferSelectModel<typeof lessons>;
