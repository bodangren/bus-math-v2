## Tech Debt Registry

> Keep this file at or below **50 lines**. Track only debt that should influence near-term planning.

| Date | Track | Item | Severity | Status | Notes |
|------|-------|------|----------|--------|-------|
| 2026-03-14 | security_audit | 26 vulns found: Rollup (8.8, RCE), tar (8.8, File Overwrite), minimatch (8.7, ReDoS), serialize-javascript (8.1, RCE), next (7.5, DoS), flatted (7.5, DoS), ajv (5.5, ReDoS), esbuild (5.3, CSRF) | High | Open | CRITICAL: Update rollup to v4.59.0+, serialize-javascript to v7.0.3+, and tar to v7.5.11+ immediately. Also requires Next.js v16.1.5+ and flatted v3.4.0+ to resolve DoS risks. |
| 2026-03-19 | practice_contract_planning | `activity_submissions.submissionData` was too generic to preserve part-level answers, artifacts, scaffold usage, or deterministic misconception tags consistently across practice families | Medium | Closed | Resolved by the practice evidence track with a dedicated `practice.v1` validator in Convex schema/mutations. |
| 2026-03-19 | practice_contract_planning | Teacher submission detail only showed spreadsheet artifacts, leaving non-spreadsheet practice work invisible after submission | Medium | Closed | Resolved by the generalized teacher evidence assembly and read-only modal. |
| 2026-03-20 | practice_component_legacy_backfill | Remaining legacy practice families still diverge by response shape, especially spreadsheet and simulation-backed components that have not yet been normalized onto one backfill wave | Medium | Closed | Spreadsheet and simulation families now emit canonical envelopes; the last adapter/completion-payload shims and component-key aliases were removed. Legacy table deprecation can be tracked separately if needed. |
| 2026-03-20 | practice_component_legacy_backfill | Drag/drop practice families still need the live-region summary strip and richer screen-reader announcements from the UI design spec | Low | Open | Keyboard-friendly move controls now exist in CategorizationList; the remaining gap is accessible status narration and review-feedback polish. |
| 2026-03-20 | practice_preview_page | The dev practice preview now renders multiple heavy activity examples, so its regression test needs a longer timeout than the default Vitest budget | Low | Closed | Raised the page test timeout to 20s after adding the Family K scenario panel and additional SelectionMatrix instances. |
| 2026-03-20 | classification_conceptual_families | The `normal-balances` contract entry lagged behind the live `normal-balance` family key, and the preview route did not yet show a dedicated Family M sample | Low | Closed | Resolved by updating the practice contract registry, wiring Family M into the preview route, and registering the family in the engine registry. |
| 2026-03-20 | normal_balance_family | Readonly account-ontology tuples require helper-based narrowing before touching optional contra fields in generator filters | Low | Closed | Resolved by routing contra filtering through `isContra` instead of direct `contraOf` access. |
| 2026-03-20 | accounting_engine_foundation | New practice registry helpers needed explicit map-key widening and contra-property guards to satisfy production type-checking | Low | Closed | Resolved during the foundation track; keep future registry helpers aligned with their public string lookup APIs. |
| 2026-03-20 | journal_transaction_families_20260319 | Mixed-shape practice responses need row-only projections before they are passed into `SelectionMatrix` preview defaults | Low | Open | Family C/F/L preview wiring proved the need to strip numeric metadata out of matrix props; add a shared helper if later families feed mixed response shapes into the same UI. |
| 2026-03-20 | journal_entry_preview_ui | The shared `JournalEntryTable` still lacks the stacked mobile row fallback and grouped-date header treatment from the preview spec | Low | Open | The Family H preview now documents the layout pattern, but if this surface becomes interactive the responsive/table behavior should move into the shared component. |
| 2026-03-21 | statement_completion_preview_shell | Family D now shows three explicit statement preview variants, but the same guided-plus-review shell is likely to be repeated for the remaining statement families unless it is factored into a shared helper | Low | Open | Keep the demo explicit for now; if later statement families need the same split guided/review presentation, extract a reusable preview builder to avoid copy drift. |
| 2026-03-20 | repo_wide_component_suite_regressions | Full `npm test` previously failed across unrelated component suites, including `FeedbackCollector`, `PitchPresentationBuilder`, `BreakEvenAnalysisCalculator`, `InterestCalculationBuilder`, `DepreciationMethodBuilder`, `SpreadsheetEvaluator`, `CashFlowChallenge`, `TeacherStudentActions`, `update-password-form`, `SubmissionDetailModal`, and other UI/API areas | High | Closed | Resolved after the statement-family cleanup and preview-contract fixes; the clean suite now passes end to end, with only the baseline-browser-mapping warning remaining. |
| 2026-03-21 | curriculum_guided_independent_pairing_20260316 | Unit 1 lessons 2, 4, and 7 reused the same activity ids for guided and independent practice, so mode-specific prompts and artifacts could drift together | Medium | Closed | Resolved by giving independent practice distinct ids and prompts, then locking the published manifest with regression tests. |
| 2026-03-21 | statement_computation_families_20260319 | Family N preview initially assumed a raw depreciation `cost` field and failed type-checking until the original asset cost was derived from `depreciableBase + salvageValue` | Low | Closed | Fixed in the depreciation preview/build path; keep future adjustment-family consumers aligned with the actual scenario shape instead of assuming a raw cost field. |
| 2026-03-21 | trial_balance_error_family_20260319 | Trial Balance Error Analysis uses a bespoke worksheet-card preview shell instead of a reusable matrix helper | Low | Open | Keep the custom shell while the family is the only consumer; extract a shared helper if another family needs the same guided/teacher review pattern. |
| 2026-03-16 | cloudflare_production_hardening_launch | Cloudflare launch still depends on manual Wrangler secret setup and deploy execution because no CI-backed Worker deployment workflow exists yet | Medium | Open | The checked-in checklist now documents the exact handoff, but future launch work should automate it once credential ownership is defined. |
| 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved; reconfirmed during 2026-03-20 and 2026-03-21 full-suite verification and the warning still appears during `npm test` and `npm run build`, including the practice-engine slice and this final statement/computation track closeout. |
| 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Open | Add revocation-aware validation or a shorter-lived credential strategy later. |
| 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
| 2026-03-22 | dev_stack_startup | Convex module path `practice-submission.ts` rejected by Convex (hyphens not allowed); `lib/db/schema/activity-submissions.ts` `@/` alias failed under Convex tsc | Medium | Closed | Renamed to `practice_submission.ts`, switched import to relative path. |
| 2026-03-22 | categorization_exercise | `CategorizationInteractive` setState-during-render: `evaluate` called inside `setAttempts` updater fires `onComplete` → upstream `setIsCompleting`, crossing component boundaries | Medium | Closed | Moved `evaluate` call after `setAttempts` so `onComplete` fires outside the state updater. |
| 2026-03-24 | practice_visual_teaching_upgrade_20260323 | Shared practice artifacts now use document-style shells and T-account boards instead of generic flat controls | Low | Closed | Phase 2 finished the statement, journal, categorization, and posting-balance visual redesigns; the remaining preview-page panel duplication is a later cleanup. |
| 2026-03-23 | practice_engine_stabilization_20260323 | Full `npm test` still reports baseline failures in landing/curriculum/preface suites unrelated to the Phase 1 practice-engine fixes | Medium | Open | `npm run lint` and `npm run build` pass, but the repo-wide test gate still needs separate cleanup before this track can close. |
| 2026-03-24 | practice_visual_teaching_upgrade_20260323 | Scenario-panel JSX is still duplicated between teacher and guided preview variants in a few dev surfaces | Low | Open | The shared component seam is now in place; if later phases keep expanding the preview page, extract a reusable panel builder to keep the context blocks consistent. |
| 2026-03-27 | practice_visual_teaching_upgrade_20260323 | Shared teaching chrome is live, but the remaining family-specific narration/polish pass for K, M, G, N, and preview-page edge cases still needs a final sweep before phase 3 can close | Low | Open | Keep this separate from engine correctness; the remaining work is pedagogical consistency, not contract shape. |
| 2026-03-24 | practice_visual_teaching_upgrade_20260323 | Posting-balance practice still rendered as a flat numeric list instead of a T-account board | Low | Closed | Resolved by `TAccountInteractive`, the classic debit-left / credit-right layout, and the preview-page swap away from `PostingBalanceList`. |
| 2026-03-23 | curriculum_guided_independent_pairing_20260316 | Unit 1 lesson 2 and lesson 7 instruction phases needed explicit worked-example callouts so intro copy did not read like unsupported practice reuse | Low | Closed | Resolved by adding source-level example callouts and manifest regressions that require the instruction phase to surface the worked example. |
| 2026-03-24 | curriculum_guided_independent_pairing_20260316 | Later authored-unit instruction phases needed explicit teacher-model / worked-example callouts so Wave 1 and Wave 2 intros did not read like generic setup text | Low | Closed | Resolved by adding example callouts to representative Units 2-8 instruction phases and locking them with manifest regressions. |
