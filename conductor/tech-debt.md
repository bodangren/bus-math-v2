## Tech Debt Registry

> Keep this file at or below **50 lines**. Track only debt that should influence near-term planning.

| Date | Track | Item | Severity | Status | Notes |
|------|-------|------|----------|--------|-------|
| 2026-03-14 | security_audit | 26 vulns found: Rollup (8.8, RCE), tar (8.8, File Overwrite), minimatch (8.7, ReDoS), serialize-javascript (8.1, RCE), next (7.5, DoS), flatted (7.5, DoS), ajv (5.5, ReDoS), esbuild (5.3, CSRF) | High | Open | CRITICAL: Update rollup to v4.59.0+, serialize-javascript to v7.0.3+, and tar to v7.5.11+ immediately. Also requires Next.js v16.1.5+ and flatted v3.4.0+ to resolve DoS risks. |
| 2026-03-20 | practice_component_legacy_backfill | Drag/drop practice families still need the live-region summary strip and richer screen-reader announcements from the UI design spec | Low | Closed | CategorizationList now has aria-live announcements for drag events, placement changes, and score updates via milestone7_closure Phase 1. |
| 2026-03-20 | journal_transaction_families_20260319 | Mixed-shape practice responses need row-only projections before they are passed into `SelectionMatrix` preview defaults | Low | Closed | projectToRowSelections helper added in shared/utils.ts; Family C/F/K preview wiring updated in milestone7_closure Phase 1. |
| 2026-03-20 | journal_entry_preview_ui | The shared `JournalEntryTable` still lacks the stacked mobile row fallback and grouped-date header treatment from the preview spec | Low | Closed | Phase 2 of milestone7_closure: added mobile stacked-card fallback (`md:hidden`) with grouped-date headers and desktop table wrapped in `hidden md:block`. |
| 2026-03-21 | statement_completion_preview_shell | Family D now shows three explicit statement preview variants, but the same guided-plus-review shell is likely to be repeated for the remaining statement families unless it is factored into a shared helper | Low | Open | Keep the demo explicit for now; if later statement families need the same split guided/review presentation, extract a reusable preview builder to avoid copy drift. |
| 2026-03-21 | trial_balance_error_family_20260319 | Trial Balance Error Analysis uses a bespoke worksheet-card preview shell instead of a reusable matrix helper | Low | Open | Keep the custom shell while the family is the only consumer; extract a shared helper if another family needs the same guided/teacher review pattern. |
| 2026-03-16 | cloudflare_production_hardening_launch | Cloudflare launch still depends on manual Wrangler secret setup and deploy execution because no CI-backed Worker deployment workflow exists yet | Medium | Open | The checked-in checklist now documents the exact handoff, but future launch work should automate it once credential ownership is defined. |
| 2026-03-13 | student_study_runtime | `baseline-browser-mapping` data is stale, so lint/test/build emit repeated warnings during verification | Low | Open | Refresh the pinned dev dependency when dependency changes are explicitly approved. |
| 2026-03-11 | replan | Signed session cookies remain valid until expiry even if the backing credential is deactivated | Medium | Closed | Phase 2 of milestone7_closure: added `requireActiveRequestSessionClaims` that checks credential `isActive` via Convex. Fails open on transient errors. JWT + active check now required for API mutation routes. |
| 2026-03-12 | curriculum_runtime_foundation | Legacy Supabase/Drizzle seed and helper surfaces still remain in migration-era folders even though Convex is canonical for runtime flows | Medium | Open | They no longer drive active product routes, but they still increase maintenance and type-check surface area. |
| 2026-03-23 | practice_engine_stabilization_20260323 | Full `npm test` still reports baseline failures in landing/curriculum/preface suites unrelated to the Phase 1 practice-engine fixes | Medium | Open | `npm run lint` and `npm run build` pass, but the repo-wide test gate still needs separate cleanup before this track can close. |
| 2026-03-24 | practice_visual_teaching_upgrade_20260323 | Scenario-panel JSX is still duplicated between teacher and guided preview variants in a few dev surfaces | Low | Closed | ScenarioPanel shared component created in milestone7_closure Phase 1; Family C/F/K/Q preview pairs now use shared component. Complex panels (E, H, L) remain inline due to Badge/dynamic content. |
| 2026-04-04 | code_review_legacy_cleanup | Simulations 4-8 use ad-hoc inline activity prop types instead of canonical Zod schemas | Medium | Open | First 3 simulations wrap the Activity type; last 5 should follow the same pattern for consistency and runtime validation. |
| 2026-04-04 | code_review_legacy_cleanup | No test coverage for practice.v1 envelope emission in 5 of 8 simulations (CashFlowChallenge and NotebookOrganizer have tests; 4 more added in Phase 1) | Medium | Closed | Phase 2 added envelope emission tests for StartupJourney, BudgetBalancer, and LemonadeStand. All 8 simulations now have envelope coverage. |
| 2026-04-04 | code_review_legacy_cleanup | 2 pre-existing security test files (competency-rls.test.ts, rls.test.ts) failing in test suite | Medium | Open | Likely need Supabase client credentials; not related to current changes. |
| 2026-04-04 | code_review_track5_phase1 | AssetTimeMachine "Back to Lesson" button does not reset game state (currentYear, currentValue, totalExpenses, history), causing replay to resume from stale state | Medium | Closed | Fixed in simulation_contract_hardening_20260404 Phase 1: reset now clears all game state. |
| 2026-04-04 | code_review_track5_phase1 | CashFlowChallenge uses hardcoded activityId 'cash-flow-challenge' instead of activity.id like other simulations | Low | Open | Inconsistent with StartupJourney/BusinessStressTest pattern of `activity.id ?? fallback`. Low priority since the slug is stable. |
| 2026-04-04 | code_review_track5_phase1 | CashFlowChallenge exposes dual onSubmit/onSubmitLegacy callback pattern; onSubmitLegacy bypasses practice.v1 contract | Medium | Open | Tech debt: onSubmitLegacy should be removed once all consumers are migrated to the practice contract. |
| 2026-04-04 | code_review_track5_phase1 | No test coverage for AssetTimeMachine or BusinessStressTest components (no test files exist) | Medium | Closed | Test files added in simulation_contract_hardening_20260404 Phase 1 with rendering and envelope emission tests. |
| 2026-04-04 | code_review_track5_phase1 | StartupJourney test file does not verify practice.v1 envelope emission (only tests rendering and onStateChange) | Low | Closed | Envelope emission test added in Phase 2 of simulation_contract_hardening_20260404. |
| 2026-04-04 | code_review_track5_phase2-4 | `canTeacherAccessSubmission` and `canTeacherAccessLessonSummary` are exported and tested but never called in production code | Low | Open | Dead code from Phase 1; should be wired into Convex query guards or removed if API-layer auth is sufficient. |
| 2026-04-04 | code_review_track5_phase2-4 | `getLessonErrorSummary` uses N+1 queries (one per activityId) to fetch submissions | Low | Open | Acceptable for current scale; refactor to batch if lesson activity count grows. |
| 2026-04-04 | practice_contract_completion_20260404 | CashFlowChallenge cleanup (remove onSubmitLegacy, use activity.id) deferred to remaining phase work | Low | Open | 5 components now emit practice.v1 envelopes. CashFlowChallenge still has legacy path alongside working practice.v1 onSubmit. |
| 2026-04-04 | code_review_audit_tracks6-7 | InventoryManager, PayStructureDecisionLab, PitchPresentationBuilder lacked double-submit guards — fixed in audit | High | Closed | Added `submitted` state + button disable + callback guard to all 3 components. |
| 2026-04-04 | code_review_audit_tracks6-7 | GrowthPuzzle missing explicit double-submit guard (UI-only via disabled button) | Medium | Open | `isComplete` disables button but no code-level guard; add `submitted` state for defense-in-depth. |
| 2026-04-04 | code_review_audit_tracks6-7 | PayStructureDecisionLab declares `onComplete` and `activity` props but never uses them | Low | Open | Remove dead props or wire them in a future phase. |
| 2026-04-04 | code_review_audit_tracks6-7 | 8 of 12 simulation test files never assert `parts` or `status` on emitted envelopes | Medium | Open | AssetTimeMachine, BudgetBalancer, BusinessStressTest, CafeSupplyChaos, CapitalNegotiation, LemonadeStand, StartupJourney, CashFlowChallenge all skip these fields. |
| 2026-04-04 | code_review_audit_tracks6-7 | No test file verifies `familyKey` on emitted envelopes (0/12 files) | Medium | Open | Systemic gap — familyKey is never asserted in any simulation envelope test. |
| 2026-04-04 | code_review_audit_tracks6-7 | Track metadata files (Track 5, Track 6, Track 7) had stale status — fixed in audit | Low | Closed | Updated metadata.json for all three tracks to match tracks.md status. |
| 2026-04-04 | code_review_audit_tracks6-7 | Hardcoded `activityId` in 6 simulation components instead of `activity.id ?? fallback` pattern | Low | Open | Only LemonadeStand and AssetTimeMachine use the fallback pattern. Should standardize when activity types migrate to schema-derived. |
| 2026-04-06 | code_review_audit_m7 | DynamicMethodSelector: `completed` state value was discarded, no disabled prop on submit button — allowed repeated envelope emission | High | Closed | Fixed: added `completed` guard to `handleComplete` callback and `disabled={completed}` to button. |
| 2026-04-06 | code_review_audit_m7 | journal-entry grader: same-account wrong-amount misclassified as `debit-credit-reversal` instead of `computation-error` | Medium | Closed | Fixed: now compares debit/credit side placement before tagging; same-side wrong-amount gets `computation-error`. |
| 2026-04-06 | code_review_audit_m7 | classification grader: `wrong-account-type` tag was dead code (searched practiceAccounts by category id, never matched) | Medium | Closed | Fixed: simplified to tag `wrong-account-type` whenever answer is wrong in account-type category sets. |
| 2026-04-06 | code_review_audit_m7 | transaction-matrix: reasoning-stage errors tagged as `debit-credit-reversal`/`computation-error` instead of `classification-error` | Low | Open | Semantic mismatch: stage-column selection errors should use `classification-error` or a context tag. Low analytics impact. |
| 2026-04-06 | code_review_audit_m7 | StraightLineMastery: `options.sort(() => Math.random() - 0.5)` re-shuffles on every render, making options visually jump | Medium | Open | Move shuffle to useMemo keyed on problem reset; current behavior is confusing but not data-corrupting. |
| 2026-04-06 | code_review_audit_m7 | AssetRegisterSimulator, DepreciationMethodComparison, MethodComparison: `parseFloat('')` → NaN in envelopes; no input validation before submit | Medium | Open | Add empty-input check or NaN guard in submit handler. Currently NaN propagates into artifact data. |
| 2026-04-06 | code_review_audit_m7 | AssetRegisterSimulator, DepreciationMethodComparison, MethodComparison: `onComplete()` fires on every "Practice Another Asset" cycle with no ref guard | Medium | Open | Add `hasCompleted` ref to prevent duplicate onComplete calls during replays. |
| 2026-04-06 | code_review_audit_m7 | CapitalizationExpenseMastery: "Show Example" button sets `submitted=true` and shows red "Not quite" feedback — misleading UX | Low | Open | Should show example answer inline without setting submitted state or showing incorrect feedback. |
| 2026-04-06 | code_review_audit_m7 | DDBComparisonMastery: `computeDDBSchedule` has no final-year catch-up to salvage value | Low | Open | Accounting accuracy issue; book value may end above salvage. Low priority since exercise is conceptual. |
