Completed the Phase 3 statement-family expansion for Families D, E, and Q. The shared `StatementLayout` now supports label-editable account placement rows plus subtotal rows, and the practice preview shows separate guided/review panels for each statement variant.

Validation passed: `npm run lint`, `npm test` (`246` files, `1283` tests), and `npm run build`.

The active statement/computation track remains in progress for the later adjustment, depreciation, and merchandising phases.
